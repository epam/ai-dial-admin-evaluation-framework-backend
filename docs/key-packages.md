# Key Packages Reference

Package-by-package map of the three modules. Linked from [AGENTS.md](../AGENTS.md) — read this when navigating or placing new code.

## Main application (root package `com.epam.aidial.evaluation`)

| Package                               | Purpose                                                                                                                                                                                                                                                            |
|---------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `com.epam.aidial.evaluation`          | Root package                                                                                                                                                                                                                                                        |
| `.client.dialcore`                    | DIAL Core client (deployments, models, deployment invocation, file operations)                                                                                                                                                                                      |
| `.client.mcp`                         | MCP SDK client (tool invocation, tool listing via DIAL Core MCP proxy)                                                                                                                                                                                              |
| `.client.apikey`                      | `ApiKeyIntrospectionClientConfiguration` — dedicated `RestClient` bean for DIAL API-Key introspection (`GET /v1/user/info`), kept separate from `.client.dialcore` since it's an inbound-auth concern                                                              |
| `.client.dialadas`                    | dial-adas query-execute client (`DialAdasClient`, `DialAdasClientConfiguration`, `DialAdasClientException`) — powers `GET /test-suite-runs/{id}/costs`, mirrors `.client.dialcore`'s shape (own base URL/timeouts, reuses its auth/tracing interceptors)            |
| `.configuration`                      | Spring @Configuration classes                                                                                                                                                                                                                                       |
| `.configuration.datasource`           | DataSource setup                                                                                                                                                                                                                                                    |
| `.configuration.jackson`              | Jackson ObjectMapper customizations                                                                                                                                                                                                                                 |
| `.configuration.logging`              | Logging, correlation IDs                                                                                                                                                                                                                                            |
| `.configuration.properties`           | ConfigurationProperties classes                                                                                                                                                                                                                                     |
| `.configuration.properties.analytics` | Analytics-specific config (EvalSummaryProperties, AnalyticsResultsProperties)                                                                                                                                                                                       |
| `.configuration.properties.dial`      | DIAL-specific config (DialProperties, DialCoreProperties, DialFileStorageProperties)                                                                                                                                                                                |
| `.configuration.security`             | Security & OIDC configurations                                                                                                                                                                                                                                      |
| `.constants`                          | Constants (Security, TestSuiteRun, Validation)                                                                                                                                                                                                                      |
| `.data.db.exception`                  | DB-specific exceptions                                                                                                                                                                                                                                              |
| `.data.db.jooq.meta`                  | Generated jOOQ classes for meta schema (Tables, Records, Keys, Indexes)                                                                                                                                                                                             |
| `.data.db.jooq.analytics`             | Generated jOOQ classes for analytics schema (Tables, Records, Keys, Indexes)                                                                                                                                                                                        |
| `.data.db.mapper`                     | RecordMapper `@Component`s for meta entities (maps jOOQ Record → domain model)                                                                                                                                                                                      |
| `.data.db.model`                      | Meta domain models                                                                                                                                                                                                                                                  |
| `.data.db.model.filter`               | Filter models                                                                                                                                                                                                                                                       |
| `.data.db.model.pagination`           | Page, PageRequest, SortKey                                                                                                                                                                                                                                          |
| `.data.db.repository`                 | Meta data access with jOOQ DSLContext                                                                                                                                                                                                                               |
| `.data.db.repository.sql`             | SQL builders (WhereBuilder, OrderByBuilder, PageRequestSqlBuilder, FilterWhitelists, SortWhitelists)                                                                                                                                                                |
| `.data.db.repository.sql.json`        | JsonPathAccessor — JSONB path access abstraction (`->`, `->>`, numeric cast)                                                                                                                                                                                        |
| `.data.db.transaction.timestamp`      | Transaction timestamp context (meta only)                                                                                                                                                                                                                           |
| `.data.db.analytics.mapper`           | RecordMapper `@Component`s for analytics entities                                                                                                                                                                                                                   |
| `.data.db.analytics.model`            | Analytics domain models (TestCaseRunResult, EvalSummary, RunMetricSnapshot, ExecutionStatus)                                                                                                                                                                        |
| `.data.db.analytics.model.cursor`     | Cursor, CursorPage — keyset pagination carriers                                                                                                                                                                                                                     |
| `.data.db.analytics.repository`       | Analytics data access with jOOQ DSLContext (append-only, batch writes)                                                                                                                                                                                              |
| `.experimental.query.model`           | **(experimental)** Structured query DSL request model — sealed `Expr`/`FilterNode`/`PageSpec`, `StructuredQuery`, enums, `FilterNodeDeserializer`                                                                                                                   |
| `.experimental.query.service`         | **(experimental)** Schema discovery (`QueryableEntitySchemaProvider` SPI + `QueryEntityRegistry`, `JooqTableSchemaResolver`, per-entity providers) and entity-agnostic query dispatch/execution/translation (`StructuredQueryService`, `…repository`, `…translate`) |
| `.experimental.query.service.metricscore` | Metric-score computation over the DSL, both batch and on-demand. Phase 3: `BuiltInMetricStatistics`, `MetricScoreComputationExecutor` (implements `service.domain.job.MetricScoreComputation`), `OverallScoreDefinitionResolver` (typed `OverallScoreDefinition` → `StructuredQuery`), `MetricFieldDiscoverer`/`MetricField` (shared field discovery). Run comparison (request path, never persisted): `RunComparisonService` (implements `service.domain.analytics.RunComparisonProvider`), `FilteredMetricScoreAggregator` (Phase 3's own queries + one ANDed row-exclusion predicate) |
| `.experimental.query.service.translate.function` | Registry-driven DSL function catalog: `QueryFunction` SPI, `QueryFunctionRegistry`, `FunctionContext`, `BuiltInQueryFunctions`                                                                                                        |
| `.experimental.query.web`             | **(experimental)** Structured query controllers under `/api/v1/queries` (schema discovery + `POST /execute`)                                                                                                                                                        |
| `.service.domain`                     | Business logic services                                                                                                                                                                                                                                             |
| `.service.domain.analytics`           | Analytics services, CursorCodec                                                                                                                                                                                                                                     |
| `.service.domain.csv`                 | CSV processing utilities                                                                                                                                                                                                                                            |
| `.service.domain.dto`                 | DTOs with validation (meta entities)                                                                                                                                                                                                                                |
| `.service.domain.dto.analytics`       | Analytics DTOs (BatchWriteRequestDto, CursorPageResponseDto, etc.)                                                                                                                                                                                                  |
| `.service.domain.exception`           | Custom exceptions                                                                                                                                                                                                                                                   |
| `.service.domain.filter`              | Filter parsing & execution                                                                                                                                                                                                                                          |
| `.service.domain.job`                 | Job execution models and orchestration (`TestSuiteEvaluationJob`, `MetricEvaluationWorker`, batch writers); Phase 1 execution itself (`EvaluationWorker`, SSE parsing) lives in `com.epam.aidial.evaluation.runner.job` — see below                              |
| `.service.domain.mapper`              | MapStruct mappers                                                                                                                                                                                                                                                   |
| `.service.domain.sort`                | Sort parsing & execution                                                                                                                                                                                                                                            |
| `.service.infrastructure.health`      | Actuator health indicators                                                                                                                                                                                                                                          |
| `.service.infrastructure.logger`      | Dynamic log level config                                                                                                                                                                                                                                            |
| `.service.infrastructure.transaction` | Transaction aspects                                                                                                                                                                                                                                                 |
| `.utils`                              | Utilities                                                                                                                                                                                                                                                           |
| `.web.controller`                     | REST controllers                                                                                                                                                                                                                                                    |
| `.web.handler`                        | Global exception handler                                                                                                                                                                                                                                            |
| `.web.pagination`                     | PaginationParamResolver (page/size resolution), `@FilterParam` + FilterParamArgumentResolver (repeatable `filter` query param binding without comma-splitting)                                                                                                      |
| `.web.path`                           | WildcardPathResolver — decoded `/**` tail of the matched mapping pattern, for path values containing slashes                                                                                                                                                        |
| `.web.security`                       | JWT/OIDC security                                                                                                                                                                                                                                                   |
| `.web.security.apikey`                | DIAL API-Key authentication (alternative to OIDC/JWT, `oidc` mode only, gated by `config.rest.security.api-key.enabled`): `ApiKeyAuthenticationFilter`, `CoreApiKeyIntrospector` (DIAL Core `GET /v1/user/info`), `ApiKeyAuthorityResolver`, `ApiKeyCache` (Caffeine, SHA-256-hashed key), `ApiKeyAuthenticationToken`. `ApiKeyProperties` itself lives in `.configuration.properties.security` (not here) so `.client.apikey` can depend on it without crossing the `web` layer boundary `LayeredArchitectureTest` enforces. |

### `evaluation-runner-core` subproject (root package `com.epam.aidial.evaluation.runner`)

| Package                                    | Purpose                                                                                                                          |
|---------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------|
| `com.epam.aidial.evaluation.runner`         | Root package of the shared Phase 1 execution engine module                                                                       |
| `.client.dialcore`                          | DIAL Core client (deployments, models, deployment invocation, file operations) — used by Phase 1 execution                        |
| `.client.mcp`                               | MCP SDK client (tool invocation, tool listing via DIAL Core MCP proxy)                                                            |
| `.config`                                   | `EvaluationRunnerAutoConfiguration` (Spring Boot autoconfiguration entry point)                                                   |
| `.config.logging`                           | `LogExecution` annotation + logging config for this module                                                                        |
| `.config.properties`                        | `@ConfigurationProperties` classes for this module (`EvaluationRunProperties`, `DialCoreProperties`, `McpClientProperties`, `JsonataProperties`, etc.) |
| `.dto`                                      | Request/response templates, bindings, endpoint contracts — Phase 1's execution-time DTOs; also the shared REST-layer DTOs moved here to avoid duplication between the main app and `eval-cli` (`OverallScoreDefinition` family, `TestCaseResponseDto`, `TestSuiteCloneRequestDto`, `PageResponseDto`, `TestSuiteResponseDto`, and the full `TestSuiteRunResponseDto` family: `RunConfigDto`/`ExecutionSettingsDto`/`RetryPolicyDto`/`SuiteSnapshotDto`/`DatasetReferenceDto`/`FieldDefinitionDto`/`RunErrorDetailsDto`) |
| `.exception`                                | `ValidationException` (base type), `TypeMismatchException`, `RequestBodyEvaluationException`                                      |
| `.job`                                      | Phase 1 execution: `EvaluationWorker`, `RequestChainExecutor` (orchestrates a suite's request chain — builds the ordered `RequestExecutionSpec` list from the run snapshot, threads one accumulated frame through it, concatenates rows, stops on the first aborting request), the unified `TurnLoopExecutor` (per-request turn loop, single-turn and multi-turn alike; `PerTurnBindingDetector` decides turn count; takes a `RequestExecutionSpec` and returns a `RequestExecutionResult`), `DeploymentTurnInvoker`, SSE parsing (`SseEventParser`, `SseEvent`, `SseParseResult`), `StreamingResponseAccumulator`, `CustomContentAccumulator` (DIAL `custom_content` merge across SSE deltas) |
| `.model`                                    | Pure execution-path models: `TestCaseRunInput`, `TestCaseRunResult`, `TestSuiteRun`, `ExecutionStatus`, `SuiteType`                |
| `.service`                                  | `RequestResolver` (JSONata-powered URL/query/header/body template resolution), `RequestBodyEvaluator`, `JsonataSourcePreprocessor`, `TemplateContentResolver`, `JsonataEvaluationService`/`DashjoinJsonataEvaluationService`, request body serializers (`RequestBodySerializerRegistry` + JSON/multipart/urlencoded strategies), `ResponseColumnExtractor`, `DialCoreUrlBuilder`                                       |
| `.util`                                      | `RunnerJsonbMapper` (read-only subset — `mapRequestTemplate`/`mapInputBindings`), `TokenPropagationHelper`, `AuthorizationTokenHolder`, `ValidationWarningsSerializer`                                                                  |
| `.util.jackson`                              | `HttpMethodSerializer`/`HttpMethodDeserializer` — shared Jackson (de)serializers for Spring's `HttpMethod` (used by `EndpointContractDto`); both the EF backend's and `eval-cli`'s own `JsonMapperConfiguration` register these                                                                  |

### `eval-cli` subproject (root package `com.epam.aidial.evaluation.cli`)

Standalone Spring Boot picocli CLI that consumes `evaluation-runner-core` and the EF backend's public REST API to enable cross-environment evaluation (clone → fetch → run → import). DB-free. Configuration documented in [`eval-cli/README.md`](../eval-cli/README.md).

| Package                                              | Purpose                                                                                                                                                               |
|------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `com.epam.aidial.evaluation.cli`                     | Root package; `EvalCliApplication` (`@SpringBootApplication`, picocli bootstrap, exit-code propagation)                                                               |
| `.client.source`                                     | HTTP clients for the source EF: `TestSuiteApiClient`, `TestCaseApiClient`, `DatasetApiClient` (dataset test-case schema, bound to a minimal local record — not a `DatasetResponseDto` mirror), `TestSuiteRunImportApiClient`, `SourceClientConfiguration`                                  |
| `.client.source.dto`                                 | Only `TestSuiteUpdateResultDto` remains here — a deliberate `{suite}`-only subset of the canonical `runner.dto.TestSuiteUpdateResultDto` (omits `revalidationTask`, which the clone endpoint's contract guarantees is always null). Every other source-EF response DTO (`TestSuiteResponseDto`, `TestCaseResponseDto`, `TestSuiteCloneRequestDto`, `PageResponseDto`, `TestSuiteRunResponseDto`, ...) has been consolidated into `evaluation-runner-core`'s `runner.dto` and is consumed directly — no local mirror |
| `.client.target`                                     | Target DIAL Core wiring: `TargetDialCoreClientConfiguration` (supplies `"dialCoreTryOutRestClient"`/`"dialFileRestClient"`, authenticates via `Api-Key` header read from `TargetProperties`, env var `DIAL_CORE_API_KEY`)          |
| `.command`                                           | Picocli subcommands: `RootCommand`, `CloneCommand`, `FetchCommand`, `RunCommand`, `ImportCommand`, `EvaluateCommand` (the full clone→fetch→run→import chain; named `evaluate` — the domain action — not `pipeline`); shared `--suites`/`--clone-suffix` required-option mixins (`SuitesOption`, `CloneSuffixOption`), composed per command via `@Mixin` — both CLI-only, no configuration/env-var fallback, consistent with `--deployment-id`. `run`/`evaluate` accept only `--deployment-id` (no `--deployment-name`); target host/auth (`DIAL_CORE_URL`, `DIAL_CORE_API_KEY`) have no CLI-flag override, env-var-only |
| `.config`                                            | `ClockConfiguration` (`Clock.systemUTC()` bean); `JsonMapperConfiguration` (this module's own `ObjectMapper`/`JsonMapper` bean — required because `spring.main.web-application-type=none` disables Spring Boot's Jackson autoconfiguration)                                                                                                                       |
| `.config.properties`                                 | `@ConfigurationProperties` classes: `SourceProperties` (`eval.source.*`), `EvalCliProperties` (`cli.*`: `work-dir`/`test-run-name`/nested `run.*` only — suite selection and clone suffix are `--suites`/`--clone-suffix` CLI-only, not config-bound), `TargetProperties` (`dial.components.core.api-key`, env-var-only, shares the `dial.components.core` prefix with `evaluation-runner-core`'s `DialCoreProperties`) |
| `.csv`                                               | `CsvResultBatchWriter` — thread-safe `ResultBatchWriter` implementation writing CSV rows via Apache Commons CSV in the EF import contract column order                 |
| `.exception`                                         | CLI-specific exception types                                                                                                                                          |
| `.model`                                             | `SuiteFetchBundle` — persisted JSON bundle from the `fetch` step (suite config + test cases + bound dataset's test-case schema + destination clone ID; a pre-schema bundle still loads with null schema, and `run` rejects it only when a multi-turn case is present)                                                   |
| `.service`                                           | Orchestration services: `CloneService`, `FetchService`, `RunOrchestrationService`, `ImportService`, `EvaluationContextFactory`, `TestCaseRunInputMapper` (MapStruct), `SuiteContractValidator` (pre-flight `endpointRef`/`requestTemplate` check, called from `RunOrchestrationService.run`)  |

