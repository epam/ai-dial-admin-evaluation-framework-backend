# Tasks — try-out support for multi-request test suites

Groups are sized for the ≤5-tasks / 1-group-per-iteration batch rule. Reference precedents: `openspec/changes/archive/2026-08-12-try-it-out-multi-turn/` (loop-in-service architecture) and `openspec/changes/archive/2026-08-04-add-multi-request-suite/` (chain semantics).

## 1. Planning layer — chain plan in `ResolvedRequestService`

- [x] 1.1 Add a `ChainPlan` result type in `service/domain/ResolvedRequestService.java` (design D3): ordered list of per-request plans, each pairing a `runner.job.RequestExecutionSpec` with that request's `turnDataList` (done: record(s) compile, javadoc explains request-major ordering and per-request turn planning)
- [x] 1.2 Implement `@Transactional(value = "metaTransactionManager", readOnly = true)` `planChain(testSuiteId, testCaseId)`: spec 0 from suite fields with `suite.getRequestName()` threaded into the spec's `name` component, specs 1..N from `jsonbMapper.mapAdditionalRequests(...)` with null bindings/columns normalized to `List.of()`; per-request turn count via `perTurnBindingDetector.referencesPerTurnField(spec.inputBindings(), schema)` with the dataset schema loaded **lazily** — only when the case has readable `multiTurnData` turns, at most once for the whole chain (no unconditional DB read); reuse `mergeSharedAndTurn` (done: mirrors `RequestChainExecutor.buildSpecs` + `TurnLoopExecutor.buildTurnPlan` decisions)
- [x] 1.3 Keep `planTurns` behavior intact for the single-invocation fast path — delegate to `planChain` (request #0's plan) or fold in, implementor's choice per D3 (done: **folded in** per the code review — `planTurns`/`TurnPlan` were dead once `tryWithTestCase` unpacked `planChain` directly, so both were removed and their unit tests migrated to `planChain`)
- [x] 1.4 Add a DB-only variables-mode chain loader (suite fields + additional requests, no test case, no turn planning — every request one turn) for `tryWithVariables` (done: method exists, never touches `TestCaseRepository`)
- [x] 1.5 Run existing + new planning unit tests: `./gradlew test --tests "com.epam.aidial.evaluation.service.domain.ResolvedRequestServiceTest"` (done: green; new tests cover chain spec ordering, per-request turn detection, null-normalization, suite `requestName` → spec `name` threading, lazy schema loading)

## 2. Execution layer — chain runner in `TryItOutService`

- [x] 2.1 Generalize `runTurnSequence` in `service/domain/TryItOutService.java` into a chain runner (design D4): outer request loop over `ChainPlan`, inner turn loop; **accumulate** the frame (`mergeAccumulated` semantics, later key wins) across turns AND requests instead of replacing per turn (done: request i's first turn resolves against everything 0..i-1 extracted)
- [x] 2.2 Use each spec's own `endpointRef().getMethod()` per invocation and implement `validateChainPreconditions(DeploymentReferenceDto, EndpointContractDto, String rawTemplateJson, List<RequestDefinitionDto>)` per design D7: request #0 keeps the existing raw-JSON blank check, chain elements null-check their typed `RequestTemplateDto` + `endpointRef`/`getMethod()`, per-element messages use the `additionalRequests[i]` prefix; called in `TryItOutService` on the already-loaded suite (no extra DB read) BEFORE the planning call so 400-before-404 precedence is preserved (done: misconfigured chain element ⇒ 400 with zero HTTP calls issued, message names the element; misconfigured suite + nonexistent testCaseId ⇒ 400)
- [x] 2.3 Stamp identity fields per history entry and top level per design D1 (`requestIndex`/`totalRequests`/`requestName` only when totalRequests > 1; `turnIndex`/`totalTurns` only when that request planned > 1 turns); capture per-entry `extractedColumns`/`extractionWarnings` by parsing the `ExtractionResult` JSON strings via `objectMapper.readTree(...)` into the DTO's `JsonNode` fields (mandatory explicit-null mechanism, design D6); extraction runs for every successful invocation including the last, but ONLY when the suite defines at least one response column (zero-column suites: extractor never called, fields stay null); fail-fast breaks both loops on non-SUCCESS or `RequestBodyEvaluationException` (done: matches delta-spec stamping + extraction scenarios)
- [x] 2.4 Wire `tryWithTestCase` entry condition per design D5 (chain runner iff plan spans > 1 invocation) and route `tryWithVariables` multi-request suites through the chain runner with `convertVariablesToBindings` applied to every request, chain elements' own `inputBindings` ignored, `resolveForRun(template, bindings, Map.of(), frame)` (design D8; single-request path keeps lenient `resolve`); add fast-path extraction per D5/D6: (a) the test-case fast path takes `responseColumns` from the plan it already fetched, (b) `tryWithVariables` adds `jsonbMapper.mapResponseColumns(suite.getResponseColumns())`, (c) `invokeAndBuildResponse` gains the extraction step (skipped when no columns defined), (d) the MCP path (`invokeMcpAndBuildResponse`) must NOT extract (done: fast paths gain only the additive fields; MCP responses unchanged)
- [x] 2.5 Run `./gradlew test --tests "com.epam.aidial.evaluation.service.domain.TryItOutServiceTest"` (done: green — existing multi-turn tests updated where accumulate-not-replace changes expectations)

## 3. DTO + OpenAPI

- [x] 3.1 Add 7 additive `@JsonInclude(NON_NULL)` fields to `service/domain/dto/TryItOutResponseDto.java` (`requestIndex`, `totalRequests`, `requestName`, `turnIndex`, `totalTurns`, `extractedColumns`, `extractionWarnings`) with `@Schema` descriptions covering the stamping rules; `extractedColumns`/`extractionWarnings` MUST be typed `JsonNode` populated via `objectMapper.readTree(...)` — a `Map<String,Object>` field would drop explicit nulls through `JsonMapperConfiguration`'s NON_NULL content inclusion (design D6); the `extractedColumns` `@Schema` description must state "this invocation's own extraction, not the accumulated frame"; update the `history` field's `@Schema` description to the multi-invocation (turns × requests) contract (done: DTO compiles, descriptions match delta spec)
- [x] 3.2 Add `chained` example files (suffix already whitelisted in `OpenApiExampleCustomizer.EXAMPLE_NAMES`) under `src/main/resources/openapi/examples/`: `api-v1-test-suites-testSuiteId-test-cases-testCaseId-try-it-out-POST-response-200-chained.json` and `api-v1-test-suites-testSuiteId-try-it-out-POST-response-200-chained.json`, showing a 2-request chain with stamps + per-entry `extractedColumns` (done: examples load in Swagger UI without customizer warnings)
- [x] 3.3 Update existing `…-try-it-out-POST-response-200-multi-turn.json` / `…-full.json` examples where the new additive fields now appear (`extractedColumns`/`extractionWarnings`; multi-turn entries gain `turnIndex`/`totalTurns`) (done: examples reflect the new contract per AGENTS.md OpenAPI-examples rule)
- [x] 3.4 Run `./gradlew spotlessApply checkstyleMain` and compile (done: clean)

## 4. Unit tests

- [x] 4.1 Add nested `MultiRequestTryWithTestCase` to `src/test/java/com/epam/aidial/evaluation/service/domain/TryItOutServiceTest.java`: frame threading across requests (request 1 resolves with request 0's extracted columns), per-request HTTP method, fail-fast mid-chain (later requests never invoked; failing entry last in history) (done: tests written and green)
- [x] 4.2 Add stamping + extraction unit tests: `requestIndex`/`totalRequests`/`requestName` only when >1 requests, `turnIndex`/`totalTurns` only per multi-turn request, per-entry + top-level `extractedColumns`/`extractionWarnings`, explicit-null preservation for a failed column extraction — assert the SERIALIZED response body literally contains `"col":null` (not `{}`), and a zero-response-column suite omits both fields entirely (no `{}`/`[]`) (done: green)
- [x] 4.3 Add mixed multi-turn + multi-request case (only request #0 multi-turn) asserting request-major/turn-minor history order and frame accumulation across the turn/request boundary (done: green)
- [x] 4.4 Add chain cases to the `TryWithVariables` nested class (variables applied to every request with chain elements' own `inputBindings` ignored, empty data map, frame from prior responses, fail-fast, single-request path unchanged) plus a regression test pinning accumulate-not-replace within one request's turns per the delta-spec scenario "Frame accumulates instead of being replaced (behavioral fix)" (done: green)
- [x] 4.5 Run `./gradlew test --tests "com.epam.aidial.evaluation.service.domain.TryItOutServiceTest" --tests "com.epam.aidial.evaluation.service.domain.ResolvedRequestServiceTest"` (done: green)

## 5. Functional tests

- [x] 5.1 Add a chain try-out scenario to `src/test/java/com/epam/aidial/evaluation/functional/tests/TryItOutFunctionalTests.java` reusing the fixture style of `MultiRequestChainRunFunctionalTests.createChainSuite` / `AbstractMultiTurnFunctionalTest` helpers: 2-request suite where request #1's body binds a column extracted from request #0's real (stubbed) response (done: test asserts request #1's outgoing body contains the value request #0 returned)
- [x] 5.2 Assert response contract end-to-end: history order, identity stamps present/absent per the delta-spec scenarios, per-entry + top-level `extractedColumns`/`extractionWarnings`, top level == last history entry (done: deterministic assertions, no branching)
- [x] 5.3 Add functional coverage for fail-fast mid-chain (request #0 non-2xx ⇒ one history entry, no second call) and for the variables-mode chain endpoint (done: green)
- [x] 5.4 Add a single-request regression scenario asserting the response is unchanged except additive `extractedColumns`/`extractionWarnings` (no stamps, no history), and that a suite defining no response columns gets a byte-identical response (neither field serialized) (done: green)
- [x] 5.5 Ensure the tests are registered/nested in `PostgresFunctionalTests` and run `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$TryItOutTests"` (done: green, context boots)

## 6. Docs & housekeeping

- [x] 6.1 Add a try-out coverage line to `docs/patterns/multi-request-suites.md` (both endpoints execute the chain; preview `resolved-request` stays empty-frame) (done: pattern doc current)
- [x] 6.2 Verify no `openspec/config.yaml` or `AGENTS.md` update is needed (feature follows existing patterns — no new layer/convention) and no `openspec/specs/README.md` change (no new spec folder; try-it-out summary still accurate — extend it only if the one-liner became materially inaccurate) (done: checked, decision noted in PR description)
- [x] 6.3 Run `./gradlew spotlessApply build` (full build incl. checkstyle + tests) (done: green)
- [ ] 6.4 `/opsx:verify` the change, then sync the delta spec to `openspec/specs/try-it-out/spec.md` at archive time per `openspec/config.yaml` `rules.archive` (done: verify passes; archive checklist items completed)

## 7. Code-review fixes

- [x] 7.1 **F1** Streaming extraction now runs against the run-equivalent assembled document: `TryItOutService.readStream` parses the stream once and hands the `SseParseResult` to `StreamingResponseAccumulator.assemble(...)` (new DB-free assembly seam in `evaluation-runner-core`, `accumulate(InputStream)` = parse + assemble), so an OpenAI-mode stream's `choices[0].message.content` extracts the assembled reply while the DTO keeps its `{"events":[…]}` body + `events` list; delta spec gained the "Streaming try-out extracts the run-equivalent assembled document" requirement (done: 3 new unit tests green)
- [x] 7.2 **F2** A non-`SUCCESS` SSE parse status (`TIMEOUT`/`ERROR`) is now a failed invocation: extraction skipped, chain fail-fast, condition surfaced on the new `TryItOutCoreResponseDto.streamingStatus`/`truncationWarning` fields with the received events kept visible; delta spec scenario + DTO field docs added (done: 2 new unit tests green)
- [x] 7.3 **F3** Dead `ResolvedRequestService.planTurns` + `TurnPlan` removed, their unit tests migrated to `planChain`, stale javadoc and task 1.3's note corrected (done)
- [x] 7.4 **F4** Functional test pinning 400-before-404: broken `additionalRequests[0]` + nonexistent `testCaseId` ⇒ 400 naming the element, zero invocations (done)
- [x] 7.5 **F5** Unit test pinning the `!failed` extraction-skip guard: failing turn with response columns ⇒ null `extractedColumns`/`extractionWarnings` and `verify(extractor, times(1))` (only the successful turn extracted) (done)
- [x] 7.6 **F6** Unit test asserting a single-request multi-turn suite stamps no `requestIndex`/`totalRequests` on entries or top level while still stamping `turnIndex`/`totalTurns` (done)
- [x] 7.7 **N7–N9** Unit tests for the null-`requestTemplate` chain-element precondition, a blank `requestName` not being stamped, and frame accumulation across a **request** boundary (request #1 extracts nothing ⇒ request #2 still sees request #0's column) (done)
- [x] 7.8 **N10–N12** `final` on unchanging new locals; `docs/patterns/multi-request-suites.md` reworded (per-entry `extractedColumns` is that invocation's own extraction, plus the streaming note); `additionalRequests` deserialized once per try-out — `planChain`/`planChainForVariables` now take the caller's already-mapped list (done)
- [x] 7.9 Re-ran `./gradlew :test --tests "…TryItOutServiceTest" --tests "…ResolvedRequestServiceTest"`, `./gradlew :test --tests "…PostgresFunctionalTests\$TryItOutTests"`, a full `./gradlew spotlessApply build`, and `openspec validate add-multi-request-try-out --strict` (done: all green)
