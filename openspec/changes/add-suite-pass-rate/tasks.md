Implement in batches of **max 5 tasks / max 1 task group** per iteration (AGENTS.md Agent Workflow Rules).
A task is not complete until its tests have been **executed** and pass — static review does not count.

## 1. Configuration and context boot

- [x] 1.1 Add `configuration/properties/analytics/PassRateProperties.java` — `@ConfigurationProperties(prefix = "analytics.pass-rate")`, `@Validated`, `@LogExecution` (import `com.epam.aidial.evaluation.runner.config.logging.LogExecution`), `@NotNull @Min(1) Integer defaultLastN`, `@NotNull @Min(1) Integer maxLastN`, **no Java field initializers**; picked up by `@ConfigurationPropertiesScan`, no registration (done: compiles, no default in Java)
- [x] 1.2 Add `analytics.pass-rate.default-last-n: ${ANALYTICS_PASS_RATE_DEFAULT_LAST_N:10}` and `max-last-n: ${ANALYTICS_PASS_RATE_MAX_LAST_N:100}` under the existing `analytics:` block in `src/main/resources/application.yml` (done: the yml is the only place the defaults live)
- [x] 1.3 `docs/configuration.md` — insert `### 6.13 Analytics Pass Rate` after `### 6.12 Analytics Run Comparison`, renumber `6.13 JSONata Evaluation` → `6.14` (header and any references), one intro sentence naming the endpoint, six-column table with two rows (`Required` = `No`, `Applied when` = `-`; description of `max-last-n` says it also bounds the analytics `IN` bind count); also update the Table of Contents (`docs/configuration.md:5-53`) — insert `- [Analytics Pass Rate](#613-analytics-pass-rate)` after the `- [Analytics Run Comparison](#612-analytics-run-comparison)` bullet, and change the JSONata bullet's anchor from `#613-jsonata-evaluation` to `#614-jsonata-evaluation` (done: two rows, six columns, four-term vocabulary, numbering contiguous; TOC bullet present and both TOC and header anchors resolve)
- [x] 1.4 Boot the context to prove the new `@ConfigurationProperties` bean binds: `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$NoSecurityStartupTests"` (done: green)

## 2. Data layer — meta window and analytics aggregate

- [x] 2.1 Add `data/db/model/TestSuiteRunRef.java` record `(UUID id, String status, long createdAtMs)` and `TestSuiteRunRepository.findRecentByTestSuiteId(UUID testSuiteId, int limit)` in `PostgresTestSuiteRunRepository` — `select(ID, STATUS, CREATED_AT_MS)` only (no `suite_snapshot`), `where TEST_SUITE_ID = ?`, `orderBy(CREATED_AT_MS.desc(), ID.desc())`, `limit` (done: returns newest-first ids for a 3-run suite fixture; a repository functional test in 2.5 covers it)
- [x] 2.2 Add `data/db/analytics/model/RunPassRateStats.java` record `(UUID runId, UUID computationId, long failed, long successPassed, long successNotPassed, long successNoVerdict, long total)`; no `RecordMapper` (done: pure carrier)
- [x] 2.3 Add `EvalSummaryRepository.countPassRateByLatestComputation(List<UUID> runIds)` in `PostgresEvalSummaryRepository` — typed jOOQ per `design.md` decision 2: `select(...).distinctOn(TEST_SUITE_RUN_ID)` derived table ordered `TEST_SUITE_RUN_ID, COMPUTED_AT_MS.desc(), COMPUTATION_ID.desc()`, join back on `(run, computation)`, `leftJoin(TEST_CASE_EVAL_SCORES)`, four `count().filterWhere(...)` + `count()`, `groupBy`; empty `runIds` → `List.of()` without a query (done: executes against Postgres; SQL uses no text blocks)
- [x] 2.4 Add `COMPUTATION_ID.desc()` as the second `orderBy` field of `PostgresEvalSummaryRepository.findLatestComputationId` — this is text (lexicographic) ordering of the `VARCHAR(36)` canonical UUID string, not `java.util.UUID.compareTo` order; `ComputationResolverTest` is a pure Mockito test and cannot observe this change, so it is NOT the regression guard. Guard: 2.6(b)'s tie fixture (covers both `countPassRateByLatestComputation` and `findLatestComputationId`) plus the existing functional suites that exercise `findLatestComputationId` end-to-end through `ComputationResolver` — run `./gradlew test --tests "*PostgresFunctionalTests\$PassRateRepositoryTests" --tests "*PostgresFunctionalTests\$EvalSummaryTests" --tests "*PostgresFunctionalTests\$EvalSummaryExportTests" --tests "*PostgresFunctionalTests\$RunComparisonTests"` (done: all green, unmodified assertions in the three existing suites)
- [x] 2.5 Extend `functional/helper/AnalyticsTestDataHelper.java` with `createEvalScore(UUID evalSummaryId, Double score, Boolean passed, long computedAtMs)` (insert via `analyticsDsl` into `TEST_CASE_EVAL_SCORES`) and `cleanupEvalScores()`; call the cleanup wherever `cleanupEvalSummaries()` is called in shared `@BeforeEach`/`@AfterEach` (done: no orphan score rows after the suite; no raw SQL outside the helper)
- [x] 2.6 Add `functional/tests/PassRateRepositoryFunctionalTests.java` + `@Nested class PassRateRepositoryTests` in `PostgresFunctionalTests` — fixtures: (a) one run, two computations with different status mixes → only the newer counted, its id returned; (b) tie on `computed_at_ms` using two computation ids whose canonical 36-character strings differ in their **first hex character** (chosen so `String.compareTo` and `java.util.UUID.compareTo` would disagree) → both `countPassRateByLatestComputation` and `findLatestComputationId` return the id that is greater per `String.compareTo` (assert with `String.compareTo`/`Comparator.comparing(UUID::toString)`, never `UUID.compareTo`); (c) every bucket: FAILED/TIMEOUT/ERROR → `failed`; SUCCESS + `passed=true`/`false`; SUCCESS without score row and SUCCESS with `passed=null` both → `successNoVerdict`; buckets sum to `total`; (d) run id with no summaries → absent from the result; (e) meta `findRecentByTestSuiteId` newest-first + limit — seed via `MetaTestDataHelper.createTestSuiteRun(suiteId, status)` then pin ordering with `MetaTestDataHelper.forceRunCreatedAt(runId, createdAtMs)` using explicit, well-separated values (no timestamped `createTestSuiteRun` overload exists) (done: `./gradlew test --tests "…PostgresFunctionalTests\$PassRateRepositoryTests"` green)

## 3. Service and DTOs

- [x] 3.1 Add public `TestSuiteRunService.findRecentRuns(UUID suiteId, int limit)` — `@Transactional(value = "metaTransactionManager", readOnly = true)`, delegates to `findRecentByTestSuiteId` (done: compiles; used only by 3.3)
- [x] 3.2 Add `service/domain/dto/analytics/SuitePassRateResponseDto.java` (`testSuiteId`, `runs`) and `RunPassRateDto.java` (`testSuiteRunId`, `computationId`, `status`, `runCreatedAtMs`, five counts) — Lombok `@Data @Builder`, `@Schema(example, description)` on every field; descriptions state that `failedCount` includes ERROR rows from metric-evaluation failures and that `successNoVerdictCount` counts every SUCCESS row with no available verdict — no `overallScoreThreshold` on the suite snapshot, a score row not yet written (scores are written after each flush batch of summaries, so a RUNNING run always has a trailing window), or a score write that failed and was swallowed — so a non-zero count on a COMPLETED run with a threshold means missing score rows, not "no threshold configured" (done: DTOs render in Swagger with descriptions)
- [x] 3.3 Add `service/domain/analytics/PassRateService.java` — `@Service @LogExecution`, explicit constructor injecting `TestSuiteService`, `TestSuiteRunService`, `EvalSummaryRepository`, `PassRateProperties`, `@Qualifier("analyticsTransactionManager") PlatformTransactionManager` (build a read-only `TransactionTemplate` in the constructor). `getPassRate(UUID suiteId, Integer lastN)`: resolve default; `> maxLastN` → `ValidationException` naming `analytics.pass-rate.max-last-n`; `testSuiteService.getById` (404); `findRecentRuns`; analytics aggregate inside the template; re-order by meta order via `LinkedHashMap`, drop runs absent from the aggregate, attach `status`/`runCreatedAtMs`. **No** `@Transactional` on the method (done: compiles; behaviour pinned by 3.4)
- [x] 3.4 Add `service/domain/analytics/PassRateServiceTest.java` (Mockito) — default `lastN` applied; `lastN > max` → `ValidationException` before any meta call; unknown suite → `EntityNotFoundException` propagates and no run/analytics call is made; meta ordering preserved when the aggregate returns rows shuffled; runs missing from the aggregate dropped; `status`/`runCreatedAtMs` copied from the meta ref; empty run list → no analytics call and `runs: []` (done: `./gradlew test --tests "*PassRateServiceTest"` green)

## 4. Web layer and OpenAPI

- [x] 4.1 Add `@GetMapping(value = "/test-case-pass-rate/{testSuiteId}", produces = APPLICATION_JSON_VALUE)` to `EvalSummaryController` — `@PathVariable UUID testSuiteId`, `@RequestParam(required = false) @Min(1) Integer lastN` (class already `@Validated`), inject `PassRateService`; `@Operation` description's **first sentence** SHALL state the counting unit — one count per eval-summary row, i.e. per test case × repetition (run_index) × request (request_index) × turn (turn_index), not per test case — followed by window semantics, omitted runs, RUNNING partial counts, and bucket definitions including that `successNoVerdictCount` counts every SUCCESS row with no available verdict (no threshold, score not yet written, or a swallowed score-write failure — not only "no threshold"); explicit `@ApiResponse(responseCode = "200", content = @Content(mediaType = "application/json", schema = @Schema(implementation = SuitePassRateResponseDto.class)))` plus 400/404 responses (done: endpoint answers; `lastN=0` → `400 VALIDATION_ERROR` — assert on response status/error code, since Spring may throw either `ConstraintViolationException` or `HandlerMethodValidationException`)
- [x] 4.2 Add `src/main/resources/openapi/examples/api-v1-analytics-eval-summaries-test-case-pass-rate-testSuiteId-GET-response-200-minimal.json` (one COMPLETED run) and `…-full.json` (a RUNNING run with partial counts and a non-zero `successNoVerdictCount` — its own description notes the suite *has* an `overallScoreThreshold` and the count reflects rows whose score has not been written yet, not a missing threshold — followed by two terminal runs) — **no braces** in the filename (done: both example names appear under the operation in `/v3/api-docs`)
- [x] 4.3 Add `functional/tests/PassRateFunctionalTests.java` + `@Nested class PassRateTests` in `PostgresFunctionalTests` — `TestRestTemplate` via `apiUrl(...)`: 3 runs seeded via `MetaTestDataHelper.createTestSuiteRun(suiteId, status)` then `forceRunCreatedAt(runId, createdAtMs)` with explicit, well-separated timestamps (no timestamped `createTestSuiteRun` overload exists); `lastN=2` → the 2 newest; omitted `lastN` → default; newest run PENDING without summaries → omitted; RUNNING run with summaries → present with `status: RUNNING`; two eval-summary rows for the same test case with `turnIndex` 0 and 1 → `totalCount` 2 for that run (per-turn counting, not collapsed per test case); suite with no runs at all → `200` + `runs: []`; suite that **has** runs but none of them carries eval summaries → `200` + `runs: []` (distinct fixture from the no-runs case); unknown suite → 404 `NOT_FOUND`; `lastN=0` → `400 VALIDATION_ERROR` (assert on response status/error code, not exception type — Spring may throw either `ConstraintViolationException` or `HandlerMethodValidationException`); inject `PassRateProperties` into the test and request `lastN = passRateProperties.getMaxLastN() + 1` → `400 VALIDATION_ERROR` naming the configured `getMaxLastN()` value in the message, proving the bound is read from configuration and not a hardcoded constant; before/after every call above, assert via `MetaTestDataHelper`/`AnalyticsTestDataHelper` row-count helpers (not raw SQL) that no meta or analytics row was created, updated or deleted ("nothing is written"); `/v3/api-docs` carries `minimal` and `full` examples for `/api/v1/analytics/eval-summaries/test-case-pass-rate/{testSuiteId}` GET (done: `./gradlew test --tests "…PostgresFunctionalTests\$PassRateTests"` green)
- [x] 4.4 Run `./gradlew test --tests "*LayeredArchitectureTest" --tests "*LoggingConventionTest"` (done: green, unmodified)

## 5. Docs, specs, quality gates

- [x] 5.1 `docs/patterns/eval-summaries-read-surface.md` — one paragraph under the `score`/`passed` section: the pass-rate endpoint counts rows of each run's **latest** computation via one `DISTINCT ON (test_suite_run_id) … ORDER BY computed_at_ms DESC, computation_id DESC` (text/lexicographic tie-break, not `UUID.compareTo`) derived table, LEFT JOINs the scores, and `passed IS NULL` deliberately merges "no score row" (no threshold, not yet written, or a swallowed write failure) into `successNoVerdictCount`; `docs/patterns/computation-versioning.md` — add the `computation_id DESC` (text ordering) tie-break to the resolution rule; also fix the stale `@LogExecution` import in `AGENTS.md` to `com.epam.aidial.evaluation.runner.config.logging.LogExecution` (the only real usage path) (done: both pattern docs state the same rule as the specs; AGENTS.md import line matches real usage)
- [x] 5.2 Sync delta specs: `openspec/specs/suite-pass-rate/spec.md` created from the change delta with every requirement flipped from `Status: **Planned**` to `Status: **Implemented**`; `openspec/specs/metrics-storage/spec.md` *Computation versioning model* replaced with the MODIFIED block, keeping `Status: **Implemented**` (done: `openspec validate --strict` passes; no `Planned` left in the new spec; every path listed under *Implementation notes* resolves to code that actually exists)
- [x] 5.3 Update openspec/specs/README.md per Spec Index Maintenance Policy (done: index reflects current specs) — new `suite-pass-rate` bullet under `### Analytics` (endpoint path, buckets, latest-computation rule, `analytics.pass-rate.*`, related: metrics-storage, eval-summary-scoring, run-comparison-metric-scores)
- [x] 5.4 `./gradlew spotlessApply checkstyleMain checkstyleTest` then the full `./gradlew test` (done: all green; formatter owns layout)
- [ ] 5.5 `/review` on the diff, then `/opsx:verify`; fix findings; `/opsx:archive` after reading `openspec/config.yaml` `rules.archive` (done: review findings addressed, verify passes)
