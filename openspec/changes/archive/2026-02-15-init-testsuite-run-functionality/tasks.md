## 1. Database & Data Layer

- [x] 1.1 Create Flyway migration `V1.6__CreateTestSuiteRunsTable.sql`: `test_suite_runs` table (id VARCHAR(36) PK, test_suite_id VARCHAR(36) NOT NULL FK→test_suites ON DELETE CASCADE, test_run_name VARCHAR(255) NOT NULL, status VARCHAR(20) NOT NULL, run_config JSONB NOT NULL, number_of_test_cases INTEGER NOT NULL, started_at_ms BIGINT, completed_at_ms BIGINT, error_message TEXT, error_details JSONB, created_at_ms BIGINT NOT NULL, updated_at_ms BIGINT NOT NULL), sequence `test_suite_run_name_seq`, indexes on (test_suite_id), (status), (created_at_ms DESC), UNIQUE constraint on (test_suite_id, test_run_name). Column names use `_ms` suffix for epoch ms columns (project convention). (done: migration applies cleanly, table/indexes/sequence/constraint exist)
- [x] 1.2 Create `RunStatus` enum in `data.db.model` package: PENDING, RUNNING, COMPLETED, FAILED, CANCELLED. Pure enum, no logic. Add `isTerminal(String status)` utility method for status checks. (done: enum compiles, used by model/repository)
- [x] 1.3 Create `TestSuiteRun` model in `data.db.model`: pure data carrier with all table columns as fields (id UUID, testSuiteId UUID, testRunName String, status String, runConfig String, numberOfTestCases int, startedAt Long, completedAt Long, errorMessage String, errorDetails String, createdAt Long, updatedAt Long). Use @Data/@Builder/@NoArgsConstructor/@AllArgsConstructor. Note: Java field names use camelCase without `Ms` suffix (matching TestSuite convention); RowMapper reads from `_ms`-suffixed DB columns. (done: compiles, no logic)
- [x] 1.4 Create `TestSuiteRunRowMapper` in `data.db.mapper`: maps ResultSet to TestSuiteRun. @Component, @LogExecution. Read from `_ms`-suffixed columns (e.g., `rs.getLong("created_at_ms")`), map `rs.getString("id")` via `UUID.fromString()`. Follow existing TestSuiteRowMapper/TestCaseRowMapper pattern. (done: compiles, maps all columns)
- [x] 1.5 Create `TestSuiteRunRepository` interface in `data.db.repository` and `PostgresTestSuiteRunRepository` implementation. Implementation: @Repository, @LogExecution, @RequiredArgsConstructor, @ConditionalOnProperty(name = "datasource.vendor", havingValue = "POSTGRES"). Inject NamedParameterJdbcTemplate, TestSuiteRunRowMapper, TransactionTimestampContext. Methods: `save` (uses TransactionTimestampContext for created_at_ms/updated_at_ms, generates UUID, uses PostgresJsonbSqlParameter.fromJson() for JSONB columns), `findById`, `findAll` (with filtering/sorting/pagination using SQL text blocks), `countByStatuses`, `countByTestSuiteIdAndStatuses`, `updateStatusOptimistic` (for @Transactional callers: `SET status = :newStatus, completed_at_ms = :now, updated_at_ms = :now WHERE id = :id AND status = :expectedStatus` — uses TransactionTimestampContext, returns int), `updateToRunning`/`updateToCompleted`/`updateToFailed`/`updateToCancelled` (for non-transactional async job callers: accept explicit timestamp parameters), `updateTestRunName` (uses TransactionTimestampContext for updated_at_ms), `deleteById`, `failOrphanedRuns` (batch UPDATE for reconciliation — uses TransactionTimestampContext since caller is @Transactional), `nextRunNameSequenceValue` (SELECT nextval). SQL allowlisting for sort/filter fields (including testRunName). (done: all methods compile, SQL uses text blocks, parameterized queries, JSONB params use PostgresJsonbSqlParameter)
- [x] 1.6 Add `countEnabledAndValidByTestSuiteId(UUID testSuiteId)` method to existing `TestCaseRepository` interface and `PostgresTestCaseRepository` implementation: `SELECT COUNT(*) FROM test_cases WHERE test_suite_id = :testSuiteId AND is_enabled = true AND is_valid = true`. Note: this queries the `test_cases` table, so it belongs in TestCaseRepository (not TestSuiteRunRepository). (done: method compiles, returns correct count)

## 2. Configuration & Constants

- [x] 2.1 Create `TestSuiteRunConstants` in `constants` package: MAX_TEST_RUN_NAME_LENGTH (255), MAX_SORT_PARAMS (32), MAX_FILTER_PARAMS (32), plus sort/filter field allowlists (including testRunName). (done: constants referenced by DTOs and repository)
- [x] 2.2 Create `TestSuiteRunProperties` in `configuration.properties`: @ConfigurationProperties(prefix = "test-suite-run") with nested classes for executor (corePoolSize, maxPoolSize, queueCapacity), sse (timeoutMinutes, cleanupIntervalMs), mockJob (minDurationMs, maxDurationMs, failureProbability), runConfig (maxNumberOfRuns), limits (maxConcurrentRunsGlobal, maxConcurrentRunsPerSuite). @Validated with @NotNull/@Min constraints. No Java field initializers — all defaults in application.yml. (done: binds correctly from application.yml)
- [x] 2.3 Add `@EnableAsync` to `Application.java` main class (project convention: all `@Enable*` annotations on bootstrap class). Update existing `AsyncConfiguration` to remove `@EnableAsync`, keep only `@Configuration` + `@LogExecution`. Define `testSuiteRunExecutor` bean (ThreadPoolTaskExecutor) with AbortPolicy, thread prefix "test-suite-run-", pool settings from TestSuiteRunProperties. (done: @EnableAsync on Application, bean created, uses AbortPolicy)
- [x] 2.4 Add `test-suite-run.*` properties to `application.yml`: executor (core-pool-size: 5, max-pool-size: 10, queue-capacity: 50), sse (timeout-minutes: 30, cleanup-interval-ms: 300000), mock-job (min-duration-ms: 0, max-duration-ms: 60000, failure-probability: 0.20), run-config (max-number-of-runs: 64), limits (max-concurrent-runs-global: 20, max-concurrent-runs-per-suite: 5). (done: application starts without errors)
- [x] 2.5 Create `RunErrorCategory` enum in `service.domain.dto`: VALIDATION, TIMEOUT, RESOURCE_LIMIT, TEST_SUITE_ERROR, INTERNAL. (done: enum compiles)

## 3. DTOs, Mappers & Error Handling

- [x] 3.1 Create `RunConfigDto` in `service.domain.dto`: @Data, fields: numberOfRuns (@NotNull, @Min(1)), testRunName (@Size(max = MAX_TEST_RUN_NAME_LENGTH), nullable). Note: configurable max for numberOfRuns is enforced in the service layer (not via @Max annotation) — see task 4.1. (done: compiles with validation annotations)
- [x] 3.2 Create `TestSuiteRunRequestDto` in `service.domain.dto`: @Data, field: runConfig (@NotNull, @Valid RunConfigDto). (done: compiles)
- [x] 3.3 Create `TestSuiteRunResponseDto` in `service.domain.dto`: @Data/@Builder, fields: id (UUID), testSuiteId (UUID), testRunName (String), status (String), runConfig (RunConfigDto), numberOfTestCases (int), startedAt (Long), completedAt (Long), errorMessage (String), errorDetails (RunErrorDetailsDto), createdAt (Long), updatedAt (Long). (done: compiles)
- [x] 3.4 Create `RunErrorDetailsDto` in `service.domain.dto`: @Data/@Builder, fields: code (String), category (String), message (String), details (Map<String, Object>). (done: compiles)
- [x] 3.5 Create `TestSuiteRunUpdateDto` in `service.domain.dto`: @Data, field: testRunName (@NotBlank, @Size(max = MAX_TEST_RUN_NAME_LENGTH)). (done: compiles)
- [x] 3.6 Create `SseStatusEventDto` in `service.domain.dto`: @Data/@Builder, fields: runId (UUID), testSuiteId (UUID), status (String), message (String), timestamp (Long). (done: compiles)
- [x] 3.7 Create `TestSuiteRunMapper` using MapStruct in `service.domain.mapper`: abstract class with `@Mapper(componentModel = MappingConstants.ComponentModel.SPRING)`. Inject ObjectMapper via `@Autowired`. Map TestSuiteRun → TestSuiteRunResponseDto with `@Mapping` + `@Named` methods for JSONB deserialization: `runConfig` (String → RunConfigDto via ObjectMapper), `errorDetails` (String → RunErrorDetailsDto via ObjectMapper). Handle null JSON gracefully (return null). Throw `DataIntegrityException` on deserialization failure (fail-fast per project convention). (done: compiles, mapping tests pass)
- [x] 3.8 Add `TOO_MANY_REQUESTS` and `INVALID_OPERATION` to `ErrorCode` enum. Create `TooManyRunsException` (extends RuntimeException, holds `Map<String, Object> details` field with @Getter) and `InvalidOperationException` (extends RuntimeException) in `service.domain.exception`. Add handlers in `DefaultExceptionHandler`: TooManyRunsException → 429 + TOO_MANY_REQUESTS (include details from exception in ErrorView), InvalidOperationException → 409 + INVALID_OPERATION. (done: exceptions thrown → correct HTTP status + error code + details map populated)

## 4. Service Layer — Core

- [x] 4.1 Create `TestSuiteRunService` in `service.domain`: @Service, @LogExecution, @RequiredArgsConstructor. Inject TestSuiteRunRepository, TestSuiteRepository, TestCaseRepository, TestSuiteRunProperties, TestSuiteEvaluationJob, TestSuiteRunSseService, TestSuiteRunMapper, FilterParser, SortParser. Method `createRun(UUID testSuiteId, RunConfigDto config)` with `@Transactional`: validate test suite exists via `testSuiteRepository.findById()` (throw EntityNotFoundException), validate test suite is in valid state (throw InvalidOperationException if `valid = false`), validate numberOfRuns against configurable max from TestSuiteRunProperties (throw ValidationException if exceeded), check concurrent limits (global + per-suite → throw TooManyRunsException with details map), snapshot numberOfTestCases via `testCaseRepository.countEnabledAndValidByTestSuiteId()`, resolve testRunName (from config or auto-generate via sequence), persist with PENDING status, dispatch async job via afterCommit callback (handle RejectedExecutionException → mark FAILED with RESOURCE_LIMIT), return response DTO. (done: run creation works end-to-end, limits enforced, configurable max validated, suite validity checked)
- [x] 4.2 Implement `TestSuiteRunService.getRun(UUID runId)` with `@Transactional(readOnly = true)`: find by id, map to DTO, throw EntityNotFoundException if not found. (done: returns DTO or 404)
- [x] 4.3 Implement `TestSuiteRunService.listRuns(filters, sort, pageRequest, includeTotalCount)` with `@Transactional(readOnly = true)`: delegate to repository with parsed filter/sort/page, map results to PageResponseDto. Reuse existing FilterParser/SortParser patterns (inject as @Component dependencies). (done: filtering, sorting, pagination work)
- [x] 4.4 Implement `TestSuiteRunService.cancelRun(UUID runId)` with `@Transactional`: validate status (only PENDING/RUNNING allowed, terminal → throw InvalidOperationException). PENDING → optimistic SQL update via `repository.updateStatusOptimistic(runId, CANCELLED, PENDING)` (repository uses TransactionTimestampContext for timestamps), if 0 rows affected (race with async job), re-read and fall through to RUNNING cancellation. RUNNING → interrupt thread via `evaluationJob.interruptRun(runId)`. Notify SSE on successful PENDING cancel. (done: cancel works for PENDING and RUNNING, 409 for terminal, race condition handled)
- [x] 4.5 Implement `TestSuiteRunService.deleteRun(UUID runId)` with `@Transactional`: validate status (only terminal allowed: COMPLETED/FAILED/CANCELLED; PENDING/RUNNING → throw InvalidOperationException). Delete from DB. (done: delete works for terminal, 409 for non-terminal)

## 5. Service Layer — Async Execution, SSE & Reconciliation

- [x] 5.1 Implement `TestSuiteRunService.updateRunName(UUID runId, String newName)` with `@Transactional`: find run, update testRunName via `repository.updateTestRunName()` (repository uses TransactionTimestampContext for updated_at_ms), return updated DTO. Handle UniqueConstraintViolationException for duplicate names (use UniqueConstraintViolationDetector.rethrowIfUniqueViolation pattern). (done: PATCH updates name, 409 on duplicate)
- [x] 5.2 Create `TestSuiteEvaluationJob` in `service.domain.job`: @Component, @LogExecution, @RequiredArgsConstructor. Inject TestSuiteRunRepository, TestSuiteRunSseService, TestSuiteRunProperties (NOT TestSuiteRunService — avoids circular dependency). Own `ConcurrentHashMap<UUID, Thread> activeRunThreads` field. Expose `interruptRun(UUID runId)` method for service cancel. Method `executeRunAsync(UUID runId)` annotated with `@Async("testSuiteRunExecutor")`. NO `@Transactional` on async method — call repository methods that accept explicit timestamp parameters (System.currentTimeMillis()). Register thread in activeRunThreads on entry, always remove in finally block. Set RUNNING (via `repository.updateToRunning`) + notify SSE, sleep random duration (configurable min/max from properties, poll interruption every 500ms), random failure (configurable probability from properties), set COMPLETED/FAILED (via `repository.updateToCompleted`/`updateToFailed`) + notify SSE. Handle InterruptedException → CANCELLED (via `repository.updateToCancelled`). Handle unexpected exceptions → FAILED with INTERNAL category (intentional broad catch as safety net to prevent runs stuck in RUNNING — document rationale in code comment). (done: mock job runs, transitions statuses, handles cancellation, thread cleanup in finally)
- [x] 5.3 Create `TestSuiteRunSseService` in `service.domain`: @Component, @LogExecution, @RequiredArgsConstructor. Manage ConcurrentHashMap<String, SseEmitterWrapper>. Methods: createEmitter(filter criteria) → SseEmitter, notifyStatusUpdate(TestSuiteRun) → iterate emitters + filter + send, removeEmitter, @Scheduled cleanupStaleEmitters (heartbeat ping, remove broken). SseEmitterWrapper holds emitter + filter (runIds, testSuiteIds, statuses). (done: SSE connections created, status updates sent to matching clients, stale emitters cleaned)
- [x] 5.4 Create `TestSuiteRunReconciliation` in `service.domain`: @Component, @LogExecution, @RequiredArgsConstructor. `@Transactional` + `@EventListener(ApplicationReadyEvent.class)` method: call `repository.failOrphanedRuns` for PENDING+RUNNING → FAILED with ORPHANED_RUN code + INTERNAL category. @Transactional ensures atomic batch update and enables TransactionTimestampContext for timestamps in the repository. Log count at INFO (if >0) or DEBUG (if 0). (done: orphaned runs marked FAILED on startup, verified by test)

## 6. Web Layer

- [x] 6.1 Create `TestSuiteRunController` in `web.controller`: @RestController, @LogExecution, @Validated, @RequiredArgsConstructor, @Tag("Test Suite Runs"). Inject TestSuiteRunService, PaginationParamResolver. Endpoints: POST /api/v1/test-suites/{testSuiteId}/runs (202 Accepted), GET /api/v1/test-suite-runs (paginated list), GET /api/v1/test-suite-runs/{id} (single run), PATCH /api/v1/test-suite-runs/{id} (update name), POST /api/v1/test-suite-runs/{id}/cancel (200), DELETE /api/v1/test-suite-runs/{id} (204). Add @Operation, @ApiResponse, @Parameter annotations. Use PaginationParamResolver for page/size. Declare sort/filter as `@RequestParam(required = false) @Size(max = ...) List<String>` (following TestSuiteController pattern). Include `@RequestParam(required = false) Boolean includeTotalCount` parameter on list endpoint. (done: all endpoints respond correctly, OpenAPI docs render)
- [x] 6.2 Create `TestSuiteRunSseController` in `web.controller`: @RestController, @LogExecution, @RequiredArgsConstructor. Endpoint: GET /api/v1/test-suite-runs/status-stream (produces MediaType.TEXT_EVENT_STREAM_VALUE). Query params: runIds, testSuiteIds, statuses (all optional, comma-separated). Returns SseEmitter. Parse filter params, delegate to SseService. (done: SSE endpoint connects, streams events, filters work)

## 7. Tests

- [x] 7.1 Create unit tests for `TestSuiteRunMapper`: test model→DTO mapping, JSONB deserialization of runConfig and errorDetails, null handling for optional JSONB fields. (done: tests pass)
- [x] 7.2 Create functional tests for run creation: shouldCreateRun, shouldReturn404_whenTestSuiteNotFound, shouldReturn400_whenInvalidConfig, shouldReturn400_whenNumberOfRunsExceedsConfigMax, shouldReturn429_whenGlobalLimitExceeded, shouldReturn429_whenPerSuiteLimitExceeded, shouldReturn409_whenDuplicateTestRunName, shouldReturn409_whenTestSuiteNotValid. (done: all tests pass)
- [x] 7.3 Create functional tests for run CRUD: shouldGetRunById, shouldReturn404_whenRunNotFound, shouldListRuns, shouldListRuns_withFilters, shouldListRuns_withSorting, shouldListRuns_filterByTestRunName, shouldListRuns_withIncludeTotalCount, shouldUpdateTestRunName, shouldReturn409_whenUpdateDuplicateTestRunName, shouldDeleteTerminalRun, shouldReturn409_whenDeleteNonTerminal. (done: all tests pass)
- [x] 7.4 Create functional tests for cancel: shouldCancelPendingRun, shouldCancelRunningRun, shouldReturn409_whenCancelTerminalRun. Use polling pattern (like existing revalidation tests) to await async status transitions. (done: all tests pass)
- [x] 7.5 Create functional test for reconciliation: shouldMarkOrphanedRunsAsFailed_onStartup. Insert PENDING/RUNNING runs before app context refresh, verify they are FAILED after startup. (done: test passes)
- [x] 7.6 Create functional tests for SSE: shouldConnectToStatusStream, shouldReceiveStatusUpdateEvent, shouldFilterByRunIds, shouldFilterByTestSuiteIds, shouldFilterByStatuses. (done: SSE endpoint tests pass)

## 8. Documentation & Specs

- [x] 8.1 Update `docs/database-schema.md`: add test_suite_runs table schema with `_ms`-suffixed columns, indexes, UNIQUE constraint, sequence, JSONB structures for run_config and error_details. (done: schema doc matches actual migration)
- [x] 8.2 Update `docs/configuration.md`: add all `test-suite-run.*` properties with descriptions and defaults. (done: config doc matches application.yml)
- [x] 8.3 Update `openspec/specs/README.md` per Spec Index Maintenance Policy: add test-suite-runs spec entry with status Partial/Implemented. (done: README lists new spec folder)
- [x] 8.4 Run `./gradlew checkstyleMain checkstyleTest` and fix any violations. (done: checkstyle passes)
- [x] 8.5 Run `./gradlew clean build` to verify full build (compile + checkstyle + tests). (done: build succeeds)
