## 1. DB Migration

- [x] 1.1 Create `src/main/resources/db/migration/meta/POSTGRES/V1.16__AddTsmdValidationAndEnabledColumns.sql`: `ALTER TABLE test_suite_metric_definitions ADD COLUMN is_enabled BOOLEAN NOT NULL DEFAULT TRUE, ADD COLUMN is_valid BOOLEAN NOT NULL DEFAULT TRUE, ADD COLUMN validation_warnings JSONB NOT NULL DEFAULT '[]'` (done: migration file exists and applies cleanly in Testcontainers)
- [x] 1.2 Update `docs/database-schema.md` to add `is_enabled`, `is_valid`, `validation_warnings` columns to the `test_suite_metric_definitions` table documentation (done: table entry reflects all three new columns)

## 2. Domain DTOs and Validation Code

- [x] 2.1 Add `UNRESOLVED_REFERENCE` entry to `ValidationWarningCode` enum with description `"Reference to a column or resource that does not exist"` (done: enum compiles and serializes correctly)
- [x] 2.2 Remove `@NotNull` from `ConstantBindingSourceDto.value`; update `@Schema` description to note that `null` is accepted as stored state and triggers a soft validation warning on required properties (done: null value is accepted by Jackson deserialization, `@NotNull` removed)

## 3. Repository Layer

- [x] 3.1 Add `findByIdAndMetricDeclarationId(UUID id, UUID metricDeclarationId): Optional<MetricDeclarationVersion>` to `MetricDeclarationVersionRepository` interface (done: method signature compiles)
- [x] 3.2 Implement `PostgresMetricDeclarationVersionRepository.findByIdAndMetricDeclarationId()` with a `SELECT * FROM metric_declaration_versions WHERE id = :id AND metric_declaration_id = :metricDeclarationId` query (done: returns full entity including `configSchema`, `inputSchema`, `outputSchema`)
- [x] 3.3 Add `is_enabled`, `is_valid`, `validation_warnings` to `INSERT_SQL` and `UPDATE_SQL` constants in `PostgresTestSuiteMetricDefinitionRepository`; add the three columns to all SELECT queries (`SELECT_BY_ID_SQL`, `SELECT_BY_ID_AND_SUITE_SQL`, `SELECT_ALL_BASE_SQL`, `SELECT_AGGREGATED_BY_ID_AND_SUITE_SQL`, `SELECT_ALL_AGGREGATED_BY_SUITE_SQL`) (done: INSERT/UPDATE/SELECT SQLs include all three new columns)
- [x] 3.4 Add `updateValidation(UUID id, boolean valid, String warningsJson)` to `TestSuiteMetricDefinitionRepository` interface and implement in `PostgresTestSuiteMetricDefinitionRepository` with `UPDATE test_suite_metric_definitions SET is_valid = :valid, validation_warnings = :warnings, updated_at_ms = :updatedAt WHERE id = :id`; annotate the implementation method with `@Transactional("metaTransactionManager")` consistent with other write methods in this repository (done: method exists and updates correctly)
- [x] 3.5 Add `findAllEnabledAndValidAggregatedByTestSuiteId(UUID testSuiteId): List<AggregatedMetricDefinition>` to `TestSuiteMetricDefinitionRepository` interface and implement in `PostgresTestSuiteMetricDefinitionRepository` by extending `SELECT_ALL_AGGREGATED_BY_SUITE_SQL` with `AND md.is_enabled = true AND md.is_valid = true` (done: method returns only enabled+valid TSMDs)

## 4. Domain Models and Row Mappers

- [x] 4.1 Add `boolean enabled`, `boolean valid`, `String validationWarnings` fields to `TestSuiteMetricDefinition` model (done: model compiles with new fields)
- [x] 4.2 Add `boolean enabled`, `boolean valid`, `String validationWarnings` fields to `AggregatedMetricDefinition` model (done: model compiles with new fields)
- [x] 4.3 Update `TestSuiteMetricDefinitionRowMapper` to map `is_enabled → enabled`, `is_valid → valid`, `validation_warnings → validationWarnings` from `ResultSet` (done: mapper compiles and maps all three columns)
- [x] 4.4 Update `AggregatedMetricDefinitionRowMapper` to map `is_enabled → enabled`, `is_valid → valid`, `validation_warnings → validationWarnings` from `ResultSet` (done: mapper compiles and maps all three columns)

## 5. New Validation Service

- [x] 5.1 Create `MetricDefinitionValidationService` in `service.domain`: annotate with `@Service`, `@LogExecution`, `@RequiredArgsConstructor`; inject `ObjectMapper` and `ValidationWarningsSerializer` (done: class compiles with correct annotations)
- [x] 5.2 Implement `validate(List<MetricParameterBindingDto> configBindings, List<MetricParameterBindingDto> inputBindings, String configSchemaJson, String inputSchemaJson, String testCaseSchemaJson, String responseColumnsJson): ValidationResult` — parse schemas via `ObjectMapper.readTree()`, extract `properties` keys and `required` array from each metric schema, collect test case field names and response column names, apply all five validation checks (ADDITIONAL, REQUIRED-null-constant, UNRESOLVED_REFERENCE-testcase, UNRESOLVED_REFERENCE-response, REQUIRED-no-binding), return `ValidationResult` (done: method compiles and passes all checks correctly)
- [x] 5.3 Write unit tests for `MetricDefinitionValidationService` covering: all-valid bindings, TestCase column ref unresolved, Response column ref unresolved, required property missing, required property bound to null constant, optional property bound to null constant (no warning), binding targets unknown property (ADDITIONAL), schema without "properties" key (graceful degradation) (done: all scenarios produce expected warnings/valid state)

## 6. Service Layer Updates

- [x] 6.1 Update `TestSuiteMetricDefinitionService.create()`: replace the `validateTestSuiteExists()` call (which uses `existsById`) with `testSuiteRepository.findById(testSuiteId).orElseThrow()` to obtain the full `TestSuite` object; replace the `validateVersionBelongsToDeclaration()` call (which uses `existsByIdAndMetricDeclarationId`) with `metricDeclarationVersionRepository.findByIdAndMetricDeclarationId(versionId, declarationId).orElseThrow()` to obtain the full `MetricDeclarationVersion` object; pass `suite.getTestCaseSchema()`, `suite.getResponseColumns()`, `version.getConfigSchema()`, `version.getInputSchema()` as arguments to `metricDefinitionValidationService.validate()`; add `checkNoDuplicateProperties()` call for `configBindings` and `inputBindings` (throw `ValidationException` on duplicate); set `entity.setValid(result.isValid())`, `entity.setEnabled(dto.isEnabled())`, `entity.setValidationWarnings(warningsSerializer.serializeWarnings(result.getWarnings()))` before `repository.save()` (done: create flow compiles and returns `valid`, `enabled`, `validationWarnings` in response)
- [x] 6.2 Update `TestSuiteMetricDefinitionService.update()`: replace the `validateTestSuiteExists()` call with `testSuiteRepository.findById(testSuiteId).orElseThrow()` to obtain the full `TestSuite` object; replace the `validateVersionBelongsToDeclaration()` call with `metricDeclarationVersionRepository.findByIdAndMetricDeclarationId(versionId, declarationId).orElseThrow()` to obtain the full `MetricDeclarationVersion` object; pass `suite.getTestCaseSchema()`, `suite.getResponseColumns()`, `version.getConfigSchema()`, `version.getInputSchema()` as arguments to `metricDefinitionValidationService.validate()`; add duplicate-binding check; recompute validation before `repository.update()` (done: update flow compiles and recomputes validation state)
- [x] 6.3 Inject `MetricDefinitionValidationService`, `ValidationWarningsSerializer`, and `TestSuiteRepository` (already present as `testSuiteRepository`) into `TestSuiteMetricDefinitionService` (done: service compiles with all new dependencies)

## 7. DTO and Mapper Updates

- [x] 7.1 Add `enabled` (boolean, default `true`) to `TestSuiteMetricDefinitionRequestDto` with `@Schema(example = "true")` (done: field present and defaults to true)
- [x] 7.2 Add `enabled` (boolean), `valid` (boolean), `validationWarnings` (List<ValidationWarningDto>) to `TestSuiteMetricDefinitionResponseDto` with `@Schema` annotations; do NOT add `@JsonInclude(NON_NULL)` to `validationWarnings` — the field MUST always be serialized as `[]` (empty array) when there are no warnings, never as null; ensure the mapper always initializes the field to an empty list rather than null (done: response DTO includes all three fields and validationWarnings is always present)
- [x] 7.3 Update `TestSuiteMetricDefinitionMapper.toEntity()` to map `dto.isEnabled() → entity.enabled`; map `valid` and `validationWarnings` from the service-computed result (done: entity builder sets all three fields)
- [x] 7.4 Update `TestSuiteMetricDefinitionMapper.toDto()` to map `entity.enabled → dto.enabled`, `entity.valid → dto.valid`, `entity.validationWarnings → dto.validationWarnings` (deserializing via `ValidationWarningsSerializer`) (done: DTO includes all three fields)
- [x] 7.5 Update `TestSuiteMetricDefinitionMapper.update()` to copy `dto.enabled → entity.enabled` so the enabled flag is updated on PUT (done: mapper compiles and copies enabled)
- [x] 7.6 Update OpenAPI examples for TSMD create/get/list/update to include `enabled`, `valid`, `validationWarnings` fields in the example bodies (done: Swagger UI shows correct examples)

## 8. Revalidation and Evaluation Job Updates

- [x] 8.1 Add `revalidateAllForSuite(UUID suiteId, String testCaseSchemaJson, String responseColumnsJson): void` to `TestSuiteMetricDefinitionService`: load all TSMDs via `testSuiteMetricDefinitionRepository.findAllAggregatedByTestSuiteId(suiteId)`, call `metricDefinitionValidationService.validate()` for each using `versionConfigSchema` / `versionInputSchema` from the aggregated result as metric schemas, persist via `testSuiteMetricDefinitionRepository.updateValidation()`; annotate with `@Transactional("metaTransactionManager")` (done: method compiles and revalidates all TSMDs for the suite)
- [x] 8.2 Update `TestSuiteService.update()`: add private `isTsmdSchemaChanged(TestSuite existing, TestSuiteRequestDto normalized)` helper that returns `true` when `testCaseSchema` OR `responseColumns` changed — use the existing `jsonEquals()` utility; call `testSuiteMetricDefinitionService.revalidateAllForSuite(id, updated.getTestCaseSchema(), updated.getResponseColumns())` synchronously when `isTsmdSchemaChanged()` returns true (call it after `testSuiteRepository.save()` within the same transaction); inject `TestSuiteMetricDefinitionService` into `TestSuiteService` (done: TSMD validation state is refreshed inline on every suite update that touches schema or response columns)
- [x] 8.3 Update `RevalidationService.runRevalidationAsync()`: before the paginated test-case loop, call `testSuiteMetricDefinitionService.revalidateAllForSuite(testSuiteId, suite.getTestCaseSchema(), suite.getResponseColumns())` — the `TestSuite suite` object is already fetched earlier in `runRevalidationAsync()`, so no additional `testSuiteRepository.findById()` call is needed (done: TSMDs are also revalidated when the manual revalidation endpoint is triggered)
- [x] 8.4 Inject `TestSuiteMetricDefinitionService` and `Clock clock` into `RevalidationService`; replace the three existing `System.currentTimeMillis()` calls in `RevalidationService` with `clock.millis()` to comply with the AGENTS.md convention against direct clock calls in production code (`Clock` bean is provided by `ClockConfiguration`) (done: service compiles with new dependencies and uses injected clock)
- [x] 8.5 Add `findAllEnabledAndValidAggregatedByTestSuiteId(UUID testSuiteId): List<AggregatedMetricDefinition>` to `TestSuiteMetricDefinitionService` delegating to the new repository method; annotate with `@Transactional(value = "metaTransactionManager", readOnly = true)` (done: method exists and compiles)
- [x] 8.6 Update `TestSuiteEvaluationJob.buildMetricEvaluationContext()` to call `testSuiteMetricDefinitionService.findAllEnabledAndValidAggregatedByTestSuiteId(run.getTestSuiteId())` instead of `findAllAggregatedByTestSuiteId()` (done: metric phase context loads only enabled+valid TSMDs)

## 9. Functional Tests

- [x] 9.1 Update `TestSuiteMetricDefinitionFunctionalTests` to assert `enabled`, `valid`, `validationWarnings` are present in create/get/update responses with correct default values (done: existing tests updated, compile and pass)
- [x] 9.2 Add functional test: create TSMD with `TestCase` binding referencing non-existent column → `valid = false`, `UNRESOLVED_REFERENCE` warning; use `MetricDeclarationTestDataHelper` to insert a metric declaration version with a non-trivial schema (containing `"properties"` and `"required"` arrays) — do NOT use the default `'{}'` seed schema or the test will pass vacuously (done: test passes)
- [x] 9.3 Add functional test: create TSMD with `Response` binding referencing non-existent response column → `valid = false`, `UNRESOLVED_REFERENCE` warning; use `MetricDeclarationTestDataHelper` to insert a metric declaration version with a non-trivial schema (containing `"properties"` and `"required"` arrays) — do NOT use the default `'{}'` seed schema or the test will pass vacuously (done: test passes)
- [x] 9.4 Add functional test: create TSMD with required metric property bound to `null` constant → `valid = false`, `REQUIRED` warning; use `MetricDeclarationTestDataHelper` to insert a metric declaration version with a non-trivial schema (containing `"properties"` and `"required"` arrays) — do NOT use the default `'{}'` seed schema or the test will pass vacuously (done: test passes)
- [x] 9.5 Add functional test: create TSMD with duplicate `property` in `configBindings` → HTTP 400 `VALIDATION_ERROR` (done: test passes)
- [x] 9.6 Add functional test: create TSMD with `enabled = false`, verify response and that revalidation does not change `enabled` (done: test passes)
- [x] 9.7 Add functional test: manual revalidation corrects stale default validation state — use `MetaTestDataHelper` to insert a TSMD row directly with `is_valid = true` but a `TestCase` binding referencing a column that does not exist in the suite's `testCaseSchema`; call `POST /api/v1/test-suites/{id}/revalidation`; assert the TSMD is updated to `is_valid = false` with an `UNRESOLVED_REFERENCE` warning (done: test passes)
- [x] 9.8 Add functional test: create TSMD with `Constant` binding where `value = null` for optional property → `valid = true` (done: test passes)
- [x] 9.9 Add functional test: all TSMDs are disabled (`is_enabled = false`) or invalid (`is_valid = false`) → metric evaluation phase produces no EvalSummary records for the run (done: test passes)
- [x] 9.10 Add functional test: suite update that removes a `responseColumn` referenced by a TSMD `Response` binding auto-revalidates TSMDs synchronously — assert TSMD is `valid = false` immediately after the suite PUT response, without calling the manual revalidation endpoint (done: test passes)
- [x] 9.11 Add functional test: suite update that removes a `testCaseSchema` column referenced by a TSMD `TestCase` binding auto-revalidates TSMDs synchronously — assert TSMD is `valid = false` immediately after the suite PUT response, without calling the manual revalidation endpoint (done: test passes)
- [x] 9.12 Run `./gradlew checkstyleMain checkstyleTest` and fix any violations introduced by this change (done: no Checkstyle errors)

## 10. Spec Sync

- [x] 10.1 Update `openspec/specs/test-suite-metric-definitions/spec.md` via delta sync to reflect new fields and requirements (done: main spec reflects all changes from this change's delta spec)
- [x] 10.2 Update `openspec/specs/metric-evaluation/spec.md` via delta sync to reflect `findAllEnabledAndValidAggregatedByTestSuiteId` and all-TSMDs-disabled scenario (done: main spec reflects changes)
- [x] 10.3 Create `openspec/specs/tsmd-validation/spec.md` from this change's new capability spec (done: new spec folder exists in openspec/specs/)
- [x] 10.4 Update `openspec/specs/README.md`: add `tsmd-validation` entry in the "Core Domain" section after `test-suite-metric-definitions`; update `test-suite-metric-definitions` summary to mention `enabled` / validation state (done: index reflects all new and updated specs)
