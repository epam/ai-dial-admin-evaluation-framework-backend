## 1. Data Layer — Batch Insert Methods

- [x] 1.1 Add `batchInsert(List<TestCase> testCases, long timestamp)` to `TestCaseRepository` interface and `PostgresTestCaseRepository` implementation — uses JDBC batch insert with `NamedParameterJdbcTemplate.batchUpdate()`, follows existing `batchUpdate()` pattern; accepts explicit `timestamp` parameter for `created_at_ms`/`updated_at_ms` and does NOT call `TransactionTimestampContext.getTimestamp()`; no `ON CONFLICT` clause needed — target suite is newly created and has no pre-existing test cases; name uniqueness within a suite is guaranteed by the source (done: method exists, inserts N test cases in single JDBC round-trip)
- [x] 1.2 Add `batchInsert(List<TestSuiteMetricDefinition> tsmds, long timestamp)` to `TestSuiteMetricDefinitionRepository` interface and `PostgresTestSuiteMetricDefinitionRepository` implementation — same JDBC batch pattern; accepts explicit `timestamp` parameter for `created_at_ms`/`updated_at_ms` and does NOT call `TransactionTimestampContext.getTimestamp()` (done: method exists, inserts N TSMDs in single round-trip)
- [x] 1.3 Add `createWithId(TestSuite entity, long timestamp)` to `TestSuiteRepository` interface and `PostgresTestSuiteRepository` implementation — performs INSERT with a caller-supplied UUID; does NOT generate a new UUID (the private `create()` method called by `save()` unconditionally calls `UUID.randomUUID()`, and `save()` routes to `update()` when `id != null` — so neither path can insert with a caller-supplied UUID; a dedicated method is required); accepts explicit `timestamp` parameter for `created_at_ms` and `updated_at_ms` and does NOT call `TransactionTimestampContext.getTimestamp()` (done: method exists, inserts row with supplied UUID)
- [x] 1.4 Add `findBatchByTestSuiteId(UUID testSuiteId, int offset, int limit)` to `TestSuiteMetricDefinitionRepository` interface and `PostgresTestSuiteMetricDefinitionRepository` implementation — `SELECT ... WHERE test_suite_id = :suiteId ORDER BY created_at_ms ASC LIMIT :limit OFFSET :offset`, no filter/sort overhead; the SQL MUST include a JOIN (inner join) on `metric_declarations` to populate `metric_declaration_name` (required by `TestSuiteMetricDefinitionRowMapper` which unconditionally reads this column — omitting the JOIN causes `SQLException: Column 'metric_declaration_name' not found`); use the existing `TestSuiteMetricDefinitionRowMapper` with the INNER JOIN, consistent with the existing INNER JOIN pattern in other list queries; **Note:** if a TSMD references a deleted metric_declaration (no matching row in metric_declarations), the INNER JOIN silently skips that TSMD row — the paginated loop in task 4.3 MUST terminate when the returned batch is empty, not when count equals batchSize, to handle cases where fewer rows are returned due to JOIN exclusions (done: method exists, returns correct batch for given offset/limit, RowMapper does not throw)

## 2. DTO and Mapper

- [x] 2.1 Create `TestSuiteCloneRequestDto` in `service.domain.dto` — `@NotBlank @Size(max = 255) name`, all other suite-level fields optional/nullable, `@LogExecution` not needed (DTO, not component); optional override fields that have size constraints in `TestSuiteRequestDto` MUST carry over the same constraints (e.g., `description: @Size(max=2000)`, `responseColumns: @Size(max=50)`) — do not omit validation annotations just because the field is optional (done: DTO compiles, name is required, all others nullable, size constraints present on overridable fields)
- [x] 2.2 Add clone-related mapping methods to `TestSuiteMapper` — (a) method that takes source `TestSuite` entity + `TestSuiteCloneRequestDto` + `newId` + `createdBy` and produces a new `TestSuite` entity with overrides applied, `version=0`, file refs rewritten in suite-level JSONB fields (`inputBindings`, `requestTemplate`, `argumentTemplate`) using `String.replace("@ef/suites/" + sourceId + "/", "@ef/suites/" + newId + "/")`; the mapper MUST NOT force `isValid=false` or `validationWarnings=[]` — those values are set by the synchronous suite-level validation step in the clone service (task 4.4), not by the mapper; (b) also add a `toRequestDto(TestSuite entity)` method to `TestSuiteMapper` that converts a `TestSuite` entity to a `TestSuiteRequestDto` by mapping all entity fields to their DTO equivalents (needed by the clone service for synchronous suite validation in task 4.4); the mapping MUST include `suiteType` — `SuiteValidationService.validateSuite()` branches on `dto.getSuiteType()` to call the correct type-specific validator; if `suiteType` is null in the DTO, the service defaults to `DEPLOYMENT` and an `MCP_TOOL` suite would be validated with the wrong rules; the mapping MUST also reverse-map all JSONB-backed fields through `JsonbMapper`: `deploymentRef`, `endpointRef`, `requestTemplate`, `inputBindings`, `responseColumns`, `testCaseSchema`, `mcpDeploymentRef`, `toolRef`, `argumentTemplate` — for any of these that are null in the entity, the DTO field must be null (not NPE); if any JSONB-backed field that `validateSuiteTypeFields` reads (e.g., `deploymentRef` for DEPLOYMENT type) is null due to missing reverse mapping, the hard validator will reject a valid clone with HTTP 400; cross-reference `TestSuiteMapper.toDto()` as the template for how each JSONB field is deserialized — the same `JsonbMapper` method used in `toDto()` for each field MUST be used in `toRequestDto()` (e.g., `deploymentRef` → `jsonbMapper.map(entity.getDeploymentRef())` returning `DeploymentReferenceDto`, `endpointRef` → `jsonbMapper.mapEndpointContract(entity.getEndpointRef())`, `requestTemplate` → `jsonbMapper.mapRequestTemplate(entity.getRequestTemplate())`, `inputBindings` → `jsonbMapper.mapInputBindings(entity.getInputBindings())`, `responseColumns` → `jsonbMapper.mapResponseColumns(entity.getResponseColumns())`, `testCaseSchema` → `jsonbMapper.mapFieldDefinitions(entity.getTestCaseSchema())`, `mcpDeploymentRef` → `jsonbMapper.mapMcpDeploymentRef(entity.getMcpDeploymentRef())`, `toolRef` → `jsonbMapper.mapToolRef(entity.getToolRef())`, `argumentTemplate` → `jsonbMapper.mapArgumentTemplate(entity.getArgumentTemplate())`); do NOT introduce new deserialization logic — mirror exactly what `toDto()` does for each shared field (done: mapper methods exist, null DTO fields inherit from source, file refs rewritten in suite-level fields only, toRequestDto produces a valid TestSuiteRequestDto from entity, suiteType is mapped correctly)

## 3. File Service — Copy Between Suites

- [x] 3.1 Add `copyFilesBetweenSuites(UUID sourceId, UUID targetId)` to `FileService` — lists source folder, downloads each file, uploads to target folder, skips missing files with warning log, returns list of successfully copied filenames; MUST NOT call `validateTestSuiteExists(targetId)` — the target suite does not exist in DB yet at call time (file copy happens before the DB transaction); `sourceId` validation is already handled by the clone service's 404 check before this method is called; the upload path for each copied file MUST be constructed using `buildSuiteFolderPath(targetId) + filename` (i.e., `{realBucket}/suites/{targetId}/{filename}`), NOT via `resolveToRealPath` on a `@ef` ref — since the target suite does not exist in DB, any path that goes through suite existence validation would fail; the upload method path must be constructed directly from the target UUID (done: method exists, handles missing files gracefully, does not validate targetId against DB)
- [x] 3.2 Extract hard-validation methods from `TestSuiteService` into a new `@Component` named `TestSuiteRequestValidator` in `service.domain` — extract `validateSuiteTypeFields(TestSuiteRequestDto dto)`, `validateTestSuiteSchemas(TestSuiteRequestDto dto)`, and `validateTemplateLimits(TestSuiteRequestDto dto)` as public methods on `TestSuiteRequestValidator`; inject `TestSuiteRequestValidator` into `TestSuiteService` (replacing the private method calls with delegation) and into `TestSuiteCloneService` (task 4.1); `TestSuiteRequestValidator` must carry `@LogExecution` at class level; inject all the same dependencies these private methods currently use (`endpointSchemaRefResolver`, `jsonataEvaluationService`, `schemaValidationService`, `jsonbMapper`, `objectMapper`, `validationProperties`) via constructor injection (done: `TestSuiteRequestValidator` is an injectable `@Component`, `TestSuiteService` delegates to it, `TestSuiteCloneService` can inject it)

## 4. Clone Service

- [x] 4.1 Create `TestSuiteCloneService` in `service.domain` — `@Service`, `@LogExecution`, constructor-injected dependencies: `TestSuiteRepository`, `TestCaseRepository`, `TestSuiteMetricDefinitionRepository`, `FileService`, `TestSuiteMapper`, `SuiteValidationService`, `TestSuiteRequestValidator` (new component from task 3.2), `RevalidationService`, `AuthorResolver`, `RevalidationProperties` (for batch size), `PlatformTransactionManager` (`@Qualifier("metaTransactionManager")`) — construct `TransactionTemplate` locally in the constructor (no `TransactionTemplate` bean is registered in the context; same pattern as `TestSuiteService.delete()`), `Clock` (from `ClockConfiguration` bean), `EndpointSchemaRefResolver` (needed for `endpointRef` resolution in task 4.4) (done: service class compiles, all dependencies injected)
- [x] 4.2 Implement `clone(UUID sourceId, TestSuiteCloneRequestDto dto, Jwt jwt)` method — orchestrates: (1) fetch source suite or 404, (2) pre-generate newId, (3) build new suite entity with overrides + file ref rewriting (performed by mapper — see task 2.2), (4) copy files, (5) apply synchronous suite validation (see task 4.4), (6) execute DB writes via `TransactionTemplate` (see task 4.3), (7) trigger async revalidation, (8) return `TestSuiteUpdateResultDto`. Handle `DataIntegrityViolationException` for name uniqueness: use `UniqueConstraintViolationDetector.rethrowIfUniqueViolation(e, "A test suite with name '" + dto.getName() + "' already exists", dto.getName())` (the three-arg overload, same pattern as `TestSuiteService.create()`) — this throws `DomainException` with `UNIQUE_CONSTRAINT_VIOLATION` code and `duplicatedName` set, consistent with the create/update error contract; catch `DataIntegrityViolationException` **outside** the `transactionTemplate.execute()` call, in the `clone()` method body — at this point the transaction has already rolled back and `cloneSucceeded` is still `false`, so the `finally` block will execute best-effort file cleanup before rethrowing. Insert clone suite using `createWithId()` (task 1.3). Best-effort file cleanup runs in `finally` block using a `boolean cloneSucceeded = false` flag set to `true` before returning — cleanup only executes when `!cloneSucceeded`, covering 409 duplicate-name path and all other failures without deleting files on success. Note: Step (3) building the entity is followed immediately by file copy (step (4)) and then synchronous suite validation (step (5), per task 4.4) before the DB transaction in step (6). (done: full flow works end-to-end, finally-block cleanup runs only on failure)
- [x] 4.3 Implement DB write block — before calling `transactionTemplate.execute(...)`, capture `long cloneTimestamp = clock.millis()`; pass `cloneTimestamp` explicitly to `createWithId(entity, cloneTimestamp)`, `batchInsert(testCases, cloneTimestamp)`, and `batchInsert(tsmds, cloneTimestamp)`; do NOT use a `@Transactional` method on `TestSuiteCloneService` — Spring AOP self-invocation (`this.method()`) bypasses the proxy and the transaction would never start; `TransactionTimestampAspect` does NOT fire when using `TransactionTemplate.execute()`, so calling `TransactionTimestampContext.getTimestamp()` would throw `IllegalStateException`; use `TransactionTemplate` (same pattern as `TestSuiteService.delete()`) instead: insert suite via `createWithId()`, paginated loop for test cases using `findBatchByTestSuiteId` (read batch from source → rewrite file refs → assign new UUIDs → batch insert), paginated loop for TSMDs using `findBatchByTestSuiteId` (read batch → rewrite file refs in `configBindings` and `inputBindings` using `String.replace("@ef/suites/" + sourceId + "/", "@ef/suites/" + newId + "/")` — apply replacement only when the field is non-null (null fields remain null; no NPE) → assign new UUIDs → batch insert); **both loops MUST terminate when the returned batch is empty — do NOT rely on comparing count to batchSize as the termination condition**, because the TSMD INNER JOIN on metric_declarations can return batches smaller than batchSize even when more rows exist (orphaned TSMDs referencing deleted metric declarations are silently excluded by the JOIN), which would cause premature loop termination and skip the remaining valid rows (done: all entities inserted in single transaction, paginated, timestamp consistent across all inserts)

- [x] 4.4 Apply synchronous suite-level validation in `TestSuiteCloneService` — after building the new suite entity (step (3) in task 4.2 / step 3 in D7) and before the DB transaction in step (6): (a) call `testSuiteMapper.toRequestDto(newSuiteEntity)` to obtain the DTO (the entity is fully built with all inherited + overridden fields, so null override fields in the request have already been replaced by the source's values — `testSuiteRequestValidator.validateSuiteTypeFields(dto)` is safe); after calling `toRequestDto`, apply null-to-empty-list normalization on the produced DTO before passing to validators: set `inputBindings`, `testCaseSchema`, `responseColumns` to empty list if null (mirrors the `normalizeRequest()` step in `TestSuiteService.create()` that runs before validation); this prevents NPE inside `validateSuite()` and `validateTestSuiteSchemas()` when the source suite had null values for these JSONB fields; also apply `dto.setEndpointRef(endpointSchemaRefResolver.resolve(dto.getEndpointRef()))` — this mirrors `TestSuiteService.normalizeRequest()` which runs this resolution before the validation chain (note: `EndpointSchemaRefResolver` must be injected into `TestSuiteCloneService` — add to task 4.1 dependency list); (b) invoke the FULL validation chain matching `TestSuiteService.create()`, in order: `testSuiteRequestValidator.validateSuiteTypeFields(dto)` (hard, throws HTTP 400 on invalid type-specific fields), `testSuiteRequestValidator.validateTestSuiteSchemas(dto)` (hard, throws on invalid schemas), `testSuiteRequestValidator.validateTemplateLimits(dto)` (hard, throws on limit violations), `suiteValidationService.validateSuite(dto, null)` (soft, returns result); (c) apply the `validateSuite` result by setting `entity.setValid(result.isValid())` and `entity.setValidationWarnings(...)` on the new suite entity; if any hard validator throws, the exception propagates and the `finally` block invokes best-effort file cleanup since `cloneSucceeded` is still `false` (done: hard validators called before validateSuite; invalid overridden fields return 400; suite entity's isValid/validationWarnings set by synchronous validation, not hardcoded; suiteType correctly mapped by toRequestDto so MCP_TOOL suites are validated with the correct rules)

## 5. Controller Endpoint

- [x] 5.1 Add `POST /api/v1/test-suites/{id}/clone` to `TestSuiteController` — accepts `@Valid @RequestBody TestSuiteCloneRequestDto`, returns `ResponseEntity<TestSuiteUpdateResultDto>` with HTTP 201, add constructor dependency on `TestSuiteCloneService` to `TestSuiteController` (Lombok `@RequiredArgsConstructor` handles this), includes OpenAPI annotations (`@Operation`, `@ApiResponse`, `@Schema`, `@io.swagger.v3.oas.annotations.parameters.RequestBody`) — the `@RequestBody` annotation must reference the `TestSuiteCloneRequestDto` schema so Swagger UI renders the request body schema correctly, consistent with existing `TestSuiteController` endpoint annotations (done: endpoint accessible, returns 201 with cloned suite + revalidation task)

## 6. OpenAPI Examples

- [x] 6.1 Add OpenAPI example JSON files for clone endpoint under `src/main/resources/openapi/examples/` — minimal request (name only) and full request (with overrides), response example (done: examples render in Swagger UI)

## 7. Unit Tests

- [x] 7.1 Unit tests for `TestSuiteMapper` clone mapping — test override application, null-means-inherit, file ref rewriting in suite-level JSONB fields (done: all mapper paths covered)
- [x] 7.2 Unit tests for `FileService.copyFilesBetweenSuites` — test successful copy, missing file skip, empty folder (done: edge cases covered)
- [x] 7.3 Unit tests for `TestSuiteCloneService.clone()` orchestration — use mocked dependencies for all repositories, `FileService`, and `RevalidationService`; cover: (a) successful path does not invoke file cleanup (`copyFilesBetweenSuites` NOT followed by delete), (b) `DataIntegrityViolationException` during DB write triggers best-effort cleanup AND is rethrown as `UNIQUE_CONSTRAINT_VIOLATION`, (c) unexpected runtime exception triggers cleanup AND rethrows, (d) `AuthorResolver` is called with the supplied `Jwt` argument, (e) TSMD cloning loop rewrites non-null `configBindings`/`inputBindings` via `String.replace` and leaves null fields as null; this covers the "File copy cleanup on transaction failure" spec scenario (done: `finally`-block `!cloneSucceeded` logic verified, files not deleted on success, cleanup runs on any failure, TSMD `configBindings` and `inputBindings` are rewritten when non-null; null binding fields do not cause NPE)

## 8. Functional Tests

- [x] 8.0 Configure functional test infrastructure for clone endpoint tests that involve file operations — the existing `PostgresFunctionalTests` base class already provides a mock `dialFileStore` (in-memory map) with pre-configured stubs for `list()`, `download()`, and `upload()` on the `DialFileClient` mock; `FileService.copyFilesBetweenSuites()` calls these mocked methods directly — **do NOT mock `FileService.copyFilesBetweenSuites()` itself**; for file-copy scenarios, pre-populate `dialFileStore` with source suite files before calling the clone endpoint; for no-file scenarios, no additional setup is needed (the mock `list()` returns an empty result by default); functional tests 8.1–8.3 can rely on the existing mock infrastructure without additional stubs or WireMock configuration (done: clone functional tests run without real DIAL storage, dialFileStore is pre-populated for file-copy scenarios)
- [x] 8.1 Functional tests for clone endpoint — happy path (clone with name only), clone with overrides, source not found (404), duplicate name (409), blank name (400); **Important**: `FileService` is NOT mocked in functional tests — only `DialFileClient` is mocked via `dialFileStore`; rely on task 8.0 infrastructure (done: all scenarios from spec covered)
- [x] 8.2 Functional tests for deep copy verification — assert cloned test cases have new UUIDs and correct data, assert cloned TSMDs have new UUIDs and correct bindings, assert source suite is unchanged, assert no runs on cloned suite; include a scenario where source suite has more TSMDs than the configured batch size to verify paginated TSMD copying completes all records; also include a scenario where one source TSMD references a deleted metric_declaration (orphaned TSMD) — the INNER JOIN in `findBatchByTestSuiteId` silently excludes it; the loop MUST NOT terminate early and MUST copy all remaining valid TSMDs; this verifies the 'terminate when batch is empty' loop guard works correctly when batches are smaller than batchSize due to JOIN exclusions; **Important**: `FileService` is NOT mocked — pre-populate `dialFileStore` with source suite files as described in task 8.0 (done: entity-level assertions pass, all TSMDs copied across pagination boundaries)
- [x] 8.3 Functional tests for file reference rewriting — create source suite with file-ref-containing test cases and TSMDs (with file refs in `configBindings`/`inputBindings`), clone, verify refs point to new suite ID in cloned test cases and cloned TSMDs (done: file ref replacement verified in DB for both test cases and TSMD binding fields)

## 9. Documentation and Specs

- [x] 9.1 Update `openspec/specs/README.md` per Spec Index Maintenance Policy (done: new `test-suite-clone` spec listed, `test-suites` summary reflects clone capability)
