## 1. Expose placeholder detection and MCP type hint warnings

- [x] 1.1 Add `isPlaceholder(String value)` instance method to `TemplateVariableExtractor` that returns `true` when the entire string matches `PLACEHOLDER_PATTERN` (uses `Matcher.matches()`, not `find()`). Returns `false` for null/empty. This is needed by `SuiteValidationService` to detect FILE part template variables. Instance method (not static) for consistency with the rest of the component's API.
- [x] 1.2 Add `extractFromArgumentTemplateWithWarnings(ArgumentTemplateDto)` method to `TemplateVariableExtractor` that returns `ExtractionResult` (same return type as `extractWithWarnings()`). Refactor existing `extractFromArgumentTemplate()` to delegate to the new method and return only the variables list.
- [x] 1.3 Unit tests: `isPlaceholder()` — verify `${{var}}`, `${{var|file}}`, `${{var|string}}`, `${{var:default}}`, `${{var|file:@ef/path}}` return true; `"@ef/suites/abc/file.csv"`, `"some ${{var}} text"`, `""`, `null` return false. `extractFromArgumentTemplateWithWarnings()` — verify type hint warnings are returned for unrecognized hints.

## 2. Fix FILE form part placeholder validation

- [x] 2.1 In `SuiteValidationService.validateDeploymentSuite()`, in the multipart FILE part validation loop, after `String refValue = part.getValue().toString()` (line 166) and before `fileRefValidator.validate(refValue, suiteId)` (line 167), add a guard: if `TemplateVariableExtractor.isPlaceholder(refValue)` is true, `continue` to skip `fileRefValidator.validate()` for that part. Only validate literal file reference values.
- [x] 2.2 Unit test for `SuiteValidationService` (note: `SuiteValidationServiceTest` does not exist yet — create it with mocked dependencies: `TemplateVariableExtractor`, `EvaluationRunProperties`, `FileRefValidator`): FILE part with `${{contract_file}}` placeholder produces no warning. FILE part with `${{attachment|file}}` placeholder produces no warning. FILE part with literal `@ef/suites/abc/report.pdf` still validates normally. FILE part with invalid literal `public/..` still warns.

## 3. Extract shared binding validation into `BindingValidator` component

- [x] 3.1 Create `BindingValidator` `@Component` in `service.domain` with `@RequiredArgsConstructor`, `@Slf4j`, `@LogExecution`. Inject `FileRefValidator`. Method: `validate(List<ExtractedVariable> variables, List<InputBindingDto> bindings, List<FieldDefinitionDto> schema, UUID suiteId)` returning `List<ValidationWarningDto>`. Move the template→binding (required variable, unknown field, `|file` constant-value validation) and binding→template (orphan) cross-validation logic from `validateDeploymentSuite()` into this component.
- [x] 3.2 Refactor `SuiteValidationService` to inject `BindingValidator` and call `bindingValidator.validate(...)` in `validateDeploymentSuite()` for the shared checks, keeping deployment-specific checks (FILE form part placeholder skip, content-type, headers) inline after the shared call. Similarly, `validateMcpSuite()` calls `bindingValidator.validate(...)`.
- [x] 3.3 Verify existing functional tests for suite validation still pass after refactoring. Run `./gradlew test --tests "*SuiteValidation*"` (this runs both functional and any new unit tests from task 2.2).

## 4. Add MCP binding cross-validation

- [x] 4.1 Update `validateMcpSuite()` signature to accept `UUID suiteId`, and update `validateSuite()` dispatch to pass `suiteId` to `validateMcpSuite(dto, suiteId)`. Change `.valid(true)` to `.valid(warnings.isEmpty())` so MCP suites correctly become `valid: false` when warnings exist. After the existing null `argumentTemplate` check, extract variables using `templateVariableExtractor.extractFromArgumentTemplateWithWarnings()`, add type hint warnings, then call `bindingValidator.validate(...)` with the extracted variables, `dto.getInputBindings()`, `dto.getTestCaseSchema()`, and `suiteId`. Append returned warnings.
- [x] 4.2 Unit tests for MCP binding validation (add to `SuiteValidationServiceTest` created in task 2.2): required variable without binding produces REQUIRED warning. Binding to unknown schema field produces UNKNOWN warning. Orphan binding produces ADDITIONAL warning. `|file` constant binding with valid ref produces no warning. `|file` constant binding with invalid ref produces TYPE warning. Unrecognized type hint produces TYPE warning. All bindings valid produces no binding warnings. Null argumentTemplate only produces the existing ADDITIONAL warning and `valid: false` (no binding validation). Empty bindings list `[]` with required variables produces REQUIRED warnings (identical to null bindings). Duplicate variable in argumentTemplate (`{"q1": "${{query}}", "q2": "${{query}}"}`) with single binding produces no REQUIRED warning (deduplicated). `|file` binding with `dataField` (not `constantValue`) produces no file ref warning (value unknown at save time).

## 5. MCP |file runtime resolution in McpRequestResolver

- [x] 5.1 Update `McpRequestResolver.PLACEHOLDER_PATTERN` to capture type hint: change `(?:\|[^:}]+)?` to `(?:\|([^:}]+))?`. Update group references in `resolveStringValue()`: varName stays group 1, type hint becomes group 2, default becomes group 3. Specifically update: (a) full-value branch — `matcher.group(1)` (varName, unchanged), `matcher.group(2)` → `matcher.group(3)` (default value); (b) embedded branch — `matcher.group(1)` (varName, unchanged), `matcher.group(2)` → `matcher.group(3)` (default value). Verify no other `matcher.group()` calls exist in the class.
- [x] 5.1b Verify existing `McpRequestResolverTest` tests still pass after the regex group change — especially default value resolution tests. Run `./gradlew test --tests '*McpRequestResolver*'`.
- [x] 5.2 Inject `DialFileRefResolver` into `McpRequestResolver` via constructor injection (add field + Lombok generates constructor param).
- [x] 5.3 In `resolveStringValue()`, after resolving a full-value placeholder, add file resolution check: if type hint is `"file"` (case-insensitive via `SchemaFieldType.FILE.name()`) and resolved value is a `String`, call `dialFileRefResolver.resolveToDialRef(resolved)` and return the result. No-op for null, non-String, or embedded placeholders.
- [x] 5.4 Unit tests for `McpRequestResolver` file resolution: `${{doc|file}}` with string value resolves via `DialFileRefResolver`. `${{doc|file}}` with null value produces REQUIRED warning (no file resolution). `${{doc|file}}` with non-String value (e.g., integer) returns as-is. `${{doc}}` without `|file` hint returns raw string (no file resolution). Embedded `"prefix/${{doc|file}}/suffix"` does NOT trigger file resolution. `${{doc|file:@ef/suites/abc/default.pdf}}` with no binding/data resolves default via `DialFileRefResolver`. `${{doc|FILE}}` uppercase hint resolves as file (case-insensitive). Multiple `|file` variables in same template (`{"a": "${{x|file}}", "b": "${{y|file}}"}`) each resolved independently.

## 6. Functional tests and verification

- [x] 6.1 Functional test: create DEPLOYMENT test suite with multipart FILE part containing `${{contract_file}}` placeholder and matching binding — assert `valid = true` and no FILE form part warning in `validationWarnings`.
- [x] 6.2 Functional test: create MCP_TOOL test suite with `argumentTemplate` containing `${{query}}`, no binding for `query` — assert `valid = false` and REQUIRED warning in `validationWarnings`.
- [x] 6.3 Functional test: create MCP_TOOL test suite with `argumentTemplate` containing `${{doc|file}}`, binding with valid constant file ref — assert `valid = true` and no warnings.
- [x] 6.4 Functional test: create MCP_TOOL test suite with `argumentTemplate` as null — assert `valid = false` and ADDITIONAL warning for missing argumentTemplate in `validationWarnings`. This covers the breaking behavioral change from hardcoded `valid(true)` to `valid(warnings.isEmpty())`.
- [x] 6.5 Run `./gradlew checkstyleMain checkstyleTest` — verify no violations.
- [x] 6.6 Run `./gradlew test` — verify full test suite passes.
