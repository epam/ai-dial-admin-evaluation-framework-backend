## 1. Baseline inventory

- [x] 1.1 Extract the current property list from `docs/configuration.md` into a scratch file (e.g., `grep -oE '\| \`[a-z][a-z0-9.-]+\.[a-z][a-z0-9.-]+\`' docs/configuration.md | sort -u`). This is the authoritative "before" list that must be preserved byte-for-byte in property coverage (done: scratch file has one property key per line, deduped)
- [x] 1.2 Cross-check the scratch list against `@ConfigurationProperties` classes under `src/main/java/com/epam/aidial/evaluation/configuration/properties/` and top-level bindings in `src/main/resources/application.yml`. Record any properties referenced in code but missing from the doc as a **separate follow-up gap list** (e.g., `scratch/configuration-md-gaps.md`). This PR is a pure reformat — it MUST NOT add, remove, or rename properties. Any gap discovered here is filed as a follow-up ticket/change, not resolved inline (done: gap list file exists OR is empty; zero gap entries are merged into the rewrite itself)
- [x] 1.3 For each property, record the correct `Required` vocabulary term (`Yes` / `No` / `Conditional` / `Recommended`) and the `Applied when` expression in the scratch file. Cross-check `Yes` assignments against `@NotBlank` / `@NotNull` annotations in properties classes (done: scratch file has a 4-column pre-table: `property | Required | Applied when | notes`)

## 2. Supporting artifacts

- [x] 2.1 Add a new `DO ✅` bullet to `AGENTS.md` titled "When adding a configuration property", referencing `openspec/specs/configuration-docs/spec.md` for the full rule. Keep the AGENTS.md bullet terse — one sentence plus a link. Do NOT enumerate the 9 top-level groups or the 4-term vocabulary inline; those live in the spec (done: AGENTS.md renders cleanly; the new bullet is under `DO ✅`; linter / checkstyle unaffected)
- [x] 2.2 Confirm the change does NOT trigger `openspec/config.yaml` update (per design decision D5 and the Config Maintenance Policy litmus test). No edits to `openspec/config.yaml` in this PR (done: `git diff openspec/config.yaml` is empty)

## 3. Rewrite `docs/configuration.md` — Overview, Spring Framework, Security

- [x] 3.1 Replace the top-of-file intro with an **Overview** group covering: Spring config precedence (env → YAML → defaults), the fail-fast `@ConfigurationProperties` validation guarantee, and the uppercase-dot-to-underscore env-var conversion rule. End the Overview with a one-line link to `openspec/specs/configuration-docs/spec.md` describing the doc's governance. This content is stated exactly once in the whole document (done: Overview group present with governance link; old "Environment Variables" section at bottom of file removed)
- [x] 3.2 Rewrite the Table of Contents to list the 9 top-level groups in the prescribed order, with each section nested under exactly one group (done: ToC has 9 top-level entries in the correct order)
- [x] 3.3 Under **Spring Framework Configuration**, produce 6-column tables for: Server, Actuator, OpenAPI, Logging (general + dynamic log level). The Correlation ID prose stays as a subsection AFTER the Logging table (done: all four sections exist with 6 columns filled; prose is after tables)
- [x] 3.4 Under **Security**, produce 6-column tables for: mode (`config.rest.security.*`), Identity Providers (`providers.*.*`), JWT Claim Resolution (`security.jwt.*`). The YAML example for a provider entry stays as a code block AFTER the Identity Providers table (done: all three sections exist with 6 columns filled; YAML example is after the providers table)

## 4. Rewrite `docs/configuration.md` — Data Layer, DIAL Integration

- [x] 4.1 Under **Data Layer**, produce 6-column tables for: Meta datasource, Analytics datasource, Azure AD authentication (`auth.azure.*` + related `postgres.*.datasource.username` conditional rows), Flyway, Datasource Validation. The YAML example for Azure AD config stays as a code block AFTER the Azure AD table (done: all sections exist; Azure AD rows use `Applied when` expressions like `datasource.meta.auth.type=azure`; Transaction Managers content that describes bean wiring is REMOVED from this file)
- [x] 4.2 Under **DIAL Integration**, produce 6-column tables for: DIAL Core Client (`dial.components.core.*` + try-out timeout), DIAL API Key (`dial.api-key`), DIAL File Storage (`dial.file-storage.*` + Spring multipart limits), DIAL MCP Client (`dial.mcp.*`). The narrative on bucket alias translation, file reference prefix whitelist, and health indicator behaviour is MOVED to `docs/design/infrastructure-architecture.md` or deleted if duplicated there (done: all 4 sections exist; prose in `configuration.md` describes only operator-visible behaviour, not internal architecture)

## 5. Rewrite `docs/configuration.md` — Evaluation Engine, Data Management, Observability, Notes

- [x] 5.1 Under **Evaluation Engine**, produce 6-column tables for: Test Suite Run (Executor, SSE, Execution Settings, Retry Settings, Run Config, Concurrency Limits — one subsection per original grouping), Analytics Results Batch Write (`analytics.results.*`), Analytics Eval Summaries Batch Write (`analytics.eval-summaries.*`), Metric Providers (sync + provider map), Metric Evaluation (concurrency + retry). The per-provider env-var override pattern explanation stays as a prose subsection AFTER the Metric Providers table (done: all five sections exist; all sub-tables are 6 columns)
- [x] 5.2 Under **Data Management**, produce 6-column tables for: Pagination, CSV Export, CSV Import, Validation, Test Case Batch (done: all five sections exist; Fixed-limits note stays as a post-table subsection)
- [x] 5.3 Under **Observability**, produce the 6-column table for Grafana Integration (`app.grafana.*`). The list of response fields populated when enabled stays as a prose subsection AFTER the table (done: section exists; prose is after table)
- [x] 5.4 Under **Notes**, carry over the Docker run example (as a single code block) and any deployment/security reminders. This group contains NO property tables (done: `Notes` group present as the last section; does not repeat any property rows)

## 6. Migrate or delete non-configuration narrative

- [x] 6.0 Audit `docs/design/infrastructure-architecture.md`, `docs/design/entity-relationship-model.md`, and `AGENTS.md` for current coverage of the non-configuration topics that live in `docs/configuration.md` today (lazy bucket discovery, `TransactionTimestampAspect`, `CursorCodec` encoding, TOAST column-projection tiers, computation versioning, etc.). Produce a coverage matrix in the scratch file — one row per topic with columns: `topic | covered in design doc? (path + heading or "no") | covered in AGENTS.md? | move-vs-delete recommendation`. This matrix drives the move/delete decisions in 6.1–6.2 (done: matrix exists; every topic has a non-ambiguous recommendation)
- [x] 6.1 Identify every prose paragraph in `docs/configuration.md` describing component internals (e.g., lazy bucket discovery details, AOP aspect mechanics, cursor encoding strategy, TOAST column-projection tiers, computation-versioning rationale). List them in a scratch file (done: list exists; each entry has a target: `move-to:<path>` or `delete:<reason>`)
- [x] 6.2 For each entry marked `move-to:docs/design/infrastructure-architecture.md` (or another design doc), check if the target doc already covers the topic. If yes → mark as `delete:duplicate`. If no → append the paragraph to the target design doc under an appropriate heading (done: every `move-to` entry is either actioned or downgraded to `delete:duplicate`; no source paragraph remains in `configuration.md`)
- [x] 6.3 Remove every `delete:*` paragraph from `configuration.md` (done: `docs/configuration.md` contains no internal-architecture prose; only property tables, behavioural notes about property effects, and the Overview/Notes groups)

## 7. Verification

- [x] 7.1 Rerun the property extraction from task 1.1 against the rewritten `docs/configuration.md`. Diff against the baseline scratch list — the symmetric difference MUST be empty (i.e., no property dropped, and any newly-added property is explicitly listed in the scratch file's gap notes from task 1.2) (done: `diff` output shows no unaccounted differences)
- [x] 7.2 Grep the rewritten doc for any `Required` column value outside `{Yes, No, Conditional, Recommended}` — zero matches expected. Command: extract the 4th column of every property-table row and confirm only the four allowed terms appear (done: non-conforming matches = 0)
- [x] 7.3 Grep every property-table row for a non-empty `Property`, `Default`, `Required`, `Applied when`, `Description` cell. Count rows with any empty cell (other than documented `-` sentinels) — zero matches expected (done: empty-cell count = 0)
- [x] 7.4 Spot-check 5 randomly chosen property rows against `application.yml`: does the `Default` column match the actual default? Does the `Environment Variable` column match the trivially-converted env-var name or a deliberately aliased one? (done: 5/5 match; any mismatch fixed)
- [x] 7.5 Render the doc on GitHub (push branch, open the file in the browser) and visually confirm: ToC has 9 top-level groups in prescribed order; every property table is 6 columns; no table overflows its container in a way that destroys readability (done: visual check passes; screenshots captured in PR description if desired)
- [x] 7.6 Run `openspec validate formalize-configuration-md-structure --strict` and fix any reported issues (done: validator exits 0)

## 8. Spec index

- [x] 8.1 Update `openspec/specs/README.md` per the Spec Index Maintenance Policy: add `configuration-docs` under an appropriate category (suggested: under an "Operational Documentation" or "Cross-Cutting" heading — check existing categories first; create a new one only if none fit) with status `Planned` (flipping to `Implemented` happens at archive time per the Spec Index Maintenance Policy) and a one-line summary (done: entry added; existing categories and ordering preserved)
