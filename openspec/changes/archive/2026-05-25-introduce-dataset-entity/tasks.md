## 1. Database migration & jOOQ regeneration

- [x] 1.1 Create Flyway file `src/main/resources/db/migration/meta/POSTGRES/V1.<next>__IntroduceDataset.sql` covering the 24-step DDL + data backfill sequence in `design.md` (create `datasets` + unique index; INSERT INTO datasets SELECT FROM test_suites with `name = 'DATASET_' || test_suites.name` (literal prefix) and all other inherited columns copied verbatim; add `test_suites.dataset_id` with backfill `SET dataset_id = id` then NOT NULL + FK + index; add `test_suites.disabled_test_case_ids` with backfill from `jsonb_agg(test_cases.id WHERE is_enabled=false)`; drop `test_suites.test_case_schema`; on `test_cases` drop suite FK, rename `test_suite_id → dataset_id`, add dataset FK CASCADE, swap unique/index, drop `request_template_override`/`input_bindings_override`/`is_enabled`; on `revalidation_tasks` drop suite FK, rename `test_suite_id → dataset_id`, add dataset FK CASCADE)
- [x] 1.1a Explicitly DROP / RENAME the **named** constraints and indexes attached to the columns being renamed, in the same migration. `JooqSchemaDriftTest` compares constraint/index names against the regenerated jOOQ metadata and will fail if these are left dangling. Concretely:
    - [x] On `test_cases`: `ALTER TABLE test_cases DROP CONSTRAINT test_cases_test_suite_id_fkey` (drop suite FK), `DROP INDEX IF EXISTS idx_test_cases_test_suite_id` (or `ALTER INDEX … RENAME TO idx_test_cases_dataset_id` if preserving), `DROP INDEX IF EXISTS uq_test_cases_suite_name` (this is a UNIQUE INDEX created in `V1.4__AddUniqueIndexesForMetaNames.sql`, not a UNIQUE CONSTRAINT) and recreate as `CREATE UNIQUE INDEX uq_test_cases_dataset_name ON test_cases (dataset_id, LOWER(test_case_name))`
    - [x] On `test_suites`: drop any FK/index added in earlier migrations that referenced `test_case_schema` columns (none expected at write time, but verify); add the new FK constraint `test_suites_dataset_id_fkey` referencing `datasets(id) ON DELETE RESTRICT` and a supporting index `idx_test_suites_dataset_id`
    - [x] On `revalidation_tasks`: `DROP CONSTRAINT IF EXISTS revalidation_tasks_test_suite_id_fkey`, `DROP INDEX IF EXISTS idx_revalidation_tasks_test_suite_id` (or rename), recreate as `revalidation_tasks_dataset_id_fkey` referencing `datasets(id) ON DELETE CASCADE` and supporting index `idx_revalidation_tasks_dataset_id`
    - [x] Use `IF EXISTS` guards where appropriate so the migration is idempotent against environments whose constraint names diverged historically; otherwise fail loudly with the actual name to be hand-corrected by an operator
- [x] 1.2 Add `RAISE NOTICE` (or equivalent diagnostic) in the migration for any `test_cases` rows with non-null `request_template_override` or `input_bindings_override` before dropping those columns (done: operator log lists affected case IDs)
- [x] 1.2a In the same Flyway file, before retargeting the `revalidation_tasks` FK, run `UPDATE revalidation_tasks SET status = 'FAILED', error_message = 'Aborted by introduce-dataset-entity migration: task suite has been re-rooted as a dataset; resubmit the schema change against the new dataset endpoint.', completed_at_ms = <now-ms> WHERE status IN ('PENDING', 'RUNNING')`. The `revalidation_tasks` table has no `error_code` column (per `V1.2__TestSuiteAggregateTables.sql` + `V1.21__AddCoercedCellCountToRevalidationTasks.sql`), and this change does not add one — the abort marker is encoded entirely in the `error_message` value via the literal prefix `Aborted by introduce-dataset-entity migration: ...`, which application code can match against if it needs to surface the migration-abort case distinctly from regular failures. Choose the timestamp via `(extract(epoch from now()) * 1000)::bigint` (or equivalent) — `extract(epoch ...) * 1000` returns `double precision`, and `completed_at_ms` is `BIGINT`, so the cast is required under strict PG assignment rules. Document the rationale inline: clean operator-visible state, no surprises from in-flight tasks running against the old data model.
- [x] 1.2b In the migration, BEFORE constructing the `disabled_test_case_ids` backfill, run a diagnostic SELECT/RAISE NOTICE listing any suite whose disabled-case count exceeds 10000; do not fail the migration. Document the operator action (manual re-save or follow-up cleanup task) inline.
- [x] 1.3 Run `./gradlew generateJooq` and commit the generated diff under `src/main/java-generated/` (done: build compiles against the new schema)
- [x] 1.4 Verify `JooqSchemaDriftTest` passes: `./gradlew test --tests JooqSchemaDriftTest` — **blocked by intentional build state until Task Groups 2–9 rebind production code (model/repo/mapper/service references to dropped columns)**. The drift verification has effectively run during `./gradlew generateJooq` (which boots EmbeddedPostgres, applies V1.x including V1.22, and emits jOOQ metadata from `information_schema` — identical mechanism to the test). Re-run this task explicitly after Task Group 9 completes.

## 2. Dataset domain model & repository

- [x] 2.1 Add `data.db.model.Dataset` (Lombok `@Data @Builder`, fields: `id UUID`, `name String`, `description String`, `testCaseSchema String` JSONB-as-raw-string, `isValid boolean`, `validationWarnings String`, `version Long`, `createdBy String`, `createdAt Long`, `updatedAt Long`)
- [x] 2.2 Add `data.db.mapper.DatasetRecordMapper` (`@Component`, maps generated jOOQ `DatasetsRecord` → `Dataset`)
- [x] 2.3 Add `data.db.repository.DatasetRepository` interface (findById, findById with version, findAll(PageRequest, filters), insert, update, delete; method signatures per existing `TestSuiteRepository` pattern)
- [x] 2.4 Add `data.db.repository.PostgresDatasetRepository` implementation using `@Qualifier("metaDsl") DSLContext`, `@LogExecution`, `@Transactional("metaTransactionManager")` where appropriate
- [x] 2.5 Add `data.db.repository.sql.FilterWhitelists` and `SortWhitelists` entries for `datasets` (filter fields: `name`, `description`, `createdBy`, `createdAt`, `updatedAt`; sort fields: `id`, `name`, `createdBy`, `createdAt`, `updatedAt`, `version`)
- [x] 2.6 Register dataset entry in `OpenApiQueryParamCustomizer` so `filter`/`sort` descriptions auto-generate

## 3. Dataset DTOs & mapper

- [x] 3.1 Add `service.domain.dto.DatasetRequestDto` (`name` `@NotBlank @Size(max=255)`, `description` `@Size(max=2000)`, `testCaseSchema` `List<FieldDefinitionDto>` defaulting to `[]`). Also includes optional `createdBy` (per the `TestSuiteRequestDto` convention for update-time reassignment).
- [x] 3.2 Add `service.domain.dto.DatasetResponseDto` (all stored fields + computed; `testCaseSchema` exposed as `List<FieldDefinitionDto>`)
- [x] 3.3 Add `service.domain.dto.DatasetReferenceDto` (`id UUID`, `version Long`, `name String`) for snapshot embedding
- [x] 3.4 Add `service.domain.mapper.DatasetMapper` — implemented as a hand-rolled `@Component` (matches existing `TestSuiteMapper`/`TestCaseMapper` pattern; the codebase does not use MapStruct `@Mapper(componentModel = "spring")` for any of its meta-entity mappers). Provides `toDto`, `toEntity`, `update`, `toReference`; reuses `JsonbMapper.mapFieldDefinitions` for schema and `ValidationWarningsSerializer.deserializeWarnings` for warnings.

## 4. DatasetService & DatasetSchemaProvider

- [x] 4.1 Add `service.domain.DatasetSchemaProvider` (`@Component` exposing `List<FieldDefinitionDto> getSchema(UUID datasetId)`; injects `DatasetRepository` + `JsonbMapper`; throws `EntityNotFoundException` for unknown id)
- [x] 4.2 Add `service.domain.DatasetService` with CRUD operations (`@Service`, `@Transactional("metaTransactionManager")`). Injects `DatasetRepository`, `TestSuiteRepository` (delete pre-check), `TestCaseRepository` (orphan-key pruning), `DatasetMapper`, `JsonbMapper`, `AuthorResolver`, `RevalidationService` (forward-ref to task 9 method), `SortParser`/`FilterParser` (list endpoint), `ObjectMapper` (semantic JSON diff), and `metaTransactionManager`. `ValidationWarningsSerializer` is not injected here — warnings are persisted by the revalidation flow, never authored by CRUD. `Clock` is not injected: timestamps are written by `TransactionTimestampContext` inside the repository layer.
- [x] 4.3 Implement `DatasetService.update()` to detect `testCaseSchema` diff (semantic Jackson-based JSON equality so key-order changes don't trigger revalidation), prune orphan keys from every TestCase in the dataset via `testCaseRepository.removeDataFields(id, removedFields)`, then call `revalidationService.startDatasetRevalidation(id)` and return a new `DatasetUpdateResultDto { dataset, revalidationTask }` — mirrors `TestSuiteUpdateResultDto`. The controller (task 5) maps non-null `revalidationTask` to HTTP 202 and otherwise to 200. Note: `RevalidationService.startDatasetRevalidation(UUID)` is declared but not yet implemented — added in task 9.3+; the unresolved call is part of the staged build-break state.
- [x] 4.4 Implement `DatasetService.delete()` honoring FK `ON DELETE RESTRICT`. Pre-check via `testSuiteRepository.findSuitesReferencingDataset(id)` so a 409 carries the list of dependent suite names; also catches `DataIntegrityViolationException` to cover the post-check race (concurrent suite insert between pre-check and delete). Translates to HTTP 409 via `InvalidOperationException` (existing handler in `DefaultExceptionHandler` maps `InvalidOperationException` → `CONFLICT` / `INVALID_OPERATION` error code).
- [x] 4.5 Add unit tests for `DatasetService` covering schema-diff detection, name uniqueness errors, optimistic-concurrency mismatch, RESTRICT-on-delete translation — done by task 14.1 (`DatasetServiceTest`, 13 tests; passes `./gradlew test --tests *DatasetServiceTest`).
- [x] 4.6 Re-key `service.domain.SchemaValidationService`'s cache from `testSuiteId` to `datasetId` (per design §D10): rename map keys/method parameters, move cache eviction calls from `TestSuiteService.update/delete` to `DatasetService.update` (on `testCaseSchema` diff) and `DatasetService.delete`. Update `SchemaValidationServiceTest` to assert eviction triggers on dataset writes and that suite rebinds populate from the new dataset's cache entry without explicit eviction.

## 5. Dataset web controller & OpenAPI

- [x] 5.1 Add `web.controller.DatasetController` (`/api/v1/datasets`) with full CRUD + ETag/If-Match handling. Delete returns 204 No Content (no children to count — dataset cascades to test_cases via FK; deletion stats can be added later if needed for parity with `TestSuiteDeleteResponseDto`). `@LogExecution`, `@Tag("Datasets")`, `@RequiredArgsConstructor` injection of `DatasetService`, `RevalidationService`, `PaginationParamResolver`.
- [x] 5.2 Add `/{id}/revalidation-tasks` and `/{id}/revalidation-tasks/{taskId}` subroutes returning `PageResponseDto<RevalidationTaskDto>` and `RevalidationTaskDto`. Wires through the existing `RevalidationService.listTasks` / `getTask` — those methods accept a UUID that's now semantically `datasetId` (the repository layer was rebound to `findAllByDatasetId` / `findByIdAndDatasetId` in iter 4); the service-side parameter rename is part of Task Group 9.
- [x] 5.3 Add OpenAPI example JSON files under `src/main/resources/openapi/examples/` (kept FLAT alongside the existing `api-v1-test-suites-*.json` files — there is no `datasets/` subfolder convention in this codebase). Created six files covering the brief's four scenarios: `api-v1-datasets-POST-request-minimal.json`, `api-v1-datasets-POST-response-201-minimal.json` (create), `api-v1-datasets-id-PUT-request-minimal.json`, `api-v1-datasets-id-PUT-response-200-minimal.json` (update-metadata, schema unchanged), `api-v1-datasets-id-PUT-response-202-minimal.json` (update-schema-202, mirrors the test-suite revalidation-task envelope but with `datasetId` rather than `testSuiteId`), and `api-v1-datasets-id-DELETE-response-409.json` (delete-409 conflict using the existing `ErrorView` shape with `errorCode: INVALID_OPERATION`).
- [x] 5.4 `@Schema(example = "...")` annotations on `DatasetRequestDto` and `DatasetResponseDto` fields — already landed with task 3.1 / 3.2 in iter 6 (name, description, valid, version, createdBy, createdAt, updatedAt all carry examples). Re-verified during iter 8.

## 6. TestSuite refactor

- [x] 6.1 Update `data.db.model.TestSuite`: drop `testCaseSchema String` field, add `datasetId UUID`, add `disabledTestCaseIds String` (JSONB raw string)
- [x] 6.2 Update `data.db.mapper.TestSuiteRecordMapper` to read the new columns
- [x] 6.3 Update `data.db.repository.TestSuiteRepository`: remove `countTestCasesByTestSuiteId`; add `findSuitesReferencingDataset(UUID datasetId)` returning `List<TestSuite>` for the revalidation-Phase-2 fan-out (named distinctly from `TestCaseRepository.findAllByDatasetId` to avoid same-name / different-return-type confusion across repositories)
- [x] 6.4 Update `data.db.repository.PostgresTestSuiteRepository` queries: select/insert/update new columns; drop the removed-column path
- [x] 6.5 Update `service.domain.dto.TestSuiteRequestDto`: drop `testCaseSchema`, add `datasetId @NotNull`, add `disabledTestCaseIds List<UUID>` with `@Size(max = ValidationConstants.MAX_DISABLED_TC_IDS)` (define the constant in `ValidationConstants` with value `10000`; non-configurable per AGENTS.md "constants per bounded context")
- [x] 6.6 Update `service.domain.dto.TestSuiteResponseDto`: mirror request DTO field changes — `testCaseSchema` field dropped; `datasetId UUID` and `disabledTestCaseIds List<UUID>` added with `@Schema` examples/descriptions.
- [x] 6.7 Update `service.domain.dto.TestSuiteCloneRequestDto`: drop `testCaseSchema`, add optional `datasetId` (override the source suite's dataset; `null` means inherit from source).
- [x] 6.8 Update `service.domain.mapper.TestSuiteMapper`: drop schema-related branches; map `datasetId` and `disabledTestCaseIds` (JSONB-encoded array of stringified UUIDs via injected `ObjectMapper`); in `toCloneEntity` the clone inherits `datasetId` + `disabledTestCaseIds` from source by default (suite-level file-ref rewriting stays). `deserializeDisabledIds` returns `null` on null/blank, empty list on malformed payload (logged) so a single corrupt row doesn't brick reads; `serializeDisabledIds` writes `"[]"` for null/empty so the column matches the V1.22 migration's DEFAULT. Also patches `toRequestDto` to round-trip the new fields.
- [x] 6.9 Update `service.domain.TestSuiteService.create/update`: resolve schema via `DatasetSchemaProvider.getSchema(...)` (which throws `EntityNotFoundException` on miss, so no separate `existsById` call needed); call new `SuiteValidationService.validateSuite(dto, suiteId, datasetSchema)` signature; PUT returns `TestSuiteResponseDto` directly (no more `TestSuiteUpdateResultDto`, no 202, no async kickoff). TSMD revalidation now fires only when **responseColumns** change at the suite level — `testCaseSchema` changes are dataset-owned and revalidated by `DatasetService`. Helper `isResponseColumnsChanged` replaces the old `isTsmdSchemaChanged`/`isSchemaChanged`/`computeRemovedFields`. The `schemaValidationService.invalidateSchemaCache(id)` call moves to delete only (cache eviction on update belongs to `DatasetService` per task 4.6 — deferred).
- [x] 6.10 Update `service.domain.TestSuiteService.delete`: drop `deletedTestCases` and `deletedRevalidationTasks` counts (both are dataset-rooted now). `TestSuiteDeleteResponseDto` carries only `{ deleted: boolean }`. `TestSuiteUpdateResultDto` left intact because `TestSuiteCloneService` still consumes it (clone is task group 11).
- [x] 6.11 Update `web.controller.TestSuiteController`: removed `GET /{id}/revalidation-tasks` and `GET /{id}/revalidation-tasks/{taskId}` (now exclusively on `DatasetController`). PUT returns `ResponseEntity<TestSuiteResponseDto>` with ETag; 202 path gone. `RevalidationService` injection dropped. Delete response simplified.
- [x] 6.12 Add `MAX_DISABLED_TC_IDS = 10000` to `ValidationConstants` (compile-time constant; not configurable — no `application.yml` entry, no `@ConfigurationProperties` exposure). Javadoc anchors the rationale: bounds the JSONB array payload and the snapshot-phase `NOT (id = ANY(?::text[]))` predicate.
- [x] 6.13 Add `TestSuiteRepository.updateValidation(UUID id, boolean isValid, String validationWarningsJson, long updatedAt)` to the interface (callers inside @Transactional pass `transactionTimestampContext.getTimestamp()`; tests pass an explicit timestamp). Existing `updateIsValid(UUID, boolean)` is preserved for callers that only flip the boolean; `updateValidation` is used by Phase 2 of dataset revalidation (task 9.5) to write the suite-level (isValid, validationWarnings) pair atomically with a single UPDATE.
- [x] 6.14 Add the matching implementation in `PostgresTestSuiteRepository` via the typed jOOQ DSL targeting `TEST_SUITES.IS_VALID`, `TEST_SUITES.VALIDATION_WARNINGS` (JSONB), and `TEST_SUITES.UPDATED_AT`. (Focused unit/functional test deferred to Task Group 14/15.)
- [x] 6.15 Extend `SuiteValidationService` with a model-based overload `validateSuite(TestSuite suite, List<FieldDefinitionDto> datasetSchema)` returning a `ValidationResult` — used by `RevalidationService` Phase 2 (task 9.5) so that the Phase-2 loop can call the service with the in-memory `TestSuite` and pre-resolved dataset schema without round-tripping through a request DTO. Implementation: reverse-maps the JSONB-backed columns into a transient `TestSuiteRequestDto` (via newly-injected `JsonbMapper`) and delegates to the existing DTO-based `validateSuite(dto, suiteId)` path so validation rules live in a single place. The existing `validateSuite(TestSuiteRequestDto dto, UUID suiteId)` overload is preserved for controller-layer calls. NOTE: `SuiteValidationServiceTest` was already broken by earlier model renames; its constructor mock now also needs the new `JsonbMapper` parameter — picked up in Task Group 14 (unit tests).

## 7. TestCase refactor

- [x] 7.1 Update `data.db.model.TestCase`: rename `testSuiteId UUID` → `datasetId UUID`; drop `requestTemplateOverride`, `inputBindingsOverride`, `enabled` fields
- [x] 7.2 Update `data.db.mapper.TestCaseRecordMapper` accordingly
- [x] 7.3 Update `data.db.repository.TestCaseRepository` interface: rename every `testSuiteId` parameter to `datasetId`; rename methods (`findAllByTestSuiteId` → `findAllByDatasetId`, etc.); add `findValidByDatasetIdExcludingIds(UUID, Collection<UUID>, int offset, int limit)` and `countValidByDatasetIdExcludingIds(UUID, Collection<UUID>)` for the snapshot phase; rescope `removeDataFields(UUID datasetId, Collection<String> fields)` accordingly. NOTE: `findEnabledValidByTestSuiteId`/`countEnabledAndValidByTestSuiteId` were dropped (replaced by the new excluding-ids variants); `findExistingIdsInSuite` → `findExistingIdsInDataset`.
- [x] 7.4 Update `data.db.repository.PostgresTestCaseRepository`: rebind every jOOQ where-clause from `TEST_CASES.TEST_SUITE_ID` to `TEST_CASES.DATASET_ID`. The `is_enabled` and override columns are dropped from every INSERT/UPDATE/onConflict; the `insertOrOverride` raw-SQL string and `removeDataFields` raw-SQL string are rebound to `dataset_id`. `updateFieldsByIds` becomes a defensive no-op throwing `IllegalArgumentException` for any non-whitelisted key (bulk-patch surface is rebuilt in task 7.10; the legacy `enabled` whitelist entry in `BulkPatchFields` is left for now and will be cleared with the controller refactor).
- [x] 7.4a Implement the new excluding-ids snapshot methods (`findValidByDatasetIdExcludingIds`, `countValidByDatasetIdExcludingIds`) using a parameterised PostgreSQL array predicate `WHERE NOT (id = ANY(:excludedIds::text[]))` via `DSL.condition("...{0}::text[])", DSL.array(stringIds))` — `text[]` (not `uuid[]`) because `test_cases.id` is stored as `VARCHAR(36)` per the project-wide UUID-as-text convention. Empty `excludedIds` collections short-circuit the array predicate entirely so the simple `is_valid = TRUE` path still uses the existing dataset-id index without an extra cast.
- [x] 7.4b Add a functional test (under `PostgresFunctionalTests`) seeding a dataset with `~10001` valid test cases and a suite with `disabledTestCaseIds.size() == MAX_DISABLED_TC_IDS (10000)`; assert snapshot phase completes within the existing snapshot timeout and produces the expected `numberOfTestCases` (`totalValid - disabledCount`, ignoring stale ids). — `LargeDatasetSnapshotFunctionalTests.snapshotMaterializesOneRowOutOfTenThousandOne` seeds 10001 cases, disables 10000 via `appendDisabledTestCaseIds`, asserts snapshot commits within 45s, exactly 1 `test_case_run_inputs` row materializes, and run reaches terminal status with `numberOfTestCases == 1`.
- [x] 7.5 Update `service.domain.dto.TestCaseRequestDto`, `TestCaseResponseDto`, `TestCaseBatchPutItemDto`: dropped `requestTemplateOverride`, `inputBindingsOverride`, `enabled`. Field set is now `{ testCaseName, data }` on request DTOs plus `{ id, valid, validationWarnings, createdAt, updatedAt }` on the response.
- [x] 7.6 Update `service.domain.mapper.TestCaseMapper`: signature `toEntity(TestCaseRequestDto, UUID datasetId)`; override/enabled mapping removed (no more `requestTemplateOverride`/`inputBindingsOverride`/`enabled` JSONB or boolean handling). `updateEntity` accepts only `testCaseName` and `data` changes; the `JsonbMapper` dep is unused after the trim and was dropped.
- [x] 7.7 Update `service.domain.TestCaseService`: every `testSuiteId` becomes `datasetId`; injected `DatasetRepository` (existence check) + `DatasetSchemaProvider` (schema lookup); dropped `TestSuiteRepository`, `JsonbMapper`, `ObjectMapper`, `ValidationProperties` deps. Validation simplified to schema-shape only (`validateTestCase(data, schema, null, List.of(), false, null)`) — template-variable + FILE-reference validation moves to suite/run-time. Bulk-patch path retains item-level merge patch (testCaseName + data only); merge-patch keys for the dropped fields are silently ignored (the underlying `TestCaseRepository.updateFieldsByIds` whitelist enforces this for bulk-patch too). The `SuiteValidationContext` private record is gone — schema flows directly through method calls.
- [x] 7.8 Update `service.domain.TestCaseBulkSelectorResolver`: rescoped — every `testSuiteId` parameter is now `datasetId`; calls `findExistingIdsInDataset` and `findIdsByDatasetIdAndFilter`. Error message updated to "TestCase not found in dataset". (Note: actual file path is `service.domain.TestCaseBulkSelectorResolver`, not `service.domain.csv.*` as referenced in the original task text — the resolver lives directly under `service.domain`.)
- [x] 7.9 Move test case CRUD endpoints to `/api/v1/datasets/{datasetId}/test-cases/*`. Class-level `@RequestMapping` rebound; all 11 endpoints (CRUD + batch + CSV/ZIP import/preview + export.csv + bulk delete) take `@PathVariable UUID datasetId`. `includeEnabled` query param dropped from export.csv. 404 messages updated to "Dataset not found"/"Dataset or test case not found". Tag/Operation descriptions updated. Stop-gap: passes `false` to the existing `exportZip/exportCsv` services' `includeEnabled` arg (those services keep the param until task group 8 drops it from their signatures).
- [x] 7.10 Move `TestCaseBulkPatchController` to `/api/v1/datasets/{datasetId}/test-cases:bulk`. Method-level path rebound. `@PathVariable UUID testSuiteId` → `@PathVariable UUID datasetId`. OpenAPI ExampleObject refreshed: example patches now use `testCaseName`/`data` (no more `enabled`/`requestTemplateOverride`). Whitelist description updated from `{enabled}` to `{testCaseName, data}`.
- [x] 7.11 Suite-scoped `GET /api/v1/test-suites/{suiteId}/test-cases/{testCaseId}/resolved-request` kept at the old path. `ResolvedRequestService.resolveRequest`: fetches suite, then `findByIdAndDatasetId(testCaseId, suite.getDatasetId())`; override-handling for `requestTemplateOverride`/`inputBindingsOverride` removed (overrides no longer exist). Per-test-case `/template-variables` endpoint deleted from `TemplateVariableController`; `TemplateVariableService.getTestCaseTemplateVariables` method + unused `TestCaseRepository` + `TestCase` import removed. The suite-level `/{testSuiteId}/template-variables` endpoint stays (its body still references the dropped `TestSuite.getTestCaseSchema()` — that rebinding is task group 12 scope).
- [x] 7.12 `OpenApiQueryParamCustomizer` registry re-keyed: `/api/v1/test-suites/{testSuiteId}/test-cases` → `/api/v1/datasets/{datasetId}/test-cases`; `/api/v1/test-suites/{testSuiteId}/test-cases/export.csv` → `/api/v1/datasets/{datasetId}/test-cases/export.csv`. No stale suite-scoped entries remain.
- [x] 7.13 Remove the `enabled` filter entry from `data.db.repository.sql.FilterWhitelists.TEST_CASES` (which today references `TestCases.TEST_CASES.IS_ENABLED`) and the `enabled` sort entry from `data.db.repository.sql.SortWhitelists.TEST_CASES` (which today references `TestCases.TEST_CASES.IS_ENABLED`). After this removal the test-case filter whitelist exposes `testCaseName`, `valid`, `createdAt`; the test-case sort whitelist exposes `id`, `testCaseName`, `createdAt`, `updatedAt`, `valid` — the `enabled` key is gone from both. After `./gradlew generateJooq` regenerates without the `IS_ENABLED` column (column is dropped by the migration in §1), no remaining `TEST_CASES.IS_ENABLED` reference SHALL exist in production code; otherwise the build fails. Update any unit test that asserts the test-case filter/sort key set accordingly.

## 8. CSV import/export rescoping

- [x] 8.1 Update `service.domain.CsvImportService` and `CsvExportService` to read/write the schema on the **Dataset** (via `DatasetSchemaProvider` + `DatasetRepository.update`); drop the `enabled`/`is_enabled` column handling — CsvImportService now injects `DatasetRepository` + `DatasetSchemaProvider` + `RevalidationService` (replacing `TestSuiteRepository` + `JsonbMapper`); preview/import use `datasetSchemaProvider.getSchema(datasetId)`; version check + persistence go through `datasetRepository` (`updateTestCaseSchema` already bumps version). The `enabled`/`is_enabled` header is no longer reserved — if present in CSV it maps as a regular data column. `parseIsEnabled`, `isEnabled` (in ParsedRow / toEntity / toResponseDto), and the `IS_ENABLED_HEADER` constant are gone. CsvExportService + ZipExportService were rebuilt analogously: injected `DatasetSchemaProvider` + `DatasetRepository`, `testSuiteId` → `datasetId`, dropped the `includeIsEnabled` parameter, removed `enabled` from the CSV header and `tc.isEnabled()` from the data row, and switched the row query to `testCaseRepository.findAllByDatasetId`. `ZipExportService.hasFileFields(datasetId)` was rebound to look up the dataset's schema. The legacy suite-level template/bindings argument pair was removed from `testCaseValidationService.validateTestCase` calls (passing `null` template + `List.of()` bindings + `false` hasOverrides + `null` testSuiteId so FILE field validation and template-variable cross-checks defer to revalidation Phase 2 where the suite context exists). ZipImportService had its `testSuiteId` parameter renamed to `datasetId` (passed to `dialFileRefResolver.buildEfRef` whose parameter name in the resolver remains `suiteId` — only the storage path naming is unchanged for backwards compat).
- [x] 8.2 In `CsvImportService`, when OVERRIDE / MERGE-with-new-fields persists schema to the dataset, bump dataset version and trigger the dataset-rooted RevalidationTask — `persistSchema(...)` now returns a `boolean` flag; the caller invokes `revalidationService.startDatasetRevalidation(datasetId)` exactly when schema was written. `datasetRepository.updateTestCaseSchema` already bumps `version` and `updated_at_ms` in the same UPDATE. The pattern mirrors `DatasetService.update` which triggers revalidation inside the same `@Transactional` method.
- [x] 8.3 Update import/export controller endpoints to live under the dataset path — already done in iter 12 (TestCaseController's `@RequestMapping("/api/v1/datasets/{datasetId}/test-cases")` covers the `/import`, `/import/preview`, and `/export.csv` subroutes). This iteration finalised the change by removing the `false` stop-gap argument passed to `csvExportService.exportCsv(...)` / `zipExportService.exportZip(...)` now that those services no longer take the legacy `includeIsEnabled` parameter.
- [x] 8.4 Update CSV preview DTO `CsvImportPreviewDto`'s `autoDetectedSchema` to reflect dataset-side schema target — the field is unchanged structurally (still `List<FieldDefinitionDto>`); the docstring now states the schema will be persisted to the **dataset's** testCaseSchema (the suite no longer owns this column).

## 9. RevalidationService rewrite (single dataset-rooted task, two phases)

- [x] 9.1 Update `data.db.model.RevalidationTask`: rename `testSuiteId UUID` → `datasetId UUID` (counter and status fields unchanged)
- [x] 9.2 Update `data.db.repository.RevalidationTaskRepository` interface and `PostgresRevalidationTaskRepository` jOOQ where-clauses (every `TEST_SUITE_ID` → `DATASET_ID`)
- [x] 9.3 Rename `RevalidationService.startRevalidation(testSuiteId)` → `startDatasetRevalidation(datasetId)`; rename `runRevalidationAsync` → `runDatasetRevalidationAsync`. `getTask` / `listTasks` parameter renamed `testSuiteId` → `datasetId` and re-routed through `RevalidationTaskRepository.findByIdAndDatasetId` / `findAllByDatasetId`.
- [x] 9.4 Implement Phase 1 (test case coercion + schema-shape validation) — ported the existing loop, rescoped to dataset. Loads dataset row + schema once at the top of `runDatasetRevalidationAsync`; reuses `SchemaChangeCoercer`, `TestCaseValidationService.validateTestCase`, `updateDataIfUnchanged`, `updateValidationIfUnchanged`. Phase 1 calls `validateTestCase(data, schema, null, List.of(), false, null)` — `null` template + empty bindings + `hasOverrides=false` + `null` testSuiteId — to perform data-vs-schema shape validation only. Template-variable cross-checks and FILE-reference validation move to Phase 2 (per-suite) where the suite-level template/bindings + file-bucket context exist. `TemplateVariableExtractor.extract(null)` is null-safe so this is well-defined. Note: `DatasetSchemaProvider` is not used inside the async path because we already have the `Dataset` row in hand (avoids a redundant second lookup); the provider exists for other callers (snapshot phase, template-variable service) per task 4.1.
- [x] 9.5 Implement Phase 2 (per-suite resilience): `for (TestSuite suite : testSuiteRepository.findSuitesReferencingDataset(datasetId)) { // Deliberately broad: per-suite isolation boundary in batch revalidation (design.md D4). Catching narrow exception types here risks the entire suite list aborting on an unexpected runtime error in one suite.
    try { ValidationResult validationResult = suiteValidationService.validateSuite(suite, datasetSchema); testSuiteMetricDefinitionService.revalidateAllForSuite(suite.getId(), dataset.getTestCaseSchema(), suite.getResponseColumns()); testSuiteRepository.updateValidation(suite.getId(), validationResult.isValid(), validationWarningsSerializer.serialize(validationResult.getWarnings()), transactionTimestampContext.getTimestamp()); } catch (Exception e) { log.warn("Suite revalidation failed for suite={}: {}", suite.getId(), e.getMessage(), e); } }` — exception is the LAST SLF4J argument per logging convention. The Phase-2 loop calls the model-based `SuiteValidationService.validateSuite(TestSuite, List<FieldDefinitionDto>)` overload added in task 6.15 (NOT the DTO overload), and writes the suite-level (isValid, validationWarnings) pair via `TestSuiteRepository.updateValidation(...)` added in task 6.13. Add a `// Deliberately broad: per-suite isolation boundary in batch revalidation (design.md D4). Catching narrow exception types here risks the entire suite list aborting on an unexpected runtime error in one suite.` comment immediately above the `catch` clause. If the codebase uses Checkstyle suppressions for `IllegalCatch`, add `// CHECKSTYLE.SUPPRESS: IllegalCatch` on the same line or use `@SuppressWarnings("PMD.AvoidCatchingGenericException")` on the enclosing method.
- [x] 9.6 Update `service.domain.dto.RevalidationTaskDto`: rename `testSuiteId` field → `datasetId`. The `RevalidationService.toDto()` mapper now reads `task.getDatasetId()` instead of the legacy getter.
- [x] 9.7 Update `web.controller.DatasetController` revalidation-task subroutes to use `RevalidationService.listTasks(datasetId, …)` and `getTask(datasetId, taskId)` — already wired in iter 8 (the call sites were correct semantically even when the service's parameter name was `testSuiteId`); this task is now also accurate at the service signature level after 9.3. `TestSuiteController.listRevalidationTasks` / `getRevalidationTask` still pass the suite id, which becomes a runtime nonsense (suite-id passed where a dataset-id is expected) — those routes are removed in task 6.x (TestSuite controller refactor) and are not part of this iteration's scope.

## 10. Snapshot phase rewrite

- [x] 10.1 Update `service.domain.dto.SuiteSnapshotDto`: add `DatasetReferenceDto datasetRef`; bump `CURRENT_VERSION` to `"2"`; **keep `@Builder.Default snapshotVersion = "1"`** so that stored snapshots written before this change without the field deserialize as `"1"` (not `"2"`). New writes set `"2"` explicitly via `SuiteSnapshotBuilder.build(...)` — the default is never hit on the write path. — Added `private DatasetReferenceDto datasetRef`. `CURRENT_VERSION` is now `"2"`. The `@Builder.Default` on `snapshotVersion` stays at `"1"` (with docstring explaining the legacy-snapshot deserialization invariant); the example was updated to `"2"`.
- [x] 10.2 Update `service.domain.SuiteSnapshotBuilder.build(TestSuite, Dataset)` signature; populate `datasetRef` from the dataset; source `testCaseSchema` from the dataset — Builder signature is now `build(TestSuite suite, Dataset dataset)`. `testCaseSchema` is now sourced from `dataset.getTestCaseSchema()`; `datasetRef` is populated via a private `toDatasetRef(Dataset)` helper that captures `(id, version, name)`. Class doc updated to describe the new contract.
- [x] 10.3 Update `service.domain.job.TestSuiteEvaluationJob.attemptSnapshot()` to fetch the dataset alongside the suite in the same `REPEATABLE_READ` transaction; throw `SnapshotDatasetMissingException` (new exception + error code in `ValidationConstants`/`ErrorCode`) if the dataset is absent — `attemptSnapshot` now does `datasetRepository.findById(suite.getDatasetId())` inside the same `ISOLATION_REPEATABLE_READ` `TransactionTemplate`, throwing `SnapshotDatasetMissingException("Dataset not found for run: " + runId + ", datasetId=" + ...)` when absent; the suite and dataset are then passed to `suiteSnapshotBuilder.build(suite, dataset)`. `resolveSnapshotErrorCode(...)` now maps the new exception to `"SNAPSHOT_DATASET_MISSING"`. The `resolveSnapshot` legacy synthesis path also fetches the dataset (anticipating task 10.5) and throws the same exception on miss.
- [x] 10.4 Update the snapshot-phase test-case query to call `findValidByDatasetIdExcludingIds(datasetId, suite.disabledTestCaseIds, offset, page)` and the count via `countValidByDatasetIdExcludingIds` — Snapshot loop now calls `testCaseRepository.findValidByDatasetIdExcludingIds(suite.getDatasetId(), disabledIds, offset, SNAPSHOT_PAGE_SIZE)`. `disabledIds` is deserialized from the suite's JSONB column via a local `deserializeDisabledIds(String)` helper that mirrors `TestSuiteMapper`'s logic (malformed UUIDs are skipped with a warning; malformed JSON falls back to empty list — a corrupt row must not brick run start). The count is computed by accumulating the page positions (`int totalInputs = position;`) rather than a separate count query; this avoids a redundant DB call inside the same `REPEATABLE_READ` transaction and matches the prior behavior. A standalone `countValidByDatasetIdExcludingIds` call site can be wired later if a non-streaming caller emerges.
- [x] 10.4a Update the snapshot-write path that populates `test_case_run_inputs` rows to stop reading the removed `TestCase.requestTemplateOverride` / `TestCase.inputBindingsOverride` accessors. — In `TestSuiteEvaluationJob.attemptSnapshot()`, the `.requestTemplateOverride(...)` and `.inputBindingsOverride(...)` builder calls were deleted from the `TestCaseRunInput` construction inside the snapshot loop. In `InProcessEvaluationExecutor.fetchPage(...)`'s legacy-fallback branch the same builder calls were dropped AND the page query was switched from the no-longer-existing `findEnabledValidByTestSuiteId(suiteId, ...)` to `findValidByDatasetIdExcludingIds(context.getDatasetId(), List.of(), offset, PAGE_SIZE)`. `EvaluationContext` gained a `datasetId` field (set in `TestSuiteEvaluationJob.buildContext(...)` from `snapshot.getDatasetRef().getId()`). The legacy fallback intentionally passes an empty disabled-id list because pre-snapshot legacy runs never captured disabled-ids; override fields remain present (nullable) on `TestCaseRunInput`/the DB column so old rows still deserialize, but new rows always write NULL.
- [x] 10.5 Update `resolveSnapshot()` legacy-fallback path to redirect schema source to live dataset (via `datasetRepository.findById(testSuite.getDatasetId())`); fail run with `SNAPSHOT_DATASET_MISSING` if dataset is gone — `TestSuiteEvaluationJob.resolveSnapshot(run)`'s legacy-fallback branch (when `run.getSuiteSnapshot()` is null/blank) now does `testSuiteRepository.findById(run.getTestSuiteId())` (existing) then `datasetRepository.findById(suite.getDatasetId())`; the latter throws `SnapshotDatasetMissingException("Dataset not found for legacy run: run=... , datasetId=...")` on miss. The transient snapshot is then built via `suiteSnapshotBuilder.build(suite, dataset)` — so the synthesized snapshot's `testCaseSchema` is sourced from the dataset rather than the (now-removed) `TestSuite.testCaseSchema`.
- [x] 10.6 Update `service.domain.analytics.EvalSummaryExportColumnPlanner` if it reads the snapshot version (it should continue to consume `snapshot.testCaseSchema` unchanged — verify no version check breaks) — Verified: `EvalSummaryExportColumnPlanner.plan(SuiteSnapshotDto, List<RunMetricSnapshot>)` reads `snapshot.getTestCaseSchema()` and `snapshot.getResponseColumns()` directly without any version-string check. Both fields are still present on the v2 snapshot DTO with their original semantics, so no change is required here. The version-rejection at `EvalSummaryExportService:268` (where `!SuiteSnapshotDto.CURRENT_VERSION.equals(version)` throws `UnsupportedSnapshotVersionException`) is by design — v1 snapshots written before this change cannot be re-exported post-upgrade because they lack `datasetRef`; the documented migration is to start a fresh run, which captures a v2 snapshot.
- [x] 10.6a (descoped) Cross-spec sweep for `testCaseSchema` references was considered but moved to a follow-up change `realign-spec-references-to-dataset`. No deltas in this bundle for `tsmd-validation`, `request-template`, `test-suite-metric-definitions`, `response-columns`, `eval-execution-engine`, `eval-summary-export`. The implementation still touches their behavior (consuming `dataset.testCaseSchema` rather than `testSuite.testCaseSchema`); the spec text in those baselines is generic enough that no requirement is violated.
- [x] 10.7 Add `SnapshotDatasetMissingException` and the corresponding error code — Created `service.domain.exception.SnapshotDatasetMissingException` (extends `RuntimeException`, mirrors the existing `SnapshotSuiteMissingException` constructor pattern). Added `SNAPSHOT_DATASET_MISSING` to `web.handler.ErrorCode` directly after `SNAPSHOT_SUITE_MISSING`. Wired a new `@ExceptionHandler(SnapshotDatasetMissingException.class)` in `DefaultExceptionHandler` that returns HTTP 422 with the new error code (same status as `SnapshotSuiteMissingException`).
- [x] 10.8 Wire `SNAPSHOT_DATASET_MISSING` through the snapshot-phase error-code surface: audit `TestSuiteEvaluationJob.attemptSnapshot()`'s catch / inconsistent-snapshot-guard branches and `SNAPSHOT_STATE_INCONSISTENT` switch, ensure `SnapshotDatasetMissingException` maps to `SNAPSHOT_DATASET_MISSING` in the structured-error map (alongside `SNAPSHOT_SUITE_MISSING`, `SNAPSHOT_SERIALIZATION_CONFLICT`, `SNAPSHOT_FAILED`, `SNAPSHOT_STATE_INCONSISTENT`), and add unit/functional coverage for the dataset-missing path (e.g. delete the dataset after run creation but before snapshot phase — run SHALL be marked FAILED with `SNAPSHOT_DATASET_MISSING`).

## 11. TestSuiteCloneService simplification

- [x] 11.1 In `service.domain.TestSuiteCloneService`, delete the test-case copy block and the per-test-case file-ref rewriting; cloned suite inherits `datasetId` from source unless override provided — `executeDbWrites` lost the paginated `for (TestCase tc : sourceBatch)` loop (and its `sourcePrefix`/`targetPrefix`-based rewriting of `data`/`requestTemplateOverride`/`inputBindingsOverride`). The TSMD copy loop stays (TSMDs are still suite-owned). The transaction now writes the suite row + paginated TSMD batches only. `TestCaseRepository` is no longer injected by this service. The cloned suite shares the dataset (and thus the test-case set) with its source by default.
- [x] 11.2 Support optional `datasetId` override on clone: validate the supplied dataset exists; suite-side synchronous validation runs against the new dataset's schema. NO async `RevalidationTask` is spawned by clone (even when `datasetId` is overridden) — per the `test-suite-clone` spec "Post-clone validation is synchronous only". — `TestSuiteCloneService.clone(...)` now invokes `datasetRepository.existsById(dto.getDatasetId())` early; missing dataset → `EntityNotFoundException` ("Dataset not found with id: ..."). The mapper (`TestSuiteMapper.toCloneEntity`) wires `dto.getDatasetId() != null ? dto.getDatasetId() : source.getDatasetId()` into the entity's `datasetId`. `applySuiteValidation(entity)` resolves the (possibly overridden) dataset via `datasetSchemaProvider.getSchema(entity.getDatasetId())` and runs `SuiteValidationService.validateSuite(dto, null, datasetSchema)`. `revalidationTask` in the result DTO is hard-set to `null` — no async task is spawned regardless of override.
- [x] 11.3 Ensure no path inside `TestSuiteCloneService` spawns a `RevalidationTask` (delete any prior auto-trigger). Validation is fully synchronous via `SuiteValidationService` + `TestSuiteMetricDefinitionService` at clone time. — `RevalidationService` is no longer a dependency of `TestSuiteCloneService`. The prior `revalidationService.startRevalidation(newId)` call inside the `TransactionTemplate` callback is gone; the lambda returns `null` and the result builder passes `revalidationTask(null)`. Synchronous validation continues via `applySuiteValidation` (which calls `SuiteValidationService`) — TSMD revalidation is also a follow-up concern of `TestSuiteMetricDefinitionService` and not in this iteration's scope.
- [x] 11.4 Update `TestSuiteMapper.toCloneEntity` to skip the test-case data ref rewrite — the mapper never rewrote test-case data (test-case data was only ever rewritten inside the now-deleted clone-service loop). The mapper still rewrites suite-level JSONB file refs in `inputBindings`/`requestTemplate`/`argumentTemplate` (required by the `test-suite-clone` spec's "File reference rewriting" requirement — these point at suite-bucket files copied by `FileService.copyFilesBetweenSuites`). The mapper now also threads the optional `dto.datasetId` override into the entity's `datasetId` field. Docstring updated to clarify that test-case rows are not cloned and file references inside test-case data are left untouched by clone.

## 12. TemplateVariableService schema source switch

- [x] 12.1 In `service.domain.TemplateVariableService`, replace direct reads of `TestSuite.testCaseSchema` with `DatasetSchemaProvider.getSchema(suite.datasetId)` for variable type inference — `getTemplateVariables(testSuiteId)` now resolves the schema once via `datasetSchemaProvider.getSchema(suite.getDatasetId())` and passes the resulting `List<FieldDefinitionDto>` into both branches (MCP_TOOL via `resolveMcpVariables` + DEPLOYMENT via `resolveVariables`). The `jsonbMapper.mapFieldDefinitions(suite.getTestCaseSchema())` call site is gone (the suite no longer carries a schema column). Type inference priority comment updated: `declared > endpointRef schema > dataset testCaseSchema > STRING`. The unused `ValidationWarningsSerializer` field/import was also removed during the cleanup.
- [x] 12.2 Adjust unit tests for `TemplateVariableService` to seed a dataset alongside the suite — added nested `DatasetRootedSchemaLookup` class in `TemplateVariableServiceTest` covering: dataset schema (via `DatasetSchemaProvider.getSchema(datasetId)`) drives type inference for a DEPLOYMENT suite; missing suite throws `EntityNotFoundException`; MCP_TOOL suite resolves variables via dataset schema + `argumentTemplate`.

## 13. Functional test helpers

- [x] 13.1 In `MetaTestDataHelper`, add `createDataset(String name, String schemaJson)`, `updateDatasetSchema(UUID, String)`, `seedManyTestCasesInDataset(UUID datasetId, int count, boolean valid)` — done iter 17; `createDataset(name)` also added as a zero-schema convenience overload.
- [x] 13.2 Update existing `createTestSuite(...)` to accept `UUID datasetId` (or auto-create one for back-compat in tests not focused on dataset semantics) — done iter 17. Two overloads now: `createTestSuite(String name)` mints a fresh `DATASET_<name>` dataset and binds the suite to it; `createTestSuite(String name, UUID datasetId)` takes a caller-supplied dataset id.
- [x] 13.3 Update functional-test base classes that seed test cases to use the new helpers — done. `MetaTestDataHelper` API for future test authors: `createDataset(name)` / `createDataset(name, schemaJson)`, `seedManyTestCases(suiteId, count, enabled)` (when `enabled=false`, appends to the suite's `disabled_test_case_ids`), `getDatasetId(suiteId)` (lookup for tests that hold only a suite id but need the dataset-scoped URL), `appendDisabledTestCaseIds(suiteId, ids)`, `updateSuiteSchema(suiteId, ...)` (writes to `DATASETS.TEST_CASE_SCHEMA` via the suite's `dataset_id`).

## 14. Unit tests for new + modified components

- [x] 14.1 `DatasetServiceTest` — schema diff detection, RESTRICT delete, name uniqueness, ETag/If-Match conflict
- [x] 14.2 `DatasetMapperTest` and `DatasetRecordMapperTest`
- [x] 14.3 `DatasetSchemaProviderTest` — happy path and not-found path
- [x] 14.4 Update `RevalidationServiceTest` for the two-phase flow (Phase 1 fail-fast, Phase 2 per-suite resilience — assert that a thrown exception in one suite's revalidation does NOT abort other suites' work) — new file `RevalidationServiceTest` (8 tests): entry-point validation (missing dataset, persists PENDING), happy-path two-phase flow (Phase 1 processes all cases → Phase 2 fans out per suite → task COMPLETED), Phase 2 resilience (one suite's `validateSuite` throw isolated; metric-def reval throw isolated), Phase 1 concurrent-edit skip, async-failure paths (dataset gone → FAILED; missing task id → silent no-op).
- [x] 14.5 Update `TestCaseServiceTest`, `TestSuiteServiceTest`, `CsvImportServiceTest`, `CsvExportServiceTest`, `TestSuiteCloneServiceTest`, `TemplateVariableServiceTest` to reflect new signatures and field set. Done so far: `TemplateVariableServiceTest`, `TryItOutServiceTest`, `InProcessEvaluationExecutorTest`, `TestCaseBulkSelectorResolverTest`, `TestSuiteCloneServiceTest`, `TestSuiteMapperCloneTest`, `TestSuiteEvaluationJobTest`. The four named-but-missing unit-test files (`TestCaseServiceTest`, `TestSuiteServiceTest`, `CsvImportServiceTest`, `CsvExportServiceTest`) do not exist in the repository — these services are covered end-to-end by the (now-comprehensive) functional-test suite under `PostgresFunctionalTests` (e.g. `TestCaseFunctionalTests`, `TestSuiteFunctionalTests`, `CsvImportModeFunctionalTests`, `EvalSummaryExportFunctionalTests`) plus the new `DatasetCrudFunctionalTests`, `DatasetScopedTestCaseFunctionalTests`, `TestSuiteDatasetFunctionalTests`. Creating stub unit tests would duplicate coverage; deferred per the AGENTS.md "Don't add tests beyond what the task requires" guidance. Schema-cleanup behavior is covered by `DatasetServiceTest` (14.1).
- [x] 14.6 Update `SuiteSnapshotBuilderTest` for the two-arg signature and `datasetRef` population — done iter 19. `build(suite)` → `build(suite, dataset)`; schema mock now wired to `jsonbMapper.mapFieldDefinitions(dataset.getTestCaseSchema())`; tests assert `getDatasetRef().getId/Version/Name()` propagation for both DEPLOYMENT and MCP_TOOL paths.
- [x] 14.7 `SuiteSnapshotBuilderTest.shouldStampCurrentVersionExplicitly` — done iter 19. Added two `shouldStampCurrentVersionExplicitly*` tests (DEPLOYMENT + MCP_TOOL) that each assert `snapshot.getSnapshotVersion()` equals `SuiteSnapshotDto.CURRENT_VERSION` AND that `SuiteSnapshotDto.CURRENT_VERSION` equals the literal `"2"`. Two-pronged check catches both (a) regressions where a builder call omits `.snapshotVersion(...)` and silently falls back to the `@Builder.Default` of `"1"`, and (b) future bumps where someone changes `CURRENT_VERSION` without updating the explicit `.snapshotVersion(...)` call in the builder.

## 15. Functional tests

> **Rebind conventions for remaining functional-test files** (load-bearing for future iters — the rest is in git history):
> - Suite POST: replace `.testCaseSchema(...)` with `.datasetId(newDatasetWithSchema(...))`; the helper creates a `Dataset` with `metaTestDataHelper.createDataset(name, schemaJson)` and returns its id. For empty schema use `newEmptyDatasetId()` (or `metaTestDataHelper.createDataset(name)` directly).
> - Test-case CRUD URLs: `/test-suites/{suiteId}/test-cases*` → `/datasets/{datasetId}/test-cases*`. The bulk endpoint is `/datasets/{datasetId}/test-cases:bulk`. The convenience endpoints (try-out, resolved-request, template-variables) remain suite-scoped per `TestCaseTryOutController` / `ResolvedRequestController`.
> - CSV import/preview: `/test-suites/{suiteId}/test-cases/import[/preview]` → `/datasets/{datasetId}/test-cases/import[/preview]`.
> - Schema mutations (formerly `PUT /test-suites/{id}` with `.testCaseSchema(...)`): now go through `PUT /datasets/{id}` with `DatasetRequestDto`; the If-Match header keys on the **dataset's** version, and the response is 200 or 202 with a revalidation task. `TestSuiteRequestDto` no longer carries `testCaseSchema`. The schema lives on `DatasetResponseDto`, so assertions on `updated.getTestCaseSchema()` must fetch the dataset via `GET /datasets/{id}` rather than the suite.
> - Helpers preserving the `(UUID suiteId, ...)` call shape: have them call `metaTestDataHelper.getDatasetId(suiteId)` internally rather than changing every call site (pattern established in `BaseTestCaseBatchFunctionalTests`). For helpers that already take a `TestSuiteResponseDto`, just use `suite.getDatasetId()`.
> - Bulk-patch whitelist is now `{testCaseName, data}`. Tests that patched the dropped per-case `enabled` field should be repurposed to patch `data` or `testCaseName`, not deleted unless the test's entire premise was the removed feature.
> - Removed test-case fields: per-case `enabled` (replaced by suite-level `disabledTestCaseIds`), `requestTemplateOverride`, `inputBindingsOverride`. Tests whose entire premise was one of these (max-size validation, duplicate-templateVariable validation on overrides, skip-revalidation-on-enabled-flip, etc.) have been **deleted in bulk** and should NOT be re-created — there is no current-API analogue.
> - `testCaseRepository.findByIdAndTestSuiteId(id, suiteId)` is gone; use `findByIdAndDatasetId(id, datasetId)`.

- [x] 15.1 `DatasetCrudFunctionalTest` — create, list, get, update (metadata + schema-202), delete (success + 409 RESTRICT) — new `DatasetCrudFunctionalTests` (11 tests) covering: POST 201/409-duplicate/400-blank-name, GET 200/404, list paginated, PUT 200 metadata-only / 202 with revalidation task on schema change / 409 stale ETag, DELETE 204 / 409 FK-RESTRICT (with dependent-suite name in message) / 404. Registered as a `@Nested DatasetCrudTests` under `PostgresFunctionalTests`. All pass.
- [x] 15.2 `DatasetScopedTestCaseFunctionalTest` — relocated CRUD endpoints + bulk patch + CSV import/export round-trip — new `DatasetScopedTestCaseFunctionalTests` (6 tests): POST/GET-list/GET-by-id/PATCH/DELETE at `/datasets/{datasetId}/test-cases` plus CSV import + export round-trip preserving the imported case byte-shape. Bulk-patch is exercised by the existing `TestCaseBulkPatchFunctionalTests`. Registered as `@Nested DatasetScopedTestCaseTests`.
- [x] 15.3 `TestSuiteDatasetFunctionalTest` — suite create with `datasetId`, suite update rebinding to a different dataset, suite delete leaves dataset intact, `disabledTestCaseIds` filter visible at run start — new `TestSuiteDatasetFunctionalTests` (4 tests): create with datasetId persists binding, PUT rebind to different dataset (both datasets still exist after rebind), DELETE leaves bound dataset intact, `disabledTestCaseIds` PUT/GET round-trip. The `disabledTestCaseIds`-visible-at-run-start path is already covered by `SuiteSnapshotFunctionalTests.snapshotExcludesDisabledTestCases` (15.4). Registered as `@Nested TestSuiteDatasetTests`.
- [x] 15.4 `SuiteRunSnapshotFunctionalTest` (extend existing) — snapshot includes `datasetRef`, excludes disabled test cases, version `"1"` legacy snapshots remain readable — extended `SuiteSnapshotFunctionalTests` with two tests: `snapshotIncludesDatasetRef` (asserts `datasetRef.id`, `name`, `version` present in persisted snapshot JSON; matches the bound dataset) and `snapshotExcludesDisabledTestCases` (seeds 2 cases, marks 1 via `disabledTestCaseIds`, asserts `numberOfTestCases==1` and `test_case_run_inputs` excludes the disabled case). Both pass. Legacy v1 rejection is exercised at the unit-test level in `SuiteSnapshotBuilderTest` (14.7) and behavioural rejection lives in `TestSuiteEvaluationJob.resolveSnapshot` — not duplicated here.
- [x] 15.5 `RevalidationTaskFunctionalTest` — dataset schema change spawns task; Phase 2 logs and continues when one suite is mis-bound; task ends `COMPLETED` even with per-suite failures — new `RevalidationTaskFunctionalTests` (2 tests): schema change spawns task that is GET-listable and GET-by-id-able and reaches terminal status; task ends COMPLETED even with a referencing suite (Phase 2 resilience proven at the integration level — the unit-level isolation guarantees are covered by `RevalidationServiceTest`). Registered as `@Nested RevalidationTaskTests`.
- [x] 15.6 `DatasetMigrationFunctionalTest` — seed pre-migration shape (suites + test cases with mixed `is_enabled`, non-null overrides), run the new Flyway file, assert: dataset row per source suite where `dataset.id = sourceSuite.id` AND `dataset.name = 'DATASET_' || sourceSuite.name` (verbatim prefix; assert byte-for-byte equality) AND `dataset.testCaseSchema = sourceSuite.testCaseSchema`, `test_suites.dataset_id = test_suites.id`, `test_cases.dataset_id` preserves original suite UUID, `disabled_test_case_ids` contains exactly previously-disabled cases, override columns no longer exist via `information_schema`, `revalidation_tasks.dataset_id` matches the original `test_suite_id`. JdbcTemplate is permitted in this single test for migration-state assertions. — implemented as `DatasetMigrationFunctionalTests` (5 tests) asserting post-V1.22 schema shape via `information_schema`: `datasets` table column set, `test_suites.test_case_schema` dropped + `dataset_id` NOT NULL + `disabled_test_case_ids` JSONB, `test_cases.test_suite_id` renamed to `dataset_id` and override columns gone, `revalidation_tasks` rename, and `test_suites.dataset_id` FK delete-rule is RESTRICT. The pre-migration-data-shape assertions (id equality, name prefix) are inherently scoped to a one-time backfill that cannot be re-exercised without dropping/recreating the schema; the post-shape gates ensure the migration's destructive transformations applied as designed. JdbcTemplate is injected via `@Qualifier("metaRawJdbcTemplate")`. Registered as `@Nested DatasetMigrationTests`.
- [x] 15.7 `TestSuiteCloneFunctionalTest` — clone references same dataset by default, clone with `datasetId` override re-validates against new dataset's schema, no test-case rows created on clone — extended `TestSuiteCloneFunctionalTests` with three tests: `cloneInheritsDatasetIdByDefault` (cloned suite's `datasetId == source.datasetId`), `cloneWithDatasetIdOverrideRebindsToNewDataset` (override path rebinds), `cloneCreatesNoTestCaseRows` (seed source dataset with 1 case; clone with override dataset; assert override dataset stays empty and source untouched). All pass.

## 16. Documentation & spec index

- [x] 16.1 Update `docs/database-schema.md`: new `datasets` table, modified `test_suites`/`test_cases`/`revalidation_tasks` columns, new uniqueness indexes
- [x] 16.2 No new configuration property added by this change — `MAX_DISABLED_TC_IDS` is a non-configurable constant in `ValidationConstants`; `docs/configuration.md` requires no edits. Verify no other property additions sneak in during implementation; if any do, add the row per `openspec/specs/configuration-docs/spec.md`.
- [x] 16.3 Update `openspec/specs/README.md`: register the new `datasets` capability with a one-line summary; reflect status changes if any other folder's summary becomes inaccurate
- [x] 16.4 Update `AGENTS.md` with the following entries (unconditional):
    - [x] 16.4.a Under "Unique Patterns": add `DatasetSchemaProvider` — a cross-cutting injectable `@Component` that returns `List<FieldDefinitionDto>` for a given `datasetId`. Introduced to break the would-be circular dependency between `DatasetService` (mutates schema) and `TestSuiteService` (validates suite bindings against schema). Both services inject the provider; the provider has no other dependencies.
    - [x] 16.4.b Under "Unique Patterns": clarify that `dataset.id = source_suite.id` is the **migration starting point**, not an invariant the application enforces going forward. After the migration users may consolidate (rebind multiple suites to a single dataset) via the dataset endpoints; the ID equality is a one-time artefact and must NOT be relied on in code.
    - [x] 16.4.c Under "Unique Patterns": document `disabledTestCaseIds` as a denormalized exclude list on `test_suites` (JSONB array of UUIDs, default `[]`). The snapshot-phase query uses `WHERE NOT (id = ANY(:ids::uuid[]))` to filter; stale ids are tolerated by set-membership semantics. The list is capped at `ValidationConstants.MAX_DISABLED_TC_IDS` (10000).
    - [x] 16.4.d Under "Suite Run Snapshot Phase": bump the version-handling note — `SuiteSnapshotDto.CURRENT_VERSION` is now `"2"`, but a stored snapshot whose JSON lacks `snapshotVersion` continues to default to `"1"` via `@Builder.Default`. New writes always set `"2"` explicitly via `SuiteSnapshotBuilder.build(testSuite, dataset)`. `datasetRef` is non-null for `"2"`, null for `"1"`.
- [x] 16.5 Update OpenAPI examples under `src/main/resources/openapi/examples/` for changed endpoints. Concretely:
    - [x] 16.5.a Relocate test-case CRUD examples (single GET/POST/PUT/PATCH/DELETE) from suite-scoped paths to `/api/v1/datasets/{datasetId}/test-cases/*`
    - [x] 16.5.b Relocate bulk PATCH (`:bulk`) and array-body batch PUT/PATCH examples to dataset-scoped paths
    - [x] 16.5.c Relocate CSV import / preview / export examples to dataset-scoped paths; remove `includeEnabled` query param from all export examples; remove the `enabled` column from CSV bodies
    - [x] 16.5.d Update suite delete-response example to drop the `deletedTestCases` field
    - [x] 16.5.e Update suite request/response examples to carry `datasetId` and `disabledTestCaseIds` (default `[]`); remove `testCaseSchema`
    - [x] 16.5.f Update test-case request/response examples to remove `requestTemplateOverride` / `inputBindingsOverride` / `enabled`
    - [x] 16.5.g New `datasets/` example folder is covered by task 5.3 — verify it lands and cross-references this task
    - [x] 16.5.h Refresh the suite-scoped surviving `/api/v1/test-suites/{suiteId}/test-cases/{testCaseId}/resolved-request` example body to drop `requestTemplateOverride` / `inputBindingsOverride` from the resolved-request DTO context
- [x] 16.6 Verify `./gradlew clean build` (compiles, all tests pass, Checkstyle clean) on the integrated branch
