## 1. Service layer — restore `getTestCaseTemplateVariables`

- [x] 1.1 In `service/domain/TemplateVariableService.java`, append `private final TestCaseService testCaseService;` as the LAST `private final` field so the existing `@RequiredArgsConstructor` gains a trailing 7th constructor arg (the existing 6 args stay in place, in order).
- [x] 1.2 Add `public List<TemplateVariableDto> getTestCaseTemplateVariables(UUID testSuiteId, UUID testCaseId)` annotated `@Transactional(value = "metaTransactionManager", readOnly = true)`: load the suite via `testSuiteRepository.findById` (404 `EntityNotFoundException` if absent); **if `suite.getDatasetId() == null`, throw `EntityNotFoundException("TestCase not found: " + testCaseId)`** (an unbound suite owns no test cases → 404; this guard is REQUIRED because `findByIdAndDatasetId(id, null)` NPEs on `datasetId.toString()` → HTTP 500, and `TestCaseService.getById` has no null guard); fetch the test case via `testCaseService.getById(suite.getDatasetId(), testCaseId, false)` and read its `data` map; resolve `testCaseSchema` via `datasetSchemaProvider.getSchema(suite.getDatasetId())` (datasetId is non-null after the guard); branch on `suite.getSuiteType()` — `MCP_TOOL` → `resolveMcpVariables(argumentTemplate, bindings, testCaseSchema, data)`, otherwise `resolveVariables(template, bindings, testCaseSchema, endpoint, data)` (reuse the existing private methods unchanged, passing the real `data` instead of `null`).
- [x] 1.3 Update the class-level Javadoc to note the test-case entry point resolves `resolvedValue` against the test case's dataset-owned `data`.

## 2. Web layer — restore the endpoint

- [x] 2.1 In `web/controller/TemplateVariableController.java`, re-add `@GetMapping("/{testSuiteId}/test-cases/{testCaseId}/template-variables")` returning `List<TemplateVariableDto>`, delegating to `templateVariableService.getTestCaseTemplateVariables(testSuiteId, testCaseId)` with `@PathVariable UUID testSuiteId` and `@PathVariable UUID testCaseId`.
- [x] 2.2 Add OpenAPI annotations matching the suite-level operation style: `@Operation` summary "Get effective template variables for a test case" with a description reflecting the dataset paradigm (suite template/bindings resolved against the test case's data; no per-test-case overrides), `@ApiResponse(200, array of TemplateVariableDto)`, `@ApiResponse(404, "Test suite or test case not found")`, and `@Parameter` descriptions for both path variables.

## 3. Unit tests

- [x] 3.1 In `service/domain/TemplateVariableServiceTest.java`, update the two `new TemplateVariableService(...)` constructions for the added `TestCaseService` argument, adding it as the LAST (trailing) constructor argument at both call sites (pass `null` at the `resolveVariables`-only site; pass a Mockito mock at the wired site).
- [x] 3.2 Add a nested test class for `getTestCaseTemplateVariables(testSuiteId, testCaseId)` with mocked `testSuiteRepository`, `testCaseService`, and `datasetSchemaProvider`. Cover: data-field binding resolves `resolvedValue` from the test case's `data` (e.g. `data["promptField"] = "Hello"` → `"Hello"`); constant-value binding resolves to the constant; type inference uses the dataset schema; `404` (`EntityNotFoundException`) when the suite is missing; `404` propagated when `testCaseService.getById` throws (missing test case); **`404` (and NO call to `testCaseService.getById`) when the suite's `datasetId` is null** (verifies the unbound-suite guard, not an NPE — assert via `verify(testCaseService, never()).getById(any(), any(), anyBoolean())`); **empty list when the HTTP suite has a null `requestTemplate`**; **empty list when the `MCP_TOOL` suite has a null `argumentTemplate`**; and the `MCP_TOOL` path resolves from `argumentTemplate` + data.

## 4. Functional tests

- [x] 4.1 In `functional/tests/TemplateVariableFunctionalTests.java`, add a helper that creates a dataset (capturing its `datasetId`), POSTs a suite bound to that dataset with a template + bindings, and seeds a test case with `data` via `metaTestDataHelper.seedTestCaseInDataset(datasetId, name, dataJson)`. Note `dataJson` is a raw JSON String (deserialized by `getById` to `Map<String,Object>`), so numeric/boolean fields MUST use unquoted JSON literals (e.g. `{"temperature":0.7,"enabled":true}`, not `"0.7"`/`"true"`) so typed-value (Number/Boolean) resolution is actually exercised rather than quoted strings.
- [x] 4.2 Add functional tests hitting `GET /test-suites/{suiteId}/test-cases/{testCaseId}/template-variables`: assert `resolvedValue` resolves from data for a data-field binding, the constant for a constant-value binding, the template default when the data field is missing, `404` for a non-existent suite, and `404` for a non-existent test case. Add at least one `MCP_TOOL`-suite case (variables from `argumentTemplate`, resolved from data). **Add an unbound-suite case** (suite created with `datasetId = null`, e.g. via `metaTestDataHelper`) asserting the endpoint returns **`404` — explicitly NOT `500`** (regression guard for the null-`datasetId` NPE path). The concrete `PostgresFunctionalTests$TemplateVariableTests` already extends this base — no new wiring needed.

## 5. Verification & cleanup

- [x] 5.1 Run `./gradlew spotlessApply` to apply palantir-java-format.
- [x] 5.2 Run `./gradlew test --tests "com.epam.aidial.evaluation.service.domain.TemplateVariableServiceTest"` — all pass.
- [x] 5.3 Run `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$TemplateVariableTests"` — boots the context and exercises the restored endpoint end-to-end; all pass.
- [x] 5.4 Run `./gradlew checkstyleMain checkstyleTest` — no violations.
- [x] 5.5 Confirm the restored operation appears in Swagger UI / OpenAPI output (via build) and `/opsx:verify` passes before archiving.
