# Tasks

## 1. Configuration — dedicated global cap property

- [x] 1.1 Create `SseEventProcessingProperties` in `configuration.properties` (`@ConfigurationProperties(prefix = "sse-event-processing")`, `@Validated`, `@LogExecution`, Lombok `@Getter/@Setter`) with one field `Long maxTotalDurationMs` (`@NotNull @Min(1000)`). No Java field-initializer default. (done: class compiles, bean registered)
- [x] 1.2 Register the new properties class for binding (add to the existing `@ConfigurationPropertiesScan`/`@EnableConfigurationProperties` setup the same way peer properties classes are registered). (done: context picks up `sse-event-processing.max-total-duration-ms`)
- [x] 1.3 Add the default to `application.yml`: `sse-event-processing.max-total-duration-ms: ${SSE_EVENT_PROCESSING_MAX_TOTAL_DURATION_MS:3600000}` as a new top-level block (NOT under `test-suite-run.sse`). (done: app boots with default)
- [x] 1.4 Document the new property in `docs/configuration.md`. Because `sse-event-processing` is a NEW top-level YAML block, create a NEW numbered subsection `### 6.12 SSE Event Processing` under the **Evaluation Engine** (group 6) — 6.11 is the last existing subsection — and add the matching Table-of-Contents anchor (`- [SSE Event Processing](#612-sse-event-processing)`) nested under `6. [Evaluation Engine]`. Add a six-column row (`Property | Environment Variable | Default | Required | Applied when | Description`) with `Property = sse-event-processing.max-total-duration-ms`, `Environment Variable = SSE_EVENT_PROCESSING_MAX_TOTAL_DURATION_MS`, `Default = 3600000`, `Required = No` (sanctioned four-term value; safe default), `Applied when = -`. (done: subsection + ToC entry present and conform to configuration-docs schema)

## 2. Parser — idle timeout + max-total cap

- [x] 2.1 Change `SseEventParser.parse` signature to `parse(InputStream stream, long idleTimeoutMs, long maxTotalDurationMs, long maxBytes)`. (done: signature updated)
- [x] 2.2 Compute `startMs = clock.millis()`, `hardDeadlineMs = startMs + maxTotalDurationMs`, and a mutable `idleDeadlineMs = startMs + idleTimeoutMs`. Inside the read loop, capture `now = clock.millis()` once at the top of each iteration. Before processing the line: if `now > hardDeadlineMs || now > idleDeadlineMs` return `SseParseResult(events, TIMEOUT, null)`. After the check, reset `idleDeadlineMs = now + idleTimeoutMs` — reuse the same `now`, one clock read per line, matching the design.md snippet — for every line read. Remove the old `deadlineMs` param and the WIP `// todo` comments. (done: idle + hard cap logic in place)
- [x] 2.3 Revert comment handling to "ignored": collapse the WIP branches back to `else if (line.startsWith(":") || line.startsWith("id:") || line.startsWith("retry:"))` and delete the `HEARTBEAT_EVENT_TYPE` constant + the `currentEventType = HEARTBEAT_EVENT_TYPE` assignment. (done: comments no longer mutate `currentEventType`)
- [x] 2.4 Update the `SseEventParser` Javadoc to describe idle timeout + absolute cap (replace the "Enforces deadline" wording). (done: Javadoc accurate)

## 3. Caller wiring

- [x] 3.1 `StreamingResponseAccumulator`: replace the `long deadlineMs` field with `long idleTimeoutMs` + `long maxTotalDurationMs`; update the constructor and the `parse(...)` call; refresh the class Javadoc that mentions "deadline". (done: forwards both timeouts)
- [x] 3.2 `EvaluationWorker.invokeSingle`: inject `SseEventProcessingProperties`; drop `deadlineMs`; construct `new StreamingResponseAccumulator(sseEventParser, objectMapper, context.getRequestTimeoutMs(), sseEventProcessingProperties.getMaxTotalDurationMs(), context.getMaxResponseSizeBytes())`. (done: eval path passes idle = requestTimeoutMs, cap = global property)
- [x] 3.3 `TryItOutService.buildSseResponse`: inject `SseEventProcessingProperties`; replace the `deadlineMs` computation with `sseEventParser.parse(result.eventStream(), dialCoreProperties.getTryOut().getReadTimeoutMs(), sseEventProcessingProperties.getMaxTotalDurationMs(), maxBytes)`. (done: try-it-out path passes idle = read-timeout-ms, cap = global property)
- [x] 3.4 Confirm no other callers of `SseEventParser.parse(...)` remain on the old signature (grep). (done: project compiles)

## 4. Tests

- [x] 4.1 `SseEventParserTest`: update all `parse(...)` call sites to the new 4-arg signature; replace `FAR_FUTURE_DEADLINE` constants with large `idleTimeoutMs`/`maxTotalDurationMs` durations. (done: existing scenarios pass unchanged in behavior)
- [x] 4.2 `SseEventParserTest`: introduce a deterministic **advancing test `Clock`** — a `Clock` subclass backed by a mutable `AtomicLong` that the test increments between reads. Do NOT use `Clock.fixed`/`Clock.offset`: both return a constant per call and cannot cross a deadline mid-parse. Use it to convert the "deadline already exceeded" test into an **idle-timeout** test: advance the clock past `idleTimeoutMs` between lines so `clock.millis()` crosses `idleDeadlineMs`; assert `TIMEOUT` with partial events. This advancing clock is the shared mechanism for tasks 4.3, 4.4, and 4.6. (done: idle timeout asserted deterministically)
- [x] 4.3 `SseEventParserTest`: using the advancing clock from 4.2, add a test where lines arrive with gaps each shorter than `idleTimeoutMs` over a total span > `idleTimeoutMs` and assert NO timeout (idle deadline keeps resetting). (done: reset-on-line behavior asserted)
- [x] 4.4 `SseEventParserTest`: using the advancing clock from 4.2, add a test where the clock crosses `maxTotalDurationMs` while lines keep arriving (idle never expires) and assert `TIMEOUT` (hard cap). (done: cap behavior asserted)
- [x] 4.5 `SseEventParserTest`: add a test asserting a mid-block comment line does NOT change the emitted event type (the type-leak regression guard), and a test asserting a named `event: heartbeat` event is emitted. (done: comment/heartbeat behavior asserted)
- [x] 4.6 `StreamingResponseAccumulatorTest`: update constructor usages to the new `(…, idleTimeoutMs, maxTotalDurationMs, maxBytes)` shape; migrate the deadline-exceeded scenario to idle/cap semantics. Add a test (using the advancing clock from 4.2) asserting the accumulator propagates `ExecutionStatus.TIMEOUT` when the underlying parser hits `maxTotalDurationMs` under continuous activity (idle never expires) — the eval-path analog of parser task 4.4. (done: suite compiles and passes)
- [x] 4.7 `TryItOutServiceTest`: stub `SseEventProcessingProperties.getMaxTotalDurationMs()`; update `sseEventParser.parse(...)` mock matchers to the 4-arg signature (`anyLong()` x3). (done: suite compiles and passes)
- [x] 4.8 Run the affected suites: `./gradlew test --tests "com.epam.aidial.evaluation.service.domain.job.SseEventParserTest" --tests "com.epam.aidial.evaluation.service.domain.job.StreamingResponseAccumulatorTest" --tests "com.epam.aidial.evaluation.service.domain.TryItOutServiceTest"`. (done: all green)
- [x] 4.9 Run a functional test that boots the context to confirm `SseEventProcessingProperties` binds and `EvaluationWorker`/`TryItOutService` wire up (e.g. the relevant `PostgresFunctionalTests` nested suite). (done: context starts, test passes)

## 5. Quality gates & spec sync

- [x] 5.1 Run `./gradlew spotlessApply` then `./gradlew checkstyleMain checkstyleTest`. (done: formatting + checkstyle clean)
- [x] 5.2 During archive, sync the delta requirements into `openspec/specs/sse-event-parsing/spec.md` and `openspec/specs/try-it-out/spec.md`. In `try-it-out`, this MODIFIES both "Try-it-out SSE timeout enforcement" and "Try-it-out SSE parsing error handling" — reword the latter's Scenario "SSE timeout returns partial response" (which still says "exceeds the deadline") to the idle-timeout + absolute-cap wording, and scrub any remaining Purpose/timeout-configuration prose that references the old single `deadline`. In `sse-event-parsing`, every `deadlineMs` reference lives inside the two MODIFIED requirements ("Deadline enforcement" and "SSE event parser component" — including its "Stream never terminates" and "Comment lines are ignored" scenarios), which sync replaces wholesale; additionally verify the non-requirement prose (Purpose, Wire Format Rules table, Implementation Notes) carries no stale `deadlineMs`/comment-as-heartbeat wording — it is currently clean, but re-check at sync since those sections sit outside any requirement block and are not auto-merged. (done: main specs free of stale `deadlineMs`/`deadline` wording)
