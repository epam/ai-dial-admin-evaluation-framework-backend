## 1. Data model — migrations & jOOQ

- [x] 1.1 Add `V1.13__AddTurnColumnsToTestCaseRunResults.sql`: `ADD COLUMN turn_index INTEGER NOT NULL DEFAULT 0`, `total_turns INTEGER NOT NULL DEFAULT 1`; drop `uq_results_run_case_index` and recreate as `(test_suite_run_id, test_case_id, run_index, turn_index, created_at_ms)` (use `CREATE UNIQUE INDEX CONCURRENTLY` + non-transactional migration if analytics tables are large). Note: analytics versions are V1.13/V1.14, leaving V1.12 free for development's incoming analytics migration to avoid a rebase collision. Done: migration applies cleanly on a populated table, existing rows read `0/1`.
- [x] 1.2 Add `V1.14__AddTurnColumnsToEvalSummaries.sql`: same two columns; recreate `uq_eval_summaries_natural_key` as `(test_suite_run_id, test_case_id, run_index, turn_index, computation_id, created_at_ms)`. Done: migration applies; existing rows read `0/1`.
- [x] 1.3 Edit `V1.25__AddMultiStepToTestSuites.sql` in place to name the column `multi_turn` (branch-only; note `flyway repair` for local DBs). Done: fresh migrate yields `test_suites.multi_turn`.
- [x] 1.4 Run `./gradlew generateJooq`; commit the generated diff under `src/main/java-generated/`. Done: generated tables/records carry `turn_index`/`total_turns` and `multi_turn`, `spotlessCheck` excludes generated sources.
- [x] 1.5 Update `docs/database-schema.md` for the two analytics tables (columns, new unique indexes) and `test_suites.multi_turn`. Done: doc matches migrations.

## 2. Analytics models, mappers, repositories

- [x] 2.1 Add `turnIndex`/`totalTurns` (`int`) to `data.db.analytics.model.TestCaseRunResult` and `EvalSummary`. Done: fields present; Lombok builders compile.
- [x] 2.2 Update the result and summary `*RecordMapper`s to map the two columns. Done: unit round-trip maps `0/1` and `i/N`.
- [x] 2.3 Update `TestCaseRunResultRepository` and eval-summary batch-write insert column lists **and** the `ON CONFLICT (<natural key>)` target to include `turn_index`. Done: idempotent re-insert of a turn row is a no-op; distinct turns coexist.

## 3. Executor — per-turn emission

- [x] 3.1 Change `EvaluationWorker.execute(...)` to return `List<TestCaseRunResult>`; single-turn/MCP paths return a one-element list with `turnIndex=0,totalTurns=1`. Done: unit test asserts one-element list for single-turn.
- [x] 3.2 Rename `MultiStepConversationExecutor` → `MultiTurnConversationExecutor` and rewrite `execute` to return one scalar `TestCaseRunResult` per turn (reuse `ResponseColumnExtractor` + `TestCaseRunResultFactory`); set `turn_index=i`, `total_turns=planned N`, `test_case_data=turnPlan.project(data,i)`, `request_body`=full accumulated request. Done: unit test on a 3-turn case yields 3 scalar rows with correct turn fields.
- [x] 3.3 Implement abort semantics: `k` SUCCESS rows + 1 ERROR row (`total_turns=N`); data-shape failure → single ERROR row `turn_index=0,total_turns=0`. Done: unit tests for mid-abort and each data-shape failure.
- [x] 3.4 Delete `MultiStepColumnAccumulator`, `MultiStepWarningAccumulator`, `MultiStepResultAssembler`. Done: classes removed, no references remain.
- [x] 3.5 Update `InProcessEvaluationExecutor` to call `resultBatchWriter.addResults(buffer, workerResults)`; drop the single-result path. Done: functional run persists all turn rows.

## 4. Progress reporting

- [x] 4.1 Replace `ResultBatchWriter.addResult` with `addResults(buffer, List<TestCaseRunResult>)`: +1 completed-conversation counter per call, buffer all rows, flush by row batch size, `notifyProgress(conversationsCompleted, totalCases)`. Done: unit test — 3-turn convo buffers 3 rows, advances progress by 1.

## 5. Metric evaluation — JSONata/array rollback + turn context

- [x] 5.1 Remove `jsonataExpression` from `ResponseBindingSourceDto` and `TestCaseBindingSourceDto` (+ OpenAPI schema). Done: fields gone; DTOs compile.
- [x] 5.2 Simplify `BindingResolver.resolveSource` to return the scalar column value directly; remove `applyJsonataSelector` and the `JsonataEvaluationService` dependency. Done: unit tests for scalar Response/TestCase/Constant + missing-column fail-fast + present-null.
- [x] 5.3 Remove `ArrayBindingTypeMismatchDetector` and its use in `MetricEvaluationWorker`. Done: class removed, no references.
- [x] 5.4 Remove the `INVALID_EXPRESSION` checks (3b/4b) from `MetricDefinitionValidationService` (and drop the now-unused `ValidationWarningCode.INVALID_EXPRESSION`). Done: validation no longer emits `INVALID_EXPRESSION`.
- [x] 5.5 Remove `ExtractionWarningDto.stepIndex` (and any `ConversationOutcome` stamping). Done: warnings serialize without `stepIndex`.
- [x] 5.6 Add `turnIndex`/`totalTurns` (`int`) to `ConditionContext`; populate them in `InProcessMetricEvaluationExecutor` from the result row. Done: unit test — context carries turn position; single-turn `0/1`.
- [x] 5.7 Update `EvalSummaryBatchWriteItemDto` + `buildItem` to carry `turnIndex`/`totalTurns` and store `extractedColumns` verbatim (scalar). Make the two DTO fields **optional** (nullable `Integer`, not primitive `int`), defaulting to `0`/`1` when omitted so external single-turn callers of `POST /api/v1/analytics/eval-summaries` stay byte-compatible (a primitive `int` would send `0` for `totalTurns` and override the DB default of `1`). Done: one summary per turn with turn fields; an item omitting the fields persists `turn_index=0,total_turns=1`.

## 6. API & export exposure

- [x] 6.1 Add `turnIndex`/`totalTurns` to `TestCaseRunResultResponseDto` and `EvalSummaryResponseDto` (+ MapStruct mappers, `@Schema` examples). Done: list responses include the fields (mappers auto-map same-named `int` fields; OpenAPI response + CSV-preview examples updated).
- [x] 6.2 Within-conversation turn ordering is **client-side** (per D6, chosen decision): keep the shared `(created_at, id)` keyset spine and opaque `CursorCodec` wire format unchanged; the frontend groups by `(testCaseId, runIndex)` and sorts each group by `turnIndex` (both fields exposed on the DTO/CSV). No repository `ORDER BY` change. Done: DTOs/CSV carry `turnIndex`/`totalTurns`; keyset pagination stays stable.
- [x] 6.3 Add `turnIndex`/`totalTurns` CSV export identity columns immediately after `runIndex`. Done: export header + cells present (in `EvalSummaryExportColumnPlanner`); single-turn shows `0/1`.
- [x] 6.4 Accept `turnIndex`/`totalTurns` on the result **batch-write** DTO (`TestCaseRunResultItemDto`), mirroring `EvalSummaryBatchWriteItemDto`: nullable `Integer` + `@Min(0)`, `TestCaseRunResultMapper` null-coalesces to `0`/`1` (was hardcoded constants). Closes the round-trip/import gap the analytics-eval-results "Batch write" requirement already specified ("Each result item MAY carry `turnIndex` and `totalTurns`") — importing a multi-turn conversation without these collapsed all turns onto the same `turn_index=0` natural key and silently dropped all but one row. Done: updated OpenAPI full request example (adds a 2-turn conversation); added functional tests `shouldImportMultiTurnConversationAsDistinctRows` (3 turns coexist) and `shouldDefaultTurnFieldsWhenOmitted` (0/1 defaults). Spec already compliant — no delta change needed.

## 7. Vocabulary rename (turn everywhere)

- [x] 7.1 Rename `multi_step`/`multiStep`→`multi_turn`/`multiTurn` across `TestSuite` model, `TestSuiteRequestDto`/`ResponseDto`, `SuiteSnapshotDto`, mappers (`TestSuiteMapper`, `TestSuiteRecordMapper`, `SuiteSnapshotBuilder`), `PostgresTestSuiteRepository` (`TEST_SUITES.MULTI_TURN`), and `EvaluationContext.isSnapshotMultiStep()`→`isSnapshotMultiTurn()` (+ `TestSuiteEvaluationJob`, `EvaluationWorker`). Done: no `multiStep` identifiers remain (grep CLEAN).
- [x] 7.2 Rename `MAX_CONVERSATION_STEPS`→`MAX_CONVERSATION_TURNS` in `constants/ValidationConstants.java` and update all references (`ConversationTurnPlanner`); rename remaining `step`/`Step` identifiers→`turn`/`Turn` across the multi-turn executor/planner path — `StepOutcome`→`TurnOutcome` (file + record + all refs in `DeploymentTurnInvoker`/`MultiTurnConversationExecutor`), doc comments "multi-step"→"multi-turn". Done: no residual `step` vocabulary in the multi-turn path; constant defined once in `ValidationConstants` and referenced by name.
- [x] 7.3 Update `SuiteValidationService` multi-turn validation to reference `multiTurn` + rename `validateMultiStepBody`→`validateMultiTurnBody` (+ warning message/comments). Done: `./gradlew compileJava` green.

## 8. Tests

- [x] 8.1 Unit tests: `MultiTurnConversationExecutor` (per-turn rows, abort, data-error — new `MultiTurnConversationExecutorTest`), `BindingResolver` (scalar-direct), `ResultBatchWriter.addResults` (multi-turn buffers N rows, progress +1), `ConditionContext` population (new `InProcessMetricEvaluationExecutorTest` case captures the context's turnIndex/totalTurns and the summary's inherited turn fields). Also fixed/renamed `EvaluationWorkerTest`, `MetricEvaluationWorkerTest`, `MetricDefinitionValidationServiceTest`, `SuiteValidationServiceMultiTurnTest`, `DeploymentTurnInvokerTest`, `ConversationTurnPlannerTest`, `ValidationWarningsSerializerTest`; deleted dead `MultiStep*Test`. Record-mapper turn columns are covered by the functional round-trip (no standalone mapper unit test existed). Done: all listed unit classes pass.
- [x] 8.2 Functional (`@PostgresFunctionalTests`): renamed `MultiStepConversationRunFunctionalTests`→`MultiTurnConversationRunFunctionalTests` — multi-turn run writes one per-turn result row each carrying `turn_index`/`total_turns`; two test cases run different turn counts (2 vs 3 → 5 rows). `AnalyticsTestDataHelper.findResultsByRunId` now selects the turn columns. Done: `MultiTurnConversationRunTests` passes.
- [x] 8.3 Functional regression: existing single-turn deployment run tests (`EvaluationExecutorFailureModesTests`, `ResponseColumnTests`, `AnalyticsResultBatchWriteTests`, `AnalyticsResultListTests`) pass unchanged with the per-turn schema (single-turn rows `turn_index=0,total_turns=1`). Done: regression batch passes.

## 9. Docs, specs, config

- [x] 9.1 Update AGENTS.md per AGENTS.md Maintenance guidelines. Done: rewrote the multi-step inline convention as "Multi-turn conversation (`multiTurn`) — one result row per turn" in turn vocabulary (per-turn `TestCaseRunResult` rows, `turn_index`/`total_turns`, `MultiTurnConversationExecutor`/`ConversationTurnPlanner`/`DeploymentTurnInvoker`/`TurnOutcome`, `EvaluationWorker.execute→List`, `ResultBatchWriter.addResults`, abort + `0/0` data-shape sentinel, rolled-back jsonata/array/`ArrayBindingTypeMismatchDetector`/`stepIndex` machinery, scalar `BindingResolver`); updated the conditional-metric convention to note `ConditionContext` now carries `turnIndex`/`totalTurns` (populated in `InProcessMetricEvaluationExecutor`) and adjusted the `ConditionFunctionRegistry` "shipped empty" note.
- [x] 9.2 Update `openspec/specs/README.md` per Spec Index Maintenance Policy. Done: entry renamed to `[multi-turn-conversation](multi-turn-conversation/spec.md)` with a summary reflecting per-turn scalar rows keyed by `turn_index`/`total_turns`, abort/data-shape behavior, scalar metric evaluation, and `MultiTurnConversationExecutor` (worker returns `List<TestCaseRunResult>`).
- [x] 9.3 Synced all 9 delta specs into main specs and renamed the folder. Done: `git mv openspec/specs/multi-step-conversation → multi-turn-conversation`; applied MODIFIED/ADDED/REMOVED per delta (analytics-eval-results, conditional-metric-execution, eval-execution-engine, eval-summary-export, metric-evaluation, metrics-storage, multi-turn-conversation, test-suites, tsmd-validation); rewrote ALL `step`→`turn` headers ("Multi-turn conversation contract", "Per-turn loop with full-history resend", "Fail-fast on turn failure") and bodies incl. `MAX_CONVERSATION_STEPS`→`MAX_CONVERSATION_TURNS` in the multi-turn spec and eval-execution-engine "One concurrency permit per conversation"; removed the 3 tsmd-validation jsonata requirements and the 3 old multi-step result-shape/step-index/normalization requirements. Verified via grep: only remaining `step` hit is the intentional "were removed" note; `jsonataExpression` appears only in negations; no requirement loss.
- [x] 9.4 Ran `./gradlew spotlessApply` + full `clean build`. Done: green build, Checkstyle/Spotless clean. Fixed one stale unit assertion missed in Group 8 — `EvalSummaryExportColumnPlannerTest` identity-column list now includes `turnIndex`/`totalTurns` after `runIndex` (task 6.3 side effect); re-ran to confirm.
