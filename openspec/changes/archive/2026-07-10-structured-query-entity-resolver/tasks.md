## 1. Entity resolver SPI + registry

- [x] 1.1 Add `experimental/query/service/repository/StructuredQueryEntityResolver.java`: `entity()`, `dsl()`, `table()`, `bindings(StructuredQuery query)`, `default rewrite(StructuredQuery query)` returning `query` unchanged (done: compiles, no implementations yet).
- [x] 1.2 Add `experimental/query/service/repository/StructuredQueryEntityRegistry.java`: `@Component` collecting `List<StructuredQueryEntityResolver>` into a `Map<String, resolver>` at construction (mirroring `StructuredQueryService`'s current map-build pattern), throwing `IllegalStateException` on a duplicate entity; `require(String entity)` throws `ValidationException` (400, listing `supportedEntities()`) if absent; `supportedEntities()` (done: unit-testable with a hand-built resolver list, no Spring context needed).

## 2. Concrete resolvers (replace the four Postgres repositories)

- [x] 2.1 Add `PostgresTestSuiteEntityResolver` (`@Repository`, `@ConditionalOnProperty(name = "datasource.meta.vendor", havingValue = "POSTGRES")`): `@Qualifier("metaDsl") DSLContext`, table `TEST_SUITES`, `bindings` computed once in the constructor via `JooqTableSchemaResolver.bindings(TEST_SUITES)`, `rewrite` left as default identity (done: `dsl`/`table` wiring matches the prior repository-based implementation).
- [x] 2.2 Add `PostgresEvalSummaryEntityResolver` the same way for `analyticsDsl`/`TEST_CASE_EVAL_SUMMARIES` (done: `dsl`/`table` wiring matches the prior repository-based implementation).
- [x] 2.3 Add `PostgresMetricScoreResultEntityResolver` for `analyticsDsl`/`METRIC_SCORE_RESULT`, overriding `rewrite(query)` to delegate to the existing, unchanged `MetricScoreLatestComputationDefaulter.resolveLatestComputation(query)` (done: "latest" sentinel resolution still happens exactly once, before translation).
- [x] 2.4 Add `PostgresTestCaseEntityResolver` for `metaDsl`/`TEST_CASES`: move `requireDatasetId(...)`/`findDatasetIdEquality`/`datasetIdFromLogical`/`datasetIdFromComparison`/`datasetIdValue` verbatim from `PostgresTestCaseQueryRepository`; `bindings(query)` calls `requireDatasetId(query)` then the existing, unchanged `TestCaseFieldBindingsBuilder.build(datasetId)` (done: same 400 messages for missing/invalid `dataset_id` as today).
- [x] 2.5 Delete `StructuredQueryRepository.java` and its four marker sub-interfaces (`TestSuiteQueryRepository`, `EvalSummaryQueryRepository`, `MetricScoreResultQueryRepository`, `TestCaseQueryRepository`) and the four `PostgresXxxQueryRepository` classes (done: no remaining references; `./gradlew compileJava` succeeds).

## 3. StructuredQueryExecutor and StructuredQueryBuilder collapse to `(StructuredQuery)`

- [x] 3.1 Rewrite `StructuredQueryExecutor` to a single `execute(StructuredQuery query)`: `entityRegistry.require(query.entity())` → `resolver.rewrite(query)` → `queryBuilder.build(rewritten)` → `.fetch().intoMaps()` / `totalCount` / SQL-exception-to-400 mapping unchanged; delete the `ConcurrentHashMap<Table<?>, Map<String,QueryFieldBinding>>` bindings cache and both old overloads (done: compiles, no `dsl`/`table` params anywhere in this class).
- [x] 3.2 Rewrite `StructuredQueryBuilder.build`/`countRows` to accept only `StructuredQuery`, resolving `dsl`/`table`/`bindings` via `StructuredQueryEntityRegistry.require(query.entity())` at the top of each method; add the `StructuredQueryEntityRegistry` constructor dependency; `buildRow`/`buildAggregate`/`resolveGroupKey`/`sortFields` unchanged apart from receiving the now-locally-resolved values (done: compiles).
- [x] 3.3 Add package-private `StructuredQueryBuilder.compileSubqueryMembership(SubqueryExpr subquery)`: `build(inner)` (self-call) → `asTable(DSL.name("in_subquery"))` → first column as membership key → `subselect.configuration().dsl().select(key).from(derived)` — no same-entity check (done: compiles; unit test assertion of the rendered `IN (SELECT …)` shape is added in task 6.2).

## 4. ExprTranslator gains the sole ObjectProvider; FilterTranslator/SubqueryContext/QueryCompiler cleanup

- [x] 4.1 Add `ObjectProvider<StructuredQueryBuilder> queryBuilderProvider` to `ExprTranslator`'s constructor; add package-private `compileSubqueryMembership(SubqueryExpr subquery)` delegating to `queryBuilderProvider.getObject().compileSubqueryMembership(subquery)` (done: compiles; this is the only `ObjectProvider` in `experimental.query.service.translate`).
- [x] 4.2 Add a `SubqueryExpr` case to `ExprTranslator.toField`: compiles to a scalar `Field` via a narrow `@SuppressWarnings({"unchecked","rawtypes"})` helper (`subqueryField`) isolating the unchecked cast jOOQ's generic `DSL.field(Select<? extends Record1<T>>)` needs when `T` comes from a captured wildcard (done: a subquery now compiles to a scalar `Field` wherever `toField` is called — comparison operands, select projections, function arguments; compiles).
- [x] 4.3 Update `FilterTranslator.toComparison`'s `IN` handling to `left.in(exprTranslator.compileSubqueryMembership(subquery))`; delete the old `subquerySelect` method. `FilterTranslator`'s constructor and `toCondition`/`toLogical`/`translateAll`/`toComparison` signatures are otherwise unchanged (done: compiles, no new `FilterTranslator` dependency).
- [x] 4.4 Delete `SubqueryContext.java` and `QueryCompiler.java` (done: `grep -rn "SubqueryContext\|QueryCompiler" src/main` returns nothing).

## 5. StructuredQueryService delegates to the registry

- [x] 5.1 Rewrite `StructuredQueryService` to depend on `StructuredQueryEntityRegistry` + `StructuredQueryExecutor` + `QueryParameterResolver` instead of `List<StructuredQueryRepository>`; `execute(query, params)` calls `entityRegistry.require(query.entity())` (same 400 message as today) then `executor.execute(parameterResolver.resolve(query, params))`; `supportedEntities()` delegates to the registry (done: compiles, `StructuredQueryController` unchanged).

## 6. Tests

- [x] 6.1 Update `StructuredQueryBuilderTest`: replace the `dsl`/`bindings` triplet passed to `builder.build(...)` with hand-built `StructuredQueryEntityResolver`s (`test_suites` + `eval_summaries`) + `StructuredQueryEntityRegistry`; wire `ExprTranslator`'s new `ObjectProvider<StructuredQueryBuilder>` constructor arg via `Mockito.mock(ObjectProvider.class)` stubbed to return `builder` after it's constructed (a lambda can't be used — `ObjectProvider` isn't a single-abstract-method interface in this Spring version, and lambdas can't forward-reference a later-declared field the way an anonymous class/mock can); update every `builder.build(dsl, TABLE, bindings, query)` call site to `builder.build(query)` (done: `./gradlew test --tests StructuredQueryBuilderTest` passes).
- [x] 6.2 Add to `StructuredQueryBuilderTest`: a `SubqueryExpr` used as a scalar comparison operand (`field eq (subquery)`) asserting the rendered `= (select ... from (select ...) as in_subquery)` shape, and one used as a select projection; also updated `rejectsCrossEntitySubquery` → `allowsCrossEntitySubquery` (cross-entity now succeeds when both entities are registered) and added `rejectsUnregisteredEntitySubquery` (unknown-entity rejection still holds, now via the registry) (done: all pass).
- [x] 6.3 Update `EvalSummaryQueryRenderTest` the same way as 6.1 for its own entity/table (done: `./gradlew test --tests EvalSummaryQueryRenderTest` passes).
- [x] 6.4 Update `StructuredQueryServiceTest`: mock `StructuredQueryExecutor` instead of `StructuredQueryRepository`; build a real `StructuredQueryEntityRegistry` from fake resolvers; assert `executor.execute(...)` is called with the parameter-resolved query and that `supportedEntities()` mirrors the registry (done: `./gradlew test --tests StructuredQueryServiceTest` passes).
- [x] 6.5 Add functional test cases proving subquery-outside-`in` end-to-end through `POST /api/v1/queries/execute`, in `MetricScoreResultStructuredQueryFunctionalTests`: `filtersUsingScalarSubqueryComparison` (scalar `eq` operand) and `projectsSubqueryAsSelectColumn` (select projection), both asserting specific returned values, not just absence of errors; also updated the stale `rejectsSubqueryOutsideIn` (removed — that restriction is lifted) and re-labeled `rejectsCrossEntitySubquery` → `rejectsCrossDatasourceSubquery` (still throws `ValidationException`, but now via a real Postgres grammar error crossing meta/analytics datasources, not a structural same-entity check). Also fixed two now-stale `rejectsUnsupportedEntity` tests (`TestSuiteStructuredQueryFunctionalTests`, `EvalSummaryStructuredQueryFunctionalTests`) that asserted a *different, still-valid* entity was rejected — that was the old per-entity repository's redundant mismatch guard, now gone; both use a genuinely unregistered entity name instead. And fixed a real test-isolation bug in `filtersUsingScalarSubqueryComparison` itself (needed to also scope the outer query to its own suite, since `metric_score_result` is shared across test methods) (done: `./gradlew test --tests "PostgresFunctionalTests\$MetricScoreResultStructuredQueryTests"` and the other 4 structured-query nested suites all pass against real Postgres, 36/36).
- [x] 6.6 `FilterTranslatorArrayContainmentTest`: confirm it still passes with a `Mockito.mock(ObjectProvider.class)` in place of the old lambda (no subquery cases exercised, `FilterTranslator`'s constructor unchanged) (done: passes).

## 7. Docs and spec sync

- [x] 7.1 Sync the delta spec at `openspec/changes/structured-query-entity-resolver/specs/structured-query-model/spec.md` into `openspec/specs/structured-query-model/spec.md` at archive time (done: main spec's "Expression grammar" and "`in` predicate with array or subquery operand" requirements updated; new "Query entity resolution is centralized per entity" requirement added; "Implementation notes" class names updated to `StructuredQueryEntityResolver`/`StructuredQueryEntityRegistry` and the four resolver class names).
- [x] 7.2 Update AGENTS.md's "Query DSL subquery-valued `in`" inline convention bullet: it's no longer `in`-specific (subquery is a general scalar expression), there's no `SubqueryContext`/`QueryCompiler`, and bindings/dsl resolution is the entity registry's job, not the executor's per-table cache (done: bullet reflects the final mechanism; also added a new "Query DSL entity resolution" bullet and updated the stale `test_cases` bullet's repository/cache-bypass wording).
- [x] 7.3 Checked `openspec/specs/README.md` per Spec Index Maintenance Policy — `structured-query-model`'s summary doesn't name `SubqueryContext`/`QueryCompiler`/repository classes or restrict `subquery` to `in`'s right operand, so this change doesn't make it materially inaccurate; no update needed (done: index accurate, verified by reading the entry).

## 8. Verification

- [x] 8.1 `./gradlew spotlessApply` then `./gradlew clean build` (Checkstyle + Spotless + full test suite) passes (done: build green, exit code 0).
- [x] 8.2 Run the touched suites explicitly: `./gradlew test --tests "com.epam.aidial.evaluation.experimental.query.service.translate.StructuredQueryBuilderTest"`, `…EvalSummaryQueryRenderTest`, `…FilterTranslatorArrayContainmentTest`, `com.epam.aidial.evaluation.experimental.query.service.StructuredQueryServiceTest`, and the functional suites covering all four entities plus the new subquery-outside-`in` cases (done: covered by the full `clean build` run in 8.1, all pass).
- [x] 8.3 `grep -rn "SubqueryContext\|QueryCompiler\|StructuredQueryRepository\|bindingsCache" src/ openspec/specs/` returns no results (done: confirmed clean).
- [x] 8.4 `grep -rn "ObjectProvider" src/main` returns exactly one occurrence, in `ExprTranslator.java` (done: confirmed).
