> **Iteration rule** (AGENTS.md → Agent Workflow Rules): implement **at most one section** and **at most 5 tasks** per iteration. Sections are already sized to ≤5 tasks. A task is complete only when its tests have been **executed** and pass — writing a test does not close a task.
>
> Delivery-group mapping to the design's six groups: sections 1–2 = group 1 (DTO + meta persistence + validation), section 3 = group 2 (snapshot + EvaluationContext), section 4 = group 3 (analytics migration + jOOQ + models/writers), section 5 = group 4 (runner chain executor + turn-loop generalization), sections 6–7 = group 5 (downstream consumers), section 8 = group 6 (OpenAPI examples + docs + specs sync).
>
> No open design questions remain — see `design.md` → Resolved Questions (R1 export identity columns, R2 base-field spec home, R3 clone override non-goal). Implementation may start at section 1.

## 1. Group 1a — Request-definition DTO and meta persistence

- [x] 1.1 Add `RequestDefinitionDto` to `evaluation-runner-core/src/main/java/com/epam/aidial/evaluation/runner/dto/` — `name` (`@Size(max = 255)`), `endpointRef`, `requestTemplate`, `responseColumns`, `inputBindings`, all `@Valid`, Lombok `@Data @Builder @NoArgsConstructor @AllArgsConstructor` matching sibling DTOs. Done when it compiles and `:evaluation-runner-core:compileJava` passes.
- [x] 1.2 Add `MAX_ADDITIONAL_REQUESTS = 10` and `MAX_RESPONSE_COLUMNS = 50` to `src/main/java/com/epam/aidial/evaluation/constants/ValidationConstants.java`; replace the hardcoded `@Size(max = 50)` literal on `TestSuiteRequestDto.responseColumns` with the constant; add `additionalRequests` (`@Valid @Size(max = ValidationConstants.MAX_ADDITIONAL_REQUESTS)`) and `requestName` (`@Size(max = 255)`) to `TestSuiteRequestDto` and `TestSuiteResponseDto`, each with an OpenAPI `@Schema` description and example. Done when both DTOs expose the fields and no `50` literal remains in the DTOs.
- [x] 1.3 Add Flyway meta migration `src/main/resources/db/migration/meta/POSTGRES/V1.29__AddAdditionalRequestsToTestSuites.sql` (`ADD COLUMN additional_requests JSONB NOT NULL DEFAULT '[]'::jsonb`, `ADD COLUMN request_name VARCHAR(255)`); add `additionalRequests` (String) and `requestName` (String) to `data/db/model/TestSuite.java` and map both in `data/db/mapper/TestSuiteRecordMapper.java`; run `./gradlew generateJooq` and commit the regenerated sources under `src/main/java-generated/`. Done when `JooqSchemaDriftTest` passes.
- [x] 1.4 Add `mapAdditionalRequests` read/write to `service/domain/mapper/JsonbMapper.java` (read direction in `runner/util/RunnerJsonbMapper.java` with delegation, mirroring `mapRequestTemplate`/`mapInputBindings`); wire both new fields through `service/domain/mapper/TestSuiteMapper.java` `toDto`/`toEntity`/`update`/`toRequestDto`, and extend `toCloneEntity`'s `@ef/suites/{id}/` prefix replacement to cover the `additional_requests` JSON string alongside `requestTemplate`/`inputBindings`/`argumentTemplate`. Done when a clone rewrites refs inside the chain blob.
- [x] 1.5 **Run** the tests for this section: new/updated unit tests for `TestSuiteMapper` (chain round-trip + clone file-ref rewrite) and `JsonbMapper`, plus `./gradlew test --tests "*JooqSchemaDriftTest"` and at least one `@PostgresFunctionalTests` suite-CRUD nested class to prove the context boots with the new column. Done when all invoked commands pass.

## 2. Group 1b — Write-time and soft validation

- [x] 2.1 Add a `ResponseColumnUnionResolver` `@Component` in `service/domain/` producing the ordered suite-wide response-column union from a `TestSuiteRequestDto`, a `TestSuite` entity and a `SuiteSnapshotDto` (suite columns first, then each additional request's in chain order), plus a serialized-JSON variant for `revalidateAllForSuite`. Done when it is the single union source used by every later consumer.
- [x] 2.2 Extend `service/domain/TestSuiteRequestValidator.java`: one `seenNames` set spanning request #0 and every additional request (duplicate → 400 naming the column and its request index); union count > `MAX_RESPONSE_COLUMNS` → 400; non-empty `additionalRequests` on `suiteType=MCP_TOOL` → 400; and run the existing per-request checks (endpoint schema validity, `content`/`jsonataContent` mutual exclusivity, `jsonataContent` syntax, template size cap, bindings-count cap, duplicate `templateVariable` within one request) for each additional request with an `additionalRequests[i].` message prefix. Done when each rule has a failing-input unit test.
- [x] 2.3 Refactor `service/domain/SuiteValidationService.java` `validateDeploymentSuite` into a per-request method taking a warning-path prefix — `""` for request #0 so its existing literal paths (`$.urlTemplate`, `$.requestTemplate.body`, `$.requestTemplate.headers`, `$.endpointRef`) are byte-identical, `"$.additionalRequests[i]"` for additional requests — loop it over the chain, add the same prefix parameter to `service/domain/BindingValidator.java` (`$.inputBindings`), and populate `additionalRequests` in the entity-based `validateSuite(TestSuite, …)` overload used by `RevalidationService`. Done when warnings from both overloads carry indexed paths for additional requests and unchanged paths for request #0.
- [x] 2.4 Update `service/domain/TestSuiteService.java`: default `additionalRequests` to `List.of()` and per-column `type` to `STRING` for additional requests in `normalizeRequest`; make `isResponseColumnsChanged` diff the **union** (top-level columns JSON **or** the ordered `(name, type)` projection of `additional_requests`, so a non-column chain edit does not trigger revalidation); pass the serialized union to `testSuiteMetricDefinitionService.revalidateAllForSuite`. Done when a `urlTemplate`-only chain edit provably does not revalidate TSMDs.
- [x] 2.5 **Run** the tests for this section: unit tests for `TestSuiteRequestValidator` (cross-request duplicate → 400, union cap → 400, chain cap → 400, MCP guard → 400), `SuiteValidationService` (indexed vs legacy paths), `ResponseColumnUnionResolver`, and `TestSuiteService.isResponseColumnsChanged`; plus a `@PostgresFunctionalTests` suite create/update nested class covering the four 400s and a successful chain round-trip. Done when all invoked commands pass.

## 3. Group 2 — Run snapshot and execution context

- [x] 3.1 Add `additionalRequests` (`List<RequestDefinitionDto>`, defaulting to empty) and `requestName` (nullable `String`) to `service/domain/dto/SuiteSnapshotDto.java` keeping `CURRENT_VERSION = "2"`, and populate both in `service/domain/SuiteSnapshotBuilder.java`. Done when a snapshot JSON lacking the keys still deserializes to an empty chain.
- [x] 3.2 Add `snapshotAdditionalRequests` (`List<RequestDefinitionDto>`) and `snapshotRequestName` (`String`) to `runner/job/EvaluationContext.java` and populate them where `TestSuiteEvaluationJob` builds the context from the resolved snapshot. Done when the context exposes the chain to the executor.
- [x] 3.3 Carry the ordered request-label list (`[requestName, additionalRequests[*].name]`) on the metric-evaluation context so Phase 2 can resolve `request.name` by index without a new analytics column. Done when the context exposes the list and out-of-range lookups yield `null`.
- [x] 3.4 **Run** the tests for this section: unit tests for `SuiteSnapshotBuilder` (chain snapshotted) and `SuiteSnapshotDto` (legacy JSON without the keys → empty chain, `snapshotVersion` still `"2"`), plus a `@PostgresFunctionalTests` run-creation nested class asserting the `suiteSnapshot` echo contains the chain. Done when all invoked commands pass.

## 4. Group 3 — Analytics schema, models and writers

- [x] 4.1 Add `src/main/resources/db/migration/analytics/POSTGRES/V1.16__AddRequestColumnsToTestCaseRunResults.sql`: `request_index INTEGER NOT NULL DEFAULT 0`, `total_requests INTEGER NOT NULL DEFAULT 1`, then drop and re-create constraint `uq_results_run_case_index` as `(test_suite_run_id, test_case_id, run_index, request_index, turn_index, created_at_ms)`. Done when the migration applies cleanly on an existing populated table.
- [x] 4.2 Add `V1.17__AddRequestColumnsToEvalSummaries.sql`: the same two columns on `test_case_eval_summaries`, then `DROP INDEX uq_eval_summaries_natural_key` and re-create it as `(test_suite_run_id, test_case_id, run_index, request_index, turn_index, computation_id, created_at_ms)`. Leave `idx_eval_summaries_run_computed_at` untouched. Done when the migration applies cleanly.
- [x] 4.3 Run `./gradlew generateJooq` and commit the regenerated sources under `src/main/java-generated/` for both analytics tables. Done when `./gradlew test --tests "*JooqSchemaDriftTest"` passes.
- [x] 4.4 Add `requestIndex` (`@Builder.Default = 0`) / `totalRequests` (`@Builder.Default = 1`) to `runner/model/TestCaseRunResult.java` and `data/db/analytics/model/EvalSummary.java`; map them in `data/db/analytics/mapper/TestCaseRunResultRecordMapper.java` and all four `EvalSummaryRecordMapper` map methods; expose them on the analytics response DTOs and accept them as optional (default `0`/`1`) on the batch-write item DTOs. Done when a round-trip through the mappers preserves both values.
- [x] 4.5 Extend the `ON CONFLICT` targets in `PostgresTestCaseRunResultRepository.saveAll` and `PostgresEvalSummaryRepository.saveAll` to include `REQUEST_INDEX` (positioned after `RUN_INDEX`), and set the two new columns on insert. **Run** a `@PostgresFunctionalTests` nested class asserting (a) a chained batch persists distinct `(request_index, turn_index)` rows and (b) re-writing the same batch inserts nothing and raises no constraint violation. Done when both assertions pass.

## 5. Group 4 — Runner chain executor and turn-loop generalization

- [x] 5.1 Add `RequestExecutionSpec` (`requestIndex`, `totalRequests`, `name`, `endpointRef`, `requestTemplate`, `inputBindings`, `responseColumns`, `initialFrame`) and `RequestExecutionResult` (`rows`, `accumulatedFrame`, `aborted`) records to `runner/job/`. Done when both compile with no dependency outside the module.
- [x] 5.2 Generalize `runner/job/TurnLoopExecutor.java` to `RequestExecutionResult execute(TestCaseRunInput, EvaluationContext, int runIndex, RequestExecutionSpec, String traceId, long execStartedAtMs)`: read the template/bindings/columns/endpoint from the spec instead of the context's `snapshot*` fields, seed the per-turn frame from `spec.initialFrame()`, call `PerTurnBindingDetector` with the spec's bindings, merge each turn's extracted values into the accumulated frame, persist each row's `extracted_columns` as the accumulated union, stamp `requestIndex`/`totalRequests` only when `totalRequests > 1`, and return the final frame plus an `aborted` flag. Internals (turn plan, per-turn resolution, streaming, error outcomes, `buildEmptyTurnsErrorRow`) otherwise unchanged. Done when the single-request path produces rows identical to the pre-change baseline.
- [x] 5.3 Add `runner/job/RequestChainExecutor.java` (`@Component @LogExecution`): build the ordered spec list (spec 0 from the context's singular snapshot fields + `snapshotRequestName`, specs 1..N from `snapshotAdditionalRequests`), thread one accumulated frame through them, concatenate the rows, and stop on the first `aborted` result. Done when it satisfies `RunnerModuleConstraintsTest`.
- [x] 5.4 Delegate `EvaluationWorker`'s `DEPLOYMENT` branch to `RequestChainExecutor` (keeping the public `execute` signature and the untouched `MCP_TOOL` branch). **Run** `./gradlew :evaluation-runner-core:test` including `RunnerModuleConstraintsTest` and new unit tests for chain ordering, frame accumulation across requests and turns, fail-fast at each position, per-request turn detection, and index stamping. Done when the module suite passes.
- [x] 5.5 **Run** end-to-end functional coverage: a `@PostgresFunctionalTests` nested class executing a 2-request chain (non-multi-turn setup + multi-turn test) asserting per-row `request_index`/`total_requests`/`turn_index`/`total_turns`, the accumulated `extracted_columns` on each row, chain abort on a failing first request, and a legacy single-request suite whose rows carry the untouched defaults. Done when all assertions pass.

## 6. Group 5a — Conditions and metric evaluation

- [x] 6.1 Add `requestIndex`, `totalRequests`, `requestName` to `service/domain/ConditionContext.java`, and a `request` namespace (`index`, `total`, `last`, `name` with `putNull` for an absent label) to `service/domain/ConditionExpressionEvaluator.java` alongside the existing `turn` node, using new namespace/field constants. Done when explicit nulls survive serialization.
- [x] 6.2 Populate the three new `ConditionContext` fields in `service/domain/job/InProcessMetricEvaluationExecutor.java` from the result row's request columns and the snapshotted label list. Done when a condition on `request.name` evaluates correctly for both request #0 and an additional request.
- [x] 6.3 Change `service/domain/MetricDefinitionValidationService.extractResponseColumnNames` (and its call site) to resolve `Response` references against the suite-wide union via `ResponseColumnUnionResolver`. Done when a binding to an additional request's column no longer yields `UNRESOLVED_REFERENCE`.
- [x] 6.4 **Run** unit tests: `ConditionExpressionEvaluator` (`request.index`, `request.last`, `request.name`, `request.name = null` true when unlabelled (`$exists(request.name)` stays true — present-null), degenerate single-request namespace) and `MetricDefinitionValidationService` (union resolution, still-unresolved reference). Done when both suites pass.
- [x] 6.5 **Run** a `@PostgresFunctionalTests` nested class asserting a chained run where one TSMD carries `condition: "request.last"` produces metric values only on the final request's eval-summary rows, with earlier rows `SUCCESS` and metric-free. Done when the assertion passes.

## 7. Group 5b — Read-side consumers (export, query schema, comparison, preview)

- [x] 7.1 Change `experimental/query/service/EvalSummariesSchemaProvider.responseFields(SuiteSnapshotDto)` to iterate the suite-wide union via `ResponseColumnUnionResolver`. Done when the detailed schema lists an additional request's column.
- [x] 7.2 Update `service/domain/analytics/EvalSummaryExportColumnPlanner.java`: insert **both** `requestIndex` and `turnIndex` identity columns immediately after `runIndex`, in that order (per design R1 — the `turnIndex` addition intentionally fixes the pre-existing gap where multi-turn rows are indistinguishable in an export), wire their cell values from the eval-summary row, and derive the `response::<column>` block from the suite-wide union. Done when the preview manifest and the CSV header both emit `runIndex`, `requestIndex`, `turnIndex` as three consecutive columns and both are accepted in an explicit `columns` subset request.
- [x] 7.3 Extend the comparison match key in `data/db/analytics/repository/PostgresEvalSummaryRepository.java` at all three sites — `matchCondition`, `otherRunKeys`'s `selectDistinct`, and `findUnmatchedIds`'s `ORDER BY` — to include `REQUEST_INDEX` after `RUN_INDEX`. `RunComparisonService` and the response DTO stay unchanged. Done when a chained-vs-chained comparison matches per chain position.
- [x] 7.4 Add `requestIndex` to the Try-It-Out preview: `@RequestParam(name = "requestIndex", required = false, defaultValue = "0")` on `web/controller/ResolvedRequestController.getResolvedRequest` with an OpenAPI `@Parameter`, and a 3-arg `ResolvedRequestService.resolveRequest(UUID, UUID, int)` selecting request #0 or `additionalRequests[requestIndex - 1]` (out of range → `ValidationException` → 400), keeping a 2-arg overload delegating with `0` so `TryItOutService` is untouched. Done when preview still uses an empty frame and out-of-range returns 400.
- [x] 7.5 **Run** the tests for this section: unit tests for the export planner (asserting **both** `requestIndex` and `turnIndex` headers present, consecutive and in the order `runIndex`, `requestIndex`, `turnIndex`, plus the union-derived `response::*` block) and the schema provider; `@PostgresFunctionalTests` nested classes for CSV export and preview headers (a chained multi-turn run yielding distinct `(requestIndex, turnIndex)` cells, and a legacy single-request single-turn run yielding `0`/`0`), the comparison endpoint over two chained runs, and `resolved-request` with `requestIndex` absent / valid / out-of-range. Done when all invoked commands pass.

## 8. Group 6 — OpenAPI examples, docs and spec sync

- [x] 8.1 Add and update OpenAPI example files under `src/main/resources/openapi/examples/`: a new chained-request pair (`api-v1-test-suites-POST-request-chained.json` / `api-v1-test-suites-POST-response-201-chained.json`) wired via `@ExampleObject`, and add `additionalRequests` + `requestName` to the existing `*-full` suite examples (`api-v1-test-suites-GET-response-200-full.json`, `api-v1-test-suites-POST-request-full.json`, `api-v1-test-suites-POST-response-201-full.json`, `api-v1-test-suites-id-GET-response-200-full.json`, `api-v1-test-suites-id-PUT-request-full.json`, `api-v1-test-suites-id-PUT-response-200-full.json`) and to the run-detail snapshot echo `api-v1-test-suite-runs-id-GET-response-200-full.json`. Done when Swagger UI renders every updated example without a schema warning.
- [x] 8.2 Update `docs/database-schema.md`: `test_suites` gains `additional_requests` / `request_name` with the `additional_requests` JSONB shape documented; `test_case_run_results` and `test_case_eval_summaries` gain `request_index` / `total_requests`; both extended keys' column lists updated; migration history rows for `V1.29`, `V1.16`, `V1.17`; and fix the pre-existing mislabelling of the eval-summaries unique key (documented once as `uq_eval_summaries_run_case_comp`, actually `uq_eval_summaries_natural_key`). No `docs/configuration.md` change is expected — confirm no new property was introduced.
- [x] 8.3 Update `AGENTS.md` per its Maintenance guidelines (new inline convention for the request chain: `additionalRequests` as requests 1..N, the flat shared response-column namespace with global uniqueness, chain-wide frame accumulation, per-request multi-turn detection, the `(request_index, turn_index)` row identity, `RequestChainExecutor` in the `evaluation-runner-core` package table) and add the `multi-request-suite` entry to `openspec/specs/README.md` per the Spec Index Maintenance Policy. No `openspec/config.yaml` change is expected — this change adds no architectural layer, tooling command or cross-cutting convention beyond the above; re-confirm against the Config Maintenance Policy before skipping.
- [x] 8.4 Sync the delta specs into `openspec/specs/` with the `/opsx:sync` skill (never by copying files), then verify with `git diff` that each touched main spec gained the new requirements and lost none — in particular that `openspec/specs/multi-request-suite/spec.md` is created and that the `run-comparison-metric-scores` requirement rename landed as a rename, not a duplicate.
- [x] 8.5 **Run** the full gate: `./gradlew spotlessApply`, then `./gradlew clean build` (includes `spotlessCheck`, `checkstyleMain`, `checkstyleTest`, `JooqSchemaDriftTest`, `LoggingConventionTest`, `LayeredArchitectureTest`, `JdbcTemplateFenceTest`) and `./gradlew :evaluation-runner-core:test`. Done when both builds are green with zero deprecation warnings.

## 9. Review fixes (post-implementation review round)

> Findings and dispositions are recorded in `design.md` → Review findings & resolutions. S3 (diff asymmetry in `isResponseColumnsChanged`) and S4 (legacy per-test-case template overrides) were reviewed and intentionally left as-is; they carry no task.

- [x] 9.1 **F1 — clone hard validation.** Run the create/update hard rule set in `service/domain/TestSuiteCloneService.java` against the effective post-override suite (inherited chain + overrides) before persisting, returning 400 `VALIDATION_ERROR` and leaving no cloned suite/dataset/test-cases/TSMDs/files behind. Done when a clone whose `responseColumns` override collides with an inherited `additionalRequests[i]` column is rejected.
- [x] 9.2 **F2 — null chain element.** Reject a `null` element in `additionalRequests` at write time in `service/domain/TestSuiteRequestValidator.java` with a message naming the 0-based index; never drop it silently. Done when create and update both 400 on `[{...}, null]`.
- [x] 9.3 **F3 — per-request `$ref` inlining (closes verify W2).** Resolve every request's `endpointRef` through `EndpointSchemaRefResolver` at normalize time — the suite's own and each `additionalRequests[i]`'s — so no request in a chain persists with an unresolved schema reference. Done when an additional request's `$ref`-expressed endpoint schema is inlined identically to request #0's.
- [x] 9.4 **F4 — vacuous test.** Replace the assertion that held regardless of the behavior under test with one that discriminates. Done when the test fails against the pre-fix implementation.
- [x] 9.5 **W1 + spec sync.** Flip all delta-spec `Status: **Planned**` markers to `Status: **Implemented**` (38 across 14 delta files); add the F1/F2/F3 behavior to the `test-suite-clone` (new delta), `multi-request-suite`, `test-suites` and `request-template` deltas; re-run `/opsx:sync` so the main specs merge the new deltas on top of the earlier sync (never copy files); re-run `openspec validate add-multi-request-suite --strict`. Done when validation is green and `git diff openspec/specs/` shows only additive merges.
