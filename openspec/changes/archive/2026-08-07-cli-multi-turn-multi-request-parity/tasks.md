## 1. Backend — identity columns in the import contract

Prerequisite for every CLI task: the backend must accept the four columns before the CLI emits them (design.md, Migration Plan — deployment order is a hard requirement).

- [x] 1.1 Extend `EvalResultsCsvParser.RESERVED_COLUMNS` (`service.domain.analytics`) with `requestIndex`, `totalRequests`, `turnIndex`, `totalTurns`, and populate the four fields on each `TestCaseRunResult` stub, defaulting an absent header or blank cell to `0`/`1`/`0`/`1` (done: a 19-column CSV round-trips its identity values onto the stubs; a 15-column CSV yields the defaults).
- [x] 1.2 Add the per-row inline sanity checks to the parser alongside the existing `runIndex`/`testCaseName` checks, applied to the **effective** values after defaulting: integer-parseable when non-blank, `requestIndex >= 0`, `turnIndex >= 0`, `totalRequests >= 1`, `totalTurns >= 1`, `requestIndex < totalRequests`, `turnIndex < totalTurns`; no cross-row `total*` consistency check (design.md Decision 2) (done: violations across multiple rows are reported in one `ValidationException`; `requestIndex=2` with a blank `totalRequests` is rejected).
- [x] 1.3 Extend `EvalResultsImportService.validateBatch`'s in-batch duplicate key to `(identity, runIndex, requestIndex, turnIndex)` and update the method Javadoc, which currently documents the two-part key (done: rows differing only in `requestIndex` or `turnIndex` are accepted; fully identical rows are still rejected).
- [x] 1.4 Extend `EvalResultsCsvParserTest` for the identity columns: values parsed, all-defaults for a legacy 15-column CSV, blank cell falls back to default, index range-checked against a defaulted total, non-integer rejected with row identified, negative index and zero total rejected, differing `totalTurns` per `requestIndex` accepted, and identity violations combined with other row violations in one rejection. Run `./gradlew test --tests "com.epam.aidial.evaluation.service.domain.analytics.EvalResultsCsvParserTest"` (done: suite passes).
- [x] 1.5 Extend `EvalResultsImportServiceTest` for the new duplicate key: multi-request rows accepted, multi-turn rows accepted, exact-duplicate rows still rejected, legacy single-row batches unchanged. Run `./gradlew test --tests "com.epam.aidial.evaluation.service.domain.analytics.EvalResultsImportServiceTest"` (done: suite passes).

## 2. Backend — stable per-case identity and scope-aware validation

- [x] 2.1 Replace `EvalResultsCsvParser`'s per-row `UUID.randomUUID()` fallback (`.testCaseId(testCaseId != null ? testCaseId : UUID.randomUUID())`) with a parse-call-scoped `Map<String, UUID>` keyed by `testCaseName`, so all id-less rows naming the same test case share one generated identifier; rows supplying `testCaseId` are unaffected; a row with neither `testCaseId` nor `testCaseName` keeps a null id so `validateBatch`'s existing identity-required check rejects it with a 400 before any run exists (design.md Decision 4) (done: the map is local to one `parse` invocation, so ids never leak between requests; a nameless id-less row yields 400, not a post-run-creation persistence failure).
- [x] 2.2 Make `testCaseData` validation scope-aware in `EvalResultsCsvParser`: split the dataset schema via the existing `TestCaseFieldScopeResolver.splitSchema(...)` and build the validation schema so per-turn fields appear in `properties` but never in `required`, while shared fields keep today's behavior (design.md Decision 6). Do not change `SchemaValidationService.buildFieldSchema`'s existing callers (done: shared-only rows validate against a dataset with a required per-turn field).
- [x] 2.3 Extend `EvalResultsCsvParserTest` for both: all rows of one `testCaseName` share an id, distinct names get distinct ids, a supplied `testCaseId` is used verbatim, a row with neither id nor name keeps a null id (rejected downstream by `validateBatch` — assert the 400 in `EvalResultsImportServiceTest`), a name-only file repeating `testCaseName` + `runIndex` with defaulted positions now collides on the duplicate key; and a shared-only row passes with a required per-turn field, a missing required shared field still fails, a present per-turn field with the wrong type still fails, and a shared-only dataset validates exactly as before. Run `./gradlew test --tests "com.epam.aidial.evaluation.service.domain.analytics.EvalResultsCsvParserTest"` (done: suite passes).

## 3. Backend — persistence and end-to-end verification

- [x] 3.1 Verify `EvalResultsImportService.persistResults` carries the four identity fields through its `.toBuilder()` run-context fill-in to `TestCaseRunResultRepository.saveAll`; adjust only if a field is dropped (done: no new column, no migration — the four already exist on the table, per design.md Non-Goals).
- [x] 3.2 Add a functional test to `EvalResultsImportFunctionalTests` importing a 19-column, id-less CSV with several `(requestIndex, turnIndex)` rows per test case, asserting via the analytics repository that each row carries its own identity values **and** that all rows of one test case share a single `test_case_id` (use `AnalyticsTestDataHelper`/repositories, no raw SQL, no `JdbcTemplate`). Run `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$EvalResultsImportTests"` (done: suite passes).
- [x] 3.3 Add a legacy-compatibility functional test importing the current 15-column CSV unchanged, asserting rows persist with `requestIndex=0`, `totalRequests=1`, `turnIndex=0`, `totalTurns=1` and that the run completes. Run the same filter as 3.2 (done: pre-existing CSVs import byte-compatibly).
- [x] 3.4 Add a functional test for scope-aware validation: a dataset with a required per-turn field accepts an import of shared-only rows, and still rejects a row missing a required shared field. Run the same filter as 3.2 (done: both assertions hold).
- [x] 3.5 Add a functional test proving design.md Decision 5 — import a multi-request CSV into a chain-configured suite carrying a `request.name`-pinned TSMD condition, and assert the metric ran for exactly the rows at that chain position. No production change is expected here; if the test fails, stop and re-open Decision 5 rather than patching around it. Run the same filter as 3.2 (done: test passes with no production edit, or Decision 5 is revised).

## 4. CLI — fetch the dataset schema

- [x] 4.1 Add `eval-cli/.../client/source/DatasetApiClient` calling `GET /api/v1/datasets/{id}`, modelled on `TestCaseApiClient` (`@Component`, `@LogExecution`, `@RequiredArgsConstructor`, `@Qualifier("sourceRestClient") RestClient`), binding the response to a minimal local record exposing only `testCaseSchema: List<FieldDefinitionDto>` — not a mirror of the backend-only `DatasetResponseDto` (design.md Decision 9) (done: client returns the schema list and ignores unknown JSON properties).
- [x] 4.2 Add a `testCaseSchema` field to `SuiteFetchBundle` and populate it in `FetchService.fetch` from the new client using `suite.getDatasetId()`; update both classes' Javadoc (done: a freshly written bundle JSON contains the schema with `perTurn` preserved).
- [x] 4.3 Verify a bundle JSON written before this change still deserializes in `FetchService.load` with `testCaseSchema` left null (design.md Decision 7) (done: covered by a unit test loading a legacy bundle fixture).
- [x] 4.4 Add/extend unit tests for `DatasetApiClient` and `FetchService`: schema fetched and persisted with `perTurn` intact, legacy bundle loads with null schema. Run `./gradlew :eval-cli:test` (done: suite passes).

## 5. CLI — activate multi-turn and add the guards

- [x] 5.1 Thread the fetched schema into `EvaluationContextFactory.create` and set `.snapshotTestCaseSchema(...)` on the built `EvaluationContext`, updating the method's Javadoc (done: the context handed to `TestCaseRunnerFactory` carries the schema).
- [x] 5.2 Update `RunOrchestrationService.run` to pass `bundle.getTestCaseSchema()` to the context factory. Leave the `context.getSnapshotResponseColumns()` argument of the `testCaseRunnerFactory.create(context, …, csvWriter)` call untouched — it is the MCP-only response-column argument and is not a gap (done: the runner-factory call is unchanged).
- [x] 5.3 Add the stale-bundle guard in `RunOrchestrationService`: fail fast with a re-run-`fetch` message when the bundle's schema is absent and any fetched test case carries `multiTurnData`; do not fail when no case is multi-turn (design.md Decision 7) (done: guard fires only on the wrong-answer case).
- [x] 5.4 Add the MCP/multi-turn pre-flight guard next to the existing `suiteContractValidator.validate(...)` call: reject when `suiteType == MCP_TOOL` and any fetched test case carries `multiTurnData`, before any target invocation, with a non-zero exit code (design.md Decision 8, mirroring `TestSuiteRunService`'s 409) (done: no deployment call and no CSV written on rejection).

## 6. CLI — tests and CSV export of row identity

- [x] 6.1 Extend `EvaluationContextFactoryTest` asserting the fetched schema (including `perTurn` flags) reaches `EvaluationContext.snapshotTestCaseSchema`. This is the CLI's verifiable contribution to multi-turn activation — turn-count behavior itself is owned by `evaluation-runner-core`'s existing `TurnLoopExecutor`/`PerTurnBindingDetector` tests, which consume that field; the CLI verifies only that it supplies the input. Run `./gradlew :eval-cli:test` (done: assertion is on the built context, not on runner behavior).
- [x] 6.2 Extend `RunOrchestrationServiceTest` (which mocks `TestCaseRunnerFactory`/`TestCaseRunner`) for the two guards only: stale-bundle guard fires with multi-turn cases present and not otherwise; MCP+multi-turn rejected pre-flight with no factory interaction; MCP without multi-turn proceeds. Run `./gradlew :eval-cli:test` (done: assertions are on guard outcomes and factory interactions, not on produced results).
- [x] 6.3 Append `requestIndex`, `totalRequests`, `turnIndex`, `totalTurns` to `CsvResultBatchWriter.HEADERS` and write each result's own values (design.md Decision 10) (done: header order matches the `eval-cli` spec's CSV contract scenario exactly).
- [x] 6.4 Rewrite the `CsvResultBatchWriter` Javadoc: the paragraph asserting `turnIndex`/`totalTurns` "are not part of `EvalResultsCsvParser.RESERVED_COLUMNS` at all" becomes false with task 1.1, and the `testCaseId`-omission rationale should note that the import contract now derives one stable id per distinct `testCaseName` (done: Javadoc matches the emitted header and the current import contract).
- [x] 6.5 Extend `CsvResultBatchWriterTest` asserting the exact 18-column header string and that a single-request single-turn result writes `0,1,0,1`. Run `./gradlew :eval-cli:test` (done: suite passes).

## 7. Docs

- [x] 7.1 Update the import endpoint's `@Operation` description in `TestSuiteRunController` (~lines 79-82): it currently lists only 12 reserved columns (omitting `testCaseData`, `extractedColumns`, `extractionWarnings`) and claims every other header maps to a `testCaseData` field, which is false. Bring the whole description in line with the 19-column contract, including the optional identity columns and the name-derived identity rule (done: description matches `RESERVED_COLUMNS` exactly, with no non-reserved-header mapping claim).
- [x] 7.2 Update the OpenAPI examples and `@Schema`/`@ExampleObject` annotations for `POST /api/v1/test-suites/{testSuiteId}/runs/import` to show a multi-request/multi-turn CSV, per the openapi-examples spec (done: examples reflect the 19-column contract and state the four columns are optional).
- [x] 7.3 Update `eval-cli/README.md`: `fetch` now also retrieves the dataset schema, the bundle gains a field, the CSV gains four columns, the minimum backend version and the hard deployment-order requirement (a CLI ahead of its backend corrupts silently), and a multi-request/multi-turn run multiplies row counts against `analytics.results.batch.max-items` (design.md Risks) (done: all five points documented).

## 8. Verification and spec sync

- [x] 8.1 Verify the suite-clone path preserves `perTurn` on the destination clone's dataset schema, so a CLI-executed multi-turn run validates cleanly against the clone under the new scope-aware rule (design.md Risks). Record the finding; if it does not preserve `perTurn`, raise it rather than fixing it here — GH #120 is out of scope (done: verified or explicitly raised).
- [x] 8.2 Run `./gradlew spotlessApply` then `./gradlew checkstyleMain checkstyleTest` across all modules (done: clean).
- [x] 8.3 Run the full build `./gradlew clean build` plus `./gradlew :eval-cli:test :evaluation-runner-core:test`, confirming `evaluation-runner-core` needed no change (done: all green; any required runner-core edit means the diagnosis was wrong — stop and re-check per design.md Non-Goals).
- [x] 8.4 Update `openspec/specs/eval-results-import/spec.md` and `openspec/specs/eval-cli/spec.md` from this change's delta specs (done: main specs reflect the 19-column import contract, name-derived stable identity, scope-aware validation, the new duplicate key, dataset-schema fetch, multi-turn activation, the MCP guard, and the 18-column CSV contract).
