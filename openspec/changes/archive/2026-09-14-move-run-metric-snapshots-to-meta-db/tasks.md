## 1. Meta schema and Flyway wiring

- [x] 1.1 Add `src/main/resources/db/migration/meta/POSTGRES/V1.32__CreateRunMetricSnapshotsTable.sql` mirroring the analytics `V1.6` column set, plus `uq_run_metric_snapshots_computation_tsmd` on `(computation_id, tsmd_id)`, `idx_run_metric_snapshots_run` on `(test_suite_run_id)`, and `FOREIGN KEY (test_suite_run_id) REFERENCES test_suite_runs(id) ON DELETE CASCADE`. Include a header comment naming `V1_33` as the companion Java migration (design D2). Verify by applying meta migrations against a clean database and confirming the table, both indexes, and the FK exist. (done: file exists with table, `CREATE UNIQUE INDEX uq_run_metric_snapshots_computation_tsmd`, `CREATE INDEX idx_run_metric_snapshots_run`, `fk_run_metric_snapshots_run` FK, and a header comment naming V1_33; 3166 tests, 0 failures in clean build)
- [x] 1.2 Add `V1_33__CopyRunMetricSnapshotsFromAnalytics` as a Flyway `BaseJavaMigration` in the meta migration Java package, taking the analytics `DataSource` and the analytics **schema** (`postgres.analytics.datasource.schema`, e.g. `public` — NOT the vendor string) via constructor. No DDL (design D1). Verify it compiles, `getChecksum()` returns a stable constant, and — separately from any Testcontainers test — that `MetaFlywayConfiguration` actually passes the analytics schema value into this constructor rather than the analytics vendor value (a `String, String` constructor makes this mistake compile cleanly; the schema-qualified check in 1.3 would then filter on the wrong string and always report "not found", silently skipping every real copy). (done: `V1_33__CopyRunMetricSnapshotsFromAnalytics.java` extends `BaseJavaMigration`, constructor takes `DataSource` + schema `String`, `CHECKSUM` constant; `MetaFlywayConfiguration.java:41` passes `analyticsSchema`, not `analyticsVendor`; verified by direct source read)
- [x] 1.3 Implement the migration body: skip with a log line when a **schema-qualified** `information_schema.tables` check (`table_schema` bound to the analytics schema — design D5, C1; an unqualified check would match the meta database's own same-named table in a shared-database deployment) shows no analytics `run_metric_snapshots`; otherwise read on a separate read-only analytics connection with `autoCommit=false` + `setFetchSize`, and write through `context.getConnection()` (design D4). No vendor check is needed here — `DatasourceValidationConfiguration` already fails startup for any unsupported `datasource.analytics.vendor` before this migration can run. Verify via the tests in group 7, including a case that asserts the check queries the *schema* named by `postgres.analytics.datasource.schema`, not the vendor string. (done: `CHECK_SOURCE_TABLE_EXISTS` binds `table_schema = ?`; `CopyRunMetricSnapshotsFromAnalyticsMigrationTest` 5/5 passing, incl. the schema-vs-vendor and table-absent cases)
- [x] 1.4 Implement the orphan guard and batching: for each batch of up to 1000 source rows, look up which of the batch's `test_suite_run_id`s exist in `test_suite_runs` via one `= ANY(?)` query, drop and count as orphans the rows whose run is absent, then `INSERT ... WHERE EXISTS (SELECT 1 FROM test_suite_runs WHERE id = ?) ON CONFLICT (computation_id, tsmd_id) DO NOTHING` for the remaining rows via `addBatch`/`executeBatch`. Log `copied` / `orphaned` / `alreadyPresent` counts, where `orphaned` comes from the explicit existence check (not from insert update counts, which cannot distinguish "orphan" from "already copied" — design D6). Verify via the tests in group 7. (done: `BATCH_SIZE = 1000`, `SELECT_EXISTING_RUN_IDS` uses `= ANY (?)`, `ON CONFLICT (computation_id, tsmd_id) DO NOTHING`, logs copied/orphaned/alreadyPresent; `CopyRunMetricSnapshotsFromAnalyticsMigrationTest` 5/5 passing)
- [x] 1.5 Wire the migration into `MetaFlywayConfiguration`: inject `@Qualifier("analyticsDataSource") DataSource` and `@Value("${postgres.analytics.datasource.schema:public}") String analyticsSchema` (the schema, not `datasource.analytics.vendor` — see 1.2), register the instance via `.javaMigrations(...)`, and take the analytics `Flyway` bean as an explicitly `@Qualifier("analyticsFlywayMigration")`-annotated parameter so meta always migrates after analytics (design D2, D3). Verify with a functional test that boots the application context and asserts, via `ConfigurableListableBeanFactory.getDependenciesForBean("metaFlywayMigration")`, that `analyticsFlywayMigration` is among the returned dependencies — a bare "context boots successfully" assertion passes equally under today's incidental construction order and proves nothing about explicit ordering. (done: `MetaFlywayConfiguration.java` wired exactly as specified; `DslContextSmokeTest#metaFlywayMigrationDependsOnAnalyticsFlywayMigration` asserts the bean dependency)

## 2. Data layer move

- [x] 2.1 Move `RunMetricSnapshot` from `data.db.analytics.model` to `data.db.model`. Verify compilation. (done: `data/db/model/RunMetricSnapshot.java`; clean build compiles)
- [x] 2.2 Move `RunMetricSnapshotRecordMapper` from `data.db.analytics.mapper` to `data.db.mapper`. Verify compilation. (done: `data/db/mapper/RunMetricSnapshotRecordMapper.java`; clean build compiles)
- [x] 2.3 Move `RunMetricSnapshotRepository` and `PostgresRunMetricSnapshotRepository` from `data.db.analytics.repository` to `data.db.repository`; switch `@Qualifier("analyticsDsl")` → `"metaDsl"`, `@ConditionalOnProperty(name = "datasource.analytics.vendor")` → `"datasource.meta.vendor"`, and the static import from `jooq.analytics.Tables` → `jooq.meta.Tables` (design D10). Verify compilation and that `LayeredArchitectureTest` passes. (done: `data/db/repository/PostgresRunMetricSnapshotRepository.java` uses `@Qualifier("metaDsl")`, `@ConditionalOnProperty(name = "datasource.meta.vendor", ...)`, static-imports `jooq.meta.Tables.RUN_METRIC_SNAPSHOTS`; 3166 tests, 0 failures in clean build incl. `LayeredArchitectureTest`)

## 3. Service layer move and transaction boundaries

- [x] 3.1 Move `RunMetricSnapshotService` from `service.domain.analytics` to `service.domain` and switch all four methods from `@Transactional("analyticsTransactionManager")` to `"metaTransactionManager"` (design D10). Verify with a test that forces the insert to fail after the run-existence check has already passed (e.g. a constraint violation on one row of a multi-row batch, or a mocked repository throwing on insert) and asserts that no snapshot row from that batch is committed — proving the check and the insert now share one transaction, which merely reading the `@Transactional` annotation does not demonstrate. (done: `service/domain/RunMetricSnapshotService.java` all four methods carry `@Transactional("metaTransactionManager")`/`metaTransactionManager, readOnly = true`; atomicity covered by `RunMetricSnapshotServiceTest`/`RunMetricSnapshotFunctionalTests`, 3166 tests 0 failures)
- [x] 3.2 Move `RunMetricSnapshotBatchWriteItemDto`, `RunMetricSnapshotBatchWriteRequestDto`, and `RunMetricSnapshotResponseDto` from `service.domain.dto.analytics` to `service.domain.dto`, and `RunMetricSnapshotMapper` accordingly if its package references change. Verify `RunMetricSnapshotMapperTest` passes. (done: all three DTOs live under `service/domain/dto/`; `RunMetricSnapshotMapperTest` passing in clean build)
- [x] 3.3 Hoist `EvalSummaryExportService`'s `runMetricSnapshotRepository.findByRunId` call out of the `analyticsTransactionTemplate` block into the existing `metaTransactionTemplate` block, carrying the result on `MetaSetup` (design D9). Verify `EvalSummaryExportFunctionalTests` passes. (done: `EvalSummaryExportService.java:221` calls `findByRunId` inside the `metaTransactionTemplate.execute` block, ahead of the `analyticsTransactionTemplate.execute` block at :225; `EvalSummaryExportFunctionalTests` passing in clean build)
- [x] 3.4 Hoist `RunComparisonService`'s `findByRunIdAndComputationId` call ahead of its `analyticsTransactionTemplate.execute` block so the meta read no longer sits inside an analytics transaction (design D9). Verify `RunComparisonServiceTest` and `RunComparisonFunctionalTests` pass. (done: `RunComparisonService.java:93-96` calls `findByRunIdAndComputationId` between two `analyticsTransactionTemplate.execute` blocks, with an explanatory comment citing design D9; `RunComparisonServiceTest`/`RunComparisonFunctionalTests` passing in clean build)
- [x] 3.5 Update imports in `EvalSummariesSchemaProvider`, `MetricScoreComputationExecutor`, `MetricFieldDiscoverer`, `EvalSummaryExportColumnPlanner`, `InProcessMetricEvaluationExecutor`, `RunMetricSnapshotBatchWriteClient`, `RunComparisonService`, `EvalSummaryExportService`, `RunMetricSnapshotMapper`, and `RunMetricSnapshotController`, plus the unit test classes `MetricScoreComputationExecutorTest`, `InProcessMetricEvaluationExecutorTest`, `EvalSummaryExportColumnPlannerTest`, `EvalSummariesSchemaProviderTest`, `RunComparisonServiceTest`, and `MetricFieldDiscovererTest`. Verify compilation and that `./gradlew checkstyleMain` and `./gradlew checkstyleTest` pass. (done: `checkstyleMain`/`checkstyleTest` pass; 3166 tests, 0 failures in clean build. Note: `MetricEvaluationExecutor` was dropped from this list — it only names `RunMetricSnapshot` in a javadoc sentence, no import, never modified)

## 4. Controllers and OpenAPI

- [x] 4.1 Change `RunMetricSnapshotController`'s `@RequestMapping` to `/api/v1/run-metric-snapshots` and move the class out of any analytics-specific package/import grouping. Verify `RunMetricSnapshotFunctionalTests` passes against the new path. (done: `RunMetricSnapshotController.java` `@RequestMapping("/api/v1/run-metric-snapshots")`, package `web.controller`; `RunMetricSnapshotFunctionalTests` passing in clean build)
- [x] 4.2 Add a `@Deprecated(forRemoval = true)` alias controller at `/api/v1/analytics/run-metric-snapshots` delegating both `GET` and `POST` to the same `RunMetricSnapshotService`, with `@Operation(deprecated = true)` and descriptions naming the replacement path, and distinct operationIds (design D8). Verify a functional test asserts the alias returns the same payload and status as the canonical path for both verbs. (done: `RunMetricSnapshotDeprecatedController.java` — `@Deprecated(forRemoval = true)`, `@RequestMapping("/api/v1/analytics/run-metric-snapshots")`, both methods `@Operation(deprecated = true)` with distinct method names/operationIds; alias parity covered by `RunMetricSnapshotFunctionalTests`)
- [x] 4.3 Rename the OpenAPI example files to the `OpenApiExampleCustomizer` convention for the new path: `run-metric-snapshot-batch-write-request.json` → `api-v1-run-metric-snapshots-POST-request-minimal.json`; `run-metric-snapshot-list-response.json` → `api-v1-run-metric-snapshots-GET-response-200-minimal.json`; `run-metric-snapshot-batch-write-response.json` → `api-v1-run-metric-snapshots-POST-response-201-minimal.json` (the trailing name segment must be a member of `OpenApiExampleCustomizer.EXAMPLE_NAMES` — `minimal` qualifies). Verify a test asserts all three examples appear in `/v3/api-docs` — they are currently never injected because the old filenames do not match the convention. (done: all three files exist under `src/main/resources/openapi/examples/` with the new names; presence in `/v3/api-docs` covered by a `RunMetricSnapshotFunctionalTests` assertion)
- [x] 4.4 Verify the generated OpenAPI marks only the alias operations `deprecated: true` and leaves the canonical operations undeprecated, via assertions on `/v3/api-docs`. (done: covered by `RunMetricSnapshotFunctionalTests`; 3166 tests, 0 failures in clean build)
- [x] 4.5 Add `REGISTRY` entries in `OpenApiQueryParamCustomizer` for both `/api/v1/run-metric-snapshots` and its deprecated alias `/api/v1/analytics/run-metric-snapshots`, each mapping to `FilterWhitelists.RUN_METRIC_SNAPSHOTS` with no sort spec and `PaginationType.NONE` (the endpoint has a required `runId` filter and returns an unpaged list, matching the existing CSV-export/preview entries' shape). This closes a pre-existing registry gap (`openspec/specs/openapi-query-param-docs/spec.md:93` requires the registry to cover all list endpoints; neither the old `/api/v1/analytics/run-metric-snapshots` path nor the new one was ever registered), and this change is renaming the path anyway. Registering the alias too (rather than treating it as an exception) keeps it a genuine drop-in for clients reading the generated docs. Verify the generated `filter` parameter description for both `/api/v1/run-metric-snapshots` and `/api/v1/analytics/run-metric-snapshots` includes the field-operator matrix. (done: `OpenApiQueryParamCustomizer.REGISTRY` carries both `"/api/v1/run-metric-snapshots"` and `"/api/v1/analytics/run-metric-snapshots"` entries against `FilterWhitelists.RUN_METRIC_SNAPSHOTS`; verified by direct source read)
- [x] 4.6 Declare `produces = MediaType.APPLICATION_JSON_VALUE` on every `@GetMapping`/`@PostMapping` in `RunMetricSnapshotController` and `RunMetricSnapshotDeprecatedController` (see the new `openapi-examples` delta requirement). Without it, springdoc documents the response under `*/*` and `OpenApiExampleCustomizer` — which only ever looks up the `application/json` media type — finds nowhere to attach the response examples added in 4.3, even with correctly named files. Verify via the same `/v3/api-docs` assertions as 4.3. (done: `RunMetricSnapshotController.java:35,42` and `RunMetricSnapshotDeprecatedController.java:42,53` all declare `produces = MediaType.APPLICATION_JSON_VALUE`; verified by direct source read)

## 5. jOOQ codegen and drift guard

- [x] 5.1 Change the analytics generator in `build.gradle` to `.withExcludes("flyway_schema_history|run_metric_snapshots")` (design D7). Verify the analytics exclude does not affect the meta generator. (done: `build.gradle:325` carries the analytics exclude; meta generator's own exclude at :300 is unaffected; verified by direct source read)
- [x] 5.2 Run `./gradlew generateJooq`, delete the stale generated `jooq/analytics/tables/RunMetricSnapshots.java` and its record, and commit the new `jooq/meta` sources. Verify `RUN_METRIC_SNAPSHOTS` now resolves only under `jooq.meta.Tables`. (done: `jooq/analytics/tables/RunMetricSnapshots.java` and its record are deleted (git status: `D`); `jooq/meta/tables/RunMetricSnapshots.java` and its record are generated and present)
- [x] 5.3 Update `JooqSchemaDriftTest`: remove `Tables.RUN_METRIC_SNAPSHOTS` from the analytics list and add the meta table to the meta list as an FQN, matching how that test already writes meta tables. Verify `./gradlew test --tests "*JooqSchemaDriftTest"` passes. (done: `JooqSchemaDriftTest.java:76` lists `com.epam.aidial.evaluation.data.db.jooq.meta.Tables.RUN_METRIC_SNAPSHOTS` as an FQN under the meta list; test passing in clean build)

## 6. Test helpers and functional test churn

- [x] 6.1 Move `cleanupRunMetricSnapshots`, `countRunMetricSnapshots`, `createRunMetricSnapshot`, and `findRunMetricSnapshotsByRunId` from `AnalyticsTestDataHelper` to `MetaTestDataHelper`. Verify no raw snapshot SQL remains outside helpers. (done: all four methods present in `MetaTestDataHelper.java`, none remain in `AnalyticsTestDataHelper.java`; verified by direct source read)
- [x] 6.2 Delete the `DELETE FROM run_metric_snapshots` line from `PostgresTestPersistenceService.cleanupAnalyticsTables()` (`:61`) outright — do NOT move it to a meta cleanup path. Meta test data is already wiped in full by `dropAndCreatePublicSchema()` + `pg_restore` (`:43-49`), so a meta-side delete of this table would be redundant dead code. Verify functional tests still start from a clean state across classes. (done: no `run_metric_snapshots` reference remains in `PostgresTestPersistenceService`; 3166 tests, 0 failures in clean build)
- [x] 6.3 Update the 15 functional test classes under `functional/tests/` that reference `RunMetricSnapshot` or the helper methods (plus `PostgresFunctionalTests` and `AnalyticsTestDataHelper` — 17 total) to use `MetaTestDataHelper` and the new imports. Several of these seed a snapshot fixture against a `runId` generated via `UUID.randomUUID()` rather than a run actually created through `metaTestDataHelper` — these now violate the new FK at runtime and must be updated to reference a real created run: `RunComparisonFunctionalTests` (`:327-328`, `:348-349`, `:586`), `MetricScoreResultStructuredQueryFunctionalTests` (`:227-228`), `QuerySchemaDiscoveryFunctionalTests` (`:52`), `MetricScoreComputationFunctionalTests` (`:197`, `:226`, `:260-261`). Verify by actually running these four suites — e.g. `./gradlew test --tests "com.epam.aidial.evaluation.functional.tests.RunComparisonFunctionalTests" --tests "com.epam.aidial.evaluation.functional.tests.MetricScoreResultStructuredQueryFunctionalTests" --tests "com.epam.aidial.evaluation.functional.tests.QuerySchemaDiscoveryFunctionalTests" --tests "com.epam.aidial.evaluation.functional.tests.MetricScoreComputationFunctionalTests"` — and confirming they pass. Compiling the test source set does not catch a runtime FK violation. (done: 16 `functional/tests/*` classes reference `RunMetricSnapshot`/`MetaTestDataHelper` snapshot methods; the four named suites pass, part of 3166 tests, 0 failures in clean build)
- [x] 6.4 Repoint the REST call sites from `/analytics/run-metric-snapshots` to `/run-metric-snapshots` — 7 files, 19 occurrences (`RunMetricSnapshotFunctionalTests` alone has 11; the remaining 6 are in `EvalSummaryFunctionalTests`, `EvalSummaryExportFunctionalTests`, `GrafanaEnabledFunctionalTests`, `EvalSummaryAggregationFunctionalTests`, `EvalSummaryExportPageSizeFunctionalTests`, and `GrafanaDisabledFunctionalTests`) — keeping the alias coverage added in 4.2 pointed at the old path. Verify the affected suites pass. (done: canonical path in use across these classes with deliberate alias coverage retained in `RunMetricSnapshotFunctionalTests`; all affected suites passing in clean build)
- [x] 6.5 Add a functional test asserting that deleting a terminal run removes its `run_metric_snapshots` rows via cascade, and that its analytics result rows remain (per the `test-suite-runs` delta). Verify both assertions with repository/helper reads, not raw SQL. (done: `TestSuiteRunFunctionalTests.java:600` `@DisplayName("Should cascade-delete run metric snapshots on run deletion while leaving analytics rows untouched")`; passing in clean build)
- [x] 6.6 Add a functional test asserting that deleting a test suite (`DELETE /api/v1/test-suites/{id}`) cascades to its runs and, transitively, to each deleted run's `run_metric_snapshots` rows (per the `test-suite-runs` delta's "Cascade delete on test suite removal" scenario). Verify via repository/helper reads that both the run rows and their snapshot rows are gone. (done: `TestSuiteFunctionalTests.java` references `runMetricSnapshot` in a suite-deletion cascade assertion; passing in clean build)
- [x] 6.7 Add a test asserting that a `run_metric_snapshots` insert whose `test_suite_run_id` does not exist in `test_suite_runs` is rejected at the database level (per the `metrics-storage` delta's "Snapshot for an unknown run is rejected" scenario) — this exercises the FK constraint itself, distinct from `RunMetricSnapshotService.batchCreate()`'s own existence check that returns HTTP 404. Verify the write throws and no row is inserted. (done: `RunMetricSnapshotFunctionalTests.java:82` asserts a `DataAccessException` from a direct insert against a non-existent run; passing in clean build)

## 7. Migration tests

- [x] 7.1 Add a test that drives `V1_33__CopyRunMetricSnapshotsFromAnalytics` directly against two Testcontainers datasources — the copy cannot be exercised through an application boot because Flyway runs before fixtures exist (design, Migration Plan). Verify rows present in the analytics source land in the meta target with identical column values. (done: `CopyRunMetricSnapshotsFromAnalyticsMigrationTest#shouldCopyAllColumns_whenSourceRowsExist` — part of 5/5 passing)
- [x] 7.2 Add a case re-running the migration against an already-populated target and verify it inserts nothing and does not fail. (done: `CopyRunMetricSnapshotsFromAnalyticsMigrationTest#shouldInsertNothingAndNotFail_whenTargetAlreadyPopulated` — part of 5/5 passing)
- [x] 7.3 Add a case where a source row's `test_suite_run_id` is absent from meta and verify the row is skipped, the dropped count is logged, and the migration completes successfully. (done: `CopyRunMetricSnapshotsFromAnalyticsMigrationTest#shouldSkipOrphanRowAndCompleteSuccessfully_whenRunMissingFromMeta` — part of 5/5 passing)
- [x] 7.4 Add a case where the analytics source table does not exist and verify the migration skips and completes successfully. (done: `CopyRunMetricSnapshotsFromAnalyticsMigrationTest#shouldSkipAndCompleteSuccessfully_whenAnalyticsSourceTableDoesNotExist` — part of 5/5 passing)
- [x] 7.5 Add a case that forces a mid-copy failure (e.g. a row that violates the target schema partway through a batch) and verifies: no `run_metric_snapshots` rows landed in the meta target, and no `flyway_schema_history` row for `V1.33` exists afterward — proving the copy and its history entry commit or roll back together atomically (design D1/D4), which none of the other cases exercise. (done: `CopyRunMetricSnapshotsFromAnalyticsMigrationTest#shouldRollBackDataAndHistory_whenCopyFailsMidway` — part of 5/5 passing; observed run: `tests="5" failures="0" errors="0"`, 2026-09-14)

## 8. Documentation, specs, and config

- [x] 8.1 Update `docs/database-schema.md`: add the meta `run_metric_snapshots` table with its FK and indexes, and mark the analytics table frozen and unread as of this release. Verify both statements are present. (done: meta table section with FK/indexes present; analytics table section marked "FROZEN as of meta V1.32" with a "Do not read, write, or trust this table" callout)
- [x] 8.2 Update `docs/key-packages.md` for the moved packages. Verify no stale `data.db.analytics.*RunMetricSnapshot*` or `service.domain.analytics.RunMetricSnapshotService` references remain. (done: no stale references found; `.configuration.datasource.migration` package row added in this drift pass)
- [x] 8.3 Update `docs/patterns/computation-versioning.md`, `docs/patterns/eval-summaries-read-surface.md`, and `docs/patterns/dual-datasource.md` to reflect that snapshots are meta-resident. Verify by grepping those files for "analytics" near snapshot references. (done: all three files state the meta-resident fact explicitly; `dual-datasource.md` gating wording corrected in this drift pass)
- [x] 8.4 Update `openspec/config.yaml` per Config Maintenance Policy (done: relevant sections updated) — Flyway Java migrations are now a supported migration type, restricted to data movement, registered explicitly, with meta ordered after analytics.
- [x] 8.5 Update AGENTS.md per AGENTS.md Maintenance guidelines (done: relevant sections reflect the change) — the moved packages/qualifiers and the new Java-migration convention.
- [x] 8.6 Update `openspec/specs/README.md` per Spec Index Maintenance Policy (done: index reflects current specs) — the `metrics-storage` summary currently describes run metric snapshots as part of the analytics table and is now inaccurate.
- [x] 8.7 At archive time, apply the `metrics-storage` delta's "Supersedes" bullets (its Implementation notes section) to `openspec/specs/metrics-storage/spec.md`'s own Implementation Notes — replacing the stale `RunMetricSnapshotService` (`service.domain.analytics`), `PostgresRunMetricSnapshotRepository` ("same qualifier" = `analyticsDsl`), `RunMetricSnapshotResponseDto`/`RunMetricSnapshotBatchWriteRequestDto` (`service.domain.dto.analytics`), `RunMetricSnapshot` model package, and `V1.6__CreateRunMetricSnapshotsTable.sql` bullet (which lists it under `db/migration/analytics/POSTGRES/` with no mention of meta `V1.32`/`V1.33` or the freeze). Verify by grepping the archived `metrics-storage` spec's Implementation Notes for `service.domain.analytics.RunMetricSnapshotService`, `data.db.analytics.model.RunMetricSnapshot`, and `service.domain.dto.analytics.RunMetricSnapshot` and confirming zero hits. (done at archive: Implementation Notes now name `service.domain.RunMetricSnapshotService`, `PostgresRunMetricSnapshotRepository` in `data.db.repository` with `@Qualifier("metaDsl")`, `RunMetricSnapshot` in `data.db.model`, both DTOs in `service.domain.dto`, and a dedicated bullet marking analytics `V1.6` frozen alongside meta `V1.32`/`V1_33`; verification grep returns 0 hits)
- [x] 8.8 Verify `openspec/specs/README.md`'s one-sentence summaries for `metric-evaluation` and `typed-sql-dsl` still accurately describe those specs after this change's MODIFIED/ADDED requirements (snapshot database relocation; the analytics codegen exclusion carve-out). Update the wording only if it has become inaccurate — both summaries are likely already database-agnostic enough to stand unchanged. (done: reviewed both summaries against the current spec deltas; wording already accurate, no change needed)

## 9. Final verification

- [x] 9.1 Run `./gradlew spotlessApply` then `./gradlew clean build` and verify the full build passes, including Checkstyle, `LayeredArchitectureTest`, `LoggingConventionTest`, `JdbcTemplateFenceTest`, and `JooqSchemaDriftTest`. (done: 3166 tests, 0 failures in clean build)
- [x] 9.2 Verify no production code path reads or writes the analytics `run_metric_snapshots` table, by grepping for the table name and confirming remaining hits are only the historical analytics migrations `V1.6`, `V1.8`, and `V1.12`. (done: grep confirms remaining hits are only `V1.6__CreateRunMetricSnapshotsTable.sql`, `V1.8__NormalizeErrorShapedMetricValues.sql`, `V1.12__AddSuiteAndTimestampToMetricScoreResult.sql`, the `V1_33` copy migration itself, and two javadoc-only mentions in `ComputationResolver`/`EvalSummaryRepository`)
- [x] 9.3 Run `/review` and address findings before opening the PR. (done: 5-agent local review of the branch diff; one false positive rejected after verification, one benign ordering note recorded, two minor findings fixed — stale `analytics` javadoc in `EvalSummariesSchemaProvider` and missing `final` on single-assignment locals in `V1_33`; `clean build` green afterwards: 3166 tests, 0 failures)
