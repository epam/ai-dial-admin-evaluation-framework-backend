## 1. Meta index migration and latest-computation tiebreak

- [x] 1.1 Add `src/main/resources/db/migration/meta/POSTGRES/V1.34__ReplaceRunMetricSnapshotsRunIndex.sql`: `CREATE INDEX idx_run_metric_snapshots_run_computed_at ON run_metric_snapshots (test_suite_run_id, computed_at_ms DESC, computation_id DESC)` then `DROP INDEX idx_run_metric_snapshots_run`, with a header comment explaining the prefix relationship and the tiebreak (design D3). Verify by running `./gradlew generateJooq` and confirming the generated `jooq/meta/Indexes.java` now carries `IDX_RUN_METRIC_SNAPSHOTS_RUN_COMPUTED_AT` and no longer `IDX_RUN_METRIC_SNAPSHOTS_RUN`; commit the generated diff.
- [x] 1.2 Change `PostgresRunMetricSnapshotRepository.findLatestComputationId` to `orderBy(COMPUTED_AT_MS.desc(), COMPUTATION_ID.desc())` (design D4). Verify with a functional test in `RunMetricSnapshotFunctionalTests` (or the repository's functional suite) that seeds two computations with the same `computed_at_ms` via `MetaTestDataHelper.createRunMetricSnapshot` and asserts the greater `computation_id` is returned.
- [x] 1.3 Run `./gradlew test --tests "*JooqSchemaDriftTest"` and the `RunMetricSnapshot*` functional suites; verify all pass against the new index set (done: drift test green, no stale index constants referenced anywhere in `src/main/java`).

## 2. Query entity resolver and schema provider

- [x] 2.1 Add `query.service.TestSuiteRunQueryFields` constants class: entity name `test_suite_runs`, excluded columns (`suite_snapshot`, `run_config`, `error_details`), `suite_type` + snapshot key `suiteType`, ref descriptors (`deployment_ref` → `deploymentRef` × `id|name|version|type`; `mcp_deployment_ref` → `mcpDeploymentRef` × `id|name|type|transport`), `metric_names`, and source labels `suite_snapshot` / `run_metric_snapshots` (design D6). Verify it compiles and `checkstyleMain` passes (no duplicated literals in the two consumers below).
- [x] 2.2 Add `query.service.repository.PostgresTestSuiteRunEntityResolver` (`@Repository @LogExecution @ConditionalOnProperty(name = "datasource.meta.vendor", havingValue = "POSTGRES")`, `@Qualifier("metaDsl")`): `table()` returns the derived table `tsr` = 10 plain columns + 9 `JsonPathAccessor` text extractions (1 single-level `suite_type`, 8 two-level ref sub-fields) + the correlated `metric_names` scalar subquery (`coalesce(jsonb_agg(DISTINCT rms.tsmd_name ORDER BY rms.tsmd_name), '[]')` over `rms.computation_id = (latest computation LIMIT 1 with tiebreak)`), all via typed jOOQ (design D1, D2, D5); `bindings()` maps every field to `tsr.field(...)` with `metric_names` typed `ARRAY`. Verify with a unit test `PostgresTestSuiteRunEntityResolverTest` (mock `DSLContext`, real `JooqTableSchemaResolver`/`PostgresJsonPathAccessor`) asserting: entity name; bindings contain exactly the spec'd 20 names and none of the 3 excluded columns; types (`id` UUID, `metric_names` ARRAY, refs STRING); and that `DSL.using(SQLDialect.POSTGRES).render(resolver.table())` contains `jsonb_agg`, `order by`, `computed_at_ms desc`, `computation_id desc`, a row limit (jOOQ 3.21 renders `fetch next ? rows only` for Postgres), and does not contain `run_config`/`error_details` as projected columns.
- [x] 2.3 Add `query.service.TestSuiteRunsSchemaProvider` (`@Component @LogExecution`, simple descriptor): `baseSchema()` = `schemaResolver.resolve(TEST_SUITE_RUNS)` minus excluded columns, plus `suite_type`, the 8 ref sub-fields (STRING, source `suite_snapshot`) and `metric_names` (ARRAY, source `run_metric_snapshots`) (design D6, D7). Verify with `TestSuiteRunsSchemaProviderTest`: descriptor is `("test_suite_runs", false, null)`, excluded columns absent, virtual fields present with correct type/source, and a parity assertion that the schema's field-name set equals the resolver's binding key set and types agree per field.
- [x] 2.4 Update every test enumerating the real supported-entity catalog so `test_suite_runs` is expected in alphabetical order (only `QuerySchemaDiscoveryFunctionalTests` does; `QueryEntityRegistryTest`/`StructuredQueryServiceTest` use stub providers and need no change). Verify `./gradlew test --tests "com.epam.aidial.evaluation.query.*"` passes.

## 3. Functional tests (Testcontainers, meta datasource)

- [x] 3.1 Run FIRST (before the behavioural suites, since its outcome may change `table()` from 2.2) `EXPLAIN (ANALYZE, BUFFERS)` through a throwaway test or `psql` against a Testcontainers meta DB seeded with ≥ 200 runs × 3 computations for: (a) paged `ORDER BY created_at_ms DESC LIMIT 25`, (b) `select count(*)` (`include_total`), (c) `ORDER BY test_run_name LIMIT 25`, (d) `metric_names co 'X'`. Confirm the derived table is pulled up (no `Subquery Scan` over `tsr`), (b) has no snapshot access, and (a) evaluates the SubPlan only for emitted rows (design Risks). Record the four plans (trimmed) in the pattern doc of task 4.1. If pull-up or postponement fails, apply the fallback shape from design Risks before proceeding.
- [x] 3.2 Add `functional/tests/TestSuiteRunStructuredQueryFunctionalTests` (abstract, wired as a `@Nested` class in `PostgresFunctionalTests` next to `TestSuiteStructuredQueryTests`) using `MetaTestDataHelper.createTestSuiteRun`, `setRunSuiteSnapshot`, `createRunMetricSnapshot`. Cover: row query with empty `select` returns exactly the 20 fields and no `suite_snapshot`/`run_config`/`error_details`; `suite_snapshot IS NULL` run yields null `suite_type` and all ref fields without failing; DEPLOYMENT run yields null `mcp_deployment_ref::*` and vice versa; `deployment_ref::id` `eq` filter and `deployment_ref::name` sort. Verify the nested suite passes via `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$TestSuiteRunStructuredQueryTests"`.
- [x] 3.3 In the same suite cover `metric_names`: latest computation wins (`c1` at 1000 with two names, `c2` at 2000 with one → `["Accuracy"]`); same-millisecond tiebreak by greater `computation_id`; alphabetical order; `[]` for a run with no snapshots; `co` exact element (`AccuracyV2` not matched by `Accuracy`); `lower(metric_names) co "accuracy"`; `nc` returns metric-less runs. Verify the suite passes.
- [x] 3.4 In the same suite cover aggregate mode (`group_by: ["deployment_ref::id"]` + `count` → one row per deployment incl. a null-keyed row), `include_total`/`countRows` on a filtered query, and that referencing `suite_snapshot` in `filter` is rejected (`ValidationException` at the executor, plus one REST-level case in `StructuredQueryExecuteFunctionalTests` asserting HTTP 400). Verify the suite passes.
- [x] 3.5 Extend `QuerySchemaDiscoveryFunctionalTests`: catalog lists `test_suite_runs` as simple; `GET /api/v1/queries/entities/schema/test_suite_runs` returns the spec'd field table and none of the 3 excluded columns; the "schema matches executor" scenario — every returned field name used in a row `select` executes and each row has exactly those keys. Verify `PostgresFunctionalTests$QuerySchemaDiscoveryTests` passes.

## 4. Docs, specs, formatting

- [x] 4.1 Add `docs/patterns/test-suite-runs-query-entity.md` (derived-table projection to exclude heavy JSONB columns; scalar-subquery vs lateral vs global aggregate with the EXPLAIN evidence from 3.1; snapshot-only refs; tiebreak) and add rows to `docs/patterns/README.md` and the AGENTS.md "Unique Patterns" table; add one paragraph to `docs/patterns/query-dsl-entity-resolution.md` naming `test_suite_runs` as the fully-derived-table precedent. Verify links resolve (files exist at the linked paths).
- [x] 4.2 Update `docs/database-schema.md`: `run_metric_snapshots` index table (replace `idx_run_metric_snapshots_run` row with the composite index and its purpose), migration history row `V1.34`, and the "Last sync" line. Verify by grepping that `idx_run_metric_snapshots_run ` (single-column) is described only in the frozen-analytics section and the history.
- [x] 4.3 Update `openspec/specs/README.md` per Spec Index Maintenance Policy: add the `test-suite-runs-query-entity` entry (Planned → Implemented at archive) and adjust the `query-schema-discovery` summary if it enumerates entities (done: index reflects current specs).
- [x] 4.4 Run `./gradlew spotlessApply checkstyleMain checkstyleTest` and then a full `./gradlew build`; verify zero failures and that `LoggingConventionTest`/`LayeredArchitectureTest` pass with the new classes.
