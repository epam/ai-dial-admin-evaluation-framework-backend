## 1. Build, endpoint, security, identity probe (boots with an empty production tool set)

- [x] 1.1 `gradle.properties`: add trailing newline and `mcp_sdk_version=2.0.0` with a comment that it must equal the MCP SDK version managed by the Spring AI BOM in use; `build.gradle` and `evaluation-runner-core/build.gradle`: replace hardcoded `mcp-bom:2.0.0` with `${mcp_sdk_version}`; `build.gradle`: add `platform("org.springframework.ai:spring-ai-bom:2.0.1")` and `spring-ai-starter-mcp-server-webmvc` excluding `spring-boot-starter-web` (D-F1). Done when `./gradlew dependencies --configuration runtimeClasspath` shows `io.modelcontextprotocol.sdk:mcp-core:2.0.0` with no upgrade arrows, no `spring-boot-starter-web`, no `mcp-json-jackson2`, and `./gradlew :evaluation-runner-core:compileJava compileJava` passes.
- [x] 1.2 `application.yml`: `spring.ai.mcp.server` block — `enabled: true`, `stdio: false`, `name`, `version: "@project.version@"`, `type: SYNC`, `protocol: STREAMABLE`, `capabilities.tool: true` / `resource,prompt,completion: false`, `streamable-http.mcp-endpoint: /mcp`, `streamable-http.disallow-delete: false`, `annotation-scanner.enabled: true`, multi-line `instructions` (D-F7). No startup protocol guard — the security matcher (1.3) authenticates the single configured endpoint instead (D-F3). Done when the app boots with this block present.
- [x] 1.3 `SecurityConfiguration`: inject `@Value` for `spring.ai.mcp.server.streamable-http.mcp-endpoint`; normalise trailing slash; add `.requestMatchers(mcpEndpoint).authenticated()`, the single path both `STREAMABLE` and `STATELESS` serve (D-F3). No separate properties class — `@Value` is the only reader. `mcp.support.McpCallerContext` (D-F2) with unit test `McpCallerContextTest` (JWT principal → jwt; String principal → null; no authentication → null; `AnonymousAuthenticationToken` → null; bearer token from `AuthorizationTokenHolder`). Done when the unit test passes.
- [x] 1.4 Test harness: `functional.support.McpFunctionalTestSupport`, `functional.support.StaticJwtTestConfiguration` (`@Bean @Primary` under a different bean name than `tokenDecoderFactory`), `functional.support.CallerIdentityProbeTools` (`probe_caller_identity` → `{threadName, authenticationPresent, createdBy, bearerTokenPresent}`, using `McpCallerContext` and `AuthorResolver`) (D-F9). Done when all three compile in test scope.
- [x] 1.5 Identity probe functional tests (D-F2 verification, before any production tool): `McpServerFoundationFunctionalTests` (none mode) — `initialize` succeeds, `instructions` mention `try_out` and `get_run`, `tools/list` production set is empty after filtering `probe_*`, `probe_caller_identity` → `createdBy = anonymous`, `authenticationPresent = false`; `McpServerSecurityFunctionalTests` (one nested class with oidc + api-key properties per `ApiKeyAuthenticationTests`, `@MockitoBean CoreApiKeyIntrospector`) — raw POST without credentials → 401; malformed bearer → 401; `alice-token` → `createdBy = alice`, `bearerTokenPresent = true`, `authenticationPresent = true`; `Api-Key valid-key` → `createdBy = anonymous`, `bearerTokenPresent = false`, `authenticationPresent = true`. Register nested `McpServerFoundationTests` and `McpServerSecurityTests`. Done when `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$McpServerFoundationTests" --tests "...\$McpServerSecurityTests"` passes. **If the alice assertion fails, stop and implement D-F2c before continuing to group 2; record the outcome in design.md.** — the alice assertion PASSES; D-F2's request-thread model is verified (no D-F2c needed). See design.md D-F3/D-F9 for the runtime deviations discovered while implementing this group.

## 2. Cross-cutting tool support

- [x] 2.1 `mcp.model.McpErrorCode` (15 codes), `mcp.model.McpToolError`, `mcp.support.McpToolResults` (D-F5). Unit test `McpToolResultsTest`: success → `isError=false`, one `TextContent` parsing to the payload; error → one text block with `code`/`message`/`details`; unserialisable payload → `INTERNAL_ERROR` result, no exception. `McpErrorCodeAlignmentTest`: every code except `NOT_SUPPORTED` exists in `web.handler.ErrorCode`. Done when both pass.
- [x] 2.2 `mcp.support.McpToolErrorTranslator` (D-F5 table, incl. all `DialCoreErrorCode` values and `ResourceAccessException`). Unit test `McpToolErrorTranslatorTest`: one `@Test` per table row; `ResourceAccessException` message never contains the URL and is `UPSTREAM_TIMEOUT` only for a timeout cause; unmapped exception yields the fixed generic message and never echoes `getMessage()`. Done when it and `LoggingConventionTest` pass.
- [x] 2.3 `mcp.support.McpToolExecutor` (both overloads) and `mcp.support.UnsupportedFeatureGuard` + `UnsupportedFeatureException` (D-F2, D-F6). Unit tests `McpToolExecutorTest` (body result encoded; `EntityNotFoundException` → `NOT_FOUND` result; `Function` overload receives the caller context) and `UnsupportedFeatureGuardTest` (null/empty pass; present/non-empty throw with the literal sentence). Done when both pass.
- [x] 2.4 `mcp.constants.McpToolNames`, `mcp.constants.McpToolDescriptions` (D-F7, incl. `UNSUPPORTED_FEATURES_SENTENCE`; description constants for the deployments tools listing accepted filter values). Done when they compile and a unit test asserts `UNSUPPORTED_FEATURES_SENTENCE` equals the umbrella spec sentence literally.
- [x] 2.5 `LayeredArchitectureTest`: `mcp` layer (D-F4) with the explanatory comment. Done when `./gradlew test --tests "com.epam.aidial.evaluation.architectural.LayeredArchitectureTest"` passes with the classes from 1.x and 2.x present.

## 3. Deployments tool group

- [x] 3.1 `mcp.model`: `DeploymentKind`, `DeploymentInterface` (with `fromWireValue` throwing `ValidationException` listing accepted values), `DeploymentSummaryMcpDto`, `DeploymentMcpDto`, `ModelDetailsMcpDto`, `ToolsetDetailsMcpDto`, `ApplicationDetailsMcpDto`, `DeploymentListMcpDto` (D-F8). Unit test `DeploymentMcpDtoJsonTest`: enum wire values on the wire, nulls omitted, `fromWireValue` accepts every wire value and rejects constant names. Done when it passes.
- [x] 3.2 `mcp.mapper.DeploymentMcpMapper` (MapStruct, explicit `kindOf` subclass switch). Unit test `DeploymentMcpMapperTest`: summary from short DTOs (model, application, toolset with transport); detail model → `model` only; toolset → `toolset` only; application → `application` only and no routes/pricing; interfaces by wire value; `total` equals list size. Done when it passes.
- [x] 3.3 `mcp.tools.deployment.DeploymentTools` (`list_deployments`, `get_deployment`, string filters parsed inside `execute`, blank `deploymentId` → `ValidationException`) (D-F8). `McpToolConventionTest` (D-F10). Done when `McpToolConventionTest` and `LayeredArchitectureTest` pass.
- [x] 3.4 Extend `McpServerFoundationFunctionalTests` (stubbing `dialCoreClient.getDeployments` / the single-deployment call as `McpDeploymentFunctionalTests` does): unfiltered list with summary shape, `type` filter, `interfaceType` filter, invalid `type` → structured `VALIDATION_ERROR` listing accepted values and no `DialCoreClient` interaction, `get_deployment` model detail, toolset detail with `allowedTools`, application detail without routes, unknown id → `NOT_FOUND` naming the id, `DialCoreClientException(504)` → `UPSTREAM_TIMEOUT`, `ResourceAccessException` → message without URL, `RuntimeException` from the mock → `INTERNAL_ERROR` generic message. Done when `PostgresFunctionalTests$McpServerFoundationTests` passes.
- [x] 3.5 `tools/list` contract test in the same class: production tool names exactly `{list_deployments, get_deployment}` (ignoring `probe_*`); non-empty descriptions; every input property has a description; `type` / `interfaceType` are optional strings whose descriptions contain every accepted value; `deploymentId` required. Done when it passes.

## 4. Disabled-server and parity checks

- [x] 4.1 `McpServerDisabledFunctionalTests` with `spring.ai.mcp.server.enabled=false`: nested `McpServerDisabledTests` (none mode) → raw POST `/mcp` 404 and no `WebMvcStreamableServerTransportProvider` bean in the context. Done when `PostgresFunctionalTests$McpServerDisabledTests` passes.
- [x] 4.2 Nested `McpServerDisabledOidcTests` (oidc + `StaticJwtTestConfiguration`, `enabled=false`) → 404 with `alice-token`, 401 without. Done when it passes.
- [x] 4.3 `McpServerSecurityFunctionalTests`: end-to-end parity check — `list_deployments` as alice and as the API-key caller both succeed against the mocked `DialCoreClient`; `probe_caller_identity` after each reports the respective identity (`alice`, then `anonymous`). Done when it passes.
- [x] 4.4 `McpServerFoundationFunctionalTests`: session behaviour — a second `initialize` on a fresh client yields a different `Mcp-Session-Id`; a tool call with an unknown session id is rejected by the transport (status per SDK) without touching `DialCoreClient`. Done when it passes.
- [x] 4.5 `./gradlew spotlessApply checkstyleMain checkstyleTest` and the full `./gradlew test`. Done when green.

## 5. Documentation, governance, umbrella amendments

- [x] 5.1 `docs/configuration.md`: subsection `2.6 MCP Server (inbound)` (TOC + six-column table) with one row per property set in `application.yml`'s `spring.ai.mcp.server` block (D-F11); `docs/patterns/mcp-server.md` written per D-F11 and linked from `docs/patterns/README.md`. Done when every property in the yml block has a row and the pattern doc names the two library behaviours the thread model rests on.
- [x] 5.2 Update openspec/config.yaml per Config Maintenance Policy (done: Architecture list has item 9 `mcp`, Layering Principle has the D-F4 line incl. allowed `client`/`runner` edges, Tech Stack says Spring Boot 4.1.1, Feature Surface mentions the MCP server, testing infrastructure mentions `McpFunctionalTestSupport` and `StaticJwtTestConfiguration`)
- [x] 5.3 Update AGENTS.md per AGENTS.md Maintenance guidelines (done: Architecture Overview names the `mcp` layer and its access rules, Unique Patterns table links `docs/patterns/mcp-server.md`); `docs/key-packages.md` gains rows for `.mcp.model`, `.mcp.mapper`, `.mcp.support`, `.mcp.constants` (`McpToolNames`/`McpToolDescriptions`), `.mcp.tools.deployment`, `.configuration.mcp`.
- [x] 5.4 Update openspec/specs/README.md per Spec Index Maintenance Policy (done: `mcp-tools-deployments` listed under Integration with a sentence distinguishing MCP server specs from the MCP client specs `mcp-tool-invocation` / `toolset-listing`); set `mcp-tools-deployments` requirement statuses to Implemented with implementation notes pointing to `DeploymentTools` and `DeploymentMcpMapper`.
- [x] 5.5 Umbrella amendments in `openspec/changes/add-mcp-server/`: `design.md` D3 → verified request-thread model with its two dependencies and D-F2c as fallback; D2 → flat `@McpToolParam` parameters, constants in `constants`; D7 table → `ResourceAccessException` row, all `DialCoreErrorCode` values, binding-layer limitation; `specs/mcp-server/spec.md` → binding-layer limitation sentence in "Structured tool error contract", and statuses of "MCP endpoint over Streamable HTTP", "Structured tool error contract", "Agent guidance in server metadata", "Authenticated principal visible inside tool execution" → Implemented with notes; `specs/security/spec.md` requirement → Implemented; `tasks.md` 2.1 → no `mcp-server.*` property expectation (moved to 2.7), tick 1.1. Done when `openspec validate add-mcp-server --strict` and `openspec validate mcp-foundation --strict` pass and `./gradlew clean build` is green.
