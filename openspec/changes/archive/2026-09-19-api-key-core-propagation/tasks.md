**Sequencing:** this change lands after `mcp-foundation` is archived — tasks 3.2, 3.3, 3.5 and 4.1 edit files that change owns (`mcp/support/McpCallerContext`, `functional/support/CallerIdentityProbeTools`, `functional/tests/McpServerSecurityFunctionalTests`, `docs/patterns/mcp-server.md`).

## 1. Capture the caller credential with its kind

- [x] 1.1 Add `CredentialKind` and `CallerCredential` (factories `bearer`/`apiKey` returning `null` for a null/blank value, never throwing) to `evaluation-runner-core` `runner.util`, and rework `AuthorizationTokenHolder` onto a single `ThreadLocal<CallerCredential>`: `setCredential`/`getCredential`/`clearToken`, the legacy `setToken`/`getToken` removed, `headerName()`/`headerValue()` and a redacting `toString()` on the record, and no `getCredentialKind()` (design D1). **Done when** `./gradlew :evaluation-runner-core:test --tests "com.epam.aidial.evaluation.runner.util.*"` passes with cases for both kinds, blank/null factory input treated as absent, header name/value per kind, `toString()` not containing the value, and clearing.
- [x] 1.2 Add `withCredential` variants to `TokenPropagationHelper` (Supplier/Callable/Runnable), remove the bearer-only `withToken` variants (no production caller), and make a `null` credential a no-op that still clears in `finally` (design D1). **Done when** `./gradlew :evaluation-runner-core:test --tests "com.epam.aidial.evaluation.runner.util.TokenPropagationHelperTest"` passes with cases asserting the kind survives the thread hop, a `null` credential does not throw, and the state is cleared afterwards.
- [x] 1.3 Extend `configuration/security/AuthorizationHeaderInterceptor`: capture the `Api-Key` header only when `Authorization` is blank (mirroring `ApiKeyAuthenticationFilter.java:47`), implement `AsyncHandlerInterceptor#afterConcurrentHandlingStarted` to clear, and keep clearing in `afterCompletion`; add `AuthorizationHeaderInterceptorTest` covering bearer-only, api-key-only, bearer + `Api-Key` both present → bearer captured, `Authorization: Basic …` + `Api-Key` → nothing captured, neither header, and clearing on both completion hooks (design D2). **Done when** `./gradlew test --tests "com.epam.aidial.evaluation.configuration.security.AuthorizationHeaderInterceptorTest"` passes.
- [x] 1.4 Put the `Api-Key` header-name constant in runner-core next to `CredentialKind` and replace the literals in `client/dialcore/DialFileClientConfiguration:48`, `web/security/apikey/CoreApiKeyIntrospector:31` (its own constant deleted; `ApiKeyAuthenticationFilter` and tests reference the runner-core constant), `eval-cli` `TargetDialCoreClientConfiguration:87` and `SourceClientConfiguration:46` with references to it (design D7). **Done when** `./gradlew :evaluation-runner-core:test :eval-cli:test checkstyleMain` passes and `grep -rn '"Api-Key"' --include="*.java" src/main eval-cli/src/main` returns only the constant's own declaration.

## 2. Forward the credential on every caller-scoped outbound call

- [x] 2.1 Replace `DialCoreClientConfiguration#authorizationTokenInterceptor()` with a kind-aware `callerCredentialInterceptor()` that sets exactly one of `Authorization: Bearer` / `Api-Key` / nothing, and add a test asserting the header per kind, the absence of the other, and that all three production beans (`dialCoreRestClient`, `dialCoreTryOutRestClient`, `dialAdasRestClient`) register the interceptor (design D3, D6). **Done when** `./gradlew test --tests "com.epam.aidial.evaluation.client.dialcore.DialCoreClientConfigurationTest"` passes.
- [x] 2.2 Change `runner/client/mcp/McpToolInvoker#callTool`/`#listTools` to take `CallerCredential`, move header application into one package-private `applyCredentialHeader` used by both transport builders (no header at all for an absent credential — never `Bearer null`), and update callers `DeploymentService:153`, `TryItOutService:589`, `EvaluationWorker:283` to pass the credential. **Done when** `./gradlew :evaluation-runner-core:test --tests "com.epam.aidial.evaluation.runner.client.mcp.McpToolInvokerTest"` passes with a case per kind plus the absent-credential case.
- [x] 2.3 Carry the credential through run dispatch: `EvaluationContext#token` → `CallerCredential credential`, `TestCaseRunner:69/104` propagation via `withCredential`, `TestSuiteRunService:133` capturing `getCredential()` for `dispatchEvaluation`, and `eval-cli`'s `EvaluationContextFactory:85` building `CallerCredential.apiKey(...)` (also re-typed `TestSuiteEvaluationJob#dispatch`/`#run`/`#buildContext`, the rest of the same dispatch chain, from `String token` to `CallerCredential credential`). **Done when** `./gradlew :evaluation-runner-core:test --tests "com.epam.aidial.evaluation.runner.job.TestCaseRunnerTest" :eval-cli:test --tests "com.epam.aidial.evaluation.cli.service.EvaluationContextFactoryTest"` passes.
- [x] 2.4 Verify the bearer path is unchanged: same-thread holder visibility for a bearer caller (`DeploymentServiceTokenPropagationTest` asserts only that the mocked `DialCoreClient` call sees the holder's token — it does no pooled-thread work and no outbound-header assertion) plus the thread hop in `TokenPropagationHelperTest`. **Done when** `./gradlew test --tests "com.epam.aidial.evaluation.service.domain.DeploymentServiceTokenPropagationTest"` and `./gradlew :evaluation-runner-core:test --tests "com.epam.aidial.evaluation.runner.util.TokenPropagationHelperTest"` both pass with their bearer scenarios preserved (re-expressed through `setCredential(bearer(...))`/`withCredential*` after the legacy API's removal); `eval-cli`'s `TargetDialCoreClientConfiguration#apiKeyInterceptor` reads `getCredential()` and emits `Api-Key` only for an `API_KEY` credential, covered by a `MockRestServiceServer` test.

## 3. Attribution and caller-identity surface

- [x] 3.1 Make `AuthorResolver#getCreatedBy(Jwt)` fall back, when `jwt == null`, to the current `Authentication`'s name if it is present, authenticated, not an `AnonymousAuthenticationToken`, and its principal is not a `Jwt` — skipping display-name resolution and otherwise returning `anonymous`; no `.web` import (design D5). **Done when** `./gradlew test --tests "com.epam.aidial.evaluation.service.domain.AuthorResolverTest"` passes with cases for the project principal, `resolve-user-name=true` not calling Core for that principal, and the anonymous/no-authentication fallbacks.
- [x] 3.2 Replace `mcp/support/McpCallerContext#bearerToken()` with `callerCredential()` returning the nullable `CallerCredential`, update its javadoc (which currently documents the no-credential gap as intended), and update `McpCallerContextTest` accordingly (design D1). **Done when** `./gradlew test --tests "com.epam.aidial.evaluation.mcp.support.McpCallerContextTest"` passes with cases for a bearer caller, an API-key caller, and no credential.
- [x] 3.3 Update the test-only `functional/support/CallerIdentityProbeTools` payload: replace `bearerTokenPresent` with `credentialKind` (absent when no credential was captured), keeping `createdBy` and `authenticationPresent`. **Done when** `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$McpServerSecurityTests"` compiles and runs against the new payload.
- [x] 3.4 Extend `functional/tests/ApiKeyAuthenticationFunctionalTests` with a REST case asserting an `Api-Key` caller's created entity records `createdBy = my-project` (introspection mocked via `@MockitoBean CoreApiKeyIntrospector`). **Done when** `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$ApiKeyAuthenticationTests"` passes.
- [x] 3.5 Update `functional/tests/McpServerSecurityFunctionalTests`: the `Api-Key` caller's `probe_caller_identity` assertions become `createdBy = my-project` and `credentialKind = API_KEY` (renaming the `apiKeyCallerResolvesToAnonymous` test), the JWT caller's become `credentialKind = BEARER`, and the `list_deployments` parity test asserts the same identities. **Done when** `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$McpServerSecurityTests"` passes.

## 4. Docs and spec sync

- [x] 4.1 Update `docs/patterns/token-propagation.md` (credential + kind, capture precedence, the `withCredential` variants, the kind-checked CLI interceptor) and the "Known limitation (REST parity)" paragraph in `docs/patterns/mcp-server.md`, which documents the `anonymous` / no-credential gap as intended behaviour, replacing it with the new contract and the "attribution is probe-verified until a mutating tool exists" note. No `docs/configuration.md` change (no new property). **Done when** `./gradlew spotlessApply checkstyleMain checkstyleTest` passes and neither doc still describes API-key callers as credential-less.
- [x] 4.2 Update AGENTS.md per AGENTS.md Maintenance guidelines (done: relevant sections reflect the change) — the `AuthorResolver` inline convention and the `TokenPropagationHelper` pattern row. **Done when** `./gradlew spotlessCheck` passes and AGENTS.md describes API-key attribution and credential-kind propagation.
- [x] 4.3 Sync the delta specs into `openspec/specs/{security,dial-core-client,mcp-tool-invocation}` and update `openspec/specs/README.md` per the Spec Index Maintenance Policy (the `security` summary must mention API-key credential propagation and API-key `createdBy` attribution). **Done when** `openspec validate api-key-core-propagation --strict` passes and the archive checklist in `openspec/config.yaml` `rules.archive` is satisfied.
