## 1. Configuration and isolated async infrastructure

- [x] 1.1 Add `QueryDslTestSuiteRunCostEnrichmentProperties` under `configuration.properties` with validated `enabled` and `timeoutSec` fields; add disabled-by-default YAML values (`QUERY_DSL_ENRICHMENT_TEST_SUITE_RUN_COST_ENABLED`, `QUERY_DSL_ENRICHMENT_TEST_SUITE_RUN_COST_TIMEOUT_SEC`) and verify runtime YAML defaults bind as `false`/`2`, an override binds `enabled=true, timeout-sec=2`, and values below `1` fail in a focused `ApplicationContextRunner` test.
- [x] 1.2 Add conditional enrichment wiring in `configuration`: a named `SimpleAsyncTaskExecutor` with `test-suite-run-cost-enrichment-` thread names, virtual/platform mode from `RunExecutorFactory.isVirtualThreads()`, daemon platform threads, and `setCancelRemainingTasksOnClose(true)`; verify unit tests prove both thread modes, close-time interruption, and rejection after close.
- [x] 1.3 Mark the existing scanned `DialAdasClient` `@Primary`, retaining its `@Qualifier("dialAdasRestClient")` field, and add conditional named beans `testSuiteRunCostEnrichmentDialAdasRestClient` / `testSuiteRunCostEnrichmentDialAdasClient`; build the latter with `DialAdasProperties.baseUrl`, shared caller-credential/tracing interceptors, and matching connect/read `timeoutSec` timeouts. Verify a context/client-configuration test proves existing unqualified consumers retain normal settings, the extender receives only the qualified short-timeout client, and the normal shared ADAS settings remain unchanged.
- [x] 1.4 Add context-level conditional-bean tests for default-disabled and explicitly-enabled configuration; verify the disabled context contains neither the extender nor enrichment executor/client and the enabled context contains all three.

## 2. Reusable batch total-cost lookup

- [x] 2.1 Extract `CostService.fetchTotalCosts(Collection<UUID>)` into injectable `service.domain.BatchRunTotalCostLookup` with exact API `Map<UUID, Double> fetchTotalCosts(Collection<UUID> runIds, DialAdasClient dialAdasClient)`; it calls `AdasCostQueryBuilder.buildPageTotalCostQuery` once and parses grouped rows. Verify unit tests cover one aggregate call, valid grouped rows, null response/rows, unparseable/`other` rows, missing groups, and null totals.
- [x] 2.2 Rewire `CostService.getTotalRunCosts` to the shared lookup with the existing shared ADAS client; update `CostServiceTest` and endpoint/functional cost tests only as needed to prove ordering, null-for-missing endpoint output, and existing 502/504 propagation are unchanged.
- [x] 2.3 Run `./gradlew test --tests "com.epam.aidial.evaluation.service.domain.CostServiceTest" --tests "com.epam.aidial.evaluation.client.dialadas.DialAdasClientTest"` and verify the existing batch endpoint path remains green.

## 3. Total-cost result-page extender

- [x] 3.1 Add `TestSuiteRunQueryFields.TOTAL_COST_FIELD`, documented as extension-only and absent from resolver bindings/schema; update the structured-query result/OpenAPI descriptions to document optional `total_cost`, and verify `filter`/`select`/`sort`/`group_by` still reject it as unknown while schema discovery omits it.
- [x] 3.2 Implement conditional `TotalCostTestSuiteRunsPageExtender` in `query.service.repository`: apply only to `test_suite_runs` `QueryMode.ROW`, derive distinct eligible UUID ids, invoke the reusable lookup through the qualified client, and merge values with copied `LinkedHashMap`s. Verify unit tests cover one batch invocation; no `eval.phase` filter; missing-cost omission; non-target entity unchanged; empty pages; projection without `id`; all malformed ids; all pre-keyed rows; aggregate mode; non-overwrite; row/key order; and unchanged `totalCount`, with zero ADAS calls whenever there are no eligible IDs.
- [x] 3.3 Capture `AuthorizationTokenHolder.getCredential()` and `Context.current()` before submit; invoke the lookup with `Context.wrap(TokenPropagationHelper.withCredentialCallable(...))`; verify a unit/integration test proves bearer and API-key header identity, OTel propagation, and credential cleanup on a subsequent task without a credential.
- [x] 3.4 Implement authoritative `Future.get(timeoutSec, ...)` handling: cancel on timeout/interruption, restore interrupt status on interruption, preserve the page received by this extender on timeout/dial-adas/execution/rejection failure, and log each caught failure once with the exception last. Verify deterministic tests for timeout cancellation, execution failure, interruption restoration/cancellation, no partial merge, preserved prior extender contributions, and a later extender remaining applicable after total-cost failure.

## 4. Enabled end-to-end behavior and documentation

- [x] 4.1 Add enabled `@PostgresFunctionalTests` structured-query coverage in a nested class annotated `@TestPropertySource(properties = "query-dsl.enrichment.test-suite-run.cost.enabled=true")` that mocks the enrichment client by name (`@MockitoBean(name = "testSuiteRunCostEnrichmentDialAdasClient") DialAdasClient`); the outer by-type `@MockitoBean DialAdasClient` keeps covering the `@Primary` shared client. Verify: a `test_suite_runs` row with a mocked grouped ADAS total gets `total_cost`; a missing group omits it; one eligible page causes exactly one aggregate call on the named enrichment mock with no `eval.phase` filter and zero calls on the primary mock; default-disabled and no-eligible-id behavior make zero calls; the enabled application context boots with the qualified client/executor wiring.
- [x] 4.2 Update `docs/configuration.md` table of contents and add the two six-column `query-dsl.enrichment.test-suite-run.cost.*` rows; verify property names, environment variables, defaults, `Required` vocabulary, and applied-when descriptions match `application.yml`.
- [x] 4.3 Update durable docs and OpenAPI descriptions: `docs/patterns/query-result-page-extension.md` (second implementation; bounded async lookup that handles its own timeout/failure), `docs/patterns/test-suite-runs-query-entity.md` (`total_cost` result-only key), and the `QueryResultPageExtender` javadoc — keep "SHOULD NOT swallow its own failures" but add an explicit exception for an extender that bounds an external lookup with a deadline and must cancel it on timeout (it degrades itself, logging once with the exception last). Update sibling baseline specs' implementation notes: `openspec/specs/batch-run-costs/spec.md` (the private `CostService.fetchTotalCosts` helper becomes `service.domain.BatchRunTotalCostLookup`) and `openspec/specs/query-result-page-extension/spec.md` (list `TotalCostTestSuiteRunsPageExtender` alongside the first implementation, plus its test). No cost pattern doc exists under `docs/patterns/`; do not create one. Verify links resolve and `/v3/api-docs` describes `total_cost` as result-only and absent from schema discovery.
- [x] 4.4 Update `openspec/specs/test-suite-runs-query-entity/spec.md` from this change’s delta using `/opsx:sync` during archival workflow; verify the synced requirement preserves existing overall-score semantics and adds the `total_cost` scenarios. Update `openspec/specs/README.md` only if its existing summary becomes materially inaccurate.
- [x] 4.5 Review `AGENTS.md` and `openspec/config.yaml` per their maintenance policies; update only if the dedicated qualified client/executor establishes a project-wide convention rather than a feature-local application of existing patterns.

## 5. Verification

- [x] 5.1 Run focused unit and functional suites for the new properties/configuration, batch lookup, extender, credential/OTel propagation, and enabled structured query; verify all pass, including at least one booted application-context test.
- [x] 5.2 Run `./gradlew spotlessApply`, `./gradlew checkstyleMain checkstyleTest`, and `./gradlew clean build`; verify all modules, `LayeredArchitectureTest`, `LoggingConventionTest`, and the full Testcontainers suite pass.
- [x] 5.3 Run `openspec validate "enrich-test-suite-runs-total-cost" --strict`; verify the change is valid before implementation review/archive.
