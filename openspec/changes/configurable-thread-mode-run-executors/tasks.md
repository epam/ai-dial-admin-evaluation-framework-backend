## 1. Runner-core: executor factory (Batch A)

- [ ] 1.1 Add `RunExecutorFactory` in `evaluation-runner-core` (`runner.job`): ctor `(boolean virtualThreads)`, `isVirtualThreads()`, `newWorkerExecutor()` = `Context.taskWrapping(Executors.newThreadPerTaskExecutor(factory))` with `Thread.ofVirtual().name("run-worker-", 0)` or `Thread.ofPlatform().daemon(true).name("run-worker-", 0)` (design D2). Done: class compiles, Javadoc explains the profiling opt-out.
- [ ] 1.2 Register the bean in `EvaluationRunnerAutoConfiguration` (`@Bean @ConditionalOnMissingBean`, `Threading.VIRTUAL.isActive(environment)`). Done: `./gradlew :evaluation-runner-core:compileJava` green.
- [ ] 1.3 Add `RunExecutorFactoryTest`: virtual mode → task thread `isVirtual()`; platform mode → not virtual, daemon, name starts `run-worker-`; `shutdownNow()` interrupts a sleeping task in both modes; two calls return distinct executors. Done: `./gradlew :evaluation-runner-core:test --tests "*RunExecutorFactoryTest"` green.
- [ ] 1.4 Add an autoconfiguration test (ApplicationContextRunner or existing context test) asserting the bean is present and its mode follows `spring.threads.virtual.enabled=true|false`, and that a user-defined bean wins. Done: test green.

## 2. eval-cli adaptation (Batch A)

- [ ] 2.1 `EvaluationContextFactory`: inject `RunExecutorFactory`, use `newWorkerExecutor()`; remove `Context`/`Executors` imports; drop the `io.opentelemetry:opentelemetry-context` dependency, the `opentelemetry-instrumentation-bom` platform line and their comment block from `eval-cli/build.gradle` (`EvaluationContextFactory` was the only user). Done: `./gradlew :eval-cli:compileJava` green.
- [ ] 2.2 Adapt `EvaluationContextFactoryTest` (real `RunExecutorFactory(true)`), keep the "distinct executor per create" assertion. Done: `./gradlew :eval-cli:test` green.
- [ ] 2.3 `eval-cli/src/main/resources/application.yml`: `spring.threads.virtual.enabled: ${VIRTUAL_THREADS_ENABLED:true}`; document the property (the README's 5-column `Property | Environment Variable | Default | Required | Description` table) in `eval-cli/README.md` Configuration section with the profiling rationale. Done: yml + README updated.

## 3. Root: job executor, RunHandle, registry (Batch B)

- [ ] 3.1 `RunHandle`: ctor takes `ExecutorService`, drop no-arg ctor; update Javadoc. `ActiveRunRegistry`: inject `RunExecutorFactory`, `register` uses `factory.newWorkerExecutor()`, add `activeCount()` (design D3/D5). `TestSuiteEvaluationJob`: move `registry.remove(runId)` to the last statement of `run`'s `finally` (after `notifySse`); widen `dispatch`'s compensation catch to `RuntimeException | Error` (design D4/D5). Done: compiles; unit test asserts remove-after-notify ordering (`InOrder`) and Error cleanup.
- [ ] 3.2 `AsyncConfiguration.testSuiteRunExecutor`: `SimpleAsyncTaskExecutor("test-suite-run-")`, `setVirtualThreads(factory.isVirtualThreads())`, `setTaskDecorator(new ContextPropagatingTaskDecorator())`, `setCancelRemainingTasksOnClose(true)`, `setDaemon(true)`, no concurrency limit, return type `AsyncTaskExecutor` (design D4). Update the interrupt comment in `TestSuiteEvaluationJob.run` catch blocks to reference `SimpleAsyncTaskExecutor.close()`. Done: compiles; `grep ThreadPoolTaskExecutor src/main` empty.
- [ ] 3.3 Remove `TestSuiteRunProperties.Executor`, the `test-suite-run.executor` block in `src/main/resources/application.yml`, and any test property files referencing it. Done: `grep -r "core-pool-size\|queue-capacity" src` empty (except tests added in 3.5 if any).
- [ ] 3.4 `FunctionalTests`: replace `ThreadPoolTaskExecutor` drain with polling `ActiveRunRegistry.activeCount() == 0` (same timeout/log). Done: compiles.
- [ ] 3.5 Tests: new `AsyncConfigurationTest` (virtual factory → task on virtual thread; platform factory → platform thread named `test-suite-run-*`; decorator applied). Adapt `RunHandleTest`, `ActiveRunRegistryTest` (real `RunExecutorFactory(true)`; `activeCount` after register/remove), `TestSuiteEvaluationJobTest` (the one `new RunHandle()` site; add the ordering/Error tests from 3.1). Done: `./gradlew test --tests "com.epam.aidial.evaluation.service.domain.job.*" --tests "*AsyncConfigurationTest"` green.
- [ ] 3.6 Functional: `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$TestSuiteRunTests" --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$EvaluationExecutorFailureModesTests"` green (default virtual mode; context boots with the new bean graph).

## 4. Docs and conventions

- [ ] 4.1 `docs/configuration.md`: delete §6.1 executor rows (renumber or retitle section), add `spring.threads.virtual.enabled | VIRTUAL_THREADS_ENABLED | true | No | - | …profiling…` under §2 Spring Framework Configuration; fix Table of Contents. Done: rows present/absent as stated.
- [ ] 4.2 `docs/patterns/run-cancellation.md`: `RunHandle` receives its executor from `RunExecutorFactory`; thread mode section (virtual default, `VIRTUAL_THREADS_ENABLED=false` for profiling, identical cancellation semantics). `docs/key-packages.md`: runner-core `job` package lists `RunExecutorFactory`. Done: docs updated.
- [ ] 4.3 Update AGENTS.md per AGENTS.md Maintenance guidelines (done: Debugging Tips gains `VIRTUAL_THREADS_ENABLED=false` profiling switch; Run cancellation pattern row mentions thread mode).
- [ ] 4.4 Update openspec/config.yaml per Config Maintenance Policy (done: Key Patterns run-cancellation bullet AND the Glossary "run handle" entry no longer say "virtual-thread executor" — both say "thread-per-task executor from `RunExecutorFactory` (virtual by default, `spring.threads.virtual.enabled=false` → platform)").
- [ ] 4.5 Update openspec/specs/README.md per Spec Index Maintenance Policy (done: eval-execution-engine / test-suite-runs summaries no longer say "virtual thread executor" unconditionally; runner-core summary mentions `RunExecutorFactory`). Hand-fix `metric-evaluation` Implementation Notes and `test-suite-runs` Implementation Notes (executor wording) after `/opsx:sync`.

## 5. Verification

- [ ] 5.1 `./gradlew spotlessApply checkstyleMain checkstyleTest` then `./gradlew clean build` green; grep `core-pool-size|max-pool-size|queue-capacity|newVirtualThreadPerTaskExecutor|ThreadPoolTaskExecutor` across `src/main evaluation-runner-core/src/main eval-cli/src/main docs` → only the `DeploymentService` fan-out remains (test sources may keep `newVirtualThreadPerTaskExecutor()` for constructing `RunHandle`/contexts).
- [ ] 5.2 Manual smoke: start app with `VIRTUAL_THREADS_ENABLED=false`, create a run, `jcmd <pid> Thread.print` shows `test-suite-run-*` and `run-worker-*` platform threads; with `true` neither appears; cancel mid-flight yields CANCELLING → CANCELLED in both modes. Done: observed (or documented as skipped with reason).
