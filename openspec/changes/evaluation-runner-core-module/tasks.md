## 1. Gradle multi-module build setup

- [x] 1.1 Update `settings.gradle` to declare `evaluation-runner-core` as a subproject; verify `./gradlew projects` lists both subprojects
- [x] 1.2 Create `evaluation-runner-core/build.gradle` with the correct dependency set: Spring Boot BOM platform, `spring-boot-starter`, MCP SDK BOM + `mcp-core` + `mcp-json-jackson3`, Bucket4j, Lombok + MapStruct + annotation processors, OpenTelemetry BOM + `opentelemetry-spring-boot-starter`, `commons-lang3`, `commons-text`, `commons-collections4`, `dashjoin:jsonata`, `hibernate-validator`, `networknt:json-schema-validator`; no JDBC/jOOQ/Flyway/springdoc
- [x] 1.3 Add Spotless + palantir-java-format and Checkstyle configuration to `evaluation-runner-core/build.gradle` (point to shared config files)
- [x] 1.4 Create `evaluation-runner-core/src/main/java/` and `evaluation-runner-core/src/test/java/` directory structure
- [x] 1.5 Add `implementation project(':evaluation-runner-core')` to the root (EF backend) module's `build.gradle`; verify `./gradlew :evaluation-runner-core:build` succeeds on an empty module

## 2. Move `LogExecution` annotation to shared module

- [x] 2.1 Move `LogExecution` annotation from `com.epam.aidial.evaluation.configuration.logging` to `com.epam.aidial.evaluation.runner.config.logging.LogExecution` in the shared module
- [x] 2.2 Move `LogExecutionAspect` (the AOP aspect that processes `@LogExecution`) and its configuration to the shared module
- [x] 2.3 Update all `@LogExecution` import sites in the EF backend to use the new shared-module package; verify EF backend compiles

## 3. Move config properties to shared module

- [x] 3.1 Move `EvaluationRunProperties` from `configuration.properties.testsuite` to `com.epam.aidial.evaluation.runner.config.properties.EvaluationRunProperties`; update all import sites in the EF backend
- [x] 3.2 Move `SseEventProcessingProperties` to `com.epam.aidial.evaluation.runner.config.properties.SseEventProcessingProperties`; update all import sites
- [x] 3.3 Move `DialCoreProperties` from `configuration.properties.dial` to `com.epam.aidial.evaluation.runner.config.properties.DialCoreProperties`; update `DialCoreClientConfiguration` and `TryItOutService` in the EF backend to import from shared module
- [x] 3.4 Move `McpClientProperties` to `com.epam.aidial.evaluation.runner.config.properties.McpClientProperties`; update all import sites
- [x] 3.5 Move `DialFileStorageProperties` to `com.epam.aidial.evaluation.runner.config.properties.DialFileStorageProperties`; update all import sites
- [x] 3.6 Verify `./gradlew :build` (EF backend) compiles after properties moves

## 4. Move domain model to shared module

- [x] 4.1 Move `TestCaseRunResult` from `data.db.analytics.model` to `com.epam.aidial.evaluation.runner.model.TestCaseRunResult`; keep the class as a pure Lombok POJO with no persistence annotations
- [x] 4.2 Update `TestCaseRunResultRecordMapper` in the EF backend to import `TestCaseRunResult` from the shared module
- [x] 4.3 Update all other EF backend classes that import `TestCaseRunResult` (e.g., `ResultBatchWriter`, `ResultBatchWriterTransactional`, `TestCaseRunResultRepository`, `TestCaseRunResultFactory`, `MetricEvaluationWorker`, `InProcessEvaluationExecutor`, analytics DTOs/mappers)
- [x] 4.4 Move `EvaluationContext` from `service.domain.job` to `com.epam.aidial.evaluation.runner.job.EvaluationContext`; update all import sites in the EF backend
- [x] 4.5 Move `TurnOutcome` to `com.epam.aidial.evaluation.runner.job.TurnOutcome`; update all import sites

## 5. Move utilities to shared module

- [x] 5.1 Move `QuietJsonService` to `com.epam.aidial.evaluation.runner.util.QuietJsonService`; update all import sites
- [x] 5.2 Move `JsonbMapper` from `service.domain.mapper` to `com.epam.aidial.evaluation.runner.util.JsonbMapper`; update all import sites in both modules — **superseded by 5.10-5.14 below**: the whole-class move over-scoped the shared module with Phase-3/CRUD-only types (see Decision 10 in `design.md`); split instead
- [x] 5.3 Move `ValidationWarningsSerializer` to `com.epam.aidial.evaluation.runner.util.ValidationWarningsSerializer`; update all import sites in both modules
- [x] 5.4 Move `EvalBaggage` from `utils` to `com.epam.aidial.evaluation.runner.util.EvalBaggage`; update all import sites
- [x] 5.5 Move `TokenPropagationHelper` from `configuration.security` to `com.epam.aidial.evaluation.runner.util.TokenPropagationHelper`; update `InProcessEvaluationExecutor` in the EF backend
- [x] 5.6 Move `TemplateVariableResolver` from `service.domain` to `com.epam.aidial.evaluation.runner.service.TemplateVariableResolver`; update all import sites — done as a prerequisite for 9.1 (`RequestResolver` depends on it)
- [x] 5.7 Move `DialCoreUrlBuilder` from `service.domain` to `com.epam.aidial.evaluation.runner.service.DialCoreUrlBuilder`; update all import sites
- [x] 5.8 Move `JsonataEvaluationService` (interface) and `DashjoinJsonataEvaluationService` (impl) from `service.domain` to `com.epam.aidial.evaluation.runner.service`; update `ConditionExpressionEvaluator` and any other EF backend class that imports them
- [x] 5.9 Verify `./gradlew :build` compiles after utilities moves

### 5b. Split `JsonbMapper` (correction to 5.2 — see Decision 10 in `design.md`)

- [x] 5.10 Trim `com.epam.aidial.evaluation.runner.util.JsonbMapper` (shared module) down to exactly `mapRequestTemplate(String) → RequestTemplateDto` and `mapInputBindings(String) → List<InputBindingDto>` (read direction only), plus the private `read`/`readList` helpers each needs; delete `extractDeploymentId`/`extractHttpMethod` (dead code, no callers) and every other method (`map(DeploymentReferenceDto/EndpointContractDto/RequestTemplateDto)` write direction, `mapInputBindings` write direction, `mapFieldDefinitions`, `mapResponseColumns`, `mapMetricBindings`, `mapMcpDeploymentRef`, `mapToolRef`, `mapArgumentTemplate`, `mapEndpointContract`, `mapJsonSchema`, `mapOverallScore`, `mapTestCaseFilter`)
- [x] 5.11 Create `com.epam.aidial.evaluation.service.domain.mapper.JsonbMapper` in the EF backend carrying every method removed in 5.10; inject the shared module's `JsonbMapper` and delegate to it for `mapRequestTemplate`/`mapInputBindings` so there is one source of truth for those two
- [x] 5.12 Move `FieldDefinitionDto`, `MetricParameterBindingDto`, and the `overallscore` package (`OverallScoreDefinition`, `Mean`, `WeightedMean`, `WeightedMetric`, `CustomFunction`) back from `runner.dto`/`runner.dto.overallscore` to `service.domain.dto`/`service.domain.dto.overallscore` in the EF backend; update all import sites in both modules (they are referenced only by the EF-backend-only `JsonbMapper` methods moved in 5.11). Also moved `MetricBindingSourceDto` + its 3 sealed variants (`ConstantBindingSourceDto`, `TestCaseBindingSourceDto`, `ResponseBindingSourceDto`) back — discovered during implementation that they're referenced only by `MetricParameterBindingDto`'s `source` field, same transitive-closure issue.
- [x] 5.13 Update every caller of the removed methods (`TestSuiteMapper`, `DatasetMapper`, `TestSuiteMetricDefinitionMapper`, `MetricDeclarationVersionMapper`, `RunMetricSnapshotMapper`, `SuiteSnapshotBuilder`, `SuiteValidationService`, `TemplateVariableService`, `TryItOutService`, `DatasetSchemaProvider`, `DatasetService`, `RevalidationService`, `TestSuiteService`, `TestSuiteMetricDefinitionService`) to inject the EF-backend `JsonbMapper` from 5.11 instead of the shared module's. `EvaluationWorker`/`MultiTurnExecutor` correctly kept injecting the shared module's `JsonbMapper` (execution path).
- [x] 5.14 Verify `./gradlew clean build` compiles, Spotless/Checkstyle pass, and the full test suite (`./gradlew :test :evaluation-runner-core:test`) is green after the split — found and fixed a `ConflictingBeanDefinitionException`: both `JsonbMapper` classes shared a simple name, colliding on Spring's default bean name; gave the shared module's bean an explicit name (`"runnerJsonbMapper"`)

## 6. Move SSE, streaming, and invocation support to shared module

- [x] 6.1 Move `SseEvent`, `SseParseResult`, `SseEventParser` from `service.domain.job` to `com.epam.aidial.evaluation.runner.job`; update all import sites
- [x] 6.2 Move `StreamingResponseAccumulator` to `com.epam.aidial.evaluation.runner.job.StreamingResponseAccumulator`; update all import sites
- [x] 6.3 Move `DeploymentInvocationSupport` to `com.epam.aidial.evaluation.runner.job.DeploymentInvocationSupport`; update all import sites
- [x] 6.4 Move `AdvancingClock` test helper from the EF backend's test sources to the shared module's `src/test/java`. Also moved `SseEventParserTest`, `DeploymentInvocationSupportTest`, `StreamingResponseAccumulatorTest` (originally group 14 tasks 14.2-14.4) as required fallout — they depend on `AdvancingClock` and the classes moved in 6.1-6.3, so leaving them behind would have broken the build. `EvaluationWorkerTest` (14.1) was left in place since `EvaluationWorker` itself hasn't moved yet (group 10).

## 7. Move DIAL Core invocation client to shared module

- [x] 7.1 Move `DialCoreDeploymentInvoker` from `client.dialcore` to `com.epam.aidial.evaluation.runner.client.dialcore.DialCoreDeploymentInvoker`; update all import sites. Also moved `DialCoreClientException` (needed by the invoker's own exception mapping, and by `DialFileClient`) since it was a plain, dependency-free type reachable only by classes now in the shared module or by EF-backend classes that will import it from there — same transitive-closure pattern as Decision 10's DTOs.
- [x] 7.2 Move `DialCoreDeploymentInvokerConfiguration` to the shared module; resolve the `dialCoreTryOutRestClient` bean placement (keep in shared module or split to EF backend — document final decision in a comment) — **decision: stays in EF backend.** It composes `DialCoreClientConfiguration`'s interceptor factories (also EF-backend-only), so moving it would create a shared-module → EF-backend compile dependency. `DialCoreDeploymentInvoker` consumes the bean by `@Qualifier` name only (a runtime lookup, not a compile-time import), so the split has no wiring cost — documented in a class-level comment on both `DialCoreDeploymentInvokerConfiguration` and `DialCoreDeploymentInvoker`. Same reasoning applied to `DialFileClientConfiguration` (7.5).
- [x] 7.3 Move `DeploymentInvocationResult` and `DeploymentInvocationResponse` records to the shared module's `runner.client.dialcore` package; update all import sites
- [x] 7.4 Move `DialCoreErrorMapper` and `DialCoreErrorCode` to the shared module; update all import sites
- [x] 7.5 Move `DialFileClient` and `DialFileRefResolver` to `com.epam.aidial.evaluation.runner.client.dialcore`; update all import sites in both modules (including `DatasetService`, `TestSuiteService` in the EF backend that use `DialFileClient` for upload/download). Also moved `DialFileMetadataDto`, `DialFileFolderResponseDto`, `DialBucketResponseDto` (DTOs reachable only via the moved `DialFileClient`) to `runner.client.dialcore.dto`. `DialFileClientConfiguration` (the `dialFileRestClient` bean) stays in EF backend — same qualifier-name-only wiring reasoning as 7.2, documented in a class-level comment.
- [x] 7.6 Verify `./gradlew :build` compiles after client moves — also moved the dependent unit tests (`DialCoreDeploymentInvokerTest`, `DialCoreDeploymentInvokerEncodingTest`, `DialFileClientTest`, `DialFileRefResolverTest`, `DialCoreErrorMapperTest`) to the shared module's test sources, since their production classes moved wholesale; fixed a stale `service.domain.exception.ValidationException` import in the moved `DialFileRefResolverTest` (same class of bug as Decision 10's fixes).

## 8. Move MCP client to shared module

- [x] 8.1 Move `McpToolInvoker`, `McpClientConfiguration`, `McpInvocationException`, `McpTransport` from `client.mcp` to `com.epam.aidial.evaluation.runner.client.mcp`; update all import sites
- [x] 8.2 Move `McpRequestResolver` and `McpResponseSerializer` from `service.domain` to `com.epam.aidial.evaluation.runner.service`; update all import sites
- [x] 8.3 Verify `./gradlew :build` compiles after MCP moves — also moved `McpRequestResolverTest`/`McpResponseSerializerTest` to the shared module's test sources (their production classes moved wholesale)

## 9. Move request/response pipeline to shared module

- [x] 9.1 Extract the `resolve(RequestTemplateDto, List<InputBindingDto>, Map<String,Object>)` method from `ResolvedRequestService` into a new `@Component RequestResolver` in `com.epam.aidial.evaluation.runner.service.RequestResolver`. Also completed 5.6 (`TemplateVariableResolver` move) as a prerequisite — `RequestResolver` depends on it — and moved the `Resolved*Dto` family (`ResolvedBodyDto`, `ResolvedFormPartDto`, `ResolvedJsonBodyDto`, `ResolvedMultipartBodyDto`, `ResolvedRequestDto`, `ResolvedUrlEncodedBodyDto`) to `runner.dto`, since they are `RequestResolver`'s (and thus the execution path's) return type — same transitive-closure pattern as Decision 10.
- [x] 9.2 Update `ResolvedRequestService` in the EF backend to inject `RequestResolver` and delegate to it in the `resolveRequest(UUID, UUID)` overload; remove the duplicated resolution logic from `ResolvedRequestService`. `EvaluationWorker`/`MultiTurnExecutor`/`TryItOutService` (which called the removed `resolve(...)` overload directly) now inject `RequestResolver` instead, per Decision 3 in `design.md`.
- [x] 9.3 Move `RequestBodySerializer` (interface) and all three implementations (`JsonRequestBodySerializer`, `MultipartFormDataRequestBodySerializer`, `UrlEncodedFormRequestBodySerializer`) to `com.epam.aidial.evaluation.runner.service`; update all import sites. Also moved `SerializedBody` (their shared return type).
- [x] 9.4 Move `RequestBodySerializerRegistry` to `com.epam.aidial.evaluation.runner.service.RequestBodySerializerRegistry`; update all import sites
- [x] 9.5 Move `ResponseColumnExtractor` and `ResponseColumnTypeReconciler` to `com.epam.aidial.evaluation.runner.service`; update all import sites in both modules. Also moved `TypeMismatchException` (used only by `ResponseColumnTypeReconciler`) to `runner.exception`. Moved all dependent unit tests to the shared module's test sources; rewrote `ResolvedRequestServiceTest` (EF backend) to cover only the DB-backed lookup/delegation, since its resolution-logic assertions moved with `RequestResolver` into the new `RequestResolverTest`.

## 10. Move core execution workers to shared module

- [x] 10.1 Move `EvaluationWorker` from `service.domain.job` to `com.epam.aidial.evaluation.runner.job.EvaluationWorker`; update injection of `RequestResolver` (replacing the old `ResolvedRequestService` injection — already done in 9.2); update all import sites in the EF backend. Also moved `TestCaseRunInput` (`data.db.model` → `runner.model`) — the execution path's input counterpart to `TestCaseRunResult` (Decision 2), a pure POJO used both by `EvaluationWorker`/`MultiTurnExecutor` and the EF backend's repository/mapper layer.
- [x] 10.2 Move `MultiTurnExecutor` to `com.epam.aidial.evaluation.runner.job.MultiTurnExecutor`; update all import sites
- [x] 10.3 Move `DeploymentTurnInvoker` to `com.epam.aidial.evaluation.runner.job.DeploymentTurnInvoker`; update all import sites
- [x] 10.4 Verify `./gradlew :build` compiles after worker moves — also moved `EvaluationWorkerTest` (task 14.1) to the shared module's test sources since its production class moved wholesale

## 11. Autoconfiguration and wiring

- [x] 11.1 Write `EvaluationRunnerAutoConfiguration` `@Configuration` class in `com.epam.aidial.evaluation.runner.config`; use `@ComponentScan("com.epam.aidial.evaluation.runner")` or explicit `@Bean` declarations for all shared beans — used `@ComponentScan` (chosen over ~40+ manual `@Bean` methods given every shared class is already a proper `@Component`/`@Service` with `@LogExecution`)
- [x] 11.2 Create `evaluation-runner-core/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` with entry `com.epam.aidial.evaluation.runner.config.EvaluationRunnerAutoConfiguration`
- [x] 11.3 Enable `@ConfigurationProperties` scanning in the autoconfiguration class via `@EnableConfigurationProperties({EvaluationRunProperties.class, SseEventProcessingProperties.class, DialCoreProperties.class, McpClientProperties.class, DialFileStorageProperties.class})`
- [x] 11.4 Start the EF backend application locally (or via functional test) and confirm all shared beans are in the context; no `NoSuchBeanDefinitionException` at startup — removed the now-redundant `com.epam.aidial.evaluation.runner.config.properties` entry from `Application.java`'s `@ConfigurationPropertiesScan` (superseded by the autoconfiguration's `@EnableConfigurationProperties`) so `NoSecurityStartupTests`/`OidcStartupTests` actually exercise the autoconfiguration path, not the old explicit scan. Both pass.

## 12. EF backend wiring cleanup

- [x] 12.1 Remove any now-redundant `@Bean` declarations from EF backend configuration classes that duplicate beans now provided by `EvaluationRunnerAutoConfiguration` — audited every `@Configuration` class in the EF backend; none duplicate a shared-module bean (each moved class's config was deleted outright at move time, not left as an empty shell), so no removal needed
- [x] 12.2 Remove any now-empty configuration classes in the EF backend left over from moved classes — none found (same reason as 12.1)
- [x] 12.3 Update the EF backend's `LayeredArchitectureTest` to account for moved packages (e.g., `runner.job` may now be imported from `service.domain.job` callers; adjust allow-list if needed) — no change needed: none of the test's defined layer packages (`web`/`service`/`data`/`configuration`/`experimentalWeb`/`experimentalService`) include a `runner` segment, so ArchUnit's `layeredArchitecture()` treats `com.epam.aidial.evaluation.runner.*` as unclassified and does not constrain it; the shared module's own constraints are enforced separately by `RunnerModuleConstraintsTest` (group 13). Verified the test still passes.
- [x] 12.4 Verify no compile warnings or unused import errors remain in the EF backend after all moves — both modules already build under `-Werror` (enabled in both `build.gradle` files), so a clean compile already proves this

## 13. ArchUnit constraints for shared module

- [x] 13.1 Write `RunnerModuleConstraintsTest` in the shared module's `src/test/java` under `com.epam.aidial.evaluation.runner`; enforce: (a) no JDBC/jOOQ/Flyway imports, (b) no imports from `com.epam.aidial.evaluation` (EF backend root), (c) `runner.client.*` does not depend on `runner.job`, (d) all `@Component`/`@Service`/`@Configuration` classes have `@LogExecution`
- [x] 13.2 Run `./gradlew :evaluation-runner-core:test --tests "*.RunnerModuleConstraintsTest"` and confirm it passes — first attempt used `importPackagesOf(EvaluationRunnerAutoConfiguration.class)`, which only imports that class's own package (`runner.config`), not the whole module; fixed with `importPackages("com.epam.aidial.evaluation.runner")`. All 4 rules pass.

## 14. Move and adapt unit tests

- [x] 14.1 Move `EvaluationWorkerTest` from the EF backend's test sources to `evaluation-runner-core/src/test/java`; update imports to new package paths; run `./gradlew :evaluation-runner-core:test --tests "*.EvaluationWorkerTest"` and confirm green — done as part of 10.4
- [x] 14.2 Move `SseEventParserTest` and `AdvancingClock` to the shared module's test sources; run and confirm green — done as part of 6.4
- [x] 14.3 Move `DeploymentInvocationSupportTest` to the shared module's test sources; run and confirm green — done as part of 6.4
- [x] 14.4 Move `StreamingResponseAccumulatorTest` to the shared module's test sources; run and confirm green — done as part of 6.4
- [x] 14.5 Run the full EF backend test suite (`./gradlew :test`) and confirm no regressions — ran `./gradlew clean build` (both modules); `BUILD SUCCESSFUL`, no regressions

## 15. Full build validation

- [x] 15.1 Run `./gradlew clean build` on the root; confirm both modules compile, all tests pass, Spotless passes, and Checkstyle passes — `BUILD SUCCESSFUL in 8m 19s`, 27/27 tasks executed, both modules' `:build`/`:check` green
- [x] 15.2 Confirm no ArchUnit violations in either module's test run — `LayeredArchitectureTest`/`JdbcTemplateFenceTest` (EF backend) and `RunnerModuleConstraintsTest` (shared module) all ran as part of the `:test`/`:evaluation-runner-core:test` tasks in the clean build above and passed

## 16. Documentation and metadata updates

- [x] 16.1 Update `AGENTS.md` — add `evaluation-runner-core` subproject to Quick Reference and Key Packages Reference; add `com.epam.aidial.evaluation.runner` package tree entries; note the `RequestResolver` extraction pattern and autoconfiguration wiring. Confirmed already present (Quick Reference row, inline convention bullet, subproject table).
- [x] 16.2 Update `openspec/specs/README.md` — add entry for the new `evaluation-runner-core-module` spec; update status of `eval-execution-engine` to reflect implementation home change. Confirmed already present.
- [x] 16.3 Update `openspec/config.yaml` — add new subproject to the build tooling section; note multi-module build pattern and `runner.*` root package convention. Confirmed already present.

## 17. Pluggable `ResultBatchWriter` output destination (see Decision 11 in `design.md`)

- [x] 17.1 Create `com.epam.aidial.evaluation.runner.job.TestCaseResultsOutputWriter` interface in the shared module: single method `void write(List<TestCaseRunResult> batch)`, with Javadoc explaining it is the DB-free sink contract for a flushed batch so a standalone runner can supply an alternate (e.g. in-memory) implementation
- [x] 17.2 Rename `service.domain.job.ResultBatchWriterTransactional` to `AnalyticsDbResultsOutputWriter`; implement `TestCaseResultsOutputWriter`; rename its `saveBatch` method to `write` (keep the existing `@Transactional("analyticsTransactionManager")` annotation and `TestCaseRunResultRepository` dependency unchanged)
- [x] 17.3 Update `ResultBatchWriter` (`service.domain.job`) to depend on `TestCaseResultsOutputWriter` instead of the concrete `ResultBatchWriterTransactional`/`AnalyticsDbResultsOutputWriter`; update `doFlush` to call `.write(batch)`
- [x] 17.4 Update `ResultBatchWriterTest.java`: change the mocked field from `ResultBatchWriterTransactional` to `TestCaseResultsOutputWriter`; update `verify(...)` calls from `saveBatch` to `write`
- [x] 17.5 Run `./gradlew :evaluation-runner-core:compileJava` and confirm `RunnerModuleConstraintsTest` still passes (new interface has no JDBC/jOOQ/Flyway import and no dependency back on the EF backend)
- [x] 17.6 Run `./gradlew compileJava compileTestJava spotlessApply checkstyleMain checkstyleTest` on the root module; fix any fallout from the rename — clean on first try, no fallout
- [x] 17.7 Run `./gradlew test --tests "com.epam.aidial.evaluation.service.domain.job.ResultBatchWriterTest"` and confirm green — 8/8 tests pass
- [x] 17.8 Run the suite-run-related functional test suite (`./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$*"`) to confirm a real run still persists results end-to-end through the new interface indirection (no `NoSuchBeanDefinitionException`/ambiguity at context startup) — ran the two nested classes that exercise `ResultBatchWriter` end-to-end (`TestSuiteRunTests`: 37/37 passed, `MultiTurnRunTests`: 5/5 passed, 0 failures/errors)
- [x] 17.9 Run a full `./gradlew clean build` and grep for any remaining references to `ResultBatchWriterTransactional`/`saveBatch` in the codebase — `BUILD SUCCESSFUL in 10m 9s`, 27/27 tasks executed; grep of `src/` for `ResultBatchWriterTransactional`/`.saveBatch(` returns no matches

## 18. Extract `TestCaseRunner` (see Decision 12 in `design.md`)

- [x] 18.1 Move `TestCaseRunResultFactory` from `service.domain.job` to `com.epam.aidial.evaluation.runner.job.TestCaseRunResultFactory` (shared module); add `@LogExecution`; update the import at its one caller (`InProcessEvaluationExecutor`) — already had `@LogExecution`; also moved `TestCaseRunResultFactoryTest` to the shared module's test sources (production class moved wholesale)
- [x] 18.2 Create `com.epam.aidial.evaluation.runner.job.TestCaseRunner` (shared module): `void run(List<TestCaseRunInput> testCases, EvaluationContext context, List<ResponseColumnDefinitionDto> responseColumns, Consumer<List<TestCaseRunResult>> resultsConsumer)`, lifting the `Semaphore`/virtual-thread-executor/Bucket4j-rate-limit/cancellation-grace-period/synthetic-error-row dispatch logic from `InProcessEvaluationExecutor` essentially verbatim (including the `createRateLimitBucket` helper). The outer generic `catch (Exception e)`/flush logic stayed in `InProcessEvaluationExecutor` (it owns `ResultBatchWriter`, which `TestCaseRunner` has no reference to) — only the `InterruptedException` catch (mark cancellation, don't rethrow) moved with the dispatch loop, since it's specific to `semaphore.acquire()`/rate-limiter blocking inside dispatch.
- [x] 18.3 Thin `InProcessEvaluationExecutor`: keep the DB-paging loop (`fetchPage`, repositories) and `ResultBatchWriter` buffer/flush lifecycle; replace the inline per-page dispatch loop with `testCaseRunner.run(page, context, responseColumns, results -> resultBatchWriter.addResults(buffer, results))` — removed now-unused `EvaluationWorker`/`TestCaseRunResultFactory`/`Clock` fields, the `createRateLimitBucket` helper (moved into `TestCaseRunner`), and the Bucket4j/OpenTelemetry/concurrency imports it no longer needs
- [x] 18.4 Create `TestCaseRunnerTest` in `evaluation-runner-core/src/test/java`, moving the concurrency/error-handling/cancellation tests from `InProcessEvaluationExecutorTest` (`execute_singleTestCase_callsWorkerAndFlushesBatch`, `execute_multipleTestCases_processesAll`, `execute_multipleRuns_callsWorkerPerRun`, `execute_cancellationBeforeDispatch_stopsEarly`, `execute_withRateLimiting_processesAllTestCases`, `execute_workerException_continuesWithOtherCases`, `shouldNotTimeoutOnLongRun_whenNoCancellation`, `shouldSynthesizeErrorRow_whenWorkerThrows`, `shouldNotSynthesizeRows_whenCancelledMidFlight`, `shouldNotRetry_whenSynthesisFails`), adapted to call `testCaseRunner.run(...)` directly with a mocked `Consumer<List<TestCaseRunResult>>`, all `flush(...)` assertions dropped (no buffer/flush concept in `TestCaseRunner`). Correction to the original task text: `shouldFlushExactlyOnce_atEnd` did NOT move here — it asserts `ResultBatchWriter.flush` is called exactly once, which is purely an `InProcessEvaluationExecutor`-level concern (kept in 18.5, adapted to mock `TestCaseRunner`).
- [x] 18.5 Update `InProcessEvaluationExecutorTest` to keep only the DB-paging tests (`execute_snapshotPath_pagesFromInputsTable`, `execute_legacyPath_pagesFromLiveTestCases`), mocking `TestCaseRunner` instead of `EvaluationWorker`/`TestCaseRunResultFactory`; re-check `shouldRethrow_whenDispatchLoopFails` — kept here, confirmed it tests `findByRunId` throwing before dispatch (pre-`TestCaseRunner.run()` failure), not a `TestCaseRunner`-level concern. Also kept `shouldFlushExactlyOnce_atEnd` (flush is `ResultBatchWriter`-owned, unrelated to `TestCaseRunner`) and added a new `execute_forwardsTestCaseRunnerResultsToBuffer` test verifying the `Consumer` passed to `testCaseRunner.run(...)` correctly forwards into `resultBatchWriter.addResults`
- [x] 18.6 Run `./gradlew :evaluation-runner-core:compileJava` and `./gradlew :evaluation-runner-core:test --tests "*.RunnerModuleConstraintsTest"`; confirm both `TestCaseRunResultFactory` and `TestCaseRunner` carry `@LogExecution` and pass the ArchUnit rules — found and fixed a compile error (`CompletableFuture.get()` throws `ExecutionException`, previously caught by the outer generic `catch (Exception e)` that didn't move with the dispatch loop); added a dedicated `catch (ExecutionException e)` rethrowing as `RuntimeException`. `RunnerModuleConstraintsTest`: 4/4 pass; `TestCaseRunnerTest`: 10/10 pass; `TestCaseRunResultFactoryTest`: 4/4 pass
- [x] 18.7 Run `./gradlew compileJava compileTestJava spotlessApply checkstyleMain checkstyleTest` on the root module; fix any fallout — clean, no fallout (Spotless reformatted the newly written files, Checkstyle passed)
- [x] 18.8 Run `./gradlew :evaluation-runner-core:test --tests "*.TestCaseRunnerTest"` and `./gradlew test --tests "com.epam.aidial.evaluation.service.domain.job.InProcessEvaluationExecutorTest"`; confirm both green — `TestCaseRunnerTest`: 10/10 pass; `InProcessEvaluationExecutorTest`: 5/5 pass
- [x] 18.9 Run the suite-run-related functional tests (`PostgresFunctionalTests$TestSuiteRunTests`, `$MultiTurnRunTests`) to confirm real runs still dispatch/complete correctly through `TestCaseRunner` — `TestSuiteRunTests`: 37/37 pass, `MultiTurnRunTests`: 5/5 pass, 0 failures/errors
- [x] 18.10 Run a full `./gradlew clean build` and grep for any remaining references to the old `TestCaseRunResultFactory` package or inline dispatch logic left in `InProcessEvaluationExecutor` — `BUILD SUCCESSFUL in 10m 48s`, 27/27 tasks executed; all 229 (root) + 53 (shared module) test result XMLs show `failures="0" errors="0"`; grep of `src/` for `createRateLimitBucket`/old-package `TestCaseRunResultFactory` references returns no matches

## 19. Replace `TestCaseResultsOutputWriter` seam with a full `ResultBatchWriter` interface (see Decision 13 in `design.md`)

- [x] 19.1 Create `com.epam.aidial.evaluation.runner.job.ResultBatchWriter` interface in the shared module: `void addResults(List<TestCaseRunResult> results)`, `void flush()` — no buffer/handle parameter, an instance is scoped to one run
- [x] 19.2 Update `com.epam.aidial.evaluation.runner.job.TestCaseRunner.run(...)`: replace the `Consumer<List<TestCaseRunResult>>` parameter with `ResultBatchWriter`; replace both `resultsConsumer.accept(...)` call sites with `resultsWriter.addResults(...)`
- [x] 19.3 Create `PostgresResultBatchWriterFactory` (`service.domain.job`, EF backend): explicit constructor injecting `TestCaseRunResultRepository`, `TestSuiteRunSseService`, and `@Qualifier("analyticsTransactionManager") PlatformTransactionManager`, building `this.analyticsTransactionTemplate = new TransactionTemplate(analyticsTxManager)` once (same pattern as `EvalSummaryExportService`); `createWriter(batchSize, runId, suiteId, totalCases)` returns a new `PostgresResultBatchWriter`
- [x] 19.4 Create `PostgresResultBatchWriter` (`service.domain.job`, plain class, NOT a Spring bean, package-private) implementing `ResultBatchWriter`: merge the old `RunBuffer`'s fields (buffer list, `ReentrantLock`, `totalFlushed`/`testCasesCompleted` counters, `batchSize`/`runId`/`suiteId`/`totalCases`) as instance fields; `flush()`'s persistence call uses `transactionTemplate.executeWithoutResult(status -> resultRepository.saveAll(batch))` instead of delegating to a separate bean; keep the SSE `notifyProgress(...)` call exactly as today; kept a package-visible `getTotalFlushed()` for test assertions
- [x] 19.5 Update `InProcessEvaluationExecutor`: inject `PostgresResultBatchWriterFactory` instead of the old `ResultBatchWriter`; create the writer via `resultBatchWriterFactory.createWriter(...)` at the top of `execute(context)`; pass it directly to `testCaseRunner.run(page, context, responseColumns, writer)` (no lambda); call `writer.flush()` in `catch`/`finally` (no buffer param)
- [x] 19.6 Delete `com.epam.aidial.evaluation.runner.job.TestCaseResultsOutputWriter` (shared module), `service.domain.job.AnalyticsDbResultsOutputWriter` (EF backend), and the old `service.domain.job.ResultBatchWriter` concrete class + its `RunBuffer` nested class
- [x] 19.7 Update `TestCaseRunnerTest` (shared module): mock `ResultBatchWriter` instead of `Consumer`; update all `verify(...)` calls from `.accept(...)` to `.addResults(...)`
- [x] 19.8 Update `InProcessEvaluationExecutorTest`: mock `PostgresResultBatchWriterFactory` + a mock `ResultBatchWriter` it returns; replaced `execute_forwardsTestCaseRunnerResultsToBuffer` (moot now — no consumer indirection to forward through) with `execute_passesFactoryCreatedWriterToTestCaseRunner`, verifying `testCaseRunner.run(..., writer)` receives the exact writer instance returned by the factory
- [x] 19.9 Create `PostgresResultBatchWriterTest` (EF backend, replacing `ResultBatchWriterTest`, which is deleted) covering the merged buffering+transactional-write+SSE logic: mock `TestCaseRunResultRepository`/`TestSuiteRunSseService`, build a real `TransactionTemplate` from a Mockito-mocked `PlatformTransactionManager` (`getTransaction(any())` returns a mocked `TransactionStatus`) that just invokes the callback; ported all 8 scenarios from the old `ResultBatchWriterTest` (threshold flush, multiple flushes, explicit flush, empty flush, SSE failure swallowed, batch content correctness) to construct `PostgresResultBatchWriter` directly instead of via `createBuffer`
- [x] 19.10 Run `./gradlew :evaluation-runner-core:compileJava` and `./gradlew :evaluation-runner-core:test --tests "*.RunnerModuleConstraintsTest"`; confirm the new interface has no JDBC/jOOQ/Flyway import and no EF-backend dependency — found one compile error (missing `TestSuiteRunSseService` import in the new `PostgresResultBatchWriterTest`), fixed. `RunnerModuleConstraintsTest`: 4/4 pass
- [x] 19.11 Run `./gradlew compileJava compileTestJava spotlessApply checkstyleMain checkstyleTest` on the root module; fix any fallout — Spotless reformatted (import ordering), Checkstyle passed clean
- [x] 19.12 Run `./gradlew :evaluation-runner-core:test --tests "*.TestCaseRunnerTest"`, `./gradlew test --tests "com.epam.aidial.evaluation.service.domain.job.InProcessEvaluationExecutorTest"`, and `./gradlew test --tests "com.epam.aidial.evaluation.service.domain.job.PostgresResultBatchWriterTest"`; confirm all green — found and fixed a strict-stubbing failure (`@BeforeEach`'s `txManager.getTransaction(any())` stub was unused by the two tests that never flush); made it `lenient()`. `TestCaseRunnerTest`: 10/10, `InProcessEvaluationExecutorTest`: 5/5, `PostgresResultBatchWriterTest`: 8/8 — all pass
- [x] 19.13 Run the suite-run-related functional tests (`PostgresFunctionalTests$TestSuiteRunTests`, `$MultiTurnRunTests`) to confirm real DB-backed runs still persist results and emit SSE progress correctly through `PostgresResultBatchWriter` — `TestSuiteRunTests`: 37/37 pass, `MultiTurnRunTests`: 5/5 pass, 0 failures/errors (same counts as before this change)
- [x] 19.14 Run a full `./gradlew clean build` and grep for any remaining references to `TestCaseResultsOutputWriter`, `AnalyticsDbResultsOutputWriter`, `RunBuffer`, or the old `Consumer<List<TestCaseRunResult>>` parameter — found and fixed one Spotless failure (unused `Mockito.when` static import after switching to `lenient().when(...)`) on retry; `BUILD SUCCESSFUL in 10m 20s`, 27/27 tasks executed; all 229 (root) + 53 (shared module) test result XMLs show `failures="0" errors="0"`; grep of `src/` for the removed types/parameter returns no matches
