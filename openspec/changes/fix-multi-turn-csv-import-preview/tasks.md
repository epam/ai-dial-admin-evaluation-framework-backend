## 1. Schema carry-forward (RC1)

- [x] 1.1 Add `ParsedCsvRow` and `ColumnBinding` carriers in `service/domain/csv/` — promote `CsvImportService`'s private `ParsedRow` (rowNumber, testCaseName, turnIndex, data, hasJsonParseErrors) and `ColumnBinding` (headerName, mappedTo, fieldName) records as **public** records (the service lives in `service.domain`, so package-private would not be visible to it); update `CsvImportService` to use them. Done: compiles, no behavior change, `./gradlew :test --tests "*CsvImportServiceSchemaTest"` green.
- [x] 1.2 Add `CsvSchemaFieldBuilder` `@Component` (`@LogExecution`) in `service/domain/csv/` per design D1: build field lists from column bindings (types unknown or inferred) and the MERGE delta, copying `perTurn` by field name from the dataset's current schema, emitting new `FieldDefinitionDto` instances only. Done: class exists, injectable, no mutation of inputs; `CsvImportServiceSchemaTest`'s positional construction updated.
- [x] 1.3 Unit-test `CsvSchemaFieldBuilder`: `perTurn` carried on rebuild and on merge-delta; unknown column gets `perTurn` absent; empty/null current schema handled; input schema instances are neither mutated nor returned among the builder's emitted fields (the MERGE consumers legitimately keep the dataset's own instances for untouched fields — that is merge semantics, not a builder concern). Done: `./gradlew :test --tests "*CsvSchemaFieldBuilderTest"` green.
- [x] 1.4 Route all six `FieldDefinitionDto` construction sites through `CsvSchemaFieldBuilder` (design D1 table): delete `buildSchemaFromBindings`, `buildSchemaFromInferred`, `mergeSchemaWithBindings` and `applyScopeFromDataset`, and replace the inline new-field construction in the MERGE branches of `persistSchema`, `buildFinalSchema` and `buildAutoDetectedSchema`. Done: `grep -c "FieldDefinitionDto.builder()" CsvImportService.java` returns 0.
- [x] 1.5 Update `CsvImportServiceSchemaTest`'s positional construction of `CsvImportService` for each new constructor argument as it is added across tasks 1.2 / 2.1 / 2.3 / 5.2 (10 args today — `TestCaseFieldScopeResolver` was removed as a direct `CsvImportService` dependency in task 2.3, its only call site having moved into `MultiTurnRunAssembler`; 14 when all four components are wired), and extend it to cover every schema-building path (validation, persisted, final/fixup, auto-detected) preserving `perTurn` (thread the new constructor arg through its positional construction). Done: `./gradlew :test --tests "*CsvImportServiceSchemaTest"` green.
- [x] 1.6 Functional test: OVERRIDE import into a dataset with a `perTurn: true` field leaves the persisted `testCaseSchema` still marking it `perTurn: true`; and preview's `autoDetectedSchema` reports it. Add to `MultiTurnCsvFunctionalTests` (add the missing `previewCsv` helper). Done: `./gradlew :test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$MultiTurnCsvTests"` green.
- [x] 1.7 Functional test matching the delta scenario, with a **single** multi-turn case as the fixture (export paginates with `createdAt DESC` and no tiebreaker, and every imported row shares one transaction timestamp, so multi-case row order is unspecified): export → import (OVERRIDE) → **export again and assert the second CSV equals the first** → import again → assert turns and per-turn values are identical after both imports, and that `perTurn` survives on the schema (the issue #120 repro). Assert on `perTurn`, turn count and per-turn values only — not whole-`FieldDefinitionDto` equality, since `required` is deliberately still dropped. Done: same command green.
- [x] 1.8 Functional test: the same double round trip under `importMode=APPEND` and `importMode=MERGE` with `conflictStrategy=OVERRIDE` — the other modes the repeatability requirement covers, asserted on the same fields as 1.7. Done: `MultiTurnCsvTests` green.

## 2. Shared run grouping and multi-turn assembly (RC2 foundation)

- [x] 2.1 Add `CsvTestCase` carrier + `CsvTestCaseGrouper` `@Component` (`@LogExecution`) in `service/domain/csv/` per design D2: stateless component exposing a per-call accumulator with `add(row) → completed run or null` and `flush()`; `CsvTestCase` carries rows, case name, first row number, the multi-turn flag, and the non-contiguity flag; non-contiguity tracked for multi-turn runs only. Done: class exists, no per-request state on the bean; `CsvImportServiceSchemaTest`'s positional construction updated.
- [x] 2.2 Unit-test `CsvTestCaseGrouper`: run boundaries on name change; single-turn passthrough; run boundaries compare names with case-sensitive `equals`, exactly as `importCsv` does today (this asymmetry with case-insensitive duplicate detection is load-bearing: it is why `DupRow`/`duprow` still produces a duplicate warning); multi-turn detected when any row's `turnIndex` parses to a non-null Integer, and **not** when the cell is present but unparseable (e.g. `abc` stays single-turn, matching `parseTurnIndex`); `flush()` returns the trailing run; non-contiguity flagged for multi-turn names only (never for repeated blank-`turnIndex` names). Done: `./gradlew :test --tests "*CsvRunGrouperTest"` green.
- [x] 2.3 Add `MultiTurnRunAssembler` `@Component` (`@LogExecution`) per design D4: turn ordering by `turnIndex` (nulls last, stable by row order), scope partitioning via `TestCaseFieldScopeResolver`, and conflicts returned as data (shared-column mismatch, duplicate `turnIndex`). Absorbs `orderTurns`, `assembleMultiTurn`, `hasDuplicateTurnIndex`. Done: class exists, pure function, no repository or validation-service dependency; `CsvImportServiceSchemaTest`'s positional construction updated.
- [x] 2.4 Unit-test `MultiTurnRunAssembler`: ordering, shared/per-turn split honouring `perTurn`, shared-column mismatch detected, duplicate `turnIndex` detected, unknown column routed to shared. Done: `./gradlew :test --tests "*MultiTurnRunAssemblerTest"` green.
- [x] 2.5 Rewire `importCsv`/`processRun` onto the grouper + assembler with no behavior change; keep the conflict→`ValidationResult` merge in the service. Done: `./gradlew :test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$MultiTurnCsvTests"`, `...\$CsvImportModeTests`, and the preview assertions in `TestCaseFunctionalTests` all green **unmodified**.
- [x] 2.6 Functional test (import side of the run ≠ test case rule): a multi-turn run does not collide with itself — `conflictStrategy=FAIL` returns 200 not 409, `SKIP` leaves `skippedCount` null/0, `OVERRIDE` leaves `overriddenCount` null/0, and exactly one case is persisted. Done: `MultiTurnCsvTests` green.

## 3. Turn-aware preview (RC2)

- [x] 3.1 Add `totalTestCases` to `CsvImportPreviewDto`. Done: field present, populated below, OpenAPI `@Schema` description added.
- [x] 3.2 Rewrite `preview`'s row loop onto the accumulator: per completed run, assemble, count 1 (multi-turn) or K (single-turn) test cases, register 1 or K name occurrences for duplicate detection, validate the run the way import does (`validateMultiTurn` for a multi-turn run, per-row `validateTestCase` for a single-turn run) merged with the assembler's conflicts, and emit the merged warnings anchored to the run's first row number. Done: no additional per-row buffering beyond the name/row-number list preview already retains.
- [x] 3.3 Make `sampleRows` assembled cases: `toResponseDto` populates `multiTurnData` and carries the run's validation result as `valid`/`validationWarnings`; single-turn samples unchanged. Note `TestCaseResponseDto.valid` is a primitive `boolean`, so a sample built without a validation result would silently report `valid=false`. Done: multi-turn preview sample carries its turn array and reports the validity import would produce.
- [x] 3.4 Refactor `addCollisionWarnings` to consume `(caseName, firstRowNumber)` pairs and drop the dead `seenInCsv` parameter; no behavior change. Done: existing APPEND/MERGE collision tests green unchanged.
- [x] 3.5 Functional test: previewing a multi-turn CSV emits **no** within-CSV duplicate-name warning, returns assembled samples with `multiTurnData`, and reports `totalRows` = row count with `totalTestCases` = case count. Done: `MultiTurnCsvTests` green.
- [x] 3.6 Functional test: two **adjacent identical** `testCaseName` values with blank `turnIndex` — preview still emits the duplicate warning and reports `totalTestCases == totalRows`, and the import half still collides (`conflictStrategy=SKIP` → `skippedCount=1`). Guards design D3's highest risk; the existing `DupRow`/`duprow` test cannot, since those names differ only in case. Done: `MultiTurnCsvTests` green.
- [x] 3.7 Functional test: preview predicts multi-turn conflicts (duplicate `turnIndex`, shared-column mismatch, non-contiguous multi-turn name) with the same warnings import produces; and against a dataset with an **empty** schema, a CSV whose turn rows differ on a column still reports the all-shared conflict and invalidates the case. Done: `MultiTurnCsvTests` green.
- [x] 3.8 Update preview OpenAPI examples (`api-v1-datasets-datasetId-test-cases-import-preview-POST-response-200-{minimal,full}.json`) with `totalTestCases`, and a multi-turn sample carrying `multiTurnData` in the `full` example. Done: examples valid JSON and match the DTO.

## 4. Multi-turn-aware fixup pass (RC3)

- [x] 4.1 Branch `fixupTestCases` on a non-blank stored `multi_turn_data` (using `deserializeTurnsStrict` from task 5.4): coerce changed columns inside each turn map as well as shared `data`, widen the change detection to cover turn maps — today it inspects `data` only, so the pass is inert for a turn-only case, re-validate via `TestCaseValidationService.validateMultiTurn` passing the **full** schema (it splits by scope internally), and include the re-serialized turn array in the `batchUpdate` row (which already writes `multi_turn_data`). Done: multi-turn rows keep their turns after fixup. The re-validated result is passed through the durable-warning merger (task 5.2) before it is written.
- [x] 4.2 Implement the data-safety guard (design D6): a row whose raw `multi_turn_data` column is non-blank but deserializes to `null` is skipped entirely — never added to `toUpdate` — and logged at WARN with the exception as the **last** SLF4J argument. Use `deserializeTurnsStrict` (task 5.4) so a real exception exists to log; the lenient `deserializeTurns` is left as-is for its read-path callers. Done: `LoggingConventionTest` green.
- [x] 4.3 Unit/functional test the fixup branch: per-turn values coerced to the newly inferred type; multi-turn case validated as multi-turn; unreadable turn array leaves the row untouched (turns still present, `multi_turn_data` not NULL); single-turn rows behave exactly as before; per-turn key set unchanged after re-serialization (pins the explicit-null trade-off). Done: `./gradlew :test --tests "*CsvImportServiceSchemaTest"` and `MultiTurnCsvTests` green.

## 5. Durable import conflicts and multi-turn revalidation (RC4)

- [x] 5.1 Add `ValidationWarningCode.SOURCE_CONFLICT` (`evaluation-runner-core`, additive constant) and tag the two `validateRunAsMultiTurn` findings that enter the persisted verdict — duplicate `turnIndex` and shared-column mismatch — with it. Leave the non-contiguity finding alone: it is a response-only `CsvImportWarningDto` in `processRun`, never part of the case's `ValidationResult`, and its run is not persisted under `FAIL`/`SKIP`. Done: import returns the same messages, the two persisted ones now carrying the new code, and `nonContiguousNameRejected` passes unmodified.
- [x] 5.2 Add an injectable merger in `service.domain` (`@Component` + `@LogExecution`) implementing design D8: given a recomputed `ValidationResult` and the stored `validation_warnings` JSON, return the recomputed warnings unioned with the stored `INVALID_INPUT` entries and `valid = recomputedValid && preserved.isEmpty()`. Done: unit-tested for preserve, no-op when none stored, and no duplication when the same warning is recomputed; `CsvImportServiceSchemaTest`'s positional construction updated.
- [x] 5.3 Call the merger from both recomputation passes immediately before writing: `CsvImportService.fixupTestCases` and `RevalidationService.runPhase1`. Direct API writes (`PUT`/`PATCH`) do NOT call it. Done: `grep` shows exactly two production call sites.
- [x] 5.4 Add `deserializeTurnsStrict(String)` to `ValidationWarningsSerializer` (`evaluation-runner-core`) — propagates `JacksonException` rather than returning null; the existing lenient `deserializeTurns` and all its callers are untouched. Use it in both write paths (tasks 4.2 and 5.7) so "absent" and "unreadable" are distinguishable and a real exception exists to log. Done: `./gradlew :evaluation-runner-core:test` green with a unit test covering both methods' contracts.
- [x] 5.5 Add a guarded update writing both columns: `int updateDataAndTurnsIfUnchanged(UUID id, UUID datasetId, String dataJson, String multiTurnDataJson, long expectedUpdatedAt, long newUpdatedAt)` on `TestCaseRepository` + `PostgresTestCaseRepository` (typed jOOQ DSL, `updated_at_ms` precondition, mirroring `updateDataIfUnchanged`, which stays untouched for the single-turn path). Done: method exists; functional test shows a concurrent edit still makes it affect 0 rows.
- [x] 5.6 Branch `RevalidationService.runPhase1` on the stored turn array: coerce each turn map with `SchemaChangeCoercer`, validate via `TestCaseValidationService.validateMultiTurn` passing the **full** schema (it splits by scope internally — do not pass a pre-split list), and persist via the new guarded update. Widen the `coercion.changed()` condition that gates the data write so a turn-only change triggers it and an unchanged multi-turn case still writes nothing (otherwise every revalidation bumps `updated_at_ms` on every multi-turn case). Single-turn path — both guards and skip-on-0-rows — unchanged. Done: **no method-signature change**, so `DatasetService` is untouched.
- [x] 5.6a Add the merger as `RevalidationService`'s one new constructor dependency and update `RevalidationServiceTest`'s positional construction (12 → 13 args) accordingly. Done: `./gradlew :test --tests "*RevalidationServiceTest"` green.
- [x] 5.6b Widen `removeDataFields` (`TestCaseService` → `TestCaseRepository` → `PostgresTestCaseRepository`) to prune the removed field names from each element of `multi_turn_data` as well as from `data`, guarded on the column being non-null — without it, D7(b)'s turn validation turns every affected multi-turn case invalid, worst of all on datasets already corrupted by this bug. Done: functional test removes a per-turn field via a dataset schema PUT and asserts the key is gone from every turn and the case stays valid.
- [x] 5.7 Apply the D6 guard in Phase 1: a row whose raw `multi_turn_data` column is non-blank but fails `deserializeTurnsStrict` is skipped for **both** guarded writes and logged at WARN with the exception as the last SLF4J argument. Done: `LoggingConventionTest` green; a corrupted turn array survives a revalidation untouched.
- [x] 5.8 Functional test (the reported defect, with a value that actually coerces so the fixup path is exercised too): import `testCaseName,turnIndex,prompt` / `dup,0,1` / `dup,0,2` **and** the all-string `dup,0,a` / `dup,0,b`, asserting in both cases that the **persisted** case is `valid=false` carrying the duplicate-`turnIndex` warning after the request completes — today the response says `invalidCount=1` while the stored row is `valid=true, validationWarnings=null`. Repeat for a shared-column mismatch. Done: `MultiTurnCsvTests` green.
- [x] 5.9 Functional test: the conflict warning also survives a subsequent dataset schema change (revalidation on the PUT trigger), and is cleared by a direct `PATCH` of the case. Done: `MultiTurnCsvTests` green.
- [x] 5.10 Functional test (dataset schema PUT trigger, no import): a per-turn value violating its field's schema type makes the case invalid after revalidation rather than valid-because-shared-data-is-clean; a coercible per-turn value is coerced and persisted; a concurrent edit still makes the guarded write affect 0 rows. Done: `MultiTurnCsvTests` or the revalidation functional suite green.
- [x] 5.11 Run the existing revalidation suites unmodified to confirm the single-turn path is untouched. Done: `./gradlew :test --tests "*Revalidation*"` green with no test edits.

## 6. Specs, docs and verification

- [x] 6.1 Update `AGENTS.md` per its Maintenance guidelines: add the `perTurn`-preservation invariant to the multi-turn inline-conventions bullet. Done: bullet states that any CSV-derived schema carries `perTurn` forward by field name.
- [ ] 6.2 Sync the delta specs into `openspec/specs/{multi-turn-test-case,test-cases}/spec.md` (`/opsx:sync`, or at archive time). Done: main specs carry the new and modified requirements.
- [x] 6.3 Confirm no `openspec/config.yaml` and no `openspec/specs/README.md` update is needed (no new layer/package/convention; no spec folder or status change). Done: rationale recorded in the change, files untouched.
- [ ] 6.4 Run `./gradlew spotlessApply` then `./gradlew checkstyleMain checkstyleTest`. Done: both clean.
- [ ] 6.5 Run `./gradlew clean build`. Done: full suite green, including `CsvImportModeFunctionalTests` and `TestCaseFunctionalTests` unmodified (the latter pins preview `totalRows` and `sampleRows` size), plus `LoggingConventionTest` and `LayeredArchitectureTest`.
