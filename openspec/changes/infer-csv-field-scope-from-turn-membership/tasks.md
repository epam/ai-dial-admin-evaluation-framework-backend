## 1. Scope resolution in the schema builder

- [ ] 1.1 In `service/domain/csv/CsvSchemaFieldBuilder.java`, add a `Set<String> multiTurnColumns` parameter to `buildFromBindings(...)` and `buildMergeDelta(...)`, threaded into `newField(...)`. (done: both methods compile with the new parameter and every `CsvImportService` call site passes a value)
- [ ] 1.2 Implement design D1's three tiers in `newField`: declared wins (verbatim, including declared-shared), else membership → `TRUE`, else `null`. **The declared test MUST be `scopeByName.containsKey(name)`, not `scopeByName.get(name) != null`.** (done: a name present in both `scopeByName` (with null value) and `multiTurnColumns` resolves to shared)
- [ ] 1.3 Update the class javadoc — it currently states scope is only carried forward from the current schema and a new column "gets `perTurn` absent (shared)"; replace with the declared / membership / shared-default rule. (done: javadoc matches the modified *CSV schema rebuild preserves per-field scope* requirement)
- [ ] 1.4 Extend `src/test/java/com/epam/aidial/evaluation/service/domain/csv/CsvSchemaFieldBuilderTest.java`: declared-per-turn beats membership; **declared-shared beats membership** (the `containsKey` regression guard); undeclared + member → `perTurn=true`; undeclared + non-member → `perTurn` absent; empty membership set reproduces today's output exactly; `buildMergeDelta` obeys the same precedence. (done: `./gradlew test --tests "com.epam.aidial.evaluation.service.domain.csv.CsvSchemaFieldBuilderTest"` passes)

## 2. Single-pass wiring in CsvImportService

- [ ] 2.1 In `service/domain/CsvImportService.java`, pass the over-approximation to `buildValidationSchema` in both `preview` and `importCsv`: `multiTurnColumns` = all `MAPPED_TO_DATA` field names from `bindings` (design D2). (done: validation schema marks every undeclared data column `perTurn=true`; declared fields unchanged)
- [ ] 2.2 Track a `sawMultiTurnCase` flag for every closed case with `multiTurn() == true` — loop body and final `flush()` — in **both** the import and preview loops (design D3). (done: single-turn cases never set it; both loops track identically)
- [ ] 2.3 Thread the gate into the post-stream schema sites: `persistSchema`, `buildFinalSchema`, `buildAutoDetectedSchema` receive `multiTurnColumns` = all `MAPPED_TO_DATA` field names when `sawMultiTurnCase`, else the empty set. (done: no call to `CsvSchemaFieldBuilder` omits a membership set; validation site uses the D2 over-approximation, post-stream sites use the gate-derived set)
- [ ] 2.4 Verify `MultiTurnRunAssembler`, `TestCaseFieldScopeResolver`, `MultiTurnFieldsValidator`, `CsvExportService`, `TestCaseController`, and `ZipImportService` need no change, and grep `validationSchema` consumers to confirm only the multi-turn path reads scope (design risk 2). (done: none of those files is modified; consumer list recorded in the PR description)
- [ ] 2.5 Update `src/test/java/com/epam/aidial/evaluation/service/domain/CsvImportServiceSchemaTest.java` for the new parameters and add cases: empty current schema + membership set marks members per-turn; empty membership set preserves today's derivation. (done: `./gradlew test --tests "com.epam.aidial.evaluation.service.domain.CsvImportServiceSchemaTest"` passes)

## 3. Functional tests

- [ ] 3.1 In `src/test/java/com/epam/aidial/evaluation/functional/tests/MultiTurnCsvFunctionalTests.java`, add: multi-turn CSV imported into an **empty** dataset persists `perTurn: true` for every data column, preserves every turn in `multiTurnData`, leaves the case valid (including after the post-import dataset revalidation), and emits no shared-conflict warning. (done: covers *Multi-turn CSV into an empty dataset infers per-turn scope*)
- [ ] 3.2 Add the schema-less round trip: export a multi-turn dataset, import that CSV into a second dataset with an empty schema, export again — turn counts match the source, merged views match up to blank materialization, and the two CSVs are byte-identical. (done: covers *Exported multi-turn CSV round-trips into a schema-less dataset*)
- [ ] 3.3 Add: a declared-shared column with disagreeing turn rows still invalidates the case (schema-ful dataset); an undeclared column with disagreeing turn rows in a schema-less dataset does not. (done: covers *Declared-shared field is never re-scoped by inference* and *Conflicting shared columns are a conflict*)
- [ ] 3.4 Add: a mixed single/multi-turn CSV into an empty dataset marks every field `perTurn: true` while single-turn cases keep their values in `data` and stay valid; a purely single-turn CSV into an empty dataset produces a schema and cases identical to pre-change behavior. (done: covers *Mixed CSV marks every undeclared column per-turn*, *New CSV column defaults to shared*, and *Empty dataset schema has nothing to preserve*)
- [ ] 3.5 Add a preview assertion: for a multi-turn CSV against an empty schema, `autoDetectedSchema` marks every data column `perTurn: true`, multi-turn sample rows carry those columns in `multiTurnData`, and no shared-conflict warning is emitted — matching a subsequent import of the same CSV. (done: covers *Preview reports the scope import would persist*)
- [ ] 3.6 Run the adjacent suites unchanged: `./gradlew test --tests "com.epam.aidial.evaluation.functional.tests.MultiTurnCsvFunctionalTests" --tests "com.epam.aidial.evaluation.functional.tests.CsvImportModeFunctionalTests" --tests "com.epam.aidial.evaluation.functional.tests.RevalidationCoercionFunctionalTests"`. (done: all pass)

## 4. Docs, specs, and build

- [ ] 4.1 Update AGENTS.md per AGENTS.md Maintenance guidelines — the *Multi-turn — array-based, shared + per-turn data* inline convention states a CSV-derived schema carries `perTurn` forward from the current schema and "a column with no same-named current field is new and gets `perTurn` absent", which this change makes false. (done: the bullet states the declared / turn-membership / shared-default rule)
- [ ] 4.2 Check `openspec/specs/README.md` per the Spec Index Maintenance Policy — the `multi-turn-test-case` summary mentions flat CSV multiplication; update only if the one-line summary is now materially inaccurate. (done: index reviewed, updated or confirmed accurate)
- [ ] 4.3 Sync the delta spec into `openspec/specs/` with `/opsx:sync`, then verify with `git diff` that `openspec/specs/multi-turn-test-case/spec.md` gained the two MODIFIED requirements and lost no existing requirement. (done: main spec reflects membership-based scope resolution)
- [ ] 4.4 Run `./gradlew spotlessApply` and then `./gradlew clean build` (tests + Checkstyle + Spotless check). (done: build passes with no formatting or Checkstyle violations)
