## 1. Runner-core plumbing: `$_metrics` accumulator threading + SPI types

- [x] 1.1 Add `METRICS_FRAME_BINDING = "_metrics"` to `evaluation-runner-core/src/main/java/com/epam/aidial/evaluation/runner/constants/JsonataReservedNames.java` and include it in `FRAME_RESERVED_NAMES`; verify `TestSuiteRequestValidatorTest` (`src/test/java/com/epam/aidial/evaluation/service/domain/TestSuiteRequestValidatorTest.java`) picks up the new reserved name without code changes to the validator: `./gradlew test --tests "*TestSuiteRequestValidatorTest*"`.
- [x] 1.2 Add a fourth component `Map<String,Object> accumulatedMetrics` to `runner/job/RequestExecutionResult.java`, and thread it through `runner/job/RequestChainExecutor.java` alongside the existing response-column accumulator; update the 3-arg `RequestExecutionResult` construction sites and 7-arg `execute` stubs in `RequestChainExecutorTest.java`. Verify: `./gradlew :evaluation-runner-core:test --tests "*RequestChainExecutorTest*"`.
- [x] 1.3 Create `runner/job/InlineMetricEvaluator.java` (interface: `InlineMetricResult evaluate(InlineMetricRequest request)`), `runner/job/InlineMetricRequest.java` (`TestCaseRunResult row`, `Map<String,Object> accumulatedMetrics`), and `runner/job/InlineMetricResult.java` (`Map<String,Object> frameEntry`, `boolean failed`) — JDK/runner-local types only. Verify: `./gradlew :evaluation-runner-core:test --tests "*RunnerModuleConstraintsTest*"`.
- [x] 1.4 Add a `@Nullable InlineMetricEvaluator inlineMetricEvaluator` field to `runner/job/EvaluationContext.java` via its existing `@Builder`. Verify `eval-cli`'s `EvaluationContextFactory` still compiles without setting the new field (defaults to `null`): `./gradlew :eval-cli:build && ./gradlew :eval-cli:test`.
- [x] 1.5 Add an `initialMetrics` parameter to `runner/job/TurnLoopExecutor.execute(...)` and bind it as `$_metrics` in the per-turn frame **only when `context.getInlineMetricEvaluator() != null`** (the field added in task 1.4) — a non-inline run's frame SHALL carry NO `_metrics` key at all (not an empty map), so `$exists($_metrics)` evaluates to `false` for that run; update the 13 `execute(...)` call sites in `TurnLoopExecutorTest.java`. Write the opening "nested-null" test first, now that `$_metrics` can actually be bound: an `ObjectNode`-constructed `$_metrics` entry with a `putNull` field survives serialization/rebind so `$exists(...)` is `true` for the present-null path. Verify: `./gradlew :evaluation-runner-core:test --tests "*TurnLoopExecutorTest*"`.
- [x] 1.6 Add a regression test asserting that with `initialMetrics` empty and no evaluator wired, `TurnLoopExecutor`'s produced rows and `extracted_columns` are byte-identical to pre-change output (guards against accidental behavior drift from the accumulator plumbing alone). Verify: `./gradlew :evaluation-runner-core:test --tests "*TurnLoopExecutorTest*"`.
- [x] 1.7 Add a `ResponseColumnExtractorTest` case asserting a response-column expression referencing `$_metrics` resolves to JSONata `undefined` — `ResponseColumnExtractor`'s frame binds only `_request`/`_response`/prior columns, so `$_metrics` is never bound there regardless of run mode. Verify: `./gradlew :evaluation-runner-core:test --tests "*ResponseColumnExtractorTest*"`.

## 2. The total seam: wiring `InlineMetricEvaluator` into `TurnLoopExecutor`

- [x] 2.1 Wire the seam into `TurnLoopExecutor`'s CONTINUE branch per `design.md` D3: build the turn row first, then (when `inlineMetricEvaluator != null`) call `evaluate(...)`, fold the returned `frameEntry` into `accumulatedMetrics`, append the row, and on `result.failed()` set `aborted = true; break;`. The call site MUST NOT let any exception (including `InterruptedException`, folded into a failed result with the thread's interrupt flag re-set) escape into the existing `try/catch` that synthesizes `REQUEST_RESOLUTION_ERROR` rows. Verify with a fake `InlineMetricEvaluator` that throws: assert the row stays the real SUCCESS row, never replaced by a synthetic ERROR row: `./gradlew :evaluation-runner-core:test --tests "*TurnLoopExecutorTest*"`.
- [x] 2.2 Add fake-evaluator unit tests: frame entry is visible to the next turn's `$_metrics` binding; frame entry is visible across the chain via `RequestChainExecutor`; a failed result aborts remaining turns and requests while earlier rows persist and stay `SUCCESS`; a `condition`-skipped metric (clean `false`) contributes no entry to `accumulatedMetrics`, so a later turn/request's `$_metrics` reference to that TSMD is absent from the frame rather than present-with-null. Add a transmission-caveat test: a fake evaluator's `frameEntry` carries a present-null field (e.g. `$_metrics.judge.score.value = null` via `putNull`), a later request's JSON body binds that field into an object key, and the row's resulting `requestBody` (the emitted, serialized body — persisted as `request_body` once written by the backend) SHALL NOT contain that key, even though `$exists(...)` on the same path was `true` during body evaluation — proving the shared `NON_NULL` `ObjectMapper` drops it on the way out. Verify: `./gradlew :evaluation-runner-core:test --tests "*TurnLoopExecutorTest*" --tests "*RequestChainExecutorTest*"`.
- [x] 2.3 Confirm `context.getInlineMetricEvaluator() == null` (non-inline) produces execution byte-identical to task 1.6's baseline — extend that regression test to also assert this under a populated `EvaluationContext` that simply omits the evaluator. Verify: `./gradlew :evaluation-runner-core:test --tests "*TurnLoopExecutorTest*"`.

## 3. `MetricRowEvaluator` extraction + snapshot hoist

- [x] 3.1 Extract `service/domain/job/MetricRowEvaluator.java` (`@Component`, `@LogExecution`) from `InProcessMetricEvaluationExecutor`: `evaluateAndBuild`, `buildPropagatedItem`, `buildItem`, `computeMetricEvalDurationMs`, `checkForErrors`, `buildProviderSemaphores`, returning a record `{EvalSummaryBatchWriteItemDto item, Map<String,TsmdEvaluationResult> tsmdResults, boolean hasError}`. Behavior-preserving only. Split `InProcessMetricEvaluationExecutorTest.java`: move the duration/mapping assertions (currently around `:440,444`) into a new `MetricRowEvaluatorTest.java`. Done when: class compiles as an injectable `@Component` carrying the class-level `@LogExecution` annotation per AGENTS.md. Verify: `./gradlew test --tests "*MetricRowEvaluatorTest*" --tests "*InProcessMetricEvaluationExecutorTest*"`.
- [x] 3.2 Hoist the `buildMetricEvaluationContext(...)` call in `service/domain/job/TestSuiteEvaluationJob.java` — today inside the Phase-2 `if (!cancellationSignal.get())` block (`:159-163`) — to run BEFORE Phase 1, on both branches: on the normal branch, call it INSIDE `if (!skipDeploymentPhase)` immediately AFTER the inconsistent-snapshot guard passes (so a run that fails that guard never writes `run_metric_snapshots`); on the `skipDeploymentPhase` branch, call it immediately after that `if` block closes. Factor a small private method (e.g. `buildMetricContextAndWriteSnapshot(TestSuiteRun run, Supplier<SuiteSnapshotDto> snapshot)`) invoked from both sites, so the snapshot write added in task 3.3 follows immediately in both places without duplicating logic. Verify: `./gradlew test --tests "*TestSuiteEvaluationJobTest*"`.
- [x] 3.3 Hoist `run_metric_snapshots` writing out of `InProcessMetricEvaluationExecutor.execute()` into the private method introduced in task 3.2 (`service/domain/job/TestSuiteEvaluationJob.java`), immediately after `MetricEvaluationContext` construction, on both the `skipDeploymentPhase` branch and the normal branch. Move the snapshot-write assertion (currently around `InProcessMetricEvaluationExecutorTest.java:477`) into `TestSuiteEvaluationJobTest.java`, and add an assertion that a run failing the inconsistent-snapshot guard on the normal branch writes no `run_metric_snapshots` row at all (per the `metric-evaluation` delta's "Run failing the inconsistent-snapshot guard writes no run_metric_snapshots row" scenario). Verify the existing import-path assertions stay green: `./gradlew test --tests "*TestSuiteEvaluationJobTest*" --tests "*EvalResultsImportFunctionalTests*"`.

## 4. Inline evaluator, detector, job wiring

- [x] 4.1 Add `service/domain/job/InlineModeDetector.java` (`@Component`, `@LogExecution`): scans the run's `SuiteSnapshotDto` request-template JSON bodies (root + `additionalRequests[i]`) and the live enabled+valid TSMD list's `configBindings`/`inputBindings` raw JSON for the `"$" + JsonataReservedNames.METRICS_FRAME_BINDING` substring (never a bare string literal); returns `false` unconditionally for `suiteType = MCP_TOOL`. Done when: class compiles as an injectable `@Component` carrying the class-level `@LogExecution` annotation per AGENTS.md. Verify: `./gradlew test --tests "*InlineModeDetectorTest*"` covering root-body hit, additional-request hit, `jsonataContent` hit, TSMD-binding hit, multipart/url-encoded-body non-hit, MCP always-false, and the no-match negative case.
- [x] 4.2 Add `service/domain/job/InlineMetricEvaluatorImpl.java` and `service/domain/job/InlineMetricEvaluatorFactory.java` (`@Component`, `@LogExecution`, mirroring `PostgresResultBatchWriterFactory`): the impl reuses `context.getCancellationSignal()` (never a fresh `AtomicBoolean`), builds provider semaphores once, buffers summaries behind a `ReentrantLock` copied from `PostgresResultBatchWriter`'s add/drain-then-flush-outside-lock discipline, and never throws from `evaluate()`. Done when: the factory compiles as an injectable `@Component` carrying the class-level `@LogExecution` annotation per AGENTS.md. Verify: `./gradlew test --tests "*InlineMetricEvaluatorImplTest*"` covering Success/Failure/ConditionError frame-entry shape, interruption folding with the interrupt flag re-set, buffer flush on size threshold and on `close()`, and — mandatory — a `concurrencyLevel > 1` test asserting concurrent `evaluate()` calls from N threads lose no summaries.
- [x] 4.3 Add `boolean inlineMode` to `MetricEvaluationContext`; make `InProcessMetricEvaluationExecutor`'s row loop skip SUCCESS rows and process only non-SUCCESS rows via the existing `buildPropagatedItem` path when `inlineMode = true`, unchanged otherwise. Verify: `./gradlew test --tests "*InProcessMetricEvaluationExecutorTest*"` covering inline SUCCESS-row skip with zero provider calls, non-SUCCESS-row propagation, and full non-inline-mode parity with today.
- [x] 4.4 Wire `TestSuiteEvaluationJob`: run `InlineModeDetector` after building `MetricEvaluationContext`; when inline, construct `EvaluationContext` with the factory-supplied `InlineMetricEvaluator`; call the evaluator's final `flush()` immediately after `evaluationExecutor.execute(context)` returns, strictly before the Phase 2/3 block; add `close()` in the existing `finally` block as a no-op safety net. The detector SHALL run only when `!skipDeploymentPhase`: `executeRunAsync`'s `skipDeploymentPhase = true` branch (the `importResultsAndEvaluate` import path — no Phase 1 to be inline about) MUST force `inlineMode = false` unconditionally, without calling `InlineModeDetector` at all, so Phase 2 evaluates every SUCCESS row exactly as it does today for imported results. Add a `TestSuiteEvaluationJobTest` assertion that `executeRunAsync(runId, token, true)` never invokes `InlineModeDetector` and builds a `MetricEvaluationContext` with `inlineMode = false`. Verify ordering and end-to-end behavior: `./gradlew test --tests "*TestSuiteEvaluationJobTest*" --tests "*MultiRequestChainRunFunctionalTests*" --tests "*TestSuiteRunFunctionalTests*"` covering: (a) request #1's persisted `request_body` contains the `$_metrics`-derived value read from request #0's inline-evaluated TSMD output; one eval summary per row with the shared `computationId`, snapshot present, provider called once per row with no Phase-2 round for a passing 2-request chain; a failing metric mock aborting request #1 while request #0's row stays `SUCCESS` and its summary is FAILED; cancellation mid-Phase-1 leaving rows + summaries + snapshot consistent with run status `CANCELLED`; a suite without `$_metrics` running Phase 2 exactly as before; and, end-to-end in `TestSuiteRunFunctionalTests`, an import-path run (`skipDeploymentPhase = true`) whose suite references `$_metrics` still evaluates every SUCCESS row via Phase 2 rather than being treated as inline.
- [x] 4.5 Add tests proving `$_metrics` is unavailable inside a TSMD `condition`, covering both possible outcomes of that `undefined` reference: (a) a bare-reference `condition` (e.g. `` $_metrics.`judge`.score.value ``) resolves `$_metrics` to `undefined` in `ConditionExpressionEvaluator`'s four-namespace dictionary, and the condition's own non-boolean `undefined` result produces the broken-condition runtime outcome (`metricError::<name>`, row stays `SUCCESS`) — and, under an inline-mode run, additionally aborts the chain per the `conditional-metric-execution` delta's "Bare $_metrics reference resolves undefined and triggers the broken-condition outcome" scenario; (b) an `$exists`-wrapped `condition` (e.g. `` $exists($_metrics.`judge`.score.value) ``) resolves that same underlying `undefined` down to a clean boolean `false` — the metric is simply omitted from the row (no `metricError::<name>` entry) and, under inline mode, the chain continues rather than aborting, per the delta's "$exists-wrapped $_metrics reference resolves a clean false" scenario. Verify: `./gradlew test --tests "*ConditionExpressionEvaluatorTest*" --tests "*InlineMetricEvaluatorImplTest*"`.

## 5. `Expression` metric-binding source

- [x] 5.1 Add `service/domain/dto/ExpressionBindingSourceDto.java` (`expression: String`, required) and register a fourth `@JsonSubTypes.Type(name = "Expression")` on `MetricBindingSourceDto`, also adding `ExpressionBindingSourceDto.class` to that class's class-level `@Schema(subTypes = {...})` list alongside `TestCaseBindingSourceDto`/`ResponseBindingSourceDto`/`ConstantBindingSourceDto`; add an OpenAPI discriminator example alongside the existing `Constant`/`Response`/`TestCase` examples. Verify: `./gradlew test --tests "*MetricDefinitionValidationServiceTest*"` still passes unmodified (no behavior change to existing source types) and the new DTO deserializes in a focused unit test.
- [x] 5.2 Add a `Map<String,Object> frame` parameter to `service/domain/job/BindingResolver.resolveBindings`/`resolveSource`, with a new `Expression` branch calling `jsonataEvaluationService.evaluate(expression, "{}", frame)`; an `undefined` result or a thrown exception SHALL throw `IllegalArgumentException`, matching the existing `Response`/`TestCase` failure mode. `BindingResolver` gains a new `JsonataEvaluationService` constructor dependency alongside its existing `ObjectMapper`; update the two `new BindingResolver(new ObjectMapper())` construction sites at `MetricEvaluationWorkerTest.java:54,196` and the one at `BindingResolverTest.java:25` to pass a `JsonataEvaluationService` (real or test double). Update the 9 `resolveBindings(...)` call sites in `BindingResolverTest.java` to pass the new `frame` parameter. Verify: `./gradlew test --tests "*BindingResolverTest*" --tests "*MetricEvaluationWorkerTest*"`.
- [x] 5.3 Thread the `frame` parameter through `service/domain/job/MetricEvaluationWorker.evaluate`/`invokeWithRetries`/`buildRequest` and the dispatch site in `MetricRowEvaluator` (built as `{_metrics: accumulatedMetrics, data: testCaseData, response: extractedColumns}`; `_metrics = {}` for Phase 2's propagate-only pass and for any non-inline evaluation). Update the six `evaluate(...)` call sites in `MetricEvaluationWorkerTest.java`. Verify: `./gradlew test --tests "*MetricEvaluationWorkerTest*"`.
- [x] 5.4 Add write-time JSONata syntax validation for a non-blank `Expression.expression`, mirroring the existing `condition` syntax check in `service/domain/TestSuiteMetricDefinitionService.java` (HTTP 400 on malformed syntax; no cross-TSMD reference check). Verify: `./gradlew test --tests "*TestSuiteMetricDefinitionFunctionalTests*"` (`src/test/java/com/epam/aidial/evaluation/functional/tests/TestSuiteMetricDefinitionFunctionalTests.java` — there is no `TestSuiteMetricDefinitionServiceTest`) covering malformed-expression-on-create, malformed-expression-on-update, and a syntactically-valid expression referencing a not-yet-producing TSMD name being accepted at write time.
- [x] 5.5 Extend `MultiRequestChainRunFunctionalTests.java` (`@MockitoBean MetricProviderClient` via `MetricEvaluationTestHelper`): request #1's TSMD with an `Expression` binding receives request #0's `details.reason` string in the captured `/evaluate` payload; a malformed `Expression` is rejected with HTTP 400 on TSMD write; CSV export `metric::`/`metricInfo::` columns are unaffected by the new binding type. Verify: `./gradlew test --tests "*MultiRequestChainRunFunctionalTests*"`.
- [x] 5.6 Add a `TryItOutServiceTest` case asserting that a try-out run of a test case belonging to a suite that would be classified inline-mode for a real run makes no metric-provider `/evaluate` call — `TryItOutService` has no `MetricProviderClient` (or any other metric-binding) collaborator anywhere in its try-out chain, so no `InlineMetricEvaluator` is ever wired into it — and that a `$_metrics` reference inside the previewed request body resolves to JSONata `undefined`. Verify: `./gradlew test --tests "*TryItOutServiceTest*"`.

## 6. Docs, spec index, OpenAPI examples, spec sync, final verification

- [x] 6.1 Write `docs/patterns/inline-metric-evaluation.md` covering the `$_metrics` frame shape, `InlineModeDetector`, the total-seam requirement, thread-safety/flush-timing rules, and the D1–D7 decision summary; add a row to AGENTS.md's "Unique Patterns" table linking to it, and add the matching entry to `docs/patterns/README.md`'s index table. Verify: `test -f docs/patterns/inline-metric-evaluation.md` and `grep -n "inline-metric-evaluation" AGENTS.md docs/patterns/README.md` each show exactly one row linking to the new doc.
- [x] 6.2 Update `docs/configuration.md`'s `Applied when` column for `metric-evaluation.per-result-timeout-ms` and `metric-evaluation.default-concurrency-per-provider` to note they now also govern inline evaluation timing/concurrency during Phase 1, not only Phase 2. Verify: `grep -n "per-result-timeout-ms\|default-concurrency-per-provider" docs/configuration.md` shows both rows' `Applied when` column mentioning Phase 1 / inline evaluation, matching design.md's D5/D7 timing notes.
- [x] 6.3 Update `openspec/specs/README.md` one-line summaries per the Spec Index Maintenance Policy for six specs: `metric-evaluation` (no longer strictly "Phase 2 of test suite run lifecycle"), `response-columns` (reserved names now include `_metrics`), `tsmd-validation` (fourth `Expression` binding source), `test-suite-metric-definitions` (line 38's binding-source parenthetical `(TestCase, Response, Constant)` → `(TestCase, Response, Constant, Expression)`), `conditional-metric-execution` (inline-mode chain-abort divergence), and `evaluation-runner-core-module` (owns the `InlineMetricEvaluator` SPI). Verify with one grep per spec: `grep -n "Phase 2 of test suite run lifecycle" openspec/specs/README.md` shows no match (metric-evaluation, stale wording gone); `grep -n "_request.*_response.*_metrics" openspec/specs/README.md` shows a match (response-columns); `grep -n "Expression" openspec/specs/README.md` includes a hit inside the `tsmd-validation` entry; `grep -n "TestCase, Response, Constant, Expression)" openspec/specs/README.md` shows a match (test-suite-metric-definitions); `grep -n "InlineMetricEvaluator" openspec/specs/README.md` includes a hit inside the `evaluation-runner-core-module` entry; `grep -n "inline" openspec/specs/README.md` includes a hit inside the `conditional-metric-execution` entry.
- [x] 6.4 Add an `Expression`-typed binding into the existing metric-definition OpenAPI examples under `src/main/resources/openapi/examples/` — `api-v1-test-suites-testSuiteId-metric-definitions-POST-request-full.json`, `api-v1-test-suites-testSuiteId-metric-definitions-POST-response-201-full.json`, `api-v1-test-suites-testSuiteId-metric-definitions-id-PUT-request-full.json`, `api-v1-test-suites-testSuiteId-metric-definitions-id-PUT-response-200-full.json`, `api-v1-test-suites-testSuiteId-metric-definitions-id-GET-response-200-full.json`, and `api-v1-test-suites-testSuiteId-metric-definitions-id-aggregated-GET-response-200-full.json` — adding a fourth binding entry alongside the existing `Constant`/`TestCase`/`Response` ones, matching this directory's existing convention of one example file per endpoint+variant rather than per discriminator `$type` (there is no per-`$type` example filename precedent here). Verify: each edited `-full.json` file still parses as valid JSON and contains a `"$type": "Expression"` binding (`grep -l '"$type": "Expression"' src/main/resources/openapi/examples/*-full.json` lists all six files), and Swagger UI at `/swagger-ui.html` renders the updated example under the TSMD create/update/get endpoints (manual check).
- [x] 6.5 Update `openspec/config.yaml` per the Config Maintenance Policy: add `InlineMetricEvaluator` SPI to the Feature Surface's owned-classes description of `evaluation-runner-core-module`, and add Glossary entries for "inline metric evaluation", "`$_metrics` frame", "`InlineMetricEvaluator` SPI", and "`Expression` binding source". Verify: `grep -n "InlineMetricEvaluator\|inline metric evaluation\|_metrics. frame\|Expression. binding source" openspec/config.yaml` shows the Feature Surface addition and all four new Glossary entries.
- [x] 6.6 Sync the 10 delta specs (`conditional-metric-execution`, `eval-execution-engine`, `evaluation-runner-core-module`, `metric-evaluation`, `multi-request-suite`, `request-template`, `response-columns`, `test-suite-metric-definitions`, `try-it-out`, `tsmd-validation`) into `openspec/specs/` via `/opsx:sync`, per project convention (cf. `openspec/changes/archive/2026-08-27-add-mcp-argument-validation/tasks.md` task 5.4), and flip each delta's `Status: **Planned**` to `**Implemented**` before archiving. Done when `openspec validate inline-metric-evaluation --type change --strict` passes and `git diff` on each touched main spec shows gained content with no existing requirement lost.
- [ ] 6.7 Run full verification: `./gradlew spotlessApply && ./gradlew build`. Confirm Checkstyle, `LayeredArchitectureTest`, `RunnerModuleConstraintsTest`, and `LoggingConventionTest` all pass, and fix any violation before considering the change complete.
