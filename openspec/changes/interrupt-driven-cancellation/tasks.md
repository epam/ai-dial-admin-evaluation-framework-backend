## 1. Runner-core: executor-owned cancellation (evaluation-runner-core)

- [ ] 1.1 `runner/job/EvaluationContext.java`: replace `AtomicBoolean cancellationSignal` and `long cancellationGracePeriodMs` with `ExecutorService executor` (required, documented as owned by the caller). Done: module compiles once 1.2–1.5 land.
- [ ] 1.2 `runner/job/TestCaseRunner.java`: use `context.getExecutor()` (no executor creation, no `shutdown`/`shutdownNow`), delete both signal checks in `submit`, make `awaitCompletion()` an interruptible `allOf(futures).get()` that logs the WARN "cancelled with N test case(s) interrupted" when any future completed exceptionally/was interrupted, and implement design D8 in the worker wrapper (drop results + no synthetic row when interrupted; keep broad `catch (Exception e)`). Done: `TestCaseRunnerTest` rewritten — remove grace tests, add `shouldDropResultsAndSkipSyntheticRow_whenWorkerInterruptedByShutdownNow`, `shouldRejectSubmission_whenExecutorAlreadyShutDown`, keep `shouldNotTimeoutOnLongRun_whenNoCancellation` semantics without grace; `./gradlew :evaluation-runner-core:test --tests "*TestCaseRunnerTest"` green.
- [ ] 1.3 `runner/job/EvaluationWorker.java` (MCP retry) and `runner/job/DeploymentTurnInvoker.java` (HTTP retry): delete the pre/post-sleep `cancellationSignal` checks, keep `InterruptedException → interrupt() + break`. Done: `EvaluationWorkerTest`, `DeploymentTurnInvokerTest`, `RequestChainExecutorTest` fixtures updated (no `.cancellationSignal(...)`/`.cancellationGracePeriodMs(...)`; pass an executor) and green.
- [ ] 1.4 `runner/job/TurnLoopExecutor.java`: remove the `runOneTurn` cancel-abort and the `requestIssued || !cancelled` suppression (plain abort-row logic remains for real failures). Done: `TurnLoopExecutorTest.cancellationBeforeFirstTurn_producesNoRows` removed, rest green.
- [ ] 1.5 `runner/config/properties/EvaluationRunProperties.java`: delete `Execution.cancellationGracePeriodMs`; remove `cancellation-grace-period-ms` from `src/main/resources/application.yml` (root app) and any runner-core test resources. Done: `grep -rn "cancellationGracePeriod\|cancellation-grace-period\|cancellationSignal" evaluation-runner-core/` returns nothing; `./gradlew :evaluation-runner-core:test` green.

## 2. eval-cli adaptation

- [ ] 2.1 `cli/service/EvaluationContextFactory.java`: create `Context.taskWrapping(Executors.newVirtualThreadPerTaskExecutor())` and set `.executor(...)`; drop `.cancellationGracePeriodMs`/`.cancellationSignal`. `cli/service/RunOrchestrationService.java`: shut the context's executor down in a `finally` after `runner.awaitCompletion()`. Done: `RunOrchestrationServiceTest` fixtures updated and green.
- [ ] 2.2 Remove `cli.run.cancellation-grace-period-ms` from `EvalCliProperties.Run`, both keys from `eval-cli/src/main/resources/application.yml` (lines ~35 and ~73), and the two rows in `eval-cli/README.md`. Done: `grep -rn "cancellation-grace-period\|cancellationGracePeriod" eval-cli/` returns nothing; `./gradlew :eval-cli:test` green.

## 3. Root: status model, repository guards, run registry

- [ ] 3.1 `data/db/model/RunStatus.java`: add `CANCELLING`; add `ACTIVE_STATUSES` (`PENDING`, `RUNNING`, `CANCELLING`) + `activeStatusNames()` helper; `TERMINAL_STATUSES` unchanged. Done: unit test asserts `isTerminal("CANCELLING") == false`.
- [ ] 3.2 `data/db/repository/TestSuiteRunRepository.java` + `PostgresTestSuiteRunRepository.java`: `updateToRunning` (`WHERE status='PENDING'`), `updateToCompleted` (`WHERE status='RUNNING'`), `updateToCancelled` (`WHERE status IN ('RUNNING','CANCELLING')`) now return `int`; add `int markCancelling(UUID id)` (RUNNING→CANCELLING, `updated_at_ms` from `TransactionTimestampContext`, no `completed_at`) and `int cancelOrphanedCancellingRuns(long completedAtMs)`. Done: functional repository test (`@PostgresFunctionalTests`) covers each guard's 0-row and 1-row case via `MetaTestDataHelper` fixtures.
- [ ] 3.3 New `service/domain/job/RunHandle.java` (wraps the run's virtual-thread executor: `executor()`, `cancel()`, `isCancelled()`, `throwIfCancelled()`, `close()`) and `service/domain/job/ActiveRunRegistry.java` (`@Component @LogExecution`, `register/cancel/remove/get`). Done: `RunHandleTest` (cancel → `isCancelled`, `throwIfCancelled` throws `CancellationException`, `executor().execute` rejected after cancel) and `ActiveRunRegistryTest` (cancel of unknown id is a no-op; remove clears) green.
- [ ] 3.4 New `service/domain/job/AnalyticsWriteException.java` (unchecked, carries error code `ANALYTICS_WRITE_FAILED`). `service/domain/TestSuiteRunReconciliation.java`: after failing PENDING/RUNNING orphans, call `cancelOrphanedCancellingRuns(clock.millis())` and log both counts. Done: functional test `shouldFinalizeOrphanedCancellingRunAsCancelled_onStartup` (insert CANCELLING via helper, invoke reconciliation bean, assert CANCELLED + `completedAt` set, no error fields) green.

## 4. Root: job orchestration, Phase 2/3 executors, cancel endpoint

- [ ] 4.1 `service/domain/job/TestSuiteEvaluationJob.java`: remove signal map/methods/`@Async`; inject `@Qualifier("testSuiteRunExecutor") AsyncTaskExecutor` + `ActiveRunRegistry`; add `dispatch(runId, token, skipDeploymentPhase)` (register handle → `execute`; remove handle + rethrow on failure); implement the run flow from design D4 (guarded `updateToRunning` early-exit, `throwIfCancelled()` at each phase boundary, guarded `updateToCompleted` fallback to `updateToCancelled`, `CancellationException`/`RejectedExecutionException`/any-exception-while-cancelled → CANCELLED, `AnalyticsWriteException` → FAILED `ANALYTICS_WRITE_FAILED`, `finally` close+remove+SSE). Pass `handle.executor()` into `EvaluationContext` and `MetricEvaluationContext`. Done: `TestSuiteEvaluationJobTest` rewritten — `shouldExitWithoutRunning_whenCancelledWhilePending`, `shouldMarkCancelledAndSkipPhase2And3_whenCancelledDuringPhase1`, `shouldMarkCancelled_whenCompletedWriteAffectsNoRows`, `shouldMarkFailedAnalyticsWriteFailed_whenPhase2FlushThrows`, existing snapshot/inconsistency tests adapted; green.
- [ ] 4.2 `service/domain/job/MetricEvaluationContext.java` (`executor` replaces `cancellationSignal`), `InProcessMetricEvaluationExecutor.java` (use context executor, no own executor/`shutdownNow`, delete both signal checks, `doFlush` throws `AnalyticsWriteException`), `MetricEvaluationWorker.java` (delete pre-attempt check, replace `sleepWithCancellation` with `Thread.sleep`), `MetricScoreComputationContext.java` + `query/service/metricscore/MetricScoreComputationExecutor.java` (delete `cancellationSignal`/`isCancelled`). Done: `InProcessMetricEvaluationExecutorTest` (`shouldPropagateAnalyticsWriteException_whenBatchWriteFails`, dispatch on provided executor), `MetricEvaluationWorkerTest.shouldThrowInterrupted_whenInterruptedDuringBackoff`, `MetricScoreComputationExecutorTest` fixtures green.
- [ ] 4.3 `service/domain/TestSuiteRunService.java`: `cancelRun` per design D5 (terminal 409; PENDING optimistic → registry.cancel + SSE; CANCELLING idempotent; RUNNING → `markCancelling` + registry.cancel + SSE, re-read once when 0 rows); `enforceConcurrencyLimits` uses `RunStatus.ACTIVE_STATUSES`; `dispatchEvaluation` calls `evaluationJob.dispatch(...)` and keeps only the rejection compensation. Done: `TestSuiteRunServiceTest` updated (`shouldMarkCancellingAndCancelHandle_whenRunning`, `shouldReturnCurrentDto_whenAlreadyCancelling`, `shouldCountCancellingAsActive_forConcurrencyLimits`) green.
- [ ] 4.4 Web/OpenAPI: `web/controller/TestSuiteRunController.java` cancel `@Operation` description (RUNNING → returns `CANCELLING`; idempotent on CANCELLING), `evaluation-runner-core/.../runner/dto/TestSuiteRunResponseDto.java` `status` field gets `@Schema(allowableValues = {"PENDING","RUNNING","CANCELLING","COMPLETED","FAILED","CANCELLED"})`, add `api-v1-test-suite-runs-id-cancel-POST-response-200-cancelling.json` under `src/main/resources/openapi/examples/` and add an api-docs assertion in `functional/tests/TestSuiteRunFunctionalTests.java` mirroring `DeploymentFunctionalTests` ("OpenAPI spec carries ... examples") for the cancel operation. Done: that test green.

## 5. Functional tests, docs, specs

- [ ] 5.1 `functional/tests/TestSuiteRunFunctionalTests.java`: `shouldCancelRunningRun` asserts body `status == CANCELLING` and `completedAt == null`; add `shouldReturnCancellingUnchanged_whenCancelCalledTwice`; `shouldCancelPendingRun` unchanged. `functional/tests/EvaluationExecutorFailureModesFunctionalTests.shouldCancelRunMidFlight_...`: assert cancel response `CANCELLING`, run readable as `CANCELLING` before terminal, terminal `CANCELLED`, `results.size() < 4`, no `CANCELLED`/`INTERRUPTED` rows, and total wall-clock < 2 s. Remove the `cancellation-grace-period-ms` `@TestPropertySource` from `functional/PostgresFunctionalTests.java`; adapt `MetricScoreComputationFunctionalTests`/`RunComparisonFunctionalTests` fixtures. Done: `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$TestSuiteRunFunctionalTests" --tests "...\$EvaluationExecutorFailureModesFunctionalTests"` green.
- [ ] 5.2 Docs: delete the `cancellation-grace-period-ms` row from `docs/configuration.md`; add `CANCELLING` to `docs/database-schema.md` status column; write `docs/patterns/run-cancellation.md` and add a row to the AGENTS.md Unique Patterns table + `docs/patterns/README.md`; update `openspec/changes/ef-as-dial-app/design.md:163` to reference `ActiveRunRegistry.cancel` + mark FAILED; fix the "cancellation grace period" wording in `openspec/specs/evaluation-runner-core-module/spec.md` class inventory. Done: files updated, `grep -rn "grace" docs/ AGENTS.md` shows no cancellation grace references.
- [ ] 5.3 Full verification: `./gradlew spotlessApply checkstyleMain checkstyleTest` then `./gradlew build` (includes `LayeredArchitectureTest`, `LoggingConventionTest`, `RunnerModuleConstraintsTest`); `grep -rn "cancellationSignal\|cancellationGracePeriod" src evaluation-runner-core eval-cli` returns nothing. Done: build green, grep empty.
- [ ] 5.4 Spec sync on archive: delta specs in this change (`test-suite-runs`, `eval-execution-engine`, `metric-evaluation`, `eval-results-import`) synced to `openspec/specs/`; `openspec/specs/README.md` `test-suite-runs` summary lifecycle updated to `PENDING→RUNNING→(CANCELLING→)COMPLETED/FAILED/CANCELLED`. Done: `openspec validate --change interrupt-driven-cancellation --strict` passes before archive.
