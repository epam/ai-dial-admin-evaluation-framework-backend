## 1. Runner-core: executor-owned cancellation (evaluation-runner-core)

- [x] 1.1 `runner/job/EvaluationContext.java`: replace `AtomicBoolean cancellationSignal` and `long cancellationGracePeriodMs` with `ExecutorService executor` (required; Javadoc: owned and shut down by the caller). Done: module compiles once 1.2–1.5 land.
- [x] 1.2 `runner/job/TestCaseRunner.java`: use `context.getExecutor()` (no executor creation, no `shutdown`/`shutdownNow`); `submit(List)` returns `boolean` — catches `RejectedExecutionException` from `runAsync`, releases the acquired permit, logs at DEBUG and returns `false`; delete both signal checks; `awaitCompletion()` stays `public void`, waits via `allOf(futures).get()`, re-interrupts + throws `CancellationException` on `InterruptedException`, wraps `ExecutionException` in `IllegalStateException`, and logs the WARN "Run {} cancelled with {} test case(s) interrupted before completion" when the interrupted-task counter is > 0; worker wrapper implements design D8 (on `InterruptedException`/wrapped interrupt/`isInterrupted()` after execute → increment interrupted counter, write nothing, no synthetic row; keep broad `catch (Exception e)`). Done: `TestCaseRunnerTest` rewritten — remove grace tests; add `shouldDropResultsAndSkipSyntheticRow_whenWorkerInterruptedByShutdownNow`, `shouldReturnFalseAndReleasePermit_whenExecutorRejectsSubmission`, `shouldPreserveInterruptFlagAtWrapper_whenWorkerCallThrowsAfterInterrupt` (worker stub re-interrupts then throws `IOException`; assert no row), keep a no-timeout long-run test; `./gradlew :evaluation-runner-core:test --tests "*TestCaseRunnerTest"` green.
- [x] 1.3 `runner/job/EvaluationWorker.java` (MCP retry) and `runner/job/DeploymentTurnInvoker.java` (HTTP retry): delete the pre/post-sleep `cancellationSignal` checks, keep `InterruptedException → interrupt() + break`. Done: `EvaluationWorkerTest`, `DeploymentTurnInvokerTest`, `RequestChainExecutorTest` fixtures updated (no `.cancellationSignal(...)`/`.cancellationGracePeriodMs(...)`; pass an executor) and green.
- [x] 1.4 `runner/job/TurnLoopExecutor.java`: remove the `runOneTurn` cancel-abort and the `requestIssued || !cancelled` suppression (plain abort-row logic remains for real failures). Done: `TurnLoopExecutorTest.cancellationBeforeFirstTurn_producesNoRows` removed, rest green.
- [x] 1.5 `runner/config/properties/EvaluationRunProperties.java`: delete `Execution.cancellationGracePeriodMs`; remove `cancellation-grace-period-ms` from the root `src/main/resources/application.yml` and any runner-core test resources. Done: `grep -rn "cancellationGracePeriod\|cancellation-grace-period\|cancellationSignal" evaluation-runner-core/` returns nothing; `./gradlew :evaluation-runner-core:test` green.

## 2. eval-cli adaptation

- [x] 2.1 `cli/service/EvaluationContextFactory.java`: create `Context.taskWrapping(Executors.newVirtualThreadPerTaskExecutor())` and set `.executor(...)`; drop `.cancellationGracePeriodMs`/`.cancellationSignal`. `cli/service/RunOrchestrationService.java`: `shutdownNow()` the context's executor in a `finally` after `runner.awaitCompletion()`. Done: `RunOrchestrationServiceTest` and `EvaluationContextFactoryTest` fixtures updated and green.
- [x] 2.2 Remove `cli.run.cancellation-grace-period-ms` from `EvalCliProperties.Run`, both keys from `eval-cli/src/main/resources/application.yml` (lines ~35 and ~73), the two rows in `eval-cli/README.md`, and the property lines in `eval-cli/src/test/.../ContextStartupTest.java:45,67` and `.../config/properties/PropertiesBindingTest.java:27`. Done: `grep -rn "cancellation-grace-period\|cancellationGracePeriod" eval-cli/` returns nothing; `./gradlew :eval-cli:test` green.

## 3. Root: status model, repository guards, run registry

- [x] 3.1 `data/db/model/RunStatus.java`: add `CANCELLING`; add `ACTIVE_STATUSES` (`PENDING`, `RUNNING`, `CANCELLING`) + `activeStatusNames()` helper; `TERMINAL_STATUSES` unchanged. Done: unit test asserts `isTerminal("CANCELLING") == false` and active set contents.
- [x] 3.2 `data/db/repository/TestSuiteRunRepository.java` + `PostgresTestSuiteRunRepository.java`: `updateToRunning` (`WHERE status='PENDING'`), `updateToCompleted` (`WHERE status='RUNNING'`), `updateToCancelled` (`WHERE status IN ('RUNNING','CANCELLING')`), `updateToFailed` (`WHERE status IN ('PENDING','RUNNING','CANCELLING')`) all return `int`; add `int markCancelling(UUID id)` (RUNNING→CANCELLING, `updated_at_ms` from `TransactionTimestampContext`, no `completed_at`) and `int cancelOrphanedCancellingRuns()` (CANCELLING→CANCELLED, `completed_at_ms`/`updated_at_ms` from `TransactionTimestampContext`, mirroring `failOrphanedRuns`). Update the other `updateToFailed` callers (`TestSuiteRunService.markRunFailed`/rejection path) to ignore the count. Done: functional repository test (`@PostgresFunctionalTests`) covers each guard's 0-row and 1-row case via `MetaTestDataHelper` fixtures.
- [x] 3.3 New `service/domain/job/RunHandle.java` (wraps `Context.taskWrapping(Executors.newVirtualThreadPerTaskExecutor())`: `executor()`, `cancel()` = `shutdownNow()` + flag, `isCancelled()`, `throwIfCancelled()` → `CancellationException`, `close()` = `shutdownNow()` — never `ExecutorService.close()`) and `service/domain/job/ActiveRunRegistry.java` (`@Component @LogExecution`, `register/cancel/remove/find`). Done: `RunHandleTest` (cancel → `isCancelled`; `throwIfCancelled` throws; `executor().execute` rejected after cancel; `close()` returns promptly with a non-interruptible busy task in flight; `close()` alone does not set `isCancelled`) and `ActiveRunRegistryTest` (cancel of unknown id is a no-op; remove clears) green.
- [x] 3.4 New `service/domain/job/AnalyticsWriteException.java` (unchecked, error code `ANALYTICS_WRITE_FAILED`). `service/domain/TestSuiteRunReconciliation.java`: after failing PENDING/RUNNING orphans, call `cancelOrphanedCancellingRuns()` in the same transaction and log both counts (no new `Clock` dependency). Done: functional test `shouldFinalizeOrphanedCancellingRunAsCancelled_onStartup` (insert CANCELLING via helper, invoke reconciliation bean, assert CANCELLED + `completedAt` set, no error fields) green.

## 4. Root: job orchestration, Phase 1/2/3 executors, cancel endpoint

- [x] 4.1 `service/domain/job/TestSuiteEvaluationJob.java`: remove signal map/methods/`@Async`; inject `@Qualifier("testSuiteRunExecutor") AsyncTaskExecutor` + `ActiveRunRegistry`; add `dispatch(runId, token, skipDeploymentPhase)` (register handle → `execute`; remove handle + rethrow on any failure); package-private `run(UUID, String, boolean, RunHandle)` implementing design D4 exactly (pre-snapshot `throwIfCancelled`, guarded `updateToRunning` early-exit, SSE after RUNNING, `throwIfCancelled()` before each phase, guarded `updateToCompleted` → fallback `updateToCancelled`, catch precedence `AnalyticsWriteException` → FAILED `ANALYTICS_WRITE_FAILED` > `CancellationException`/`RejectedExecutionException` → CANCELLED > other exception → CANCELLED if `handle.isCancelled()` else FAILED `UNEXPECTED_ERROR`; `finally` close + remove + terminal SSE). Pass `handle.executor()` into `EvaluationContext` and `MetricEvaluationContext`. Done: `TestSuiteEvaluationJobTest` rewritten against `run(...)` — `shouldExitWithoutRunning_whenCancelledWhilePending` (`updateToRunning` returns 0), `shouldSkipSnapshot_whenHandleCancelledBeforeStart`, `shouldMarkCancelledAndSkipPhase2And3_whenCancelledDuringPhase1` (Phase 1 mock throws `RejectedExecutionException`), `shouldMarkCancelled_whenCompletedWriteAffectsNoRows`, `shouldMarkFailedAnalyticsWriteFailed_whenPhase2FlushThrows_evenIfCancelled`, existing snapshot/inconsistency tests adapted; green.
- [x] 4.2 `service/domain/job/InProcessEvaluationExecutor.java`: delete both signal checks; page loop `do { page = fetch; accepting = runner.submit(page); } while (accepting && page.size() == PAGE_SIZE)`; `runner.awaitCompletion()` always runs before any flush (catch/finally flush structure for genuine dispatch-loop failures unchanged). Done: `InProcessEvaluationExecutorTest` updated (`.cancellationGracePeriodMs` fixture removed; new `shouldStopPagingAndAwaitBeforeFlush_whenSubmitReportsRejection` asserting `awaitCompletion` is invoked before `flush`) green.
- [x] 4.3 `service/domain/job/MetricEvaluationContext.java` (`executor` replaces `cancellationSignal`), `InProcessMetricEvaluationExecutor.java` (use context executor, no own executor/`shutdownNow`, delete both signal checks, `doFlush` throws `AnalyticsWriteException`), `MetricEvaluationWorker.java` (delete pre-attempt check, replace `sleepWithCancellation` with `Thread.sleep`), `MetricScoreComputationContext.java` + `query/service/metricscore/MetricScoreComputationExecutor.java` (delete `cancellationSignal`/`isCancelled`). Done: `InProcessMetricEvaluationExecutorTest` (`shouldPropagateAnalyticsWriteException_whenBatchWriteFails`, dispatch on provided executor, `RejectedExecutionException` propagates), `MetricEvaluationWorkerTest.shouldThrowInterrupted_whenInterruptedDuringBackoff`, `MetricScoreComputationExecutorTest` fixtures green.
- [x] 4.4 `client/metricprovider/MetricProviderRestClientConfiguration.java`: build the per-provider `RestClient` on `JdkClientHttpRequestFactory` over a JDK `HttpClient` (`HTTP_1_1`, connect timeout, `setReadTimeout`) instead of `SimpleClientHttpRequestFactory` (design D11). Done: existing metric-provider client unit/functional tests green; a unit test asserts an in-flight `evaluate` call ends within 1 s when its virtual thread is interrupted (mock server that never responds).
- [x] 4.5 `service/domain/TestSuiteRunService.java`: `cancelRun` per design D5 (terminal 409; PENDING optimistic → `afterCommit(registry.cancel)` + SSE; CANCELLING idempotent; RUNNING → `markCancelling` + `afterCommit(registry.cancel)` + SSE, re-read once when 0 rows); `enforceConcurrencyLimits` uses `RunStatus.ACTIVE_STATUSES`; `dispatchEvaluation` calls `evaluationJob.dispatch(...)` and keeps only the rejection compensation. Done: `TestSuiteRunServiceTest` updated (`shouldMarkCancellingAndCancelHandleAfterCommit_whenRunning`, `shouldReturnCurrentDto_whenAlreadyCancelling`, `shouldCountCancellingAsActive_forConcurrencyLimits`, `shouldNotCancelHandle_whenTransactionRollsBack`) green.
- [x] 4.6 Web/OpenAPI: `web/controller/TestSuiteRunController.java` cancel `@Operation` description (RUNNING → returns `CANCELLING`; idempotent on CANCELLING); `evaluation-runner-core/.../runner/dto/TestSuiteRunResponseDto.java` `status` gets `@Schema(allowableValues = {"PENDING","RUNNING","CANCELLING","COMPLETED","FAILED","CANCELLED"})`; add `api-v1-test-suite-runs-id-cancel-POST-response-200-cancelling.json` under `src/main/resources/openapi/examples/`; add an api-docs assertion in `functional/tests/TestSuiteRunFunctionalTests.java` mirroring `DeploymentFunctionalTests` ("OpenAPI spec carries ... examples") for the cancel operation. Done: that test green.

## 5. Functional tests, docs, specs

- [x] 5.1 `functional/tests/TestSuiteRunFunctionalTests.java`: `shouldCancelRunningRun` asserts body `status == CANCELLING` and `completedAt == null` (and cleans the row up); add `shouldReturnCancellingUnchanged_whenCancelCalledTwice`; `shouldCancelPendingRun` unchanged. `functional/tests/EvaluationExecutorFailureModesFunctionalTests.shouldCancelRunMidFlight_...`: assert cancel response `CANCELLING`, run readable as `CANCELLING` before terminal, terminal `CANCELLED`, `results.size() < 4`, no `CANCELLED`/`INTERRUPTED` rows, total wall-clock < 2 s. `functional/tests/TestSuiteRunSseFunctionalTests.java`: new test asserting a RUNNING cancel emits `CANCELLING` then `CANCELLED` `status-update` events. Remove the `cancellation-grace-period-ms` `@TestPropertySource` from `functional/PostgresFunctionalTests.java`; adapt `MetricScoreComputationFunctionalTests`/`RunComparisonFunctionalTests` fixtures. Done: `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$TestSuiteRunFunctionalTests" --tests "...\$EvaluationExecutorFailureModesFunctionalTests" --tests "...\$TestSuiteRunSseFunctionalTests"` green.
- [x] 5.2 Docs: delete the `cancellation-grace-period-ms` row from `docs/configuration.md`; add `CANCELLING` to `docs/database-schema.md` status column; write `docs/patterns/run-cancellation.md` and add it to `docs/patterns/README.md`; update `openspec/changes/ef-as-dial-app/design.md:163` to reference `ActiveRunRegistry.cancel` + mark FAILED. Done: files updated, `grep -rn "grace" docs/ AGENTS.md` shows no cancellation-grace references.
- [x] 5.3 Update AGENTS.md per AGENTS.md Maintenance guidelines (done: Unique Patterns table gains the `run-cancellation` row; no other section needs changing).
- [x] 5.4 Full verification: `./gradlew spotlessApply checkstyleMain checkstyleTest` then `./gradlew build` (includes `LayeredArchitectureTest`, `LoggingConventionTest`, `RunnerModuleConstraintsTest`); `grep -rn "cancellationSignal\|cancellationGracePeriod\|executeRunAsync\|interruptRun" src evaluation-runner-core eval-cli` returns nothing. Done: build green, grep empty.
- [x] 5.5 Spec sync on archive: run `/opsx:sync` for the delta specs in this change (`test-suite-runs`, `eval-execution-engine`, `metric-evaluation`, `eval-results-import`, `observability-and-logging`, `evaluation-runner-core-module`, `eval-summary-export`, `suite-run-snapshot`); then hand-edit baseline prose outside requirement blocks: Key Terms in `test-suite-runs/spec.md:9,13`, `eval-execution-engine/spec.md:10`, `metric-evaluation/spec.md:15`, `eval-results-import/spec.md:10`, `suite-run-snapshot/spec.md:12`, and the Implementation Notes in `eval-results-import/spec.md:326`; update `openspec/specs/README.md` `test-suite-runs` summary lifecycle to `PENDING→RUNNING→(CANCELLING→)COMPLETED/FAILED/CANCELLED`. Done: `openspec validate interrupt-driven-cancellation --strict` passes before archive; `git diff openspec/specs` shows no lost requirements.
