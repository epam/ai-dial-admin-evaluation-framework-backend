## 1. Analytics schema + repository primitives

- [ ] 1.1 Add `src/main/resources/db/migration/analytics/POSTGRES/V1.15__AddEvalSummariesRunComputedAtIndex.sql` creating `idx_eval_summaries_run_computed_at ON test_case_eval_summaries (test_suite_run_id, computed_at_ms DESC, computation_id)` (done: migration applies on a clean Testcontainers boot)
- [ ] 1.2 Run `./gradlew generateJooq` and commit the regenerated sources under `src/main/java-generated/` (done: diff limited to the analytics `Indexes` class; `./gradlew test --tests "*JooqSchemaDriftTest"` passes)
- [ ] 1.3 Add `Optional<UUID> findLatestComputationId(UUID runId)` to `EvalSummaryRepository` + `PostgresEvalSummaryRepository` via typed jOOQ DSL (`select(COMPUTATION_ID) … orderBy(COMPUTED_AT_MS.desc()).limit(1)`), mirroring `PostgresRunMetricSnapshotRepository.findLatestComputationId` (done: compiles, no `JdbcTemplate`, no SQL text block)
- [ ] 1.4 Add `boolean existsByRunIdAndComputationId(UUID runId, UUID computationId)` to the same pair using jOOQ `fetchExists` (done: presence decided without fetching rows and without synthesizing `FilterCondition`/`runCreatedAtMs` arguments; consumed by task 4.1)
- [ ] 1.5 Update `docs/database-schema.md` in **both** places it records analytics schema: add `idx_eval_summaries_run_computed_at` to the `test_case_eval_summaries` **Indexes** table (next to `idx_eval_summaries_run_computation` / `idx_eval_summaries_computation` / `idx_eval_summaries_id`), and add a `V1.15` row to the Analytics **migration-history** table immediately after the `V1.14` row (done: both tables mention V1.15; the index columns listed match the migration)
- [ ] 1.6 Add a third `createEvalSummary` overload to `AnalyticsTestDataHelper` with the exact signature `createEvalSummary(UUID suiteId, UUID suiteRunId, UUID computationId, String testCaseName, String executionStatus, long execDurationMs, long createdAtMs, long computedAtMs, String testCaseDataJson, String metricValuesJson)` — i.e. the existing 9-arg variant with `computedAtMs` inserted immediately after `createdAtMs`, setting `COMPUTED_AT_MS` from it instead of from `createdAtMs`. The existing 9-arg variant delegates to it passing `createdAtMs` for `computedAtMs`; the existing 7-arg variant stays as it is (it already delegates to the 9-arg one). Arities stay distinct (7 / 9 / 10), so no ambiguous overload is created. This lets summaries of one run share `created_at_ms` — required, since reads filter on `CREATED_AT_MS.eq(runCreatedAtMs)` — while differing in `computed_at_ms` (done: compiles; consumed by tasks 2.6 and 5.1/5.2/5.3)

## 2. Computation resolution switch

- [ ] 2.1 Point `ComputationResolver`'s `latest` branch at `EvalSummaryRepository.findLatestComputationId` and drop the `RunMetricSnapshotRepository` constructor dependency; update the class javadoc's result conventions to say "no eval summaries for the run" (done: `RunMetricSnapshotRepository` no longer imported in this class)
- [ ] 2.2 Fix the now-inaccurate class javadoc on `MetricScoreLatestComputationDefaulter` — "when the run has no computations yet, the sentinel is left in place" now means "when the run has no eval summaries" (done: javadoc-only edit, no behavior change; `.orElse(query)` untouched)
- [ ] 2.3 Leave `RunMetricSnapshotRepository.findLatestComputationId` and `EvalSummariesSchemaProvider` behavior untouched. No existing **unit** test needs re-stubbing for the resolution switch — verified: `MetricScoreLatestComputationDefaulterTest` mocks `ComputationResolver` itself, and the only unit tests referencing `RunMetricSnapshotRepository` are `EvalSummariesSchemaProviderTest` and `MetricScoreComputationExecutorTest`, neither of which resolves a computation (done: `grep` confirms the schema provider still resolves metric families from snapshots; `./gradlew test --tests "*EvalSummariesSchemaProviderTest" --tests "*MetricScoreComputationExecutorTest" --tests "*MetricScoreLatestComputationDefaulterTest"` passes unchanged)
- [ ] 2.4 Unit-test `ComputationResolver`: explicit UUID never hits a repository; `latest`/`null` returns the eval-summary answer; empty when the run has no summaries; malformed string throws `ValidationException` (done: `./gradlew test --tests "*ComputationResolverTest"` passes)
- [ ] 2.5 Re-seed the four `EvalSummaryExportFunctionalTests` cases that create only `run_metric_snapshots` and no eval summaries, and so start returning 404 from `computationResolver.resolve(...).orElseThrow(EntityNotFoundException…)` after 2.1 — before column selection and filter parsing ever run: `exportCompletedRunReturns200`, `exportUnknownColumnReturns400`, `exportFilterOutOfWhitelistReturns400`, `previewFilterOutOfWhitelistReturns400`. Add an `insertEvalSummaries(testSuiteId, completedRun.getId(), computationId, 2)` call (the class's existing helper, as `exportValidFilterReturns200` already does) for the same `computationId` each test snapshots, so each case reaches its asserted 200 / 400 outcome instead of a 404 (done: `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$EvalSummaryExportTests"` passes)
- [ ] 2.6 Fix `MetricScoreResultStructuredQueryFunctionalTests.resolvesLatestSentinel()`, which today seeds only `run_metric_snapshots` + `metric_score_results` and so stops resolving after 2.1: also seed eval summaries for both computations with matching distinct `computed_at_ms` (`1_000` / `2_000`, via the 1.6 overload) so `latest` still resolves the newer one (done: `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$MetricScoreResultStructuredQueryTests"` passes)
- [ ] 2.7 Make `EvalSummaryFunctionalTests.insertEvalSummariesWithComputation` take an explicit `computedAtMs` instead of `System.currentTimeMillis()`, and update **both** of its call sites so the class still compiles: pass `baseTime` / `baseTime + 1000` from `shouldResolveComputationLatest` — after 2.1 the tie-break moves from snapshot timestamps to eval-summary `computed_at_ms`, and two same-millisecond values make `ORDER BY computed_at_ms DESC LIMIT 1` arbitrary — and pass two distinct values from `shouldUseSpecificComputation` (`EvalSummaryFunctionalTests.java:495-496`, whose assertion is on an explicit UUID and so is timestamp-independent, but whose two calls must still match the new signature) (done: `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$EvalSummaryTests"` passes; the `hasSize(3)` assertion is deterministic)

## 3. Metric evaluation executor writes summaries without metrics

- [ ] 3.1 Remove the `getAggregatedTsmds().isEmpty()` early return from `InProcessMetricEvaluationExecutor.execute`, replacing it with a single `log.info` line per run recording the metric-less run — same level as the adjacent `"Starting metric evaluation for run {}, {} TSMDs"` line (done: no early return; `writeRunMetricSnapshots` still called and no-ops on the empty list)
- [ ] 3.2 Unit-test the empty-TSMD path in `InProcessMetricEvaluationExecutorTest`: one `EvalSummaryBatchWriteItemDto` per result row, `metricValues` empty and `metricInfos` `null` (`MetricOutputMapper.buildMetricInfos` returns `null` when it has no info to record; the column is nullable — do not assert `{}`), zero `RunMetricSnapshot` writes, zero `MetricProviderClient` interactions, zero `ConditionExpressionEvaluator` interactions. The `{}` / `null` shape is the point of this change, so it must come from the real mapper: change the class's `@Mock private MetricOutputMapper outputMapper` to `@Spy private MetricOutputMapper outputMapper = new MetricOutputMapper(new ObjectMapper())` (its only dependency is `ObjectMapper`, and `@Spy` fields are still injected by the existing `@InjectMocks`), because an unstubbed `@Mock` returns `null` from `buildMetricValues` and stubbing it would make the assertion tautological. Convert the four metric-bearing tests' eight `when(outputMapper.buildMetric…(any()))` stubs to `doReturn(…).when(outputMapper).buildMetric…(any())` so stubbing a spy does not invoke the real method with placeholder arguments (done: `./gradlew test --tests "*InProcessMetricEvaluationExecutorTest"` passes — the whole class, not just the new test)
- [ ] 3.3 Unit-test status and payload fidelity on the empty-TSMD path: SUCCESS rows stay SUCCESS, non-SUCCESS rows keep their own status, and `testCaseData` / `extractedColumns` / `extractionWarnings` / `turnIndex` / `totalTurns` / `execDurationMs` / `responseStatusCode` are carried through (done: same test class passes)
- [ ] 3.4 Rewrite `TestSuiteRunFunctionalTests.shouldCompleteRunWithoutEvalSummariesWhenNoTsmds` in place — it asserts the opposite of the new behavior (`findEvalSummariesByRunId(run.getId())` is empty) and so fails after 3.1. Rename the method to `shouldWriteMetricLessEvalSummariesWhenNoTsmds` and its `@DisplayName` to `"Should write metric-less eval summaries when no TSMDs configured"`; keep the end-to-end run (still reaches COMPLETED) and assert one `test_case_eval_summaries` row per `findResultsByRunId(run.getId())` row, each with `metric_values` `"{}"` and `metric_infos` null; keep the existing `findRunMetricSnapshotsByRunId(run.getId())` empty assertion; and assert `metricScoreResultRepository.findByRunAndComputation(run.getId(), <computationId read off a summary row>)` is empty (the repository is already injected in this class). This task subsumes the end-to-end zero-TSMD coverage — there is no separate new test for it (done: `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$TestSuiteRunTests"` passes)
- [ ] 3.5 Rewrite `TestSuiteMetricDefinitionFunctionalTests.shouldProduceNoEvalSummaries_whenAllTsmdsDisabledOrInvalid` in place — its `countEvalSummaries() == 0` assertion and its `"Metric evaluation phase was skipped — no EvalSummary records produced"` comment both contradict the new behavior. Rename the method to `shouldProduceMetricLessEvalSummaries_whenAllTsmdsDisabledOrInvalid`, change the `@DisplayName` to `"Should produce metric-less EvalSummary records when all TSMDs are disabled or invalid"`, drop the stale comment, and assert one eval summary per `findResultsByRunId(completedRun.getId())` row with `metric_values` `"{}"` plus zero `findRunMetricSnapshotsByRunId(completedRun.getId())` rows (`completedRun` is this test's local, not `run`) (done: `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$TestSuiteMetricDefinitionTests"` passes)

## 4. Export accepts metric-free computations

- [ ] 4.1 In `EvalSummaryExportService.resolveContext`, replace only the `explicitComputationUuid && metricSnapshots.isEmpty()` 404 guard with `existsByRunIdAndComputationId(runId, computationId)`; leave the `latest` path on its existing resolver `orElseThrow` 404, which after 2.1 already answers the eval-summary question (done: a metric-free computation returns 200; a genuinely unknown UUID still returns 404 `NOT_FOUND`; no new constructor dependency — `EvalSummaryRepository` is already injected)
- [ ] 4.2 Verify `EvalSummaryExportColumnPlanner` and `EvalSummaryExportColumnSelector` need no change for an empty snapshot list (done: no edits; both halves are asserted by task 5.4 on the same metric-free fixture — its preview assertion (200, headers array carrying `requestBody`/`responseBody` and no `metric::*` / `metricInfo::*` / `metricError::*`) covers the planner's metric-free manifest, and its `columns: ["metric::<name>::<field>"]` → 400 `VALIDATION_ERROR` assertion covers the selector's unknown-column rejection. The delta spec's "Adding metrics later does not retro-fit old exports" scenario is covered by construction: `resolveContext` feeds the planner only the run's frozen `suite_snapshot` and the resolved computation's `run_metric_snapshots`, never live TSMDs, so a TSMD created after the run cannot enter the manifest)

## 5. Functional coverage

- [ ] 5.1 In `EvalSummaryFunctionalTests`, add a metric-less run scenario seeded via `AnalyticsTestDataHelper` (results + eval summaries, no run metric snapshots), with every seeded summary carrying the meta run's `created_at_ms` (reads filter on `CREATED_AT_MS.eq(runCreatedAtMs)`, so a mismatched value makes the rows invisible and looks like a resolver bug). The class's `setUp` today keeps only `testSuiteRunId` (`private UUID testSuiteRunId` ← `createTestSuiteRun(testSuiteId).getId()`), so first make it retain the `TestSuiteRun` itself (or add a `testSuiteRunCreatedAtMs` field) — tasks 5.1/5.2/5.3 all need `run.getCreatedAt()` to pass into the 1.6 overload. Assert three endpoints on that one fixture: `GET /api/v1/analytics/eval-summaries?filter=runId:eq:<id>` returns the rows with `metricValues: {}`; `/count` returns their number; and `GET /api/v1/analytics/eval-summaries/aggregate?filter=runId:eq:<id>&metrics=Accuracy.score` returns 200 with `computationId` equal to the seeded computation (previously `null`, because resolution found no snapshots) and a single metrics item whose `count` is `0` and whose `avg`/`min`/`max` are `null` (done: `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$EvalSummaryTests"` passes)
- [ ] 5.2 Add a multi-computation resolution test using the 1.6 overload: two computations for one run, all rows sharing the meta run's `created_at_ms` but with different `computed_at_ms`, no snapshots — `latest` resolves the newer one; an explicit older UUID returns the older rows. In the same test class also cover the change's headline read-side delta (`metrics-storage` scenario "Latest resolution requires readable rows", otherwise asserted only by construction in unit task 2.4): seed computation A with eval summaries at `computed_at_ms = 1000` (1.6 overload) and computation B with `run_metric_snapshots` **only** at `computed_at_ms = 2000` (the class's existing `insertRunMetricSnapshotsWithTimestamp`) and no eval summaries, then assert `computation=latest` returns A's rows — under the old snapshot-based resolver B would have won and the page would have been empty (done: same suite passes)
- [ ] 5.3 Add a resolution-robustness test in two deterministic parts, no timing and no `EXPLAIN`: (a) seed many summaries across two computations for one run (1.6 overload, all rows carrying the meta run's `created_at_ms`) and assert `latest` resolves the newer computation's rows; (b) assert via a new `AnalyticsTestDataHelper` schema-inspection helper (SQL stays in the helper — do not reach for `JdbcTemplate` in the test class) that `pg_indexes` lists `idx_eval_summaries_run_computed_at` on `test_case_eval_summaries` and that its `indexdef` **contains** `(test_suite_run_id, computed_at_ms DESC, computation_id)` — a contains check, not equality, because Postgres renders `indexdef` schema-qualified and with `USING btree`. `pg_indexes` is a catalog view with no generated jOOQ table, so the helper reads it through its injected `analyticsDsl` `DSLContext` using plain-SQL field/table references and returns an `Optional<String>` (e.g. `analyticsDsl.select(DSL.field("indexdef", String.class)).from(DSL.table("pg_indexes")).where(DSL.field("tablename", String.class).eq(…).and(DSL.field("indexname", String.class).eq(…))).fetchOptional(…)`) — still `DSLContext`, no `JdbcTemplate` anywhere, and no SQL in the test class (done: same suite passes; both assertions unconditional, no if/else branching)
- [ ] 5.4 In `EvalSummaryExportFunctionalTests`, add a metric-free export on one shared fixture (a run with eval summaries and no `run_metric_snapshots`) and assert all five outcomes against it: (a) the default `POST …/export.csv` header contains `data::*` / `response::*` and no `metric::*`, `metricInfo::*`, `metricError::*`; (b) `computation=<uuid>` of that computation returns 200; (c) a random UUID still returns 404; (d) `GET …/export/preview?runId=<id>` (the endpoint is `@GetMapping`, so use the class's existing `previewUrl(runId, computation)` helper — a POST would be 405) returns 200 with a headers array that contains `requestBody` and `responseBody` (preview returns the full manifest, unlike the default CSV header) and no `metric::*` / `metricInfo::*` / `metricError::*` entry — this is the delta spec's "Preview of a metric-free run" scenario; (e) an export request with `columns: ["metric::<name>::<field>"]` against that metric-free manifest returns 400 `VALIDATION_ERROR` — the delta spec's "Explicit columns on a metric-free run" scenario, which task 2.5 removes from `exportUnknownColumnReturns400`'s reach by re-seeding that case against a metric-bearing manifest (done: `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$EvalSummaryExportTests"` passes, with all five assertions unconditional)
- [ ] 5.5 Add a multi-turn metric-less assertion in `MultiTurnRunFunctionalTests` (its suites carry no TSMDs, so its runs are metric-less and `MultiTurnExecutor` writes one `TestCaseRunResult` per turn — the highest-fan-out new row shape, otherwise covered only by unit task 3.3): for a 2-turn case the run yields one eval summary per turn, with `turn_index` values `0..N-1` and `total_turns = N` on every row (done: `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$MultiTurnRunTests"` passes)
- [ ] 5.6 Cover the CSV eval-results import path (`skipDeploymentPhase=true`, which runs Phase 2 unconditionally like a normal run) in `EvalResultsImportFunctionalTests`: importing results for a suite with zero enabled+valid TSMDs yields readable eval summaries through `GET /api/v1/analytics/eval-summaries` (done: `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$EvalResultsImportTests"` passes)
- [ ] 5.7 Rename the two test `@DisplayName`s that name the old snapshot-based mechanism so each names eval-summary absence instead: `EvalSummaryFunctionalTests` "Should return empty page when no snapshots exist" and `EvalSummaryExportFunctionalTests` "Export with computation=latest when no snapshots exist returns HTTP 404". Separately, as a cosmetic clarification only (its current name names no mechanism), `EvalSummaryFunctionalTests` "Should return 0 count when no computation exists" may say "no eval summaries exist" (done: each renamed `@DisplayName` states eval-summary absence and matches what its test asserts; both suites pass)
- [ ] 5.8 Regression: confirm a metric-bearing run is unchanged end to end — snapshots written, `metricValues` populated, list/aggregate/export identical to before (done: `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$EvalSummaryAggregationTests" --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$MetricScoreComputationTests"` passes)

## 6. Quality gates and spec sync

- [ ] 6.1 Run `./gradlew spotlessApply checkstyleMain checkstyleTest` (done: clean; no hand-formatting reverted by the formatter)
- [ ] 6.2 Run the full `./gradlew clean build` (done: green, including `LoggingConventionTest`, `LayeredArchitectureTest`, `JdbcTemplateFenceTest`, `JooqSchemaDriftTest`)
- [ ] 6.3 Sync the four delta specs into `openspec/specs/` (`metric-evaluation`, `metrics-storage`, `eval-summary-export`, `test-suite-runs`) and flip their requirement statuses to `Implemented`; while editing `openspec/specs/metrics-storage/spec.md`, also correct its stale "Implementation Notes" description of `PostgresEvalSummaryRepository` (it claims `@Qualifier("analyticsJdbcTemplate")` with `SELECT_LIST_COLUMNS` / `SELECT_BY_ID_DETAIL_SQL`; the repository is jOOQ `DSLContext`-based) (done: main specs match implemented behavior; no stale "return without writing any records" text, no `JdbcTemplate` text, and no scenario left naming `run_metric_snapshots` as the source of latest-computation resolution — including the export spec's "Request body schema for export" → "Computation defaults to latest" scenario)
- [ ] 6.4 Re-check `openspec/specs/README.md` per the Spec Index Maintenance Policy and update only if a listed capability's one-line summary became inaccurate (done: index verified)
