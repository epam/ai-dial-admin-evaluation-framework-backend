## 1. Validation check

- [ ] 1.1 In `src/main/java/com/epam/aidial/evaluation/service/domain/TestCaseValidationService.java`, add the no-per-turn-columns check to `validateMultiTurn` immediately after the max-turns cap check: when `!safeTurns.isEmpty()` and the per-turn sub-schema from `schemaSplit.perTurn()` holds no field with a non-blank name, build the warning via the existing `warning(...)` helper with `fieldName=null`, `path="$.multiTurnData"`, `code=ValidationWarningCode.ADDITIONAL`, message `"Test case has <N> turns but the dataset schema declares no per-turn columns; turn data cannot be attached"`, and **prepend** it with `warnings.add(0, ...)` rather than appending. Rationale: `warnings` already starts with `placement.warnings()` (one `INVALID_SCOPE` per misplaced occurrence, up to `max-turns` = 10 of them) plus the shared-bucket warnings, so at the default `validation.max-warnings-per-case` = 5 an appended warning is exactly what the truncation at the end of the method deletes — silently breaking the delta spec's unconditional "both SHALL be reported" coexistence rule. Done: the warning is at index 0 of the returned list, no new field/bean/enum constant is introduced, and `valid` still flips because it is computed before truncation.
- [ ] 1.2 Update the `validateMultiTurn` javadoc: replace the sentence "A multi-turn case with all-empty turn maps is valid when no required per-turn field exists" with the qualified rule (valid only when the schema declares at least one per-turn field) and state the new invalidating case-level warning alongside the existing over-cap one, including that it is prepended so truncation cannot drop it. Done: javadoc matches the behavior in 1.1 and the `test-cases` delta spec.
- [ ] 1.3 Run `./gradlew spotlessApply checkstyleMain`. Done: both succeed with no manual re-formatting.

## 2. Unit tests

- [ ] 2.1 In `src/test/java/com/epam/aidial/evaluation/service/domain/TestCaseValidationServiceMultiTurnTest.java`, add a test: dataset schema with only shared fields + a case with two non-empty turns → result invalid with exactly one warning at `$.multiTurnData`, code `ADDITIONAL`, `fieldName` null, `turnIndex` null, and a message reporting `2` turns. Note the class-level `SCHEMA` constant declares `prompt` as per-turn, so this test needs its own all-shared field list. Done: asserts every field of the warning, not just invalidity.
- [ ] 2.2 Strengthen the **existing** `emptyPerTurnAllowed` test instead of adding a new negative test — it already covers the negative case (its local is misleadingly named `allShared` but declares `field("prompt", true, false)`, i.e. a per-turn field, so the new check must stay silent). Rename the local to `perTurnDeclared` and add an assertion that no warning has path `$.multiTurnData`. Done: the test still passes unchanged in outcome and now pins the no-over-firing guarantee (design Decision 3 risk).
- [ ] 2.3 Add the coexistence test: all-shared schema + turns holding values for shared-declared fields → one `INVALID_SCOPE` warning per misplaced occurrence **and** the case-level `$.multiTurnData` warning. Done: asserts both warning kinds are present.
- [ ] 2.4 Add the truncation-survival test: all-shared schema, five turns each holding one declared-shared field, and `validationProperties.getMaxWarningsPerCase()` stubbed to `5` (the class's `setUp` stubs `100`, so this test must override it) → the returned list has 5 entries and still contains the `$.multiTurnData` warning. Done: pins the prepend in 1.1; the test fails if the warning is appended.
- [ ] 2.5 Add the single-turn regression test: all-shared schema + `validateTestCase` (no `multiTurnData`) → unchanged validity, no `$.multiTurnData` warning. No existing test in this class asserts that an all-shared-schema multi-turn case is valid, and `TestCaseValidationServiceTypeTest` never calls `validateMultiTurn`, so neither needs a corrective edit. Done: `./gradlew test --tests "com.epam.aidial.evaluation.service.domain.TestCaseValidationServiceMultiTurnTest"` passes.

## 3. Functional tests

- [ ] 3.1 In `src/test/java/com/epam/aidial/evaluation/functional/tests/MultiTurnSharedDataFunctionalTests.java`, add a REST write-path test: create a dataset whose `testCaseSchema` declares only shared fields, then POST a test case with `multiTurnData` = `[{}, {}]` — the turn maps **must** be empty, since a declared shared field inside a turn is rejected with `400` by `MultiTurnFieldsValidator` before validation runs (see the existing `sharedFieldInTurnRejected` test) and an undeclared key would add its own unknown-field warnings. Assert `201` with `isValid=false` and exactly one `validationWarnings` entry, at `$.multiTurnData` with code `ADDITIONAL`; supply `data` values for any required shared field so no `REQUIRED` warning is added. Done: `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$MultiTurnSharedDataTests"` passes.
- [ ] 3.2 Add the self-clearing test to `src/test/java/com/epam/aidial/evaluation/functional/tests/MultiTurnCsvFunctionalTests.java` (**not** `MultiTurnSharedDataFunctionalTests`): that class already hosts every scope-flip revalidation test and owns the private `updateSchemaAndAwaitRevalidation` helper, so putting it there avoids duplicating an async-polling fixture. Create a dataset whose schema is all-shared plus one field that the case leaves **unset in `data`**, store a case with non-empty `multiTurnData`, assert it is invalid with the `$.multiTurnData` warning, then PUT the schema flipping that unset field to `perTurn: true` and assert the case is valid with no `$.multiTurnData` warning. The "unset in `data`" constraint matters: flipping a field whose value sits in `data` creates a `$.data.<field>` `INVALID_SCOPE` misplacement and the case stays invalid. Done: covers delta spec scenario "Declaring a per-turn column clears the warning"; `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$MultiTurnCsvTests"` passes.
- [ ] 3.3 Fix the first confirmed regression in `MultiTurnCsvFunctionalTests` → `perTurnFieldRescopedToSharedReportsMisplacementThenClearsWhenRescopedBack` (~line 627). Its dataset declares exactly one field (`tone`) as `perTurn: true` and the test flips it to shared, so afterwards the schema has **zero** per-turn columns while the case still carries 2 turns — the new case-level warning joins the two `INVALID_SCOPE` warnings and breaks **four** assertions: `hasSize(2)`, the `allSatisfy(INVALID_SCOPE, message)` block, the `turnIndex` `containsExactlyInAnyOrder(0, 1)` (the new warning's `turnIndex` is null), and the path `containsExactlyInAnyOrder`. Filter `parseWarnings(misplaced)` by `code == ValidationWarningCode.INVALID_SCOPE` once and reuse that filtered list for all four, then additionally assert the case-level warning is present (path `$.multiTurnData`, code `ADDITIONAL`, no turn index) — the test then doubles as functional coverage of delta spec scenario "Misplacement and the no-per-turn-columns warning coexist". Leave the `cleared` half (re-scoped back to `perTurn: true`) as is; it stays green. Done: `MultiTurnCsvTests` passes.
- [ ] 3.4 Fix the second confirmed regression in the same class → `fixupReportsScopeMisplacementInsteadOfGenericUnknownField` (~line 480). It seeds a 2-turn case into a dataset created with `newDatasetWithSchema(List.of())`, then imports a purely single-turn CSV that persists `tone` as **shared** (the test asserts `getPerTurn()).isNull()`), so the final schema has zero per-turn columns; the numeric→string coercion forces the fixup pass to rewrite and re-validate, and the new warning breaks `assertThat(warnings).allMatch(w -> w.getCode() == INVALID_SCOPE)` and the path `containsExactlyInAnyOrder`. Apply the same filter-by-`INVALID_SCOPE` remedy and assert the case-level warning separately. Also note in a comment that the sibling test `fixupPreservesPerTurnKeySetExceptExplicitNull` (~line 452) now silently flips to `is_valid=false` for the same reason but stays green because it asserts only turn content — leave it as is. Done: `MultiTurnCsvTests` passes.
- [ ] 3.5 Cover the runnable-exclusion consequence (delta spec scenario clause "excluded from runnable test-case selection") in `src/test/java/com/epam/aidial/evaluation/functional/tests/MultiTurnRunFunctionalTests.java`: a DEPLOYMENT suite bound to a dataset whose schema is all-shared and whose only case carries `multiTurnData` → run creation fails `409 INVALID_OPERATION` with "Suite has no valid and enabled test cases". Done: pins the behavioral break named in proposal.md — Impact; `./gradlew test --tests "com.epam.aidial.evaluation.functional.PostgresFunctionalTests\$MultiTurnRunTests"` passes.

## 4. Docs

- [ ] 4.1 Update the multi-turn bullet in `AGENTS.md` ("Inline conventions" → **Multi-turn — array-based, shared + per-turn data**) where it enumerates the warning outcomes: alongside "over-cap → warning, unchanged", record that a case carrying `multiTurnData` in a dataset declaring no `perTurn: true` field is invalidated with a case-level `ADDITIONAL` warning at `$.multiTurnData`. Done: the bullet's warning enumeration is complete.
- [ ] 4.2 Update the same enumeration in `docs/patterns/multi-turn-test-cases.md` (the sentence ending "a misplaced field → **400**, over-cap (`test-case.multi-turn.max-turns`, default 10) → warning") so the pattern doc does not contradict AGENTS.md. Done: both surfaces list the new warning; no other doc section is touched.

## 5. Verification

- [ ] 5.1 Run `./gradlew build`. Done: compiles, Spotless/Checkstyle clean, and the full test suite passes — in particular no multi-turn functional or unit test beyond those fixed in 3.3/3.4 regressed on the reversed "all-shared schema multi-turn case is valid" assumption.
- [ ] 5.2 Run `openspec validate multi-turn-without-per-turn-columns-warning --strict`. Done: the change validates; delta sync into `openspec/specs/test-cases/spec.md` is left to `/opsx:archive` per the project's archive checklist.
