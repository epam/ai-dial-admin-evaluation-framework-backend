package com.epam.aidial.evaluation.experimental.query.service.repository;

/**
 * Experimental {@link StructuredQueryRepository} for the {@code test_suites} entity (meta
 * datasource). Demonstrates the request-side query model end-to-end; the schema/translation
 * machinery it relies on is generic and shared with the other queryable entities.
 */
public interface TestSuiteQueryRepository extends StructuredQueryRepository {}
